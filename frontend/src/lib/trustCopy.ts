// Single source of truth for trust-surface copy that carries constitutional weight.
//
// A qualifier MUST accompany every rendering of overall_grade so the grade can never be
// read as a claim that the model beats the market. Consumed by both the Model Trust
// Console footer (ProvenanceFooter) and the persistent shell TrustStrip — one constant,
// so the two surfaces can never drift apart on the framing.
//
// DG-111: this said "internal model grade — not a market-edge or decision-support claim",
// which is the register David repealed on 2026-08-29, riding the shell strip onto EVERY
// surface in the product. Both of its facts survive word for word in meaning — the grade
// is OURS (internal, not the market's), and it is NOT a claim to beat the market — said
// the way he asked for them to be said.
export const MODEL_GRADE_QUALIFIER =
  "our own grade, not a claim that it beats the market";
