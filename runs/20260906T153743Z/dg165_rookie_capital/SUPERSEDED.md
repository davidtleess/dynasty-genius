# SUPERSEDED — quote the successor, not this run

This run's report renderer crashed at the final step (a KeyError while rendering the policy
section), so REPORT.md is absent and the manifest lacks the report's hash. Every other file
is complete and correct; the successor `20260906T154033Z/dg165_rookie_capital` is written by the same code with the
renderer fixed and reproduces these numbers (deterministic seeds).

Kept intact as evidence; every file is as the run wrote it.
