# FAILED before any evaluation — no outputs

The runner stopped while naming the outcome source: a leftover reference to a provisional manifest alias
(`binding['artifact']`) that the aligned adapter no longer emits. Nothing was fitted or scored; only `inputs/`
(gitignored) exists. Fixed in the next commit; the successor run carries the results.
