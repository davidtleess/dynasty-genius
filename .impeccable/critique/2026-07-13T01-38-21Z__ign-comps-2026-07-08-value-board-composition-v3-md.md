---
target: Value Board comp v3.13 round-11
total_score: 26
p0_count: 0
p1_count: 2
timestamp: 2026-07-13T01-38-21Z
slug: ign-comps-2026-07-08-value-board-composition-v3-md
---
Method: dual-agent (A: `/root/v313_design` · B: `/root/v313_detector`)

## Design Health Score

| # | Heuristic | Score | Key issue |
|---|---|---:|---|
| 1 | Visibility of system status | 2 | The actual Jul 12 degraded state is described but has no desktop or mobile composition. |
| 2 | Match system / real world | 3 | Fantasy-native rows are strong; `FOCAL`, `ctx`, and residual lifecycle copy leak specification language. |
| 3 | User control and freedom | 3 | Search, scopes, inspector, receipt, and neutral handoff remain distinct. |
| 4 | Consistency and standards | 2 | Mobile freshness and row-slot notes contradict the declared global contracts. |
| 5 | Error prevention | 3 | Percentile, tie, rounding, lane, and no-verdict laws remain explicit. |
| 6 | Recognition rather than recall | 3 | Lane labels improved, but rank hierarchy and compressed movement copy still need decoding. |
| 7 | Flexibility and efficiency | 3 | Search, filters, scope counts, and declared sorts support repeat use. |
| 8 | Aesthetic and minimalist design | 2 | Dense audit-derived labels still make the comp feel more specified than designed. |
| 9 | Error recovery | 2 | The failed-refresh recovery state lacks exact copy, placement, actions, and accessible semantics. |
| 10 | Help and documentation | 3 | Receipts and provenance are strong, but several notes disagree with their drawings. |
| **Total** | | **26/40** | **Acceptable; significant fixes remain before the shape gate clears.** |

## Anti-Patterns Verdict

**LLM assessment:** Generic AI-dashboard risk is low. The composition is bespoke, dense, fantasy-native, and avoids card-grid, gradient, verdict-color, and SaaS-metric defaults. Its remaining artificial tell is specification language leaking into the user surface: `# FOCAL`, `pos (ctx)`, `disagr.`, and audit-derived qualifiers ask the manager to read the design rationale rather than the fantasy information.

**Deterministic scan:** `detect.mjs --json` returned `[]`; no rule IDs fired and no ignore file applied. The detector did not catch semantic contradictions in prose. The complete Unicode code-point geometry audit passed all 8 frames and 134 framed lines, including separators and closing rules.

**Visual overlays:** Not available. This is a pre-code Markdown/ASCII composition with no renderable route or referenced Markdown renderer, so browser/DOM evidence would be fabricated.

## Overall Impression

The historical healthy state is substantially clearer: focal ownership, lane labels, movement verbs, replacement truth, and fixed-width frames now work. The remaining shape blocker is that the product's actual current state is the one state the comp does not draw.

## What's Working

- Movement rows use `lead grew/shrank`, units, and current state instead of ambiguous signed movement.
- Mobile DG/Mkt ownership is text-labeled, and within-band rows no longer print a magnitude.
- The FA aggregate, model-defined replacement line, selected 223 state, and no-FantasyCalc-read row form a coherent hard state.

## Priority Issues

### [P1] The current degraded experience is described, not composed

**Why it matters:** Jul 12 is the real daily-login state. Prose says banner up, market lane dashed, and last success labeled, while every desktop/mobile sketch remains healthy Jul 11. David cannot evaluate the actual five-second answer, hierarchy, mobile compression, or recovery semantics.

**Fix:** Add desktop and mobile degraded compositions for Daily Open and Value Board with exact manager copy, last successful vintage, retained model lane, suppressed movement claims, dashed/unavailable market treatment, and screen-reader announcement semantics.

**Suggested command:** `$impeccable harden`

### [P1] Mobile identity and two controls have no resilient geometry

**Why it matters:** Line 1 packs name, team, tied rank, inspector, and receipt using only short examples. Long names, 200% zoom, and larger text have no truncation/wrap rule; `▸ⓘ` reads as two tiny adjacent targets, and the existing ReceiptTrigger has no minimum touch size.

**Fix:** Reserve a fixed action track, make row activation the large inspector target, give the receipt its own 44px target, and define normal/zoom name wrapping or truncation.

**Suggested command:** `$impeccable adapt`

### [P2] The focal-rank fix leaks design annotations

**Why it matters:** `# FOCAL` and `pos (ctx)` explain the comp instead of naming fantasy information, while the generic Surface-1 hierarchy still calls position rank focal in every scope.

**Fix:** Use manager labels such as `DG OVERALL` and `POS RANK`; establish focal ownership through typography and correct the scope hierarchy text.

**Suggested command:** `$impeccable clarify`

### [P2] Mobile freshness and row grammar are not globally consistent

**Why it matters:** The main mobile Value Board omits build time; FA notes claim a three-line row that is drawn in two; Daily notes describe the opposite slot order. These are implementation traps in a fixed-row system.

**Fix:** Apply one line-slot schema and one dual-freshness header to every mobile composition, including the no-read row.

**Suggested command:** `$impeccable layout`

### [P2] Compressed manager copy loses established distinctions

**Why it matters:** `Market moved · DG held` drops the 22/23 market-price denominator while the gap answer uses 14/23, and several rows/summaries revert from `within band` to `band` or `now band`.

**Fix:** Preserve the two denominators in a compact secondary line and use the ratified `within band` phrase consistently.

**Suggested command:** `$impeccable distill`

## Persona Red Flags

**David, morning manager:** Healthy Jul 11 works, but the real Jul 12 failure state cannot be judged because it is not drawn. The current comp does not show whether he should interpret the morning as quiet, delayed, or unavailable.

**Sam, keyboard and screen-reader user:** Lane aria prose and focus are specified; the degraded announcement role and the minimum target sizes for inspector/receipt controls are not.

**Casey, distracted mobile user:** The gap-first line is fast, but long identity plus tied rank plus two controls lacks a stable responsive rule, and the main board omits build freshness.

## Minor Observations

- The active RED seed still says `2 model reads forming; 1 pre-model`.
- The primary macro contract still exposes `pre-model` despite receipt-only route detail.
- The Value Board mobile header omits build time while FA and Daily include it.
- The whole-box character-width audit now passes exactly as claimed.

## Questions to Consider

Questions skipped: the request asks for a concrete falsification list, and the required fixes follow from ratified contracts rather than unresolved design intent.
