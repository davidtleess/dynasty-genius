---
target: frozen Value Board static comp v4.10
total_score: 32
p0_count: 0
p1_count: 4
timestamp: 2026-07-13T22-03-18Z
slug: omps-2026-07-13-value-board-static-comp-v4-10-html
---
Method: dual-agent (A: v410_visual_fresh_a + independent corroboration v410_visual_fresh_b · B: v410_technical_a11y, bounded root browser fallback after harness failure)

## Design Health Score

| # | Heuristic | Score | Key issue |
|---|---|---:|---|
| 1 | Visibility of system status | 4 | Healthy/degraded freshness is explicit. |
| 2 | Match system / real world | 3 | Manager prose is strong; pp and pool distinctions still need learning. |
| 3 | User control and freedom | 3 | Inspector escape works; composed continuation actions remain disabled. |
| 4 | Consistency and standards | 4 | Row, lane, receipt, and scope patterns are coherent. |
| 5 | Error prevention | 4 | Degraded state and unavailable reads fail honestly. |
| 6 | Recognition rather than recall | 2 | Initials-only identity and multiple rank populations increase recall. |
| 7 | Flexibility and efficiency | 3 | Keyboard paths work; investigation remains row-by-row. |
| 8 | Aesthetic and minimalist design | 3 | Strong hierarchy; fallback identity keeps a table/tool residue. |
| 9 | Error recovery | 3 | Stale/no-read paths explain limits and preserve context. |
| 10 | Help and documentation | 3 | Receipts are unusually complete, but football causality is absent. |
| **Total** |  | **32/40** | **Good foundation; not visual GREEN.** |

## Anti-Patterns Verdict

This is not generic AI slop. The macro-first answer, model/market lane isolation, dense row grammar, and honest degraded state are deliberate. The principal aesthetic shortfall is category incompleteness: initials discs and plain team text make a disciplined product still read partly like a developer-built data table rather than a best-in-category fantasy surface.

The deterministic scanner returned an em-dash warning and a numbered-marker advisory. Builder-history prose inflates the dash count; the marker result is a false positive from dates/version text. No reliable browser overlay was created. Standalone runtime verification and all supplied captures were used instead.

## Overall Impression

The morning answer is now fast, trustworthy, and manager-readable. The comp is technically thoughtful and visually coherent. It is not done: evidence integrity still depends on an optional caller variable, the live movement endpoint can be incomplete, and the end-user journey still stops short of instant player recognition and a source-backed football reason.

## What's Working

- The first viewport answers scale, lane, and roster impact before rows.
- Blue model and amber market objects remain structurally and semantically isolated.
- Mobile scope, no-read receipts, degraded behavior, keyboard focus, reduced motion, and 320px containment now work in runtime tests.

## Priority Issues

1. **P1 — Movement manifest is opt-in, not fail-closed.** `comp-v33-extract.py:475-491` compares only when `_expected` is nonempty. Full-pin output is byte-identical with and without a manifest; an isolated prior-day mutation and synthetic predecessor both exit 0 without it. Require a complete two-day manifest for ref-pinned movement, require exact keys/full digests, and print a verified marker before movement facts.
2. **P1 — Live endpoint completeness is unproved.** `comp-v33-extract.py:401-412` checks only the latest date label. A one-row Jul-13 history endpoint paired with a full Jul-13 artifact exits 0 as `0 of 0`. Reconcile latest-history IDs/content/cardinality with the live artifact or suppress movement.
3. **P1 — Fantasy identity remains below the ratified floor.** Every player uses an initials disc; no headshot fallback chain or team-color identity mark is composed. Land the David-gated identity pipeline before visual GREEN.
4. **P1 — “Why it moved” remains arithmetic, not football context.** The sheet repeats price direction and the inspector says role/usage context is unavailable. Ship the source-retention/why-context capability before calling the morning investigation complete.
5. **P2 — Braelon still has two visible info affordances.** `v4.10:434-435` has the working no-read receipt plus a disabled `Why no DG value` icon. Remove the stale action-column icon; the builder note's one-control claim is currently false.

## Persona Red Flags

- **David, dynasty manager:** can identify the relevant rows and disagreement quickly, but cannot recognize every player at a glance or connect a move to injury/role/news without leaving the product.
- **Jordan, first-time user:** must decode DG, pp, overall rank, position rank, and different pools; the duplicate Braelon info icons imply two different explanations although one is disabled.
- **Sam, accessibility-dependent user:** keyboard/focus/Axe paths pass, but 320px secondary text and subtle receipt glyphs remain demanding for low vision.

## Minor Observations

- The frozen PNG set does not show the narrow mobile FA radiogroup/no-read row; runtime measurements, not image evidence, carry that closure.
- At 320px, names are 12px and supporting data 11px. Containment passes, but readability remains a deliberate trade.

## Questions to Consider

Questions skipped: the two technical fixes are contract-determined, and David already owns the identity and football-context sequencing decisions.
