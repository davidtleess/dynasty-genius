fastapi==0.136.1
starlette==1.0.0
uvicorn[standard]
httpx==0.28.1
beautifulsoup4
playwright
python-dotenv
pydantic==2.13.4
jellyfish>=1.0,<2.0
rapidfuzz>=3.0,<4.0
requests
nflreadpy
polars
pyarrow
pandas
scipy==1.17.1
scikit-learn==1.8.0
pytest==8.4.2
certifi>=2024.7.4

# Property-based testing. Installed locally and used ZERO times until
# 2026-08-02; CI installs THIS file only, so the property contract in
# tests/contract/test_ingestion_properties_red.py cannot run in CI without it
# (Codex, 2026-08-02). Pinned to the version already in the local venv.
hypothesis==6.161.0
