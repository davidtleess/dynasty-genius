resources:
  jobs:
    refresh_genius_state:
      name: ${bundle.name}_refresh_genius_state_${bundle.target}
      
      tasks:
        - task_key: refresh_genius_state_sst
          sql_task:
            warehouse_id: ${var.warehouse_id}
            file:
              path: ../src/sql/refresh_genius_state.sql
          
          timeout_seconds: 1800
          max_retries: 2
          min_retry_interval_millis: 60000
          retry_on_timeout: true
        
        - task_key: verify_refresh
          depends_on:
            - task_key: refresh_genius_state_sst
          
          sql_task:
            warehouse_id: ${var.warehouse_id}
            file:
              path: ../src/sql/verify_refresh.sql
          
          timeout_seconds: 300
      
      schedule:
        # RETIRED 2026-08-30 (David's ruling, DG-101). Was UNPAUSED with this same cron —
        # "0 * * * * ?" is EVERY MINUTE, not hourly, and it was declared unpaused with the
        # deployment/billing state unverified. PAUSED here so a stray `bundle deploy` cannot
        # re-arm it; the live workspace teardown is step 2 of the runbook in databricks.yml.
        quartz_cron_expression: "0 * * * * ?"
        timezone_id: "America/New_York"
        pause_status: PAUSED
      
      email_notifications:
        on_start: []
        on_success: []
        on_failure:
          - ${var.notification_email}
        no_alert_for_skipped_runs: true
      
      timeout_seconds: 3600
      max_concurrent_runs: 1
      
      tags:
        environment: ${bundle.target}
        framework: sovereign-unity
        managed_by: dabs
