# QB Validation Program — Increment 1: Computed Backtest of the David QB Research Pair

**Date:** 2026-07-16
**Status (v9 ratification, 2026-07-20, David-worded via Tower — "ratify it"; H2 addendum re-freeze same day, David-worded — "claude has a good reco for the h2 amendment lets go with that"):** v9 = v8 + the **D2a Computability + Contract Pins amendment** (`docs/superpowers/specs/2026-07-19-qb-validation-v9-amendment-d2a-computability.md`, **Revision 7, SHA-256 `441a11b5421408d1eebb74ee09fe774594f33ecbfd50021c8725f7ab85489663`**, Codex ENUMERATED CLEAR 2026-07-20 22:21) — **the amendment document at that SHA is binding law of this spec**. Revision lineage: r5 (`b7221a7a8b69…`, CLEAR 20:28) was ratified into the first v9 freeze at 20:36; **r7 adds only the H2 closure** — the `coverage.cpoe_non_qb_joins` audit counter with its total precedence rule (David-ratified representation) — plus the corrected active lineage/sequence. No other v9 content changed; the 22:00 slice-4 GREEN CLEAR stands, with H2 moving from *excluded* to **contracted and separately review-gated**. The contradiction-point deltas below (D1 pins/1b, D2 outcome classes, D2a contract/universe, cohort universe note, rookie predicate, H1 formulas, D5 attrition block, F14/F15/F28 rows) carry its sweep into this text, and the amendment governs wherever it is more specific. New frozen v9 SHA pinned in the 07-20 ledger. **Active gate order: amendment Revision 7 Part D.1** — the H2-only sequence (r7 text CLEAR → this v9 re-freeze → counter + S35 implementation → Codex implementation-delta review → tollgate + David's granted coherent-tree commit), which expressly bars a second slice RED/GREEN cycle and holds the post-GREEN ratchet at 17 XF. The r1–r5 sequence (behavioral RED → GREEN) is **completed and superseded**, retained only as struck audit history in Part D.2.
**Status (Amendment A, 2026-07-18, David-worded via Tower):** v8 = v7 (`144696ef…`, ratified as the RED contract 2026-07-17) + exactly one vocabulary amendment — F34 TRIAGE gains the fifth reason `missing_identity_keys` (D1.5 + F34 rows below carry the delta; the round-7 review record in `docs/agent-ledger/2026-07-18.md` is the provenance). The new frozen SHA-256 is pinned in the 07-18 ledger. Prior status preserved: Increment **AUTHORIZED** (David, 2026-07-16 via Tower: QB-1 numeric validation, interleaved with Morning Tape G4 — offline compute, no tape collision). Spec: **DRAFT v7, in its CLEAR cycle** — rounds 1–5: 7/8/6/8/5 findings, all integrated; round 6 (v6 SHA `e64fba5c…`): **NOT CLEAR, precision-only — all five round-5 dispositions PASS, no substantive or constitutional defect remains**; three precision residues (stale permutation-CI phrase, F30 not driving `ci_p_disagreement`, F33 missing the registry assertions + repo-wide scan scope) fixed here. v1 is the committed baseline (`739b352`); v7 is an uncommitted working-tree revision pending the round-7 exact delta check (commit = David's word). Separate named David gates still ahead: the `eval/` allowlist amendment, the study registration doc, study execution, any committed summary.
**Authoring lane:** Claude spec · **Codex sole binding reviewer + RED author** (per the David-ratified 2026-07-16 ops/telemetry re-role) · Gemini awareness-only (its 2026-07-16 product-edge findings, received before the re-role, remain integrated: §4 F13).
**Scope:** a research-only validation pipeline that computes the backtests both research documents defer. It produces **reports and artifacts, no product surface, no model promotion, no served-API change**. It is explicitly NOT the valuation engine, NOT a new Engine B QB head, NOT a UI card — those are gated increments 2–3 in the parking lot (§3). The replacement-basis fork rides in this packet as a David decision block (§3a) per his ruling; it is not a QB-1 dependency.

## 1. Problem (measured, not inferred)

David's directive (via Tower, 2026-07-16, verbatim): "Note both reports flag their backtests as directional rather than computed — numeric validation is part of the assessment."

Both documents say so themselves. Prediction doc: "the per-model error metrics in this report are reasoned from gathered correlations rather than fully computed... should be re-run numerically in the pipeline above before you trust exact figures." Valuation doc: "that computation was not run here... Treat the results above as directional validation, not a statistically closed proof."

**Root cause — the computation is not runnable in-repo today.** Reproduced (not asserted): `nfl_data_py` (the docs' named client) is not installed (`ModuleNotFoundError`); the repo's nflverse client is `nflreadpy` 0.1.5. The existing `nflreadpy_qb_adapter.py` is deliberately narrow — context-only `QB_CONTEXT_COLUMNS`, no feature-matrix writes — and the existing QB walk-forward harness predicts **per-horizon binary survival labels, not PPG** (`src/dynasty_genius/eval/qb_v3_walk_forward.py:8,:36-37,78-80`). There is no PPG regression target, no Sleeper-scored PPG label table, and no season/weekly stat-ingestion path. The fold machinery and `backtest_metrics.py` (Spearman/Kendall with BCa CI95, `:58-75`) ARE reusable.

**Consequence today:** five hypotheses and a valuation-engine design carry only reasoned-estimate support; building either engine on uncomputed numbers would violate Prime Directive ("be right, not fast").

## 2. Design

One increment, six deliverables (D1, D2, D2a, D3, D4, D5), all research-lane.

**D1 — nflverse ingestion, extending the EXISTING adapter (single-adapter law).** `src/dynasty_genius/adapters/nflreadpy_qb_adapter.py` is the repo's one nflreadpy adapter and stays the only one. D1 adds ingestion paths **inside the existing adapter module** for the **six nflverse datasets the study actually needs** — the round-2 review proved season summaries alone cannot build the declared matrix; round 3 proved draft capital needs its authoritative source; round 4 promoted pbp into the disciplined set and forced the all-position ingestion:

1. **Weekly regular-season player stats** (`load_player_stats(seasons, summary_level="week")`, seasons **2015–2025**, **ingested ALL-POSITION** — round-4 finding 3: team aggregates computed from QB-filtered rows are wrong by construction (live probe: 2024 team rush TDs all-position 511 vs QB-only 107, every team differing). **Team totals are aggregated from the full all-position frame BEFORE any QB filter; the QB filter applies only at matrix build.** Weekly granularity makes the qualifying-game denominator exactly computable (below) and PPG a pure aggregation.
2. **Player identity attributes** (`load_players`) — birth date (→ age_at_season_start). Draft fields in this dataset are NOT the draft-capital source (round-3 finding 2: nulls conflate UDFAs with data gaps) — they serve only as a cross-check against dataset 5.
3. **Season roster presence** (`load_rosters`, per season 2015–2025) — the cohort's t−1 roster-presence predicate, **pinned:** a player is t−1-roster-present iff they have **≥1 roster row in season t−1 with game_type REG — any week, any status**. The most inclusive rule, chosen deliberately (anti-survivorship: a midseason-cut QB with prior dropbacks is still a real prediction subject); the 2–8 QBs/season the boundary moves stay IN. Registered and disclosed.
4. **The nflverse ID crosswalk** (`load_ff_playerids`) — gsis↔sleeper mapping input for the H5 static join (D4; NOT a PIT bridge — see D4's join contract).
5. **The draft-picks dataset** (`load_draft_picks`, **coverage pinned 1980–2025**) — the **authoritative drafted list and draft-capital source**. **Join contract (round-4 finding 8; made computable per round-5 finding 4 — probe: 108/597 drafted-QB rows carry a null gsis_id):** primary key = gsis_id; rows without one resolve through the **pinned auditable fallback**, whose season key is the **draft row's OWN season column** (the match runs study-player → draft row, so a null `players.draft_year` is irrelevant): candidate = normalized name (F32's algorithm) match within one draft-row season. **Cross-checks against what each dataset actually exposes** (players: birth_date, college_name; draft_picks: age-at-draft, college): (a) **age check** — computed_age = floor((Sept 1 of the draft-row season − birth_date) / 365.25), pass iff |computed_age − draft-row age| ≤ 1 (the pinned tolerance covers the April-draft/Sept-1 reference offset); (b) **college check** — both college strings normalized by F32's algorithm; both-present mismatch = **cross-check conflict → TRIAGE**; either-missing = degraded check, the age check must pass and the audit record says so. **Audit fields (pinned, per resolution):** {study_player_id, draft_row_id, matched_by: gsis|name_season, normalized names, age_check {draft_age, computed_age, delta, pass}, college_check {result: pass|conflict|missing}, resolution}. Resolution states are exhaustive: gsis-joined DRAFTED; fallback-joined DRAFTED (age pass + college pass/missing); **fallback-ambiguous (≥2 candidates), cross-check conflict, missing draft-row season, drafted-but-unjoinable, or missing identity keys (`missing_identity_keys` — no usable gsis AND no usable name on the study row; a missing key cannot MISS, so the UDFA definition never fires on it; Amendment A, David-ratified 2026-07-18) → TRIAGE, never silently UDFA (F34 drives every state)**; a study QB whose usable keys both match nothing in the drafted list is a **UDFA by definition**: `is_udfa=1`, `draft_round := 8`, `draft_overall := 263` (pinned constants one past the 7-round/262-pick maximum). A player marked drafted here but conflicting with dataset 2 is a **true source gap → fail-closed to triage, never imputed**. Draft capital is semantic, not median-imputable (round-3 finding 2: 38/209 historical QB nulls are Romo/Hoyer/DeVito-class UDFAs, not random gaps).
6. **Play-by-play** (`load_pbp`, regular-season plays, seasons **2015–2025**) — **promoted to the enumerated set (round-4 finding 2): the team_proe derivation consumes it, so it carries the same raw-snapshot/parser-version/fail-closed discipline as every other dataset**, with its pinned columns (`pass`, `pass_oe`, `posteam` — the live 0.1.5 column name, renamed `posteam → offense_team` at parse, round-5 finding 5 — `season`, `week`). The adapter's existing context-lane pbp path is untouched; the study path snapshots separately under the validation_study role.

Each dataset gets its own raw snapshot written before parse, source timestamp + parser version + completeness flags, stale/missing → named fail-closed caveat, never silent substitution. Exact column names are pinned in the registration manifests and **verified fail-closed against the live schema (F15) — an absent pinned column fails the build, never substitutes**. **Pinned derivations (round-3 finding 1; source column corrected per round-4 finding 2 — the live weekly schema has `sacks_suffered`, not `sacks`):** the source-to-normalized rename is explicit: `sacks := sacks_suffered`; `dropbacks_proxy := attempts + sacks_suffered` (excludes scrambles — a named, disclosed limitation, identical across all lanes so paired comparisons are unaffected); a **qualifying game** = a weekly REG row with `(attempts + sacks_suffered) ≥ 1 OR carries ≥ 1`; every "dropback" reference in this spec means `dropbacks_proxy`. **Registry contract (pinned against the real code — round-5 finding 1: `SourceDefinition` has ONE shared roles set and ONE shared allowed_fields tuple (`source_registry.py:338-354`), so granting the role on `nflreadpy_qb_context` would silently widen what context may carry):** `SourceRole` gains the Literal member **`"validation_study"`**, and the registry gains a **DISTINCT second definition, `nflreadpy_qb_validation`** — same adapter module, separate entry: roles = {`validation_study`}; allowed_fields = the pinned D1 dataset columns; `provenance_required=True`; `failure_behavior="fail_closed"`; `test_gate` = the QB-1 RED path. **The existing `nflreadpy_qb_context` definition is byte-untouched.** **Study-only symbols and root (pinned so the wall is scannable):** every D1 study ingestion function in the shared adapter carries the `validation_` name prefix (`load_validation_weekly_stats`, `load_validation_players`, `load_validation_rosters`, `load_validation_ff_playerids`, `load_validation_draft_picks`, `load_validation_pbp`); raw snapshots land only under **`app/data/backtest/qb_validation/raw/`** (inside the F24/F31-governed root). **Boundary vs `context_signal`:** `validation_study` data may feed ONLY the QB-1 study package `src/dynasty_genius/eval/qb_validation/` — F33 is the mechanical wall (imports, `validation_*` call sites, and study-root path reads scanned outside the package; the shared adapter module import alone — e.g. `app/services/roster_auditor.py` today — is legal only while it calls no `validation_*` symbol). The existing context path and its consumers are untouched; nothing D1 produces writes to Engine A/B feature matrices. **v9 (Amendment §A1/§A5):** weekly pins gain `completions, sack_yards_lost, passing_epa`; rosters pins gain `position`; NEW dataset 1b **`season_summary`** (`load_player_stats(seasons, summary_level="reg")`, seasons 2015–2025, columns `player_id, season, position, passing_cpoe`) joins the set — `VALIDATION_DATASETS` becomes the pinned seven-name order `("weekly", "season_summary", "players", "rosters", "ff_playerids", "draft_picks", "pbp")`; the validation registry widens by exactly these fields; the context definition stays byte-untouched and pbp gains no player identity.

**D2 — Sleeper-scored QB PPG label table (`src/dynasty_genius/eval/qb_validation/qb_ppg_labels.py`).** **Relocated under the study package (round-4 finding 1): ALL study code — labels, matrix, runners, the static join — lives under `src/dynasty_genius/eval/qb_validation/`, so the validation_study consumer boundary is a package boundary, not a promise.** Enforcement is a RED, not metadata: **F33** pins the exact consumer allowlist (the `eval/qb_validation` package) and an import/materialization tripwire — no Engine A/B feature-assembly or served-app module may import study modules or read validation_study-role data. The pinned label contract:
- **Target:** `y(p,t)` = regular-season Sleeper-scored fantasy points per qualifying game for player p in season t. Postseason excluded everywhere.
- **Qualifying game (exact, from weekly rows, per the D1 pinned derivation):** a regular-season week row for p in season t with `(attempts + sacks) ≥ 1 OR carries ≥ 1`. The games denominator is the count of qualifying games.
- **Scoring:** derived from a versioned league-settings snapshot + hash, never hardcoded (`pass_int = −2.0` probe-verified; the research doc assumed −1). Full component coverage: passing, rushing, **receiving, fumbles lost, and every 2-point path (pass/rush/receive)**.
- **Outcome classes (exhaustive, no silent drops — v9 two-axis law, Amendment §B3/§B5):** classification = eligibility axis (`cohort_admitted` | `rookie_no_priors` | `cohort_ineligible_prior`) × target axis (`target_evaluable` | `no_target_season`), with the total mapping and games laws pinned in Amendment §B5. `OUTCOME_CLASSES = (evaluable, no_target_season, rookie_no_priors, cohort_ineligible_prior, cohort_ineligible_unobserved)`; `evaluable` = cohort-admitted with ≥1 qualifying game in season t; `no_target_season` = cohort-admitted, no qualifying season-t game — excluded from error metrics, counted in a per-fold attrition table and reported as a rate; the three non-cohort classes are label-side/pre-cohort attrition — see the rookie route in D3 and Amendment §B2-§B5. **No dropback inclusion filter** (survivorship bias); a pre-registered sensitivity slice at ≥4 qualifying games is reported as a secondary, never substituted for the primary (F29).
- Fail-closed validation: duplicate player-seasons, non-finite PPG, missing games → named rejection.

**D2a — The study matrix (validation-only, built fresh from D1).** H1–H5 run on a **new candidate matrix built entirely from D1 output for 2015–2025** — NOT on the legacy QB feature store (whose 2024 gap is a fact about that store, not this study; the study matrix has **no 2024 gap**). **Row indexing (pinned, resolving the round-2 ambiguity):** rows are indexed by **target season t**; the feature vector for row (p,t) is built strictly from seasons ≤ t−1. Each manifest feature carries a lookback tag: **[t−1]** (prior-season value), **[career≤t−1]** (career aggregate through t−1), or **[static]** (identity attribute). The earliest target season is **2016** (needs 2015 features). The legacy store is read-only context here and is not modified. **v9 (Amendment §B1-§B7):** the public contract is pinned — `build_study_matrix(sources, *, registration: dict, expected_registration_hash)` with F7-gate-first then seven-dataset F1 provenance admission then F14/F15 on defensive copies; the pre-cohort candidate universe is `label_pool(t) ∪ roster_pool_QB(t−1) ∪ cohort(t)`; the exact return schema (matrix rows carrying `eligibility` + `target`, three-class non-cohort audit, matrix ∪ audit = universe disjoint, count-source mapping, literal `matrix_version`, deterministic ordering, scanner-law flag placement) and the module-owned immutable manifest ownership/consumption contract are law per the amendment.

**D3 — H1–H5 walk-forward study (extends `src/dynasty_genius/eval/`).** All previously-selectable policies are pinned; changing any of them post-registration is a protocol violation, not a tuning knob.

- **As-of rule (model lanes):** features strictly from seasons ≤ t−1; cutoff = end of regular season t−1; no season-t data anywhere on a feature path (F2/F22 guard). (The H5 market lane has its own registered comparison date — D4.)
- **Cohort at fold t:** every QB with ≥1 career NFL dropback (dropbacks_proxy) through t−1 and season t−1 roster presence under D1 dataset 3's pinned any-REG-row/any-status rule. **No age bound** — the round-2 review is right that an age≤40 eligibility cliff is a binary predictive cutoff the constitution bars (00 aging rulings) and it would drop Brady's 41+ seasons from exactly the pocket-vs-rushing comparison. Age enters only as a continuous H4 feature; F12 now enumerates 40 alongside 29/33/36/37. **v9:** the cohort law is UNCHANGED; it now sits inside the pinned candidate universe (Amendment §B2) so every pre-cohort subject is classified exactly once.
- **Rookie route (pinned as EXCLUSION, honestly; v9 mechanical predicate, Amendment §B3):** a rookie = **no prior REG weekly row AND no prior REG roster row through t−1** (a t−1-rostered zero-stat QB is `cohort_ineligible_prior`, never a rookie); rookies are **out of the veteran study** — classified with the named attrition class `rookie_no_priors` (F5/F28), reachable via the label-pool side of the §B2 universe. **QB-1 builds no rookie PPG predictor** (no pre-NFL as-of contract in this increment; named out-of-scope item 5). The case panel is re-scoped accordingly: Daniels/Nix enter as **second-season predictions** (2025 from 2024 rookie-season features), and the panel says so explicitly.
- **Folds (pinned):** expanding window over target-season rows — train = target seasons {2016..t−1}, test = target season t, for t ∈ {2018..2025}: 8 folds, 2024 included. Rolling is rejected (N too small to spend observations on window truncation; recency lives in regularization) and this choice is registered, not revisitable.
- **Models & estimators (pinned, hashed in the registration — round-2 finding 5):**
  - **H1–H4 pipeline:** train-fitted median imputer → train-fitted StandardScaler → **Ridge**, alpha selected on train folds only by leave-one-out/GCV from the fixed grid **{0.01, 0.1, 1, 10, 100}**. No other estimator, scaling, or grid; the pipeline definition is part of the hashed registration.
  - **Naive carryforward baseline:** the player's most recent evaluable PPG within target seasons {t−3..t−1}; a row with none is excluded from naive-lane pairs (each paired comparison runs on its exact common pool; per-pair pool n reported).
  - **Market baseline (H5):** baseline lane only — D4.
- **Feature manifests (exact, positive, fail-closed, lookback-tagged):** an absent manifest column fails the build (F15). H1/H2/H3 pairwise disjoint; **H4 = H1∪H2∪H3 + exactly two identity feature groups** (F27 asserts precisely this — union plus the two groups, nothing else):
  - **H1 efficiency (v9 pinned sources + formulas, Amendment §A1-§A4):** epa_per_dropback [t−1] (weekly Σ passing_epa ÷ Σ dropbacks_proxy, paired-null exclusion), cpoe [t−1] (**official REG season-summary value from D1 dataset 1b `season_summary`, consumed as-is** — weekly recomposition rejected as non-exact), sack_rate [t−1] (Σ sacks_suffered ÷ Σ dropbacks_proxy), any_a [t−1] ((Σ passing_yards + 20·Σ passing_tds − 45·Σ passing_interceptions **+** Σ sack_yards_lost) ÷ Σ dropbacks_proxy — `sack_yards_lost` is source-signed ≤ 0), completion_pct [t−1] (Σ completions ÷ Σ attempts). **All H1 aggregates run over ALL of the player's t−1 REG qualifying rows across teams** — the attributed-team law applies ONLY to H2 rush_td_share and H3 team_proe. Semantic corruption/null refusals per Amendment §A3.
  - **H2 rushing (continuous, no binary archetype; faithful to the source's rush-TD-share claim — round-3 finding 4):** rush_att_per_game [t−1], rush_yds_per_game [t−1], **rush_td_share** [t−1] (player rush TDs ÷ team rush TDs, **team totals from D1 dataset-1's pre-QB-filter all-position aggregation — round-4 finding 3**; a zero team-TD denominator yields a null routed to the missingness path, named), rush_yds_per_att [t−1]. Designed-vs-scramble split unavailable at this grain — named limitation in every report.
  - **H3 volume (faithful to the source's dropbacks-plus-PROE claim — round-3 finding 4):** dropbacks_per_game [t−1], pass_att_per_game [t−1], qualifying_games [t−1], career_dropbacks [career≤t−1], **team_proe** [t−1] (mean `pass_oe` over the attributed team's regular-season offensive plays in t−1, from D1 dataset 6; column pinned, absent → F15 fail-closed).
  - **Multi-team t−1 seasons (pinned — round-4 finding 3):** the **attributed team** = the team with the most of the player's t−1 qualifying games; tie → the later-in-season stint. H2's share numerator = the player's rush TDs while on the attributed team; the denominator and team_proe use the attributed team. A registered, disclosed simplification for traded players — never a silent choice.
  - **H4 composite:** H1 ∪ H2 ∪ H3 + age_at_season_start [DOB-derived @ t: (Sept 1 of target season t) − birth_date — uses only static DOB + the season label, no season-t performance data; continuous, re-tagged per round-3 finding 1] + the draft-capital group {draft_round, draft_overall, is_udfa} [static, per D1 dataset 5's UDFA contract].
  - **Missingness:** the pinned train-fitted median imputer (F22) — **which explicitly EXCLUDES the draft-capital group** (semantic missingness resolved upstream by D1 dataset 5: UDFA gets the pinned constants, true source gaps fail closed — round-3 finding 2); a row missing >50% of its manifest is excluded and attrition-counted, named.
- **Inference contract (pinned end-to-end — round-2 finding 3, test validity completed per round-3 finding 3):**
  - **Point estimate & pooling:** pooled paired delta = evaluable-n-weighted mean of per-fold paired deltas.
  - **Interval:** cluster bootstrap **by player** (a resampled player carries all their player-season rows — handles repeated players across seasons), 10,000 resamples, RNG seed 20260716; CI95 = **BCa with bias correction from the bootstrap distribution and acceleration from the delete-one-PLAYER jackknife** (the cluster is the jackknife unit, matching the resampling unit).
  - **p-value (the BH-FDR input) — a real null, not a CI inversion:** a **paired cluster permutation test**: under H0 (no lane difference), each player cluster's lane assignment is swapped with probability ½ per permutation replicate (the swap applies to all of that player's player-season rows); the pooled delta is recomputed per replicate; **p_two = (1 + #{|δ*| ≥ |δ_obs|}) / (B + 1)**, B = 10,000, seed 20260716. One-sided variants use the corresponding tail with the same formula.
  - **Direction (model-lane primaries):** every model-lane comparison registers its expected direction. `supported` requires the pooled delta **in the registered direction** ≥ 0.05 (Spearman units) AND the CI95 excluding zero AND the fold floor AND surviving BH-FDR q=0.10 across the full primary set. A zero-excluding CI in the WRONG direction is **`contradicted`** — named, never folded into "supported".
  - **H5-lane direction (faithful to the source claim, round-3 finding 3; test made margin-valid per round-4 finding 4):** the source hypothesis is market "as good or better" — a **non-inferiority claim, not superiority**. The lane-swap permutation tests δ=0 and is NOT a test of H0: δ ≤ −0.05, so the H5 primaries use a **null-centered (shifted) cluster bootstrap test at the margin boundary** instead: with δ0 = −0.05 and the same player-cluster resampling/B/seed, δ*_null := δ* − δ_obs + δ0 and **p_NI = (1 + #{δ*_null ≥ δ_obs}) / (B + 1)** — a valid one-sided test of H0: δ ≤ δ0 vs H1: δ > δ0, where δ = (market − model). p_NI is the H5 primaries' BH-FDR input; the reported interval remains the one-sided 95% lower bound (both are stated per contrast). Registered secondaries on the same contrast: `market_superior` / `model_superior`, defined in the status decision function below (two-sided BCa CI95 excluding zero in that direction AND |δ| ≥ 0.05 — no permutation CI exists or is used).
  - **H5 status: a total, ordered decision function (round-5 finding 2):** exactly one `support_status` per H5 contrast, evaluated in order: **`unsupported_power` FIRST** (fold floor unmet — the power gate precedes every substantive label) → `model_superior` → `market_superior` → `market_noninferior` → `inconclusive`. **`market_noninferior` (and the `ni_met` boolean) is defined exactly:** fold floor satisfied AND the one-sided 95% lower bound of δ > δ0 AND BH-adjusted p_NI ≤ q=0.10 — **all three; if the CI and p criteria disagree, the status is `inconclusive` with a named `ci_p_disagreement` flag** (registered rule, no discretion). **Superiority secondaries use the already-pinned two-sided BCa CI** (no permutation-CI inversion exists or is claimed): `model_superior`/`market_superior` require the two-sided BCa CI95 excluding zero in that direction AND |δ| ≥ 0.05; p_perm is reported descriptively for secondaries, with no FDR claim attached. The NI result is never lost: D5 carries per-contrast `ni_met` + `p_ni` alongside the single status. The H5 vocabulary is disjoint from the model-lane vocabulary — no H5 outcome is ever labeled `supported`.
  - **The margin is a PROPOSED convention requiring David's ratification (round-5 finding 3 — his registration ruling has NOT fired; this packet seeks it):** 0.05 Spearman mirrors the model-lane materiality floor (symmetry rationale — one materiality scale across the study); it becomes binding only when David ratifies the registration doc (Sequence step 2), and the report carries a **registered sensitivity readout at margins {0.025, 0.05, 0.10}** so the NI conclusion's margin-dependence is visible, never hidden.
  - **Fold floors:** model-lane comparisons need ≥5 of 8 evaluable folds; **H5-lane comparisons need ≥3 of the 4 DP folds** (proportional, not impossible — round-2 finding 3). Below floor → `unsupported_power`. Fold evaluable n ≥ 20 else `fold_starved` (no point estimate, CI-only display); degenerate inputs (constant/all-null vectors, mass ties) emit named degenerate states, ties by average rank with tie counts reported (F20).
- **Primary comparison set (the complete registered contrast set — 14, matching the hypotheses' actual claims; round-2 finding 4):** H1 vs naive · H2 vs naive · H3 vs naive · H4 vs naive (the four base claims) · H2 vs H1 · H2 vs H3 (H2 "best single factor") · H3 vs H1 (the source H3 claim) · H4 vs H1 · H4 vs H2 · H4 vs H3 (H4 "beats every single factor") · H5 vs H1 · H5 vs H2 · H5 vs H3 · H5 vs H4 (market vs each model lane, rank-only, **registered as one-sided non-inferiority of the market per the inference contract**). BH-FDR q=0.10 applies across all 14. Everything else is labeled exploratory.
  - **Primary metric:** per-fold Spearman, paired delta, pooled per the inference contract. **Secondaries:** RMSE/MAE (H1–H4 only — never on market values), top-6/12/24 hit rate.
- **Case panel (contractual, F10):** Daniels/Nix 2024→2025 (second-season, per the rookie re-scope), Mayfield 2025, Richardson 2025, Maye 2025, and the ex-ante Allen-2019/Richardson-2024 twin test of the efficiency screen — both outcomes reported, no cherry-pick.
- **Frozen boundary (pinned to what actually exists — round-4 finding 7):** `qb_v3_candidate` has NO active model artifact or registry pointer (the ratified promotion decision record, `docs/validation/2026-07-04-build4-qb-v3-promotion-decision-record.md:12`: research-only, no active artifact changed — repo-verified this session). The frozen set F25 hashes is therefore: **qb_v2's registry pointer + model artifact + manifest, PLUS the named existing qb_v3 research/decision files** (`src/dynasty_genius/eval/qb_v3_walk_forward.py`, the 2026-07-04 decision record) asserted byte-unchanged. The existing harness driver (`backtest_harness.py:42`) and qb_v3 classifier driver are not reused; fold isolation/BCa/metric patterns are.

**D4 — Pre-registered DP-proxy market-delta study (H5, rank-only).** The loader exists (`scripts/load_dynastyprocess_archive.py`; 2,185 rows across four kickoff dates; QB rows 78/68/86/82).
- **Temporal contract (pinned — round-2 finding 2 conceded: the four DP snapshots are 2021–2024-09-08, kickoff of season t, which is AFTER the model's end-t−1 as-of).** The H5 lane registers a **kickoff-t comparison date** per fold (the DP snapshot date). Market snapshots must be dated **on/before the registered comparison date** (the after-date ban stands, F19, per-lane). Model predictions remain built from ≤ end-t−1 features. **The information asymmetry is registered and disclosed in every report: the market snapshot has seen the offseason (draft, free agency, trades, camp) that the model has not — a bias AGAINST the model, i.e. a conservative test.** No symmetric alternative exists (no earlier DP snapshots are available), and the asymmetry's direction is what makes the comparison honest rather than flattering.
- **Units:** DP values are consensus-scale (~1–10,232), not PPG. **H5 is rank-only** — Spearman/Kendall + top-k against realized PPG ranks; RMSE/MAE never computed on market values; no PPG calibration attempted.
- **Identity join (pinned, runnable — round-3 finding 5 conceded: the live crosswalk is entirely `db_season=2026`, and converting it into 2021–2024 PIT windows would FABRICATE historical validity the data does not carry).** The PIT bridge is **not used**. D4 builds a **validation-only static join module** (study-scoped, under the study's eval path): gsis↔sleeper pairs from the D1 dataset-4 crosswalk, with **honest provenance on every join: `observed_at` (the 2026 crosswalk snapshot date) and `assumed_effective` (retrospective)** — the retrospective-mapping caveat is mandatory in the registration and every report; "per-player identity is stable" is treated as an assumption to be checked, not a fact. **The check is a reconciliation gate, pinned to a runnable input (round-4 finding 6 — the existing loader DROPS the name at `load_dynastyprocess_archive.py:140-149` and the store schema has no name field):** the gate's input is the **immutable raw DP archive files themselves** (the gitignored raw CSVs, read directly for their name column; file path + SHA256 pinned per fold in the registration) — NOT the name-less store. **Normalization algorithm (pinned):** NFKD → ASCII fold → lowercase → strip punctuation and generational suffixes (Jr/Sr/II–IV) → collapse whitespace. **Denominator:** the fold's joined pairs. **Missing-name raw input counts in the mismatch numerator** (conservative — an unverifiable pair is treated as a failure, F32). **Threshold rationale (registered):** >2% of a ~70–90-row DP QB fold means ≥2 mismatched players — one normalization artifact is tolerated, two signals systematic mapping error. Mismatches → triage list; breach = named **`join_reconciliation_failed`**, fold excluded from H5 primaries (F32). Bridge-MIRRORED semantics implemented in the module (no PIT claim): duplicates collapse-fail, conflicts reject to triage, zero overlap = `identity_join_empty` abort (F16/F17), coverage <70% of the fold's evaluable pool = `join_coverage_low`, fold excluded from H5 primaries (F18). (Codex's round-3 probe: 94.2–96.2% DP QB coverage by fold — coverage is strong; the gate exists because coverage does not prove correctness.)
- **Paired population:** every model-vs-H5 comparison runs on the exact common pool (fold-evaluable ∩ DP-covered), common-pool n and per-lane coverage reported separately. H5 is scoped to folds 2021–2024, floor 3 of 4 (D3 inference contract).
- `dynastyprocess_ecr_2qb` provenance mandatory (F8); raw files gitignored; GPL-3.0 boundary respected. Honest framing in every output: this tests model-vs-**expert-consensus**; the trade-market verdict remains accrual-gated (~Dec 2026, Gate-4). KTC is NOT touched (ruled out-of-spec 2026-05-30).

**D5 — Output schema (`qb_validation_report.v1`), mechanical No-Verdict enforcement.** **Every invocation emits an artifact — including failures** (silence is not success): root `{schema_version, generated_at, registration_hash, run_status ∈ {ok, failed}, failure_reason (named, present iff failed: preregistration_missing | source_unavailable | manifest_column_missing | output_path_violation | identity_join_empty | frozen_boundary_drift | …), decision_supported: false, …}`. A failed run carries `run_status=failed` + `failure_reason` and NO metrics blocks. An `ok` run carries: `inputs: {snapshot_ids, settings_hash, matrix_version}, folds: [{test_season, n_evaluable, attrition: {no_target_season, rookie_no_priors, cohort_ineligible_prior, cohort_ineligible_unobserved, manifest_missing}  (v9, Amendment §B5), metrics_with_CIs, flags[] ⊆ {fold_starved, join_coverage_low, join_reconciliation_failed, degenerate_input}}], comparisons: [{id, registered_direction, pooled_delta, ci95, p_perm | p_ni, ni_met (H5 contrasts only), support_status}], case_panel[], sensitivity_panels[] (incl. the H5 margin sensitivity {0.025, 0.05, 0.10})}`. **Status levels are separated (round-3 finding 6):** run-level = `run_status`/`failure_reason`; fold-level = `flags[]`; comparison-level = `support_status` with **two disjoint vocabularies** — model-lane `{supported, not_separable, contradicted, unsupported_power}`, H5-lane `{market_noninferior, market_superior, model_superior, inconclusive, unsupported_power}`. **The field is `support_status`, not "verdict"** — round-2 finding 7: a field literally named `verdict` fails the report's own banned-language scan by construction. `decision_supported=False` on the root **and every nested model**; deltas unclamped (negatives raw); unavailable states named, never fabricated; the banned-language scan runs over serialized JSON, stdout, and any CSV (F9/F26). Artifacts land under `app/data/backtest/qb_validation/` only — output-path overrides refused (F24). **`app/data/backtest/qb_validation/` is NOT currently gitignored (probe-confirmed): the `.gitignore` addition ships in the same change set and F31 asserts it** — backup-manifest coverage added if the artifacts prove irreplaceable; a committed summary doc only on David's word.

## 3. Out of scope (named, not hidden)

1. **The valuation engine (DCF value-over-replacement, archetype survival curves, discount slider)** — increment 2; its scale/discount design belongs in the PVO-scale solutioning session. Building it before QB-1 reports would be building on uncomputed numbers.
2. **Any Engine B QB model change or promotion** — frozen qb_v2 stays the anchor; qb_v3_candidate stays NOT PROMOTED; a promotion needs its own pre-registered bake-off.
3. **Any David-facing surface (delta card, buy/sell anything)** — increment 3, design-gated; Buy/Sell/Hold tiers as proposed violate the No-Verdict Line and do not ship in that form regardless.
4. **KTC integration (live or historical)** — previously ruled out-of-spec; re-opening is a named David decision, and its absence surfaces by name in the study's source disclosure.
5. **A rookie PPG predictor** — withdrawn from this increment (see D3's rookie route); if wanted, it is a named future increment with its own pre-NFL source/as-of contract.
6. **In-season cadence/refresh design** — meaningless until the study exists; season-aware cadence rules apply later.

## 3a. DAVID DECISION BLOCK — the replacement-basis fork (symmetric, no default, per David's 2026-07-16 ruling)

**What it is:** increment 2's value-over-replacement engine needs a replacement level. QB-1 does not use one (it validates prediction quality, not VOR) — this block rides in the packet because David ruled the fork returns to him inside it, never crew-picked. **It does not block QB-1.** Decision needed before increment 2 opens; the PVO-scale solutioning session is a natural venue if David prefers deciding there.

**Option A — league-derived QB24** (the weekly 24th-best QB PPG stream in David's Superflex format).
- Matches the league's actual replacement stream; the denominator tracks the real scoring environment.
- Moves season to season: values re-base as the league environment shifts (honest, but less stable).
- Requires a league-history producer and season-aware refresh that do not exist yet.
- Creates a second replacement basis in the codebase alongside the static-xVAR model constants until reconciled — a consistency cost the PVO-scale session would own.

**Option B — model-defined constants** (current static-xVAR basis, `pvo_assembler.py:471-491`).
- Consistent with every shipped surface today; one basis everywhere; stable denominator.
- Frozen constants can drift from the league's real replacement stream (basis staleness), and the valuation engine inherits any miscalibration in them.
- No new producer needed; no re-basing between seasons.

**No default is set and no crew recommendation is offered.** Both options are compatible with the constitution; the tradeoff is real-environment fidelity vs single-basis consistency, and it is David's.

## 4. Falsification seeds — the RED matrix (Codex authors the binding RED)

Test path: `tests/contract/test_qb_validation_program_red.py`. All hermetic — injected fixtures/seams, no network, no gitignored artifact asserted (drive functions directly; monkeypatch paths).

| Seed | Input/state | Required behavior |
|---|---|---|
| F1 | adapter fetch with source unavailable/stale fixture (any of the six D1 datasets) | named fail-closed caveat; raw snapshot absent ⇒ no parsed rows; never silent substitution |
| F2 | fold construction fed a feature row containing target-season data for a target-season prediction | rejected loudly (leakage guard), fold aborts with named error |
| F3 | H1–H4 manifest or matrix containing any market-derived column (ktc/fc/dp/adp aliases, recursive scan) | banned-column scan fails the build; market columns legal ONLY in the H5 baseline lane |
| F4 | hand-computed golden PPG rows under league scoring fixture | D2 output matches exactly; scoring read from settings, not constants |
| F5 | rookie row (zero NFL seasons ≤ t−1) entering the veteran model path | rejected with named class `rookie_no_priors`, attrition-counted, never silently scored |
| F6 | fold with evaluable n below 20 | emits named `fold_starved`, no point estimate; metrics always carry BCa CIs |
| F7 | any D3 or D4 runner invoked with absent/mismatched registration hash | refuses to run, named `preregistration_missing` failure (study-wide, not D4-only) |
| F8 | DP history row lacking `dynastyprocess_ecr_2qb` provenance | fail-closed rejection |
| F9 | every emitted artifact root + nested models | `decision_supported=False` exactly; banned-language scan (buy/sell/hold/verdict lexicon) clean — including over the schema's own field names |
| F10 | report generation without the required case panel (incl. the Allen/Richardson twin pair and the Daniels/Nix second-season re-scope note) | report build fails — the panel is contractual, not optional |
| F11 | duplicate player-season / non-finite PPG / missing games in the label table | D2 validation fail-closed with named reasons |
| F12 | any hardcoded age constant or age discontinuity (29/33/36/37/**40**, forced terminal zero) on a model code path — including cohort eligibility | guard test fails (age enters only as a continuous feature or via versioned curve artifacts with provenance; no eligibility cliff) |
| F13 | report generated without the archetype threshold-sensitivity panel (binary dual-threat gate vs continuous rushing moderator, boundary cases ±1 yd/g) | report build fails — Gemini lane finding 2026-07-16; the existing binary `is_dual_threat` flag (`feature_assembly.py:250-269`) is in the panel's scope |
| F14 | adapter fed malformed shapes: wrong-type frame, missing required column, empty frame (per D1 dataset, all seven incl. 1b `season_summary` — v9) | fail-closed with named reason per shape class; no partial parse |
| F15 | a pinned manifest column absent from D1 output (any of the seven datasets — v9 adds weekly `completions`/`sack_yards_lost`/`passing_epa`, rosters `position`, and 1b `player_id`/`season`/`position`/`passing_cpoe` — incl. `sacks_suffered`, the pbp `pass_oe`, and `posteam` behind team_proe) | build fails, named `manifest_column_missing`; never a silent substitute |
| F16 | the static join module encounters duplicate or conflicting sleeper↔gsis crosswalk pairs | bridge-mirrored semantics enforced: duplicates collapse-fail, conflicts reject to triage — never scored |
| F17 | identity join yields zero overlap | named `identity_join_empty` abort; no empty-pool metrics emitted |
| F18 | join coverage below 70% of the fold's evaluable pool | named `join_coverage_low`; fold excluded from H5 primaries, exclusion reported |
| F19 | per-lane as-of violations: a model-lane feature after end-t−1, or a market snapshot dated after the registered kickoff-t comparison date | row/fold rejected, named (the H5 lane's registered date and disclosed asymmetry are in the registration doc) |
| F20 | constant or all-null prediction/label vector; mass rank ties | named degenerate state (no NaN/crash); average-rank ties with tie count reported |
| F21 | golden scoring rows for the edge components: QB receiving, fumbles lost, each 2-point path; settings-hash assertion incl. `pass_int = −2` | exact match; hash mismatch fails the run |
| F22 | imputer/scaler fitted on any test-fold row, or train/test target-season overlap | leakage guard fails the fold with named error |
| F23 | registration doc altered after hash pin (any pinned policy: folds, manifests, estimator pipeline, alpha grid, comparison set, directions, thresholds, seeds) | hash mismatch; every runner refuses |
| F24 | runner invoked with an output path outside `app/data/backtest/qb_validation/` (arg or env override) | refused, named `output_path_violation` |
| F25 | before/after run comparison of the **real frozen set (round-4 finding 7)**: qb_v2 registry pointer + model artifact + manifest, plus the named existing qb_v3 research/decision files (`qb_v3_walk_forward.py`, the 2026-07-04 decision record) | byte-identical hashes, asserted by the runner; any drift = `frozen_boundary_drift` failure |
| F26 | serialized report JSON, stdout, and CSV surfaces | recursive `decision_supported=False`; banned-language scan clean; negative deltas present unclamped in fixtures that produce them |
| F27 | H1/H2/H3 manifests pairwise intersected; H4 compared to the declared composition | disjointness holds; **H4 = exactly H1∪H2∪H3 + the two identity feature groups (age_at_season_start, draft_capital)** — nothing more, nothing less; any drift fails |
| F28 | classified rows under the v9 two-axis law (Amendment §B5): every (eligibility, target) combination per the pinned total mapping, shipped identity key `season` | `evaluable` ⇔ cohort-admitted with games > 0; non-cohort attrition rows may carry positive games but NEVER `ppg`/`points_total`; `no_target_season`/`cohort_ineligible_unobserved` require games = 0; the unreachable (rookie, no-target) combination refuses `universe_membership_violation`; counts carry the literal four attrition keys with explicit zeros; never imputed a zero |
| F29 | the ≥4-games sensitivity slice emitted without the unfiltered primary alongside | report build fails — the slice never substitutes for the primary |
| F30 | forced `support_status` states: a comparison below its lane's fold floor (5/8 model-lane, 3/4 H5-lane), failing BH-FDR, a wrong-direction zero-excluding CI, any H5 contrast, **and BOTH NI disagreement directions — CI-pass/p-fail and CI-fail/p-pass** | model-lane statuses forced to `unsupported_power` / `not_separable` / `contradicted` respectively; H5 contrasts can only emit the H5 vocabulary (`market_noninferior`/`market_superior`/`model_superior`/`inconclusive`/`unsupported_power`) — `supported` on an H5 contrast is impossible by construction; **each NI disagreement direction forces `inconclusive` + the `ci_p_disagreement` flag**; the test drives each state |
| F31 | `git check-ignore app/data/backtest/qb_validation/` after GREEN | passes — the `.gitignore` addition ships in this change set (probe-confirmed absent today) |
| F32 | a DP fold whose joined pairs fail name reconciliation above the 2% threshold — including raw rows with a MISSING name (counted in the mismatch numerator); a raw-file SHA mismatch vs the registration pin; a failed run of any kind | `join_reconciliation_failed` named, fold excluded from H5 primaries, triage list emitted; SHA mismatch = fail-closed; every failed invocation emits a `run_status=failed` artifact with its named `failure_reason` (never silent, never a partial-metrics artifact) |
| F33 | outside `eval/qb_validation` (and the RED tests): an import of any study module, a call site of any `validation_*` adapter symbol, or a path read under `app/data/backtest/qb_validation/` — **scanned repo-wide across `src/dynasty_genius/**` AND `app/**`**, not selected modules (importing the shared adapter module WITHOUT calling `validation_*` symbols stays legal, e.g. roster_auditor today); **PLUS the registry half of the wall: `nflreadpy_qb_validation` exists as a distinct entry with roles={`validation_study`} and its own allowed_fields, and `nflreadpy_qb_context` remains byte-stable and context-only** | tripwire test fails the build on any code-side breach; the registry assertions fail on any entry merge, role widening, or context drift — the wall is a RED over exact symbols, paths, AND registry state |
| F34 | draft-join closure, every resolution state driven: a null-gsis row resolved by the pinned name+draft-row-season fallback (age check ±1 via the DOB formula; college normalized); a fallback-ambiguous (≥2 candidates) row; a both-present college conflict; a missing draft-row season; a drafted-but-unjoinable row; a study row with NO usable identity key (missing-like gsis AND missing-like name — Amendment A) | fallback-joined = DRAFTED with the pinned audit fields; ambiguous / college-conflict / missing-season / unjoinable → TRIAGE, never silently UDFA; no-usable-key → TRIAGE `missing_identity_keys` (Amendment A, David-ratified 2026-07-18; behavioral RED precedes implementation-pinning acceptance); UDFA assignment only after both usable keys miss |

*Provenance note:* F14–F34 absorb and make explicit the v1 §4b adoption-by-reference list (Codex round-1 seeds) and the round-1/2/3/4 CLEAR-cycle findings; no RED obligation lives outside this numbered matrix.

## 5. Sequence (cockpit-TDD)

1. **Codex round-7 exact delta check of this v7** (rounds 1–6 complete: 7/8/6/8/5 substantive findings then a precision-only round 6; mechanics PASS since round 6) → CLEAR feeds the David packet. Gemini: awareness copies only.
2. **David's named sub-gates, each by name:** the `src/dynasty_genius/eval/` allowlist amendment (exact-set allowlist at `tests/contract/test_subsystem_4_audit.py:106`) + the study registration doc (hash-pinned, covering D3+D4 per §2 — folds, manifests incl. pinned derivations and renames, estimator pipelines, comparison set with directions, **the H5 non-inferiority margin — a PROPOSED convention this packet asks David to ratify, with its sensitivity set**, the shifted-bootstrap NI test and permutation contract, the H5 status decision function, seeds, the roster-presence and multi-team attribution rules, the draft-join fallback contract, the DP raw-file SHA pins, the H5 kickoff-date asymmetry AND retrospective-mapping disclosures). The §3a fork block is in David's packet for the increment-2 predecision; it does not gate QB-1. No new dependency PR is needed (nflreadpy already in the stack; no ML deps added).
3. Codex authors the RED (**F1–F34**), demonstrably red on `main`.
4. Claude GREENs; focused suite + full closeout gate (locked eval surface touched); self-probes the falsification matrix.
5. Codex independent review with cited evidence → CLEAR.
6. **Only then, David-authorized:** commit/PR/merge; CI is the merge gate. Study execution and any committed summary are separate David words.

## 6. Risks

| Risk | Mitigation |
|---|---|
| Small-N inconclusiveness (~25–30 QB-seasons/fold) — the likely honest outcome is "H4 not separable from H2 within CIs" | BUILD-4 precedent normalized this; `fold_starved` + CIs everywhere; the study's value is calibrated truth, not a promotable verdict |
| DP-proxy conflated with a trade-market edge claim | mandatory framing string in every D4 artifact; rank-only metrics; Gate-4 stays the trade-market gate |
| The H5 kickoff-date information asymmetry read as unfair to the model | it IS asymmetric — registered, disclosed, and conservative (biases against the model); the alternative (no market comparison at all) informs less |
| Garden-of-forking-paths | ONE registration hash over the whole study (folds, manifests, estimators, comparisons, directions, thresholds, seeds); F7/F23 refuse unregistered or drifted runs |
| nflverse data drift/schema change | raw snapshots + parser version per adapter rules; F14/F15 fail closed per dataset; re-parse replayable |
| Identity-join quality degrading H5 | crosswalk-sourced static join + bridge-mirrored semantics + F16–F18; coverage reported per fold; low-coverage folds excluded from primaries by rule, not judgment |
| Retrospective mapping silently wrong for 2021–2024 players | the F32 name-reconciliation gate per fold (2% threshold, triage list) + observed_at/assumed_effective provenance + a mandatory caveat — correctness is checked, not assumed |
| Scope creep toward the engine/surface | §3 parking lot; increments 2–3 open only on David's word after QB-1 reports |
| Cost | Codex estimate: 5–8 focused days Ridge-first scope (this spec); 8–12 days for the documents' full ambition — the delta is why increment 2 is gated, not bundled |
| The efficiency screen's ex-ante indistinguishability (Allen/Richardson twins) invalidating the bust-gate design | F10 makes the twin panel contractual; if the screen fails it, that finding gates increment 2's design |
| Rookie exclusion narrowing the study's claim | stated on every report (the study speaks to veterans with ≥1 NFL season); the rookie question is a named future increment, not a silent gap |
| 14 primaries diluting power via multiplicity | the price of testing the claims actually made; BH-FDR q=0.10 is the registered control, and `unsupported_power` is an honest outcome |
