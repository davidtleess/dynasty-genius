# Breakout & Edge-Patterns Brief — the insight-engine seed backlog

**Date:** 2026-07-06 · **Author:** Claude (implementation lead), consolidating three independent cockpit lanes · **Status:** DRAFT — proposal board for David; every increment individually David-gated.
**Trigger:** David directive — "figure out some patterns or lexicon for breakout candidates — some kind of age and production analysis, similar to how we have our age cliffs... i can make trades during the offseason for rising players. think about other patterns or ways to analyse the info we have to create advantages."
**Binding law:** roadmap doctrine v3 (calibrated-lexicon machinery; insight validation ladder — nothing here flips `decision_supported`); frozen-model rule; accrual honesty; No-Verdict core (patterns are cohort/base-rate/trajectory facts, never buy orders).

## 1. Lane inputs (independent, then compared)

- **Claude:** 8 seeds — breakout-window cohort grammar (the age-cliff mirrored on the upslope), ascending-trajectory words over PIT series, pre-peak curve position, young model-high/market-lag divergence, manager-behavior leverage pockets, pick seasonality, cliff-arbitrage timing, Superflex QB scarcity. Repo verification: breakout_age/dominator features EXIST (Engine A/B contracts, pvo_assembler); Sleeper transaction history NOT captured; daily FC capture is player-only (no picks).
- **Codex (technical audit, data-verified):** 10 patterns ranked with computability; key facts — `prospects_with_outcomes_v3.csv` (874 rows: capital, ages, dominator/breakout/RYPTPA/YPRR, y2–y4 outcomes) supports honest base-rate tables NOW; `universe_market_divergence_latest` already emits 109 MODEL_HIGH_MARKET_LOW rows behind gates; forward stores hold 8–12 days; pick seasonality NOT computable (no annual cycle captured); manager behavior NOT computable (no transaction ingest). Scoping guardrail: split into separate briefs/builds — accepted.
- **Gemini (product-edge, advisory):** 5 ranked patterns with manager situations, scouting bases, nudge risks, seeds. Convergent with the other lanes on the top patterns. **Defect handled in synthesis:** its specific constants (breakout age ≤19.5, YPRR ≥1.85, RYPTPA ≥1.70, "38% top-24 base rate") carry no named derivation — treated as CANDIDATE thresholds pending derivation from OUR cohort file, exactly like the Cornerstone youth thresholds (the rookie-prior v1→v2 folklore-scalar lesson). Its pick-seasonality staging corrected to Codex's technical read (instrumentation first).

## 2. Proposed increments (David orders/gates each; not one build)

**E1 — Breakout Window base-rate tables + lexicon entries** (computable NOW; the direct answer to David's ask). Derive position × draft-capital × age/experience cohort tables from `prospects_with_outcomes_v3.csv` with y2–y4 outcome rates — the rookie-prior-v2 discipline applied to the breakout question. **Outcome targets are DEFINED BEFORE derivation starts** (Codex anti-drift rule): candidates = top-24 WR/RB, top-12 QB/TE positional finishes, or position-specific xVAR/DVS bins — pinned in the E1 spec, not chosen after seeing the tables. Lexicon entries ride the doctrine machinery as **insight categories, not quality words** (Codex rule): "Breakout window — year-2 day-2 WR cohort historically reached top-24 at X% (n=Y)". Chip = base rate + denominator + window; never "will break out". Gemini's candidate production thresholds evaluated during derivation; adopted only where OUR data supports them. Validation stage at birth: `descriptive_base_rate`.

**E2 — Pick-value capture instrumentation** (tiny build, time-critical). Add picks to the daily FC forward capture. Rationale: pick-price seasonality is UNRECONSTRUCTABLE history (FC is current-only — research-confirmed for players, same for picks); every uncaptured day is lost forever, and the constitutional prior ("picks appreciate as drafts approach") becomes a MEASURED timing curve after one cycle. The dual-PIT-capture logic exactly. **Spec step 1 = adapter discovery** (Codex): verify FantasyCalc serves pick rows in the endpoint we already capture; if not, E2 begins with source discovery rather than an add-picks flag.

**E3 — Young model-high / market-lag exploratory insight** (computable NOW as Hypothesis). The off-season trade scanner: young players where the model percentile sits materially above market percentile, from the existing divergence artifact + forward stores; persistence added as daily points accrue. Renders under the exploratory bounds (dedicated insights area, `[Hypothesis]` badge, no action words, no quality-word bleed); tradeable-edge claims wait for Gate-4 accrual. Naming: **"Market Lag" / "Value Gap" / "Market Disagreement"** (three-way converged); "Buy Window" is OUT of the first pass — too action-adjacent — and returns only if David later ratifies it as a non-directive market noun.

**E4 — Counterparty roster-pressure surfacing** (computable NOW, descriptive). Structural complementarity from `team_value_matrix` + `league_opportunity` (surplus/deficit matches, capacity/clog pressure) — where trade conversations are structurally logical, without naming targets or motives.

**E5 — Pre-peak curve derivation** (SYNERGY: this is roadmap Step 2b). One derivation task over the fitted aging curves serves BOTH the Cornerstone youth thresholds AND the "Pre-Peak / Prime Window / Cliff Watch" upslope grammar. No invented age constants.

**E6 — Sleeper transaction-history ingest spec** (prerequisite for manager-behavior patterns). **VERIFIED (Codex live read-only probe, 2026-07-06):** current-league transaction history IS retroactively available by week (`GET /league/{id}/transactions/{round}` returned full trade/waiver/free-agent/commissioner events with assets, roster_ids, timestamps, waiver bids) — E6 is NOT in E2's unreconstructable time-critical class. Spec shape: week-range backfill discovery + ongoing capture + idempotent inserts + previous-season league-id chain checks. Behavior descriptors ("rookie-premium manager", "pick hoarder") come later, evidence-counted with sample floors, never motive claims.

Deferred to in-season wakes: role-growth/usage patterns (usage-leads-production, constitution-aligned); market-momentum words over thin FC history; QB-scarcity edge claims (BUILD-4 verdict binds — descriptive league-structure facts only).

## 3. Standing guards (all lanes converged)

Every pattern ships with a declared validation stage (`descriptive_base_rate` / `hypothesis` / `provisional` / `validated`); none flips `decision_supported`. Breakout/Pre-Peak/Market-Lag are insight categories — never calibrated quality words. Base-rate chips carry denominator + n-floor + window. Off-season reality: situation changes are qualitative caveats, never speculative value bumps. Sort bases disclosed; no hidden cut/target lists.
