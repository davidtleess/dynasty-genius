# NFL Mock-Draft Aggregation Increment A Due-Diligence Report

## Executive synthesis

The strongest conclusion from this due-diligence pass is that **full Increment A should not be built as a live automated system in late May 2026**. The current free-path signal is too thin, and the acquisition layer is the real bottleneck. In the small empirical probe requested here, WalterFootball surfaced two updated 2027 mocks on May 25–26, 2026; the NFL.com probe surfaced 2027 compensatory-pick content and 2026 mock pages rather than a 2027 analyst mock; and the Grinding the Mocks dashboard exposed only a JavaScript loading shell in this environment. On top of that, NFL.com’s terms expressly prohibit systematic retrieval to create a collection or database, and Posit’s shinyapps.io terms prohibit scraping, spidering, or crawling without written consent. citeturn17view0turn17view1turn12search0turn12search5turn14view1turn42view0turn43view0turn38view0

The recommended build order is therefore: **go now on Subsystem 3 (prospect identity substrate)**, **go now on Subsystem 4 (backtest harness and evaluation design)**, **conditional/manual-first go on Subsystem 1 (consensus aggregation)**, and **no-go for now on Subsystem 2 as an automated adapter program**. In practical terms, that means building the identity layer and the historical validation pipeline immediately, then supporting a **manual curated JSON / snapshot workflow** for mock inputs until either source maturity improves or explicit permission/legal clarity exists. The evidence supports a **“build substrate now, defer live scraping and user-facing near-class pick values until roughly December 2026 through April 2027”** verdict. citeturn29view0turn29view1turn29view2turn29view4turn17view0turn17view1turn14view1turn38view0

The net product recommendation is blunt: **Increment A is worthwhile as a research-and-infrastructure project now, but not yet as a production inference artifact that emits precise 2027 rookie-pick values**. The likely highest-return move in 2026 is to build the join logic, the historical scoring harness, and a fail-closed manual input path; the lowest-return move is building brittle scrapers against sources whose legal or technical posture is weak. citeturn17view0turn17view1turn20view0turn42view0turn38view0

## Mock-consensus aggregation

**Feasibility:** feasible only as a **manual-first, low-precision consensus layer** in Increment A’s current timeframe. A true “EDP-like” system is not realistic from the free-path sample currently available. Benjamin Robinson’s 2020 GTM paper describes EDP as a **Bayesian hierarchical model** built from a much larger mock corpus; his 2018 case study used **424 mock drafts**, and players needed **10 or more mock drafts** to be EDP-eligible. The same work produces credible intervals and probability distributions, not just point estimates. More recent GTM work also says its weighted metrics contextualize mocks using **publication date** and **historic draftnik track record**. That is a fundamentally different data regime from a late-May, near-class artifact built from a handful of free analyst mocks. citeturn15view0turn41search1turn41search4

The light empirical probe strongly supports a **tiered, not exact-pick**, framing. WalterFootball’s two current 2027 mocks agree on some top-end names — most notably **Arch Manning at No. 1** in both mocks and **Jeremiah Smith** in the top two or three — but divergence appears almost immediately after that. In the current Walt mock, **Dante Moore** is No. 2, **Cam Coleman** No. 6, and **Jayden Maiava** No. 5; in the current Charlie Campbell mock, **Cam Coleman** is No. 3, **Dante Moore** No. 4, and **Jayden Maiava** No. 5. That is enough to say there is directional signal around the top cluster, but not enough to claim stable exact-pick precision. citeturn18view0turn18view1

The best Increment A aggregation method is therefore an **outlier-robust median of the latest eligible pick per analyst**, not a weighted/Bayesian clone and not a simple mean. The median is the safest default because the free-path set is low-N, heterogeneous by analyst, and likely to contain large early-cycle discrepancies. The output should include: **projected_pick_median**, **projected_pick_min**, **projected_pick_max**, **IQR or MAD**, **n_sources**, **n_unique_analysts**, **staleness_days**, and a **disagreement flag**. That mirrors the spirit of GTM’s uncertainty-aware outputs without pretending to reproduce GTM’s probability model from inadequate data. citeturn15view0turn41search4turn41search5

For **bucketing**, the right product shape is: **Round 1 early**, **Round 1 mid**, **Round 1 late**, **Round 2**, **Round 3**, **Day 3**, and **UDFA**. GTM’s own public writing explicitly argues that, at some phases of the cycle, **projecting round is more appropriate than projecting exact pick**, and WalterFootball’s 2027 prospect pages already use projected-round style outputs such as “5-7.” That is the cleanest public-facing abstraction for stacked-inference governance as well. citeturn41search3turn28search10

The recommended gates are strict. In **late May through August**, if there are **fewer than three distinct analysts** in the usable source pool, the system should **abstain entirely**. With **three to four** analysts, it should emit only **round-tier outputs** and never an exact pick. Exact-pick outputs should wait until there are at least **five current analysts**, acceptable staleness, and materially tighter dispersion — conditions far more likely in the bowl/combine/final-month windows than in late May. That recommendation follows directly from the current sparse probe and from GTM’s much higher mock-count requirements for stable EDP-style inference. citeturn17view0turn17view1turn15view0

**Recommendation:** **Subsystem 1 is a conditional go**, but only as a **manual-first, bucketed consensus artifact** with abstention rules and no claim of GTM-level forecast quality. citeturn17view0turn17view1turn15view0

## Live per-source adapters and access reality

### NFL.com

**Feasibility:** technically parseable, legally problematic. Current NFL.com mock pages are accessible in server-rendered text form and expose the core fields needed for extraction: headline, explicit **Published** timestamp, and repeated **Pick / team / prospect / school-position-class** blocks. The analyst pages also cross-link related current mock versions, which is useful for “latest version only” discovery. Historical retrievability is good: this pass surfaced NFL.com mock pages from **2018, 2020, 2021, 2024, 2025, and 2026**, including seven-round Chad Reuter pages. citeturn20view0turn21search0turn21search1turn21search3turn22search0turn22search5turn22search14turn22search17

**Access and ToS reality:** NFL.com’s current robots file does **not** disallow `/news/`; it disallows specific app, account, and search paths. But the operative legal constraint is the terms page, which says the service is for **individual non-commercial informational purposes** and expressly prohibits **systematic retrieval of data or other content** to create or compile a collection, compilation, database, or directory without written consent. That language is highly relevant to exactly the kind of snapshot-plus-normalization flow contemplated here. citeturn43view0turn42view0

**Governance posture:** because the parser is technically easy but the terms language is restrictive, the clean posture is **manual snapshot only** unless explicit written permission is obtained. In other words: NFL.com remains useful as a **human-curated input source**, but it is **not cleanly greenlit for automated adapter ingestion** under the currently surfaced terms. citeturn42view0

**Recommendation:** do **not** build an automated NFL.com ingester in Increment A. If you use NFL.com at all before legal clarity changes, use a **manual export/snapshot workflow** with one raw HTML snapshot per article and one normalized parse pass. The snapshot contract should store at minimum: source URL, analyst, mock version, published_date, fetched_at, content_hash, parser_version, and parse_status. citeturn20view0turn42view0

### WalterFootball

**Feasibility:** technically the easiest of the three. WalterFootball’s 2027 mock pages expose a simple repeated pattern of **pick number → team → player → position → school**, plus explicit “Updated” metadata on Walt’s page and explicit “Last update” metadata on Charlie Campbell’s page. Historical coverage is excellent: WalterFootball’s draft archive page surfaces mock-draft history back to **2003**, and current pages explicitly reference prior-year mocks. That makes WalterFootball the strongest free historical backtest source in this pass. citeturn17view0turn17view1turn23search10turn23search1

**Access and ToS reality:** the pages are openly accessible and parsed fine in this pass, but the site’s legal posture is **unclear rather than affirmatively permissive**. The publicly surfaced policy page is a privacy page whose core legal text is old; it references “Terms of Use,” but this pass did **not** surface a dedicated, current public terms page or a discoverable robots file that clearly authorizes automated collection. The privacy page also notes RSS syndication of public content, which is not the same thing as permission to scrape and store a structured database. citeturn39view0turn40search0

**Governance posture:** because the site is technically easy but legally ambiguous, the correct posture is **manual-first** unless permission is clarified. I would not classify WalterFootball as “prohibited,” but I also would not classify it as cleanly approved for automation based on the materials surfaced here. citeturn39view0turn40search0

**Recommendation:** WalterFootball is the best current source for **manual curation** and for **historical backtesting**. It is also the best candidate for a future low-rate internal adapter **only if** legal clarity improves. One parser caution surfaced in this pass: Charlie Campbell’s current 2027 page still carries a page title that says “2026 NFL Mock Draft,” while the body clearly states it is the 2027 mock updated May 25, 2026; any parser must therefore trust the explicit body metadata over the title alone. citeturn17view1

### Grinding the Mocks

**Feasibility:** methodologically strongest, operationally weakest. GTM’s public materials show the most sophisticated approximation of draft-market consensus in this source set: Bayesian EDP, credible intervals, weighted mock-draft shares, and public claims that the system aggregates thousands of mocks into evolving probability distributions. But the actual dashboard target returned only a **“Please Wait”** loading shell in this environment, and no public export/CSV/API path was surfaced in this pass. citeturn15view0turn16view2turn14view1

**Access and ToS reality:** the dashboard currently sits on shinyapps.io infrastructure, and Posit’s service terms explicitly allow access through a web browser **but not through scraping, spidering, crawling, or other software used to access data without express written consent**. That alone is enough to classify the dashboard as **not a clean automated-adapter target**. The GTM Substack is useful for articles, archive browsing, final mocks, and methodology writeups, but that is different from a machine-readable dashboard feed. citeturn38view0turn16view1turn41search8turn41search1

**Historical retrievability:** the public Substack archive now makes **2025–2026** posts easy to retrieve, and the 2020 paper documents the **2018** dataset and method. What I did **not** verify in this pass is a public, durable, machine-readable GTM archive for **2018–2024** that would support a full historical backtest pipeline. That gap matters. citeturn41search8turn41search10turn15view0

**Recommendation:** classify GTM as **manual cross-check / methodology benchmark only**, not as an adapter target. It should inform your uncertainty design and backtest philosophy; it should not be the ingestion spine for Increment A. citeturn15view0turn38view0turn14view1

Across the free-path set, the practical conclusion is that **there are not yet three legally and operationally clean automated inputs**. The best near-term source set is therefore **manual curation from WalterFootball (Walt), WalterFootball (Charlie Campbell), and, when 2027 analyst mocks actually appear, selected NFL.com analyst pages**, with GTM used only as a manual reasonableness overlay. That satisfies the spirit of a three-source consensus without taking on unnecessary scraper and terms risk. citeturn17view0turn17view1turn12search0turn12search5turn14view1turn42view0turn38view0

**Recommendation:** **Subsystem 2 is a no-go for automated implementation now**. Use manual snapshots and revisit automation only when the source set is both richer and cleaner. citeturn42view0turn38view0turn17view0turn17view1

## Prospect identity for undrafted 2027 players

**Feasibility:** high-value and buildable now, but only with a true college-side identity layer. This pass did **not** surface a single free, canonical, ID-backed 2027 prospect universe. Instead, it surfaced a patchwork of editorial artifacts: ESPN’s early 2027 prospect watch content and early 2027 mock, FOX’s way-too-early top 10 board, CBS’s top 10 offensive prospects, and WalterFootball’s 2027 position ranking pages. Those are useful discovery surfaces, but none of the surfaced pages provides a universal public prospect ID scheme. citeturn28search2turn28search8turn28search14turn28search20turn28search7turn28search10

The strongest public identity anchor in this pass is **CollegeFootballData via cfbfastR**, not an editorial board. `load_cfb_rosters()` returns season-level roster data including **athlete_id, first_name, last_name, team, year, and position**; `cfbd_player_info()` returns a **unique athlete_id** plus searchable name/team/position context; and `cfbd_team_roster()` exposes the same roster-level fields directly. The transfer-portal feed also provides origin and destination schools by season, which is helpful for tracking school changes, even though that transfer feed does **not** expose athlete_id in the surfaced docs. citeturn29view0turn29view1turn29view2turn32view0turn31search6

The right architecture is therefore: maintain an internal **prospect_uuid** keyed first to **cfbd athlete_id** whenever available, and then store current school, prior schools, position group, class year, and aliases in your curated identity file. Editorial boards and mock pages should be treated as **candidate discovery surfaces**, not identity authorities. For name handling, copy the spirit of nflverse’s own `merge_name` approach — lowercase, strip punctuation and suffixes, and standardize common substitutions — then layer reviewable alias rules on top. That gives you a transparent and auditable college-side identity system rather than a pile of ad hoc name joins. citeturn29view3turn29view0turn29view1

A robust fail-closed matching pipeline should be: **normalize name → exact name+position+school match → alias-table match → fuzzy candidate generation against CFBD rosters/player lookup → human review queue for low-confidence or common-name collisions → no auto-match on ambiguous duplicates**. Transfers should be handled as a soft disambiguator, not a hard blocker: use current school, prior school, and transfer history together. Recommended hard gates are conservative: if you cannot maintain near-perfect precision on reviewed matches, or if more than a small minority of consensus top prospects remain unresolved, the artifact should abstain rather than project false precision. citeturn29view0turn29view1turn32view0

One important alignment point with nflverse: the surfaced nflreadr draft-picks dictionary uses **`cfb_player_id` from College Football Reference**, while nflverse’s players dataset centers post-draft/NFL-side IDs such as **`gsis_id`**, `pfr_id`, and `espn_id`. In other words, the surfaced nflverse truth layer is excellent for realized outcomes, but it does **not** give you an out-of-the-box pre-draft bridge from **CFBD athlete_id** into the NFL-side ID universe. You will need an internal bridge table when prospects are actually drafted. citeturn29view4turn30view2

**Recommendation:** **Subsystem 3 is the clearest go-now subsystem**. It is the hardest new problem, but it is also the one that most cleanly de-risks every later stage. citeturn29view0turn29view1turn29view4

## Engine A scoring and backtest feasibility

**Feasibility:** strong for the **backtest substrate**, weak for a current user-facing valuation artifact. Feeding projected capital into Engine A is defensible only if it is surfaced exactly as what it is: a second-order projection built on a first-order projection. That uncertainty stack is consistent with the public GTM methodology, which emphasizes probability distributions and credible intervals rather than single deterministic picks, and with later GTM writing that explicitly says **round** can be a more appropriate projection target than exact pick at some points in the cycle. That is the right conceptual basis for an overlay-only, `decision_supported=False` artifact. citeturn15view0turn41search3

The **mock → realized NFL capital** backtest is feasible right now. WalterFootball supplies deep historical mock coverage; NFL.com has discoverable historical final mocks across multiple analysts and years; and nflreadr exposes draft-pick truth going back to **1980**, including overall pick, round, team, position, college, `gsis_id`, `pfr_player_id`, and `cfb_player_id`. That is enough to evaluate whether consensus projections are directionally sound before you even ask whether they improve fantasy-derived pick valuation. citeturn23search10turn23search1turn21search0turn21search1turn21search3turn22search0turn22search5turn22search14turn22search17turn25search4turn29view4

The clean backtest design has **two separate stages** and they should not be conflated. First, score **mock consensus versus realized draft capital** using metrics such as overall-pick MAE, round-bucket accuracy, top-36 skill-position recall, UDFA false-positive rate, coverage after identity abstentions, and a weighted error that values early picks more heavily. GTM’s mock-quality writing is directly aligned with that philosophy: it explicitly says earlier picks matter more and that prediction error distance matters more than crude binary scoring. Second, only after that first-stage forecast is defensible should you test **projected-capital → Engine A usefulness versus Regime B**. That second test should ask whether the projected-capital overlay materially improves calibration or ranking relative to your existing historical slot curve. citeturn16view0turn41search5turn25search4turn29view4

The honest cost-benefit answer today is that **named-prospect projected capital is probably not yet a meaningful upgrade over Regime B in late May 2026**. The reasons are not theoretical; they are practical. Free-path near-class source coverage is thin, exact-pick signal is still unstable, the legally clean automated source set is not there, and identity error would dominate quickly if you relax the matching discipline. That means the current opportunity is not “ship projected 2027 mid-1st values now”; it is “build the machinery that can tell you later whether doing so is actually better than the curve.” citeturn17view0turn17view1turn12search0turn12search5turn14view1turn38view0

**Recommendation:** **Subsystem 4 is a go-now for backtesting and evaluation**, but **not** for live user-facing valuation output until the backtest proves incremental value over Regime B and the identity layer is demonstrably precise. citeturn25search4turn29view4turn17view0turn17view1

## Open questions and limitations

One unresolved legal question remains around **WalterFootball**: this pass surfaced an old privacy page that references Terms of Use, but it did **not** surface a clear current terms page or discoverable robots file that would let me classify automated collection as clearly permitted. For governance purposes, that uncertainty should be treated conservatively. citeturn39view0turn40search0

One unresolved data question remains around **Grinding the Mocks**: the public method is well documented, and the Substack archive now exposes recent public posts, but I did **not** verify a durable, machine-readable public archive for historical GTM forecast data across the full 2018–2024 span. That is why GTM is best treated here as a benchmark and manual cross-check, not a core ingestion dependency. citeturn15view0turn41search8turn14view1turn38view0

I also did **not** re-survey dynasty ADP or rookie-slot truth sources for the second-stage Engine A evaluation, because your brief explicitly said not to re-survey prior source-scoping work. So the strongest finalized conclusion from this pass is about **source legality/access, aggregation shape, identity design, and draft-capital backtesting**, not about any new downstream fantasy-market feed.