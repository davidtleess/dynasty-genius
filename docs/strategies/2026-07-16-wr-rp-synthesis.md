# WR / Reception Perception Research — Synthesis (DRAFT v3, CONSOLIDATED — all three lanes)

**Date:** 2026-07-16 (evening)
**Status:** DRAFT v3 — CONSOLIDATED. All three lanes in, independent and unanchored (Claude read → drafted v1 before either lane returned; Gemini David-directed read w/ web verification, ledgered 19:31; Codex evidence-cited read AFTER its QB-1 RED handoff per David's protection — findings delivered on the wire; **its WR ledger entry was not found at consolidation time and has been requested — flagged, not blocking: research input, not a gating verdict**). Not cleared, not a build authorization — David's direction is SYNTHESIZE ONLY; WR work is not opened and sequencing stands (QB-1 → Morning Tape → League Pulse). Returns to David through Tower.
**Source:** `docs/strategies/2026-07-16-david-wr-reception-perception-recommendation.md` (SHA `4058c42d…`, 64 lines, read in full).
**Method note:** per David's framing, this synthesis reconciles the doc against standing law rather than inheriting its wording. Repo claims below are probe-verified where marked; the rest is graded.

## 1. The core idea, in one paragraph (faithful restatement)

Adopt Matt Harmon's Reception Perception (RP) charting — separation ability vs coverage type, measured independently of QB play — as a **supplemental-never-foundational** WR layer: build the WR engine on public data first (Phase 1), acquire RP legitimately and let a walk-forward backtest decide whether its features earn model weight (Phase 2), and ship a logged, bounded, owner-applied "conviction adjustment" regardless (Phase 3). The premise: the market prices production; separation charting prices talent; the gap is where dynasty edge lives — the WR analog of rushing volume for QBs.

## 2. Claude-lane assessment (evidence-graded)

**What is strong and well-aligned (grade: aligned with standing law as written):**
- **Supplemental-never-foundational is the constitution's own posture** toward analyst data (65/35 discipline; analyst opinion after verified primary inputs). The doc's own constraint list (partial coverage, unproven predictive value, single-rater, subscription access) is honest and pre-empts the usual overclaim.
- **The acquisition ethics are right**: no scraping, subscription + manual entry or a direct author request. The manual CSV/admin-form importer maps cleanly onto the 01 source-adapter rules ("define a manual fallback if automated scraping is fragile" — here manual is the PRIMARY path, which is legitimate).
- **Missingness honesty in UI** ("never render a null RP field as a zero"; a "charted" badge only when data exists) is exactly the fail-closed display law.
- **Phase 3's auditability goal** — log every owner nudge with note + date so the owner can later measure whether his RP-informed adjustments added value — is genuinely compounding-lens aligned (an accumulating benchmark of David-vs-model judgment).
- **"Publish the result either way"** for Phase 2 matches the QB-1 no-winner discipline.

**Constitutional conflicts — the same two blockers as the QB pair, plus the standing corrections (grade: must be severed/corrected in any spec, exactly as QB-1 did):**
1. **Market values as CORE model inputs** (Phase 1, line 26: "Core WR inputs: … and market values"). KTC/FantasyCalc/DP in a training row is leakage by locked ruling — market is baseline/overlay only. QB-1 severed this identically.
2. **Buy/sell verdict outputs** (lines 26, 43, 50: "buy/sell deltas"; "buy at +15%, strong buy at +30%, sell at −15%, strong sell at −30%"). The No-Verdict Line bans these from running software; they are legal only as vision/destination language. Note also: line 50's "consistent with the QB cards" inherits a surface that does not exist — QB increments 2–3 are unauthorized; there are no shipped QB buy/sell cards to be consistent with.
3. **KTC named as a comparison source** — KTC remains ruled out-of-spec (2026-05-30); re-opening is a named David decision, not an inheritance from a research doc.
4. **`nfl_data_py` named again** (line 26) — the stack is `nflreadpy` 0.1.5 (probe-verified this morning for QB-1; unchanged).
5. **"Approved direction" status line** — per David's own Tower framing, this is direction-to-synthesize, NOT build authorization; the synthesis carries that correction forward so no future session reads the doc header as an opened lane.

**New-source escalation (grade: mandatory process step, not a footnote):**
- RP is a **new external data source** — an explicit 02 escalation trigger. Adoption (even Phase-2 candidate ingestion) requires the escalation path: David's named decision, ONE source adapter per 01 (raw snapshot before parse, source timestamp, parser version, completeness flags), and evidence-hierarchy placement. RP is analyst-produced charting — quantitative-shaped but single-rater judgment; nearest constitutional class is validated-analyst (tier 2), not ground truth (tier 1). Engine B's allowed feature classes do not currently include analyst charting; RP features would need a David-gated class decision even AFTER passing a backtest gate. The honest lane is the QB-1 pattern: **validation-only candidate matrix first**, promotion never implied.

**Design observations for whatever spec eventually opens (grade: hypotheses/design input, no authority):**
- **The Phase-2 gate as written is weaker than the house standard QB-1 just set.** "Improve out-of-sample Spearman for years-1–3 across ≥2 of 3 folds" is underspecified relative to QB-1's pinned inference contract (registered directions, player-cluster bootstrap/permutation, BH-FDR, power gates, no-winner conditions, one registration hash). The doc even asks to "reuse the QB walk-forward validation harness" — the right reading in 2026-07-16 terms is: reuse the **QB-1 study machinery and preregistration pattern**, not the qb_v3 classifier harness (which predicts survival labels, not PPG — same correction as QB-1 §1).
- **The "charted by RP" boolean as a model feature imports analyst selection bias.** Harmon charts players he (and the market conversation) finds relevant — the indicator correlates with hype/market attention, which flirts with the market wall in spirit. As a *validation-study covariate* it is interesting; as an Engine feature it needs adversarial scrutiny. Flag for the RED matrix if Phase 2 ever opens.
- **Phase 3's conviction adjustment is compatible with the No-Verdict Line only under strict shape rules:** it is DAVID's input, not the tool's — so it must be stored as a separate, labeled, dated overlay field (never mutating model output, never re-sorting a default rank silently, basis always on the surface: "owner adjustment +X%, note, date"). The doc's own UI language ("clearly labeled analyst adjustment chip… note and date on hover") already points the right way. The locked qualitative-manual ruling supports this shape.
- **Phase 1 inherits the deferred forks, not their answers.** "Mirrors the approved QB architecture" overstates: the QB *valuation engine* (increment 2) is not approved or built — only QB-1 validation is authorized. The replacement-basis fork is David-deferred to the PVO-scale session; the discount slider design belongs to the same increment-2/PVO conversation. WR Phase 1 as described (DCF × longevity − replacement vs market) sits BEHIND those same gates and behind the ruled sequencing (QB-1 → Morning Tape → League Pulse).
- **Coverage math is honest and important:** ~50–90 charted WRs/yr vs a full-universe engine means RP can never be a required field — the doc's nullable-by-design schema is right, and the years-1–3 cohort focus (where production data is thin) is exactly where a talent prior could matter most and where samples are smallest. Expect `fold_starved`-class outcomes; the synthesis should pre-register that expectation rather than promise a verdict.

## 2b. Gemini lane (David-directed read, web-verified as of 2026-07-16; ledgered 19:31 ET; attributed, non-binding input)

- **Current-source verification (the lane's load-bearing contribution; Gemini-web-sourced, re-verify before anything gates on it):** receptionperception.com is live and actively publishing; **no public API** — manual entry of sortable tables or a direct author request are the only legitimate paths (confirms the doc's acquisition constraint). Subscription tiers: Basic $19.99/yr (rookie + basic profiles), Prime $29.99/yr (+ sortable tables, in-season), **Sicko $99.99/yr (+ the complete historical library — the tier Phase 2's backfill would actually need)**. Complete sortable data exists for the 2025–26 season.
- **⚠ Flagged item (suspect, not adopted):** Gemini's "2025 rookie class" example list (Harrison Jr., Nabers, Odunze, Thomas Jr., McConkey) names **2024 draftees** — as of 2026 those are third-year players. The coverage claim may be right while the examples are wrong; graded UNVERIFIED and excluded from the synthesis's factual basis.
- **Methodological risks (independently corroborates the Claude lane):** 8-game samples are susceptible to game-plan noise/brackets/weather/undisclosed injuries; single-rater with no inter-rater reliability audit → treat as expert-analyst prior, never machine-derived data; **selection bias — the charted universe (~top-80 fantasy WRs) correlates with draft capital and production, so a "charted-by-RP" boolean must be handled as a biased indicator in any backtest** (matches the Claude-lane flag independently); a dozen RP fields on ~70 players/yr = high overfitting risk → strict folds and dimensionality penalties.
- **Product/workflow:** the Phase-1-baseline / Phase-2-candidate split is structurally correct; the Phase-3 auditable ±15% owner nudge matches real dynasty-manager workflow while keeping the pipeline independent; UI badging must render metrics only where they exist.

## 2c. Codex lane (evidence-cited read, post-RED-handoff per David's protection; attributed)

1. **Architecture:** the doc's Phase-1 architecture claim (:25-26) is directional only. The real harness (`src/dynasty_genius/eval/backtest_harness.py:409-430,442-491,498-530`) is four legacy folds, fixed alpha, CSV input, optional market overlay; the QB-1 v7 study package does not exist yet, so ":29's 'reuse QB harness' is not yet a demonstrated integration contract."
2. **Constitutional:** artifact :26 lists market values among Phase-1 model inputs vs constitution 00:121-123/:150-152 — overlay-only, never Engine A/B features. Separate comparison from inputs. (Three-lane convergence: all lanes hit this independently.)
3. **Computability:** 90×12 = **1,080 manual cells/season (~8,640 for 8 seasons)** before correction/provenance overhead. Any Phase-2 design must register the eligible WR-season denominator, charted numerator, per-season/fold missingness, and minimum n; the existing four-fold harness (:418-423) makes years-1–3 attrition and fold mapping an explicit design item, not an inheritance.
4. **Candidate RED seeds (if a WR validation increment ever opens):** source/license/staleness; malformed/duplicate rows; coverage denominator/floor; as-of violations; allowlist; identity ambiguity; train-only imputation; **market ingress**; fold starvation; no-verdict output; frozen drift; unregistered promotion.
5. **New-source escalation shape:** David approval; license/no-scrape terms; a distinct registry role + allowed fields + provenance + fail-closed behavior; raw snapshot with timestamp/parser/hash; **validation-package isolation until a ratified gate** — i.e., the QB-1 `validation_study` pattern applied to RP.

## 3. Proposed reconciled shape (for the consolidated synthesis — NOT a spec)

If David later opens WR work, the QB-1 template maps cleanly: **WR-1 = a validation-only computed study** (public-data features vs naive + market-rank baselines, QB-1-grade preregistration) sequenced behind QB-1's build; **RP Phase 2 = a second validation increment** gated on the new-source escalation + legitimate acquisition; **Phase 3 = a small descriptive-overlay feature** (owner-adjustment field, logged, basis-on-surface) that genuinely has no data dependency and could be specced independently — but none of this is opened today, and this section is a map, not a request.

## 4. Consolidated summary for David (all lanes in; convergences and divergences)

**Three-lane convergence (highest confidence):** (a) market-values-as-model-inputs is the doc's one hard constitutional blocker — sever exactly as QB-1 did (market = baseline/overlay only); (b) supplemental-never-foundational is the right posture and the doc's own constraints are honest; (c) the charted-by-RP indicator carries analyst-selection bias and must never be a naive feature; (d) acquisition ethics (no scraping; the Sicko tier or a direct author request for history) are workable.

**Two-lane items:** buy/sell thresholds = No-Verdict violation as runtime output (Claude constitutional + Codex RED seed); the Phase-2 gate must be rebuilt to the QB-1 preregistration standard, not the four-fold legacy harness (Claude design read + Codex :409-530 citations); RP = new-external-source escalation with the `validation_study` isolation pattern (Claude + Codex, shapes match).

**Single-lane items (attributed):** Codex's manual-entry cost math (1,080 cells/season); Gemini's verified access tiers + no-API confirmation (and its flagged-suspect rookie-class examples); Claude's Phase-3 shape rules (owner nudge = labeled dated overlay, never mutating model output) and the "mirrors the approved QB architecture" correction (the QB valuation engine is not approved or built — only QB-1 validation is).

**No divergences requiring adjudication** — the lanes differ in coverage, not in conclusion.

**If David later opens WR work, the map is §3** (WR-1 validation study → RP Phase-2 validation increment behind the escalation → Phase-3 overlay, spec-able independently). Nothing is opened by this synthesis; sequencing stands as ruled.
