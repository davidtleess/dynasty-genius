# DG-186 — Claude Design workspace implementation plan

## Source and five-second answer
Import project be3e0bfc-352f-4d88-a150-b49a96f8890a through authenticated Claude Design. Read workspace HTML, support.js, Board, PlayerPanel, PhoneScreen and github.md. Preserve source bytes privately with hashes. Reproduce the neutral 216px rail, compact top bar/search, dense paired readings, inline expanded four-part player explanation and mobile stacked two-lane cards. The first answer is player identity, our overall rank, market overall rank, direction of disagreement.

## Truthful adaptation
Design values, dates, ages, ranks, completeness, age cliffs, opportunity prose and timeline are samples. The actual independent comparison is rank intervals over the same cohort, not a calibrated FantasyCalc price. Retain that verified contract; never subtract model points from market prices, convert gaps to draft picks, use a six-percent agreement threshold, or invent confidence. Current/future forecasts are season totals, not PPG. Availability uses comparison API default population, never the complement of David ownership. Source-derived dates/counts only. No overnight-change sentence without historical evidence. Local watchlist additions supply dated user activity; undated events stay undated.

## Ownership
Root DG186: data join, source compatibility validation, persisted watchlist reuse, navigation/state, actual-source API tests, integration, preview and final review. Claude DG187: presentational WorkspaceFrame and WorkspaceCompare with CSS and component tests. Claude DG188: WorkspaceBoard and WorkspacePlayerPanel with CSS and component tests. Exact contracts in frontend/src/workspace/types.ts, copied to builders before implementation. Builders own only assigned files in their own worktrees; root copies reviewed files into DG186. Import lane becomes independent reviewer after its bounded import completes.

## Build sequence and evidence
1. Before behavior code, tests for source-compatible joins; 433 default unowned with missing rows retained; ranks unmodified; unresolved watch entries retained; sorting missing outside order; same-position-only forecast differences; routing/back state; search and watch persistence.
2. Join validated market-ranks context with validated comparison JSON only when report hash, report run, ownership date, forecast years and roster membership agree. A comparison failure leaves rank roster usable and offers clear unavailable discovery/forecast state. No not-on-David-roster inference of availability.
3. Pure presentation components follow imported HTML converted to repository React/CSS. Reuse fonts/tokens/headshot fallback; no dc runtime/editor dependencies or remote sample execution in app. Keyboard-native row buttons/disclosures, visible focus, reduced motion, responsive navigation.
4. Configured research rank mode adopts workspace for supported destinations; preserve existing legacy URLs/surfaces and explicit not-configured legacy app. Expose Today, Roster, Available, Watchlist, Compare, What changed. Today shows ranked roster disagreement with rule disclosed; full board filters by position and name/team/position, order by our rank/market rank/minimum gap/current/future/name. Missing values stay named outside requested numeric order. Expand any player and watch; compare only verified available versus roster.
5. Native frontend gate, relevant backend rank/comparison contract tests, independent frozen-source oracle and real desktop/mobile inspection: main nav/search, tie/missing, preserved watches, selected comparison including cross-position, failures, keyboard and axe. No duplicated full backend gate for frontend-only edits. Review final diff and record required autonomy receipts. No publication or production change in this task.
