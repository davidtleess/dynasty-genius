# 2026-08-31 — Dynasty Genius Agent Ledger

**Opened 07:35 ET by lane Fred. David's authorization, verbatim at 07:27:37 in Fred's session:**
*"fine. your call on the transparency and comms - create the ledger and make sure we get a good
convergance and plan around the findings"*.
Four lanes are live today and one of them cannot receive messages — this file exists because of that.

> **Correction, 08:2x — the header above previously rendered that authorization inside quotation
> marks in a tidied-up form David never typed.** It was substantively accurate and formally
> fabricated, which is precisely the failure this file exists to prevent, committed by its own
> author in its first paragraph. A quote is exact or it is not a quote. Found by the ruling-record
> sweep, not by Fred.

---

## ⭐ GOVERNING RULINGS AS OF 08:2x — the latest wins, everything earlier is dead

Run `python3 ~/dg-build/bin/rulings.py` for the live list across all four lanes. As of now:

1. **THE FREEZE IS LIFTED.** David, typed into Fred at 07:39:40: *"Okay, let's fix all the things
   that need to be fixed. I'm going to lift the freeze and I want this product to be fully working
   the way it should be. Step by step, you are largely approved to make changes and to improve the
   product. But I do want proper focus on the main goal and the North Star of the project, which is
   to create an edge for me over my competition."*
   **⚠ THIS APPEARS IN NO OTHER WRITTEN RECORD.** `project_season_readiness_2026.md` still reads
   "freeze FRIDAY 09-04"; `MEMORY.md`, `BOARD.md` and the rest of this ledger still reason from a
   freeze. The largest decision of the day was, until this line, unrecorded anywhere on disk.
2. **SEQUENCING: roster fix → TE → foundation.** David, typed into Fred at 08:11:03: *"we can make
   the roster and te fixes then get to the foundation"* — agreeing with his 08:06:45 selection in
   Bob ("Ship the roster fix now, then TE") and 08:02:37 in Greg. **Everything earlier on the roster
   fix is superseded**, including "Fix it before the freeze" (07:20:36), "Wait for the
   trustworthiness answer" (07:31:34) and "Hold the whole fix until TE is resolved" (07:55:14).
3. **THE AGE-SKEW FINDING OUTRANKS ALL OTHER WORK.** 08:06:45 in Bob: *"Chase it now — it outranks
   the rest (Recommended)"*. Given in Bob's lane about Greg's measurement.
4. **THE COMPETITION IS NOT NAIVE.** 07:45:56 in Fred: *"the assumption should be that my competition
   look at sites like Dynasty Nerds, Fantasy Pros, Keep Trade Cut, and other popular sites, in
   addition to box scores"*. Reframes the entire edge thesis and makes `app/data/ktc.py` — a
   one-line stub with no callers — a blocker on measuring an edge at all.
5. **RANKING PHILOSOPHY**, 06:52:02 in Greg: *"Rank everyone, always"*; the edge is *"Find where the
   market is WRONG, Predict the market's next move"*; *"Both — and they're separable"*. Plus
   07:59:01: instrument edge-(b) now and model it in 12 months; let the derived replacement number
   stand with its reasoning shown; one headline number with a contend/rebuild toggle.
   **⚠ "Post-freeze — think big, no deadline pressure" (06:52:02) had its premise removed by ruling
   1. The substance survives; the post-freeze gating does not.**

### Live propagation gaps at the time of writing — each one is costing something
- **BOB does not know the freeze is lifted**, 90 minutes on. It refused Fred's relay on principle
  (correctly), asked David directly at 07:45:15, and **received no answer**. It is still planning
  around a freeze that no longer exists.
- **GREG is BLOCKED, not merely uninformed.** David's 08:02:37 ruling ordered Greg a sequence, but
  the standing approval to write code was given only to Fred. Greg has written zero lines and said
  so twice. A work order was issued to a lane that has not been authorised to work.
- **LOU has received THREE messages all day and nothing since 07:20:56.** It holds the *identical*
  brainstorm charter to Greg, byte-for-byte — and David has since answered that shared question
  eight times in Greg's session. Lou has none of it.

---

---

## How this file works

**Append-only. One section per lane, under your own name. Do not edit another lane's section.**
If you need to correct another lane, write your correction in your own section and name what you
are correcting — the 08-25 ledger did exactly that and it is the right pattern.

**Why it exists.** Bob, Greg and Fred are Claude sessions and can message each other directly.
**Lou is Codex and is on no message bus at all.** Nothing any of us sends reaches it. David relays
to Lou by hand. This file is the only durable shared surface all four can read. Lou has used this
directory before — `docs/agent-ledger/evidence/2026-08-18/independent_model_audit_codex_v1.*` are
its artifacts — so this is a convention being restarted, not imposed.

**Evidence rule, inherited from the 08-25 ledger and from today's repeated failures: cite the
command you ran or the file:line you read. Label anything you did not verify yourself as REPORTED
BY <lane>.** Two claims were retracted today for exactly this reason, both of them mine.

---

## THE LANES

| Name | Bus handle | Started | Owns |
|---|---|---|---|
| **Bob** | `davidleess-a0` | 05:48 | Tickets. Morning brief, roster serving defect, plist land. |
| **Fred** | `davidleess-4d` | 05:57 | Tickets. Freeze pre-mortem, backups, grader, this ledger. |
| **Greg** | `davidleess-0b` | 06:40 | Brainstorm. Rank coverage, model ablation. |
| **Lou** | *(none — Codex)* | 06:43 | Brainstorm. Ranking + premium-data strategy. **Unreachable by message.** |

**File claims right now.** Bob: `ops/launchd/com.davidleess.dynasty-nflverse-vintage-sync.plist`
and `app/services/roster_auditor.py`. Fred: this file only. Greg: none (read-only + own scratch).
Lou: none in this repo — its outputs go to `~/dg-analysis-runs/2026-08-31-ranking-strategy-v1/`
(`analyze_rank_coverage.py`, `artifact.json`, `evidence_memo.md`), verified read-only here.

**Do not fold `docs/agent-ledger/2026-08-19.md` into any commit — it was already dirty in the
working tree before today and belongs to someone else.**

---

## THE CONVERGENCE — three lanes, one mechanism, independently derived

This is the day's most load-bearing result and it is stronger than any single lane's version,
because Greg and Lou could not hear each other and Bob was working a different lane entirely.

**The mechanism.** A player below the game-count thresholds gets no published dynasty value. The
designed fallback that should rescue them cannot fire.

- **Lou** (read-only audit, its own words): *"The current pipeline drops players below four games,
  then withholds DVS below eight games unless an Engine A prior happens to be present."* Cites
  `src/dynasty_genius/features/feature_assembly.py:138-143`,
  `src/dynasty_genius/models/engine_b_contract.py:129-143`,
  `src/dynasty_genius/pvo_assembler.py:414-468`.
- **Greg** (by EXECUTING the assembler, not reading it): 114 players route to Engine B, receive a
  real projection, and still resolve to null. The Bayesian fallback needs an Engine A prior built
  from draft pick and round, which the active feature table does not carry. **Zero blend firings
  across 866,861 rows over 67 days.**
- **Bob**: 23 of David's own 27 rostered players show a null value on the roster audit route — the
  same phenomenon surfacing on the one roster he actually looks at.

**AND IT IS ALREADY A KNOWN POPULATION.** DG-021, landed 2026-08-25 and recorded in
`docs/agent-ledger/2026-08-25.md`, describes the dead-window else-arm at `pvo_assembler.py:459-467`
running *"precisely because NO Engine A result existed"* and stamping a false `dvs_engine="A"` on
**114 served player cards** — the same 114. DG-021 fixed the **false self-description**. It did not
fix the **absent prior**. So today's three lanes have independently found the root cause of a
defect whose symptom was ticketed and closed six days ago. That reframes it: this is not a new
discovery, it is the unfinished half of DG-021.

### Coverage numbers, corrected in flight
- 12,226 players in the universe; **583 carry a model rank; only 468 actually produce a score.**
  The 114 difference is the routed-but-null group above. *(Bob first quoted 583 as the answer and
  corrected itself after Greg's measurement — the corrected number is the one to use.)*

### Lou's finding that no Claude lane has
`PRODUCT.md:18-22` promises **3–7 year dynasty decision support**, while Engine B predicts a
**two-year** average PPG (`feature_assembly.py:23-28`) and converts it to DVS/xVAR through fixed P90
and replacement transforms (`pvo_assembler.py:391-412,485-502`). A promise/horizon mismatch sitting
underneath every ranking question. **REPORTED BY LOU — not independently verified by Fred.**

### CONFIRMED 08:0x — the premium data never entered the model
**Greg's four refuters returned unanimous: CONFIRMED_BUT_OVERSTATED. Hold lifted, with corrections
that matter. This is the answer to the question David asked both brainstorm lanes at 06:45.**

**CONFIRMED and strengthened — eleven premium columns are computed and never used.** All six NGS
columns plus `air_yards_share`, `route_participation`, `target_share_nfl`, `dropback_count` and
`pass_attempts` are present in the feature file and in **none of the four deployed pickles**.
Ablating every premium feature simultaneously moves **no player more than 4 slots** (QB 4, WR 3,
TE 1, RB 0), Spearman ≥ 0.995 at every position. RB's entire premium block moves predictions by
**0.001 PPG**. So the paid data is not losing to the market — **it never entered the contest.**

**CORRECTED, and the corrections are real.** Greg's opening claim that Engine B is "lagged PPG ×
0.79" is **wrong**. It is a ridge on four inputs: `ppg_t`, the two prior PPG seasons, `age`, and
`games_t`. And the model DOES reorder meaningfully against a naive PPG ranking — **30% of players
move more than 10 slots, 13% move more than 20, largest move 62 slots**, and out of sample it beats
raw lagged PPG by **+0.02 to +0.06 Spearman**. The original R² 0.9208 was inflated by between-tier
variance; **within a PPG quartile it falls to 0.12–0.68.**

**The honest headline: the model beats a naive box-score ranking, but it does so with AGE and GAMES
PLAYED — two free public fields — not with anything David paid for.**

**Reproduction was exact:** applying each pickle's own imputer and model to the runtime features
matches published `projection_2y` to **0.0005 PPG**. So `projection_2y` is raw model output with no
post-processing and **no shrinkage** — which independently confirms the shrinkage hypothesis in the
blast-radius section below.

**Third independent route to the same conclusion.** The refuters' range-restriction result: within a
tier of similar players — which is where every trade decision actually happens — lagged PPG explains
far less of the projection than the headline suggests. **So the box score is weakest exactly where
decisions are made AND where the game-count gate refuses to score. Same population, third time.**

Lou reached a compatible conclusion from the other end without being able to hear Greg: *"large paid
and public datasets terminate in Layer 1 or in parallel feature branches rather than one canonical
training/serving spine."* **REPORTED BY LOU.**

### Checked and CLEAN — recorded so nobody re-spends it
Greg verified the four deployed pickles (`qb_v2`, `rb_v2`, `wr_v2` from `20260513T012309Z`, `te_v3`
from `20260626T165649Z`) are all untracked by git BUT all four are listed in
`app/config/backup_manifest.json`, along with the manifest itself, the two training CSVs and the
runtime feature file. **The deployed models are covered by the backup path.** Reporting the negative
because the check is worth as much as a defect.

### Two topology facts nobody had registered
1. **`~/dynasty-genius-product` is itself a git WORKTREE of `~/dynasty-genius`.** "The trunk" and
   "the product repo" are not the same directory, and nothing in the path says so.
2. **`v2_manifest.json` is gitignored (`.gitignore:60`).** A fresh tracked-files-only worktree — the
   tree `dg-land.sh` builds its gate in — therefore has **no manifest at all**, and no way to
   resolve which pickle is deployed for any position. Distinct from the seven skipping contract
   tests: not a test that skips, a resolution file that is absent. **Whether any land-gate path
   resolves a model by manifest lookup is UNCHASED — Bob's area.** *(Found by Greg.)*

---

## THE GATE'S VERDICT IS A FUNCTION OF WHERE YOU STAND

**Established by Bob, 07:5x, from a red gate on the trunk. This is the most consequential process
finding of the day and it bears directly on the freeze, because every break-glass fix during
09-05..09-10 must pass this same gate.**

Full suite on the trunk: **22 failed / 6,541 passed / 11 skipped in 59s.** The 22 decompose into two
independent defects that are the same defect seen from opposite sides.

**Side one — the trunk runs tests no clean checkout has.** 15 of the 22 come from
`tests/contract/test_governed_cadence_inputs_red.py`, which is **untracked**, dated Aug 8, and whose
own docstring declares it a RED slice for an unbuilt feature. It is supposed to fail. It is collected
by every gate run in the trunk and exists in no clean clone.

**Side two — the land gate silently skips tests the trunk runs.** The other 7 are
`test_qb1_green_correction_contracts.py`, tracked, which passes 177/177 on a clean `origin/main`
worktree. Those 7 are exactly the 7 that SKIP on a fresh worktree, and the skip reason says why in
its own words: *"F25 frozen product set absent (gitignored, backup-manifest-covered store) — the
frozen gate refuses frozen_boundary_drift before the behavior under test is reachable on a fresh
clone."* `dg-land.sh` runs its gate in `~/dg-wt/DG-NNN`, a fresh worktree carrying tracked files
only, so the gitignored model pickles are absent and **these seven contract tests skip on every
single land.**

**So the gate's answer depends on which tree it runs in, and the two trees disagree in opposite
directions. Neither is reproducible from the other.**

### What that let through: the QB-1 frozen boundary has been violated on main since 2026-08-28
- QB-1 pins five artifacts in `F25_FROZEN_SET` (`scripts/run_qb1_study.py:82-98`), resolved against
  the runner's own repo root, never caller-selected. Bob hashed all five: **four match, one drifted.**
- `app/config/model_registry.json` — pinned `307200148e2c251e…`, actual `ad981800711a322e…`.
- The pin matched from 2026-07-02 through **the parent of `99c238d6`**, verified by hashing the
  parent directly: exact match.
- `99c238d6` is **DG-028, 2026-08-28**, which added 17 lines registering `engine_b:v1_fallback`. Its
  own `approved_by` states it is a *"custodial registration… records the ALREADY-served fallback…
  not a new approval."* **The registry change was legitimate and documented. The F25 pin was simply
  never updated alongside it.**
- **DG-028 itself landed through the gate that skips these seven tests. So did all fifteen DG-091
  tickets.** The guard fired correctly the entire time — it fires only in the one tree nobody lands
  from.

**Nuance, so this does not read as worse than it is (Bob's framing, adopted):** this does NOT
retroactively invalidate the QB-1 study. The study ran against the pinned registry. What the drift
means is that **re-running QB-1 today would refuse by name** — the guard doing exactly its job.
Nothing published is contaminated; the boundary moved and nobody was told.

**Not fixed by bumping the pin.** Updating a frozen-boundary hash to turn a red test green defeats
the guard, and this guard protects the study behind David's headline QB model claim. That is a
governance decision and it is David's. **Open — with Bob.**

**Fred's correction, recorded:** Fred hypothesised the untracked 179MB `.oa3/` shadow tree was
polluting a recursive scan, and advised Bob to read the path in the failure message. Both were
wrong — `enforce_consumer_boundary` (`execution.py:640-670`) scans `repo/<scan_root>` so `.oa3/app`
is never reached and it raises a different reason entirely; and Bob had already read the path and
hashed all five artifacts rather than assuming one. The one part that stood was that
`"drifted or missing"` is a two-case check carrying a one-case name, which Bob accepted as a
correction to its own "misnames its own cause" line. **`.oa3/` remains real and worth knowing about
— 179MB, untracked, a complete shadow copy of the repo with its own app/, tests/, frontend/, ops/ —
but it is not implicated in this.**

---

## STANDING HAZARDS — read before touching model code

1. **`XVAR_LAMBDA_ENGINE_B['TE']` must NOT be changed from 0.648 to 0.703.** The "TE is ~8%
   undervalued" finding is **RETRACTED**. `lambda[pos] = P90[pos]/P90['WR']` exactly, and because
   `dvs_raw = projection_2y / _b_p90 * 100.0` the position P90 cancels — editing the lambda alone
   would CREATE an 8.4% distortion. DG-092 (State: done) guards it;
   `src/dynasty_genius/models/engine_b_contract.py:11-31` states the three constants are one coupled
   system; `tests/contract/test_phase15_xvar.py` holds it. Greg derived the same algebra
   independently today without having seen DG-092.
2. **The genuinely open defect in that area is clamp ORDERING** at `pvo_assembler.py:408-409`.
   DG-092 measured 11/89 TEs pinned at the DVS ceiling. Greg measured 23 players at exactly 100.0
   (11 TE — matching exactly — plus QB 0, RB 5-6, WR 6) and added the part DG-092 lacks: **the
   unclamped value is computable one line earlier and is discarded**, and it separates the ceiling
   group cleanly (McBride 156.9 vs Loveland 102.7). The resulting ties are then broken by Sleeper
   snapshot array order and presented to David as a strict rank. The tie-breaking is the worse half
   — a clamp is visible as a cluster at 100.0; array-order ranking looks considered.
3. **"Constant presented as measurement" is a class, not a set of bugs — at least four instances.**
   DG-123 and DG-124 are already filed. Greg found two more in the newest league_opportunity
   artifact: `activity_recency_score` hardcoded 0.0 (`league_opportunity_map.py:185`) and
   `divergence_density_score` saturating to 1.0 for all 41 counterparties (`:184`). Both render to
   David as named evidence. **A constant survives a code freeze perfectly — it never fails, never
   goes stale, never trips a freshness gate.** Nobody has ever swept for this class.
4. **Ablation cannot distinguish "feature is useless" from "feature was never populated."** A
   constant column shows zero importance and looks like a dead signal. Check variance in the feature
   matrix before concluding irrelevance — see hazard 3.

5. **The land gate was scoring with a DIFFERENT model than production ships — until today.**
   `app/services/engine_b_service.py:137` is `self._v2_bundles.get(position) or self._v1_bundle`, a
   silent fail-open. The manifest and every served pickle are gitignored while the **v1** pickles are
   tracked, so a fresh land worktree resolved v1 for every position and validated tickets against a
   build the repo documents as not beating a naive baseline. Share map fixed in dg-build `11021d9`
   *(note the repo — it is NOT a product-repo SHA; that ambiguity cost a lane a verification)*.
   **`:137` still fails open and that remains David's ruling.** Manifest writes are now atomic
   (Greg), so the retrain window that could be observed truncated is closed. *(Found by Bob.)*
6. **`dvs_pct` does not exist in any served payload — it is `valuation.xvar_percentile_position`**
   (`universe_pvo_batch.py:99`). Grepping an artifact or API response for `dvs_pct` returns nothing
   and reads as a dead field. It nearly caused a real defect to be reported as never reaching David.
   *(Found by Bob.)*

---

## Fred — freeze pre-mortem, backups, and the grader

### 07:00 vintage-sync: predicted to fail, fixed, and proven green
`com.davidleess.dynasty-nflverse-vintage-sync.plist` carried **no `EnvironmentVariables` block**, so
`CLOUDSDK_PYTHON` was unset. gcloud's bundled-python branch requires `ARCH = x86_64` (this is arm64)
and the interpreter is not on disk, so it fell through to launchd's minimal PATH where the only
Python is `/usr/bin/python3` = **3.9.6**, which gcloud 581.0.0 hard-refuses. Reproduced safely with
`CLOUDSDK_CONFIG=$(mktemp -d) CLOUDSDK_PYTHON=/usr/bin/python3 gcloud --version` →
`ERROR: gcloud failed to load … Python 3.9`. Bob reproduced it again under a full `env -i` launchd
simulation, both directions.

**Root cause was a half-finished M5 migration.** `git log` on the sibling plist shows commit
`36f2c7cb` *"fix(ops): the offsite backup could not run on the migrated Mac"*, and
`grep -l CLOUDSDK_PYTHON ops/launchd/*.plist` returned **exactly one file**. Scope is closed at two
jobs — only `backup_irreplaceable_data.py` and `backup_nflverse_vintages.py` touch gcloud at all.

**Fixed by Bob on David's direct approval at 06:39:55. Result at 07:00, from the receipt:**
`run_id 20260831T110001Z`, status `completed`, `files_local 1622`, `files_uploaded 38`,
`bytes_uploaded 2,271,638,322`, `sha256_verified true`, `failures []`. **First execution under
launchd in the job's existence.** The `.out`/`.err` log files were created at 07:00 and 07:04,
which retires the earlier absence-of-a-write inference.

**Correction, recorded because it changed a decision:** Fred recommended letting the job fail to
capture a clean reproduction, on the reasoning that nothing was lost. Bob checked the file tree
rather than the receipt summary and found **38 files (~2.1GB) sitting in exactly one copy**. Fred
was wrong and Bob's call was right. The lesson generalizes: *every artifact has an as-of, and the
as-of is the claim.* The `runs` counter and the backup receipt misled today in the identical way.

### `launchctl` `runs` is not evidence — twice over
All dynasty labels read `runs = 0` and `last exit code = (never exited)` after the **05:47:36**
reboot, including jobs that demonstrably ran all week (`nflverse_usage.db`, 12.5GB, written
Aug 30 06:29:49). The counter resets at **label load**, not merely at boot, so `bootout`+`bootstrap`
zeroes it with no reboot at all. **The durable substitute is the receipt** — a run that happened
wrote a file with a `run_id` and a `finished_at`.

### The alarm worked and nobody heard it
`~/DG-CAPTURE-ALERTS.txt` last lines:
```
2026-08-29 GAP com.davidleess.dynasty-model-pvo-refresh: exited 1 on its last run
2026-08-30 GAP com.davidleess.dynasty-nflverse-vintage-sync: never attempted today —
           its 07:00 slot passed and launchd records runs = 0
```
The gap alert **detected the vintage-sync failure a day early, described it correctly, and wrote it
down.** Detection was never broken; delivery was. It reaches a text file and one banner. Note the
irony: its stated reasoning is `runs = 0`, the metric proven unreliable above — right yesterday for
the wrong reason.

`dynasty-model-pvo-refresh` is a **producer that is actually failing** (exited 1 on Aug 29), and it
sits at `[2, 1]` in `capture_gap_alert_state.json`'s `reported_exits`. The dedupe key is
`[runs, exit_code]`; today's reboot reset runs to 0, so its next failure writes `[1, 1]` and every
post-reboot first-run exit-1 after that is **silently suppressed**.

### Backup gap: the crosswalk is in no copy
`app/data/identity_snapshots/` is `??` untracked and **not gitignored** — simply outside version
control. 23MB, three files, `latest.json` → the 08-29 snapshot, 12,484 rows. Decisively:
**`app/config/backup_manifest.json` does not contain it.** The manifest protects the four capture
DBs, the training CSVs, `model_registry.json` and every model pickle. So the nightly backup is
working correctly and has never had a reason to touch this directory.

**It is load-bearing in two places:** it is the crosswalk that resolves David's roster, **and** the
realized-outcome grader consumes it via `OutcomeIdentityBridge.from_identity_snapshots`
(`run_realized_outcome_scoring.py:385`).

**Severity, stated accurately rather than alarmingly:** the producer
(`run_ff_playerids_snapshot_capture.py`, 06:45 daily) re-fetches a published upstream set, so the
**current** crosswalk is reconstitutable tomorrow. What is unrecoverable is the **dated history** —
which is exactly what a grader replaying a past week needs. **Losing it costs reproducibility, not
capability.**

### The grader has never graded, and in the shipped config it cannot
`week_status` finalizes only on the literal `"final"`
(`outcome_forward_capture_store.py:67,109-111`). The shipped `_default_schedule_loader` emits
`"result_observed_unverified"` for a played game and `"scheduled"` for an unplayed one — commit
`17cfc1e9` (2026-08-14). `main()` hardcodes the loaders: no flag, no env var, no config override, so
`--season/--week` backfill is blocked too. Proven by probe twice independently: a fully played Week 1
with all 16 games scored returns `not_finalized`; force the token to `"final"` and it finalizes.

**The machinery works.** The 2026-08-18 rehearsal drove the real 501 frozen predictions through the
real identity bridge, scorer and writer to a `status: "ok"` scorecard grading 405 players, with a
shuffled-projection control collapsing Spearman from ~0.92 to −0.13. It discriminates.

**Both safety nets fail in the same direction.** `_resolve_season_week` uses the same never-true
predicate to step the target back a week (`:983-992`), so the target is perpetually ~1 day old and
the >14-day `week_nonfinal_overdue` alarm cannot fire until **2027-01-19**. And `noop` is registered
as a **success** status on a row that cannot degrade the rollup, so the surface reads *"Realized
outcomes — Fresh"* behind a *"Synced"* pill. **Every Tuesday all season it would grade nothing and
report itself healthy.**

**Killed during investigation, recorded so nobody re-raises them:** the missing weekly "freeze a
prediction set" step is not missing (the set is per-season and David declared it 2026-08-13; 581
declared / 501 eligible). The `FrozenPredictionSetUndeclared` failure was the resolver aiming at
undeclared season 2025, fixed by `9a38dc7e` on 08-19. The `Errno 24 Too many open files` crash was
fixed 2026-07-11 by `9b5fd230`; measured at full-season worst case against launchd's 256 limit, the
repaired path peaks at **4** descriptors.

**Unresolved disagreement, not smoothed over:** one agent calls `17cfc1e9` a regression, another
calls it a ratified invariant pinned by a passing contract test
(`test_score_derived_default_schedule_never_certifies_finality`). Both appear true — the door was
closed deliberately and the thing meant to reopen it was never built. **"Deliberate and unfinished"
is the defensible phrasing; "regression" is not.**

### Blast radius of Bob's roster fix — answered, and it blocks a quiet repair
**Restoring `dvs` is not a cosmetic repair; it changes what the product recommends.**
- `frontend/src/what-changed/morningRead.ts:392` — a recommendation candidate qualifies only if
  `xvar_pct != null || dvs != null`. The withheld players fail on both terms, which is *why* they
  have never appeared. Restore the field and they become **eligible**: new "Worth a look" cards for
  players the product has never recommended.
- `morningRead.ts:674-678` — the card then **prints the number as its stated reason**: *"his dynasty
  value is X."*
- `morningRead.ts:660-664` — the frontend is **not** sent `scoring_tier` or `engine_path`; it infers
  ranked-ness from the presence of these fields. Populating them for players whose tier has not
  changed breaks an invariant the frontend documents relying on.

**Greg's shrinkage hypothesis makes this sharper.** The estimator applies no shrinkage — it carries
a 4-game PPG forward at ~0.89x much as it carries a 17-game PPG at ~0.92x, which is why an 8-game
hard gate exists at all: a cliff standing in for regression to the mean the model never performs. If
so, the withheld scores are not corrupted, they are **unshrunk** — well-formed, plausibly-scaled,
and systematically overconfident. **Every numerical-sanity check would pass them.** An unshrunk
4-game estimate quoted to David as "his dynasty value is X" is worse than a blank.
*(REPORTED BY GREG, offered as a hypothesis with a measurement behind it.)*

---

### 08:4x — LANE CONTINUITY: Fred crashed and was re-seated

Fred's session (`davidleess-4d`, pid 3151) died on an API error at **08:39:31** mid-turn. David
re-seated the lane at 08:39:46. **Fred is now `davidleess-45`** — the old handle is dead and mail
sent to it is lost. Verified no live process held 4d before the seat was claimed
(`ps -eo pid,ppid,tty,lstart,args | grep [c]laude` → three sessions: a0/ttys000, 0b/ttys002,
45/ttys001), per `cockpit_session_identity`. There was never a second Fred.

**Nothing was lost.** Re-verified on disk rather than inherited from the dead transcript:
trunk on `main` at `8695bab1`, `0 behind / 0 ahead` of origin. The boot-scoped dedupe fix is
still in the working tree — `scripts/run_capture_gap_alert.py` (+18) and
`tests/contract/test_dg044_capture_gap_alert_red.py` (+50) — and re-run just now: **78 passed**.
`rt.boot_time` is a real `Runtime` field (`run_capture_gap_alert.py:605`) with the call site
wired at `:884`, so it is not an AttributeError waiting to happen. **Still uncommitted, held on
David's word.**

### 08:4x — Bob's gate finding INDEPENDENTLY VERIFIED by Fred

Bob reported that every land has been gated against `engine_b_v1` rather than the served models.
**Confirmed from artifacts, not accepted on report:**
- `app/services/engine_b_service.py:137` reads
  `bundle = self._v2_bundles.get(position) or self._v1_bundle` — a real fail-open. *(Note the path:
  it is `app/services/`, not `src/dynasty_genius/models/` as the relay had it.)*
- `git check-ignore -v` → `.gitignore:60` ignores `v2_manifest.json`, while
  `git ls-files app/data/models` lists the **v1** pickles as tracked. The asymmetry is exactly as
  described.
- `11021d9` on dg-build is real (08:46:32 today) and `app/data/models` is present in
  `SHARE_PATHS` at `bin/dg-work.sh:38`.

**Fred's addition — the "recreate your worktree" advice is SAFE HERE, and that needed checking.**
`reference_backup_architecture` warns that a `~/dg-wt` worktree is in no backup copy, so recreating
one can destroy unpushed work. Checked all nine: **every ticket branch has an upstream and none is
dirty except `DG-014`, whose 72 dirty files are ALL deletions (`git status --porcelain` → 72 × `D`),
every one of them under `app/data/backtest`, `app/data/identity` or `app/data/sources` — i.e. shared
paths, not authored work.** Recreating loses nothing. Recording the negative so nobody re-spends it,
and so nobody hesitates.

**Consequence Fred accepts for its own landing:** the alarm fix must land from a worktree created
**after `11021d9`**, or the gate that passes it is not the gate it appears to be. The seven
frozen-boundary tests now run rather than skip, and pass only because of `88451a1e`.

### 09:0x — the stray `league_history.db`: creator ESTABLISHED, and Bob's remedy corrected

The anti-rot guard failed on `app/data/league_history.db`. **Bob was right that the fix is removal,
not a manifest entry** — covering a stray would train the guard to accept junk. Independently
measured the same three facts before his message arrived: 0 bytes, 0 tables, and
`grep -rn "league_history" --include="*.py"` returns nothing.

**The creator is now established rather than guessed.** Codex's own history
(`~/.codex/thread_history_1.sqlite`, `thread_items`) records at **08:21:51** — the exact second
`stat` gives as the file's creation — Lou running, from cwd `dynasty-genius-product`:

```
sqlite3 app/data/league_history.db '.tables'
```

`sqlite3 <path>` **creates the file on open**, even for a read-only-looking query. Lou was probing
for a store it names logically in its memo (`league_history.transactions`,
`league_history.player_movements`); the store does not exist under that filename, and the CLI
obligingly made an empty one. The real store is `app/data/league_transactions.db`, already covered.

**Correction to the proposed remedy — this matters, so it is recorded rather than left.** Bob
suggested a product-side guard refusing to open a store path absent from the cadence config. **That
would not have prevented this.** The creator is not product code; it is the `sqlite3` CLI invoked
from an agent's shell, which no application guard can intercept. The effective convention is for
every lane probing a store to open it read-only —
`sqlite3 "file:<path>?mode=ro"` **errors instead of creating**. That is a one-line habit, not a
build.

**NOT established, and not assumed:** the earlier singular `app/data/league_transaction.db` Bob
removed at 07:23. Codex history contains no command carrying that string, so the same-creator
inference is unsupported. Two strays with a shared shape is a pattern worth watching, not a proven
common cause.

Preserved rather than deleted (0 bytes, so nothing was at stake, but the habit is cheap):
`scratchpad/preserved-stray-league_history.db`.

### 09:0x — trunk is FULLY GREEN, and Fred's fix is on origin

```
6552 passed, 11 skipped, 0 failed  (55.19s, full suite, trunk)
```
The 22 failures the gate section above records are gone: 15 were the untracked RED slice, now
preserved in `dg-build/preserved/`; 7 were the frozen-boundary tests, fixed by Bob's `88451a1e`
re-pin; the 1 remaining was this stray.

Landed and verified against the remote with `git rev-list --left-right --count`, 0/0 — not inferred
from a command that merely ran:
  - `200af3f9` fix(alarm): a failing producer went silent after every reboot
  - `f4e57737` docs(ledger): this file

### 09:1x — ⚠ THE PROVENANCE TOOL WAS BLIND TO 17 OF DAVID'S MESSAGES

**Every "the record shows…" claim made before dg-build `d0b002f` rested on a partial record.
Re-derive rather than inherit.** *(Consequence flagged by Bob; recorded here at its request.)*

Bob challenged GOVERNING RULING 4 as unsourced — it could find no 07:45:56 message in any user
record, and `rulings.py` showed nothing at that time. **The challenge was sound and the conclusion
was wrong, because the tool could not see the message.**

The record, from `bf2ad4c4.jsonl`:
```
{"type":"queue-operation","operation":"enqueue","timestamp":"2026-08-31T11:45:56.026Z",
 "content":"the assumption should be that my competition look at sites like Dynasty Nerds,
            Fantasy Pros, Keep Trade Cut, and other popular sites, in addition to box scores"}
```
11:45:56.026Z = 07:45:56 EDT, the cited second. Content matches the ruling character for character.
The payload is **bare** — no `<cross-session-message>` or `<task-notification>` wrapper, unlike every
peer message in that window. At 11:46:18 the same content is removed with
`"reason":"absorbed_mid_turn"`. **Ruling 4 stands exactly as written.**

**The mechanism, and it is general:** when David types while a lane is mid-turn, the message is
absorbed into the running turn and **never becomes a standalone `user` record**. `rulings.py:87` was
`if d.get("type") != "user": continue`, so it was structurally blind to that whole class.

**Measured across today's four Claude sessions: 17 inputs invisible this way** — 16 his own words,
1 a paste-back. They include **all three lane NAMES** ("call this lane Fred", "call this lane Bob",
"Call yourself (this lane) Greg"), ruling 4, "what should Lou be doing?", "i don't want pushes to be
blocked" — and, pointedly, `08:05:54` *"Greg says you reversed the roster-fix sequencing in his
session. You told me to hold it. Which stands?"* **The message that set off the morning's retraction
cascade was unreadable by the tool built to settle exactly that.**

**Fixed:** dg-build `d0b002f` reads queue enqueues, deduped by content (a queued-but-not-absorbed
message appears both ways; the enqueue timestamp wins — it is when he pressed enter).

**And the fix immediately produced a false positive of its own, caught and fixed before it could be
used.** `08:05:54` is David *pasting a lane's own AskUserQuestion prompt* — question plus three
numbered options — into a different lane. Genuinely his input; **not his instruction.** Listed
unqualified beside his rulings, the next reader attributes those options to him. `f1ecd82` renders
pastes as `❝ QUOTED`, labelled his input and NOT his instruction. Tool now: **58 entries, 24 explicit
rulings.**

**Re-verified against the complete record** rather than trusting the morning's conclusion: the
sequencing chain is `07:55:14` Bob *"Hold the whole fix until TE is resolved"* → `08:02:37` Greg
*"Unblock the roster fix now, then TE, then foundation"* → `08:06:45` Bob *"Ship the roster fix now,
then TE"*. **The morning's reconciliation was correct** — Bob's attribution was right — but it is now
confirmed from a complete record instead of a partial one.

### 09:1x — the "safe verification" that publishes live — FIXED

`run_pvo_refresh.py`: `--pvo-artifact-path`/`--coverage-artifact-path` steer the **candidate** pair;
the live publish target is `--runtime-dir`, a differently-named flag **defaulting to
`app/data/valuation_runtime`**. An operator who redirects every artifact-path flag they can find —
the careful thing — still writes production. **The more cautious the operator, the more certainly
they clobber serving state.** *(Found by Bob, whose mtime guard caught it; framing and fix mine.)*

**Correction to how this was first described, including by me:** the flags are not broken and they
do redirect. The trap is the **naming and the default**, and the script already ships `--preflight`,
which resolves every path and writes nothing — the exact tool that would have exposed it.

`1495144d` refuses the one incoherent combination and names the live target. **It cannot fire in
production:** neither the plist nor `run_daily_chain.py` passes the candidate flags at all — pinned
by `test_production_argv_shape_is_never_refused`. `--preflight` is checked *before* the guard, so the
inspect-without-writing path stays open in exactly the case that refuses.
Suite: **6555 passed, 11 skipped.**

**STILL OPEN:** of the four publish-defaulting chain runners (`run_daily_chain`,
`run_feature_refresh`, `run_market_divergence_refresh`, `run_pvo_refresh`),
**`run_market_divergence_refresh.py` is the only one with no `--preflight` at all** — no way to
resolve its paths without running it.

### 10:0x — preflight now declares a WRITE PLAN, not a settings dump

`40b6de69`. Every publish-defaulting runner printed resolved settings on `--preflight`. That is a
different question from the operator's, and the gap is what let a careful operator publish live.
A settings dump prints `"runtime_dir": "app/data/valuation_runtime"` among a dozen sibling keys; the
reader sees a **setting**. Nothing says it is the serving directory, nothing says this run overwrites
it, nothing separates written paths from read ones.

`src/dynasty_genius/write_plan.py` answers the real question — *what will this write, and is any of
it live?*
```
"verdict": "WILL OVERWRITE 3 LIVE SERVING ARTIFACTS. This is NOT a sandboxed run."
```
LIVE = lands inside the repo's `app/` tree **after resolving symlinks** — the head_a destruction the
same morning was a write that reached the trunk THROUGH a symlink, and a rule judging the spelling
would have called it sandboxed. Wired **additively** into both runners, so `5f06a4a8`'s payload
contract is intact. On real defaults: market-divergence **5 live writes**, pvo **3** — and that 3 is
exactly the three artifacts the mtime guard caught. Suite 6565.

### 10:1x — ⚠ THE GRADER CANNOT RAISE AN ALARM, BY CONFIG. **David's decision.**

Chasing alarm fix 2 (`noop` counted as success) found something that outranks it.

The grader emits **four** distinct no-op reasons, and only one is pathological: `already_scored` is
genuinely healthy-idle; `week_not_finalized` fires **forever** because no week ever finalizes.
Treating all `noop` as failure would cry wolf; separating them properly means changing a shared
health contract with heavy validation. **Not attempted at the end of a long session — deliberately
left rather than half-built.**

**But the row is `tier: auxiliary`, and `system_health_models.py:74` is explicit — auxiliary
staleness NEVER degrades the root; `:653` only appends `auxiliary_info_only`.** So even a perfectly
honest row leaves *"has this product ever checked whether it is right?"* structurally incapable of
dimming anything on David's screen. **The most important question the system can ask about itself is
filed under information that cannot raise an alarm.** Not a code defect — the code does what it says.
A judgement made once in a config file.

**Fred's recommendation: promote it out of auxiliary.** It changes David's top-line during the
season, so it is his call and is now with him.

### 10:1x — the manifest write is atomic. **Taken by Greg, verified by Fred.**

`train_engine_b.py:378-380` was a plain truncating `open(path,"w")` + `json.dump`, opening a window
where `v2_manifest.json` is empty and `engine_b_service.py:136`'s silent `or self._v1_bundle`
fail-open serves **v1 for every position, on the live product, mid-retrain**.

Greg took it as a **precondition** of its own retrain rather than an adjacent nicety — the right
call, and better than handing a known hazard across a lane boundary. Verified independently by Fred
rather than accepted on report: `mkstemp` in the destination dir, `os.replace`, cleanup on
`BaseException` (not merely `Exception`, so an interrupt leaks no temp file), matching the repo's
existing `_atomic_write_json` pattern. **3 tests pass**, and they assert behaviour — a previous
manifest surviving a mid-serialisation death — rather than that a helper was called.

*Noted, not a defect:* **no atomic writer anywhere in this repo fsyncs before replace.** `os.replace`
is atomic against readers, so the fail-open window is genuinely closed; durability across power loss
is a separate, repo-wide, and for regenerable artifacts acceptable property. Greg matched the house
pattern exactly.

**Sequencing now holds by construction:** the atomic write lands BEFORE anyone converts the fail-open
to a hard error. The other order would turn a brief silent degradation into a real mid-retrain
outage. `engine_b_service.py:136` remains untouched and remains David's ruling.

### 10:3x — ⭐ THE DAY'S CONCLUSION FOR THE MEASUREMENT LANE

**The instrumentation works. The delivery is what fails — and today proved it three times over.**
*(Bob's formulation, adopted; verified independently by Fred rather than accepted on report.)*

Three consecutive days, three correct and specific alerts:
```
08-29  GAP  a pvo retry refusal
08-30  GAP  com.davidleess.dynasty-nflverse-vintage-sync: never attempted today
08-31  GAP  chain step run_pvo_refresh: recorded exit_code 1
```
The 08-31 line named the **exact failing step out of six**, on schedule, with no idea the culprit was
the agent reading its own output. **Every one of the three reached a human only because somebody
happened to open a text file.**

**And the alarm does not cry wolf.** Bob predicted, in advance and falsifiably, that the 10:30 run
would say nothing about the backup while one was genuinely in flight. It said nothing.
`grep -ci backup ~/DG-CAPTURE-ALERTS.txt` → **0** across the file's entire history, so
`_run_still_in_flight` has never produced a false positive. Verified by Fred.

**The delivery channel is the defect:** `run_capture_gap_alert.py:1030` fires an `osascript display
notification` — a macOS banner that disappears in seconds — plus an append to a text file in the home
directory. `_live_notify`'s own docstring (`:1019-1020`) already concedes that an osascript exit 0
**cannot prove** the message was seen.

**This converges with the grader-tier finding into ONE decision, not two.** Both are the same
sentence: *the product does not tell David on the surface he actually opens.* A grader that cannot
dim the root, and an alarm that lands in a text file, fail identically — the system knows, and the
knowing does not arrive. **Recommended: the morning read is the delivery channel for both.** David's
call; recorded here rather than acted on.

*Not tidied:* Bob declined to re-run the chain to clear the red line, on the grounds that a green line
it manufactured is worth less than a red line that is true. The line reflects the recorded last run;
tomorrow's 09:00 clears it honestly. **Recorded because it is the right instinct and the opposite one
is tempting.**

### 10:5x — 4.52GB of orphaned backup scratch, reclaimed and the leak closed

*(Surfaced by Bob as unowned; taken by Fred as backup-lane work.)* `run_backup` rmtree's its **own**
staging directory on the way out (`backup_irreplaceable_data.py:416`) and **nothing has ever swept
the root**, so a run killed mid-flight leaks its scratch permanently and no check notices.

Measured: `20260801T141848Z` 1.9G and `20260812T141500Z` 2.4G. **This module's own header comment
records the 08-01 mid-flight kill** — the event was documented, the disk it cost was not.

`sweep_stale_staging()` now runs at the top of every backup, with three deliberate refusals: never
the CURRENT run (a sweep that can delete the directory it is about to fill is worse than the leak),
never anything younger than **48h** (a run takes ~36 min, so a concurrent run is safe by an enormous
margin rather than a tight one), and never a name that is not a run id (something else put it there;
guessing is how a sweep becomes a defect). **It never raises** — it runs at the top of a backup and
bookkeeping must not cancel one, the same rule this module already states for its marker write.

Reported to the caller and the log, **deliberately NOT added to the persisted marker** — that file is
`backup_status.v1` and has health-layer consumers; housekeeping is not smuggled into a versioned
schema.

**Reclaimed today: 4,515,002,167 bytes.** Done by invoking the *tested* function rather than a hand
`rm`, and only after confirming the 10:15 run had finished (`status completed`, 709 files,
`sha256_verified true`, 14:52:01Z) so no live staging could be touched.
6 tests, RED first. Suite **6579 passed, 11 skipped**.

## OPEN DECISIONS — David's, in dependency order

1. **The finality seam.** Let the schedule loader certify finality itself (smallest change, restores
   grading, but accepts "scored in the feed" as proof a game ended); build the governed finality
   authority (honest, unbuilt, riskiest four days before a freeze); fix only the alarms and grade by
   hand via `run_scorer_rehearsal.py --outcome-season 2026` (no code change); or freeze as-is and
   accept that the product cannot learn it is wrong for the season. **The two alarm fixes are worth
   doing under every option.**
2. **The roster fix.** Bob is holding it pending the trustworthiness answer. It is a product
   decision, not a bug fix, because it changes recommendations on David's own roster nine days
   before he starts trusting them.
3. **`identity_snapshots` protection.** Add to `backup_manifest.json`, or accept losing dated
   history and with it the ability to replay a past week.
4. **DG-102 / the frontend-blind land gate**, and the pre-freeze checklist which is stale in four of
   ten items. Both bear on any break-glass fix during 09-05..09-10.
5. **Season 2027**: nothing automated will ever declare a frozen prediction set, so the
   `FrozenPredictionSetUndeclared` failure returns next off-season.

---

## Bob — ops, gate integrity, roster serving

*Append-only. Everything below is mine unless labelled REPORTED BY.*

### LANDED AND PUSHED

| commit | repo | what |
|---|---|---|
| `189e68f0` | product | vintage-sync plist — `CLOUDSDK_PYTHON` |
| `88451a1e` | product | QB-1 F25 frozen boundary re-pinned |
| `8695bab1` | product | roster serving fix — 4 of 27 → **24 of 27** |
| `11021d9` | dg-build | worktrees share `app/data/models` |
| `10562f9` | dg-build | DG-102 — the land gate can see the frontend |

Product gate at each land: **6552 passed / 0 failed / 11 skipped.** dg-land harness **21 → 32 checks.**

### 07:00 — the vintage sync had never once run on its schedule
Predicted the failure, fixed it, watched it run. `env -i PATH=/usr/bin:/bin:/usr/sbin:/sbin
/opt/homebrew/bin/gcloud --version` → *"gcloud failed to load… Python 3.9"*; add
`CLOUDSDK_PYTHON=/opt/homebrew/opt/python@3.14/bin/python3.14` → *"Google Cloud SDK 581.0.0"*. Both
directions, so the variable is the whole difference. Only `backup-irreplaceable` carried it (fixed
by `36f2c7cb` after the M5 migration); this job was written later and never inherited it. Those two
are the ONLY gcloud-calling launchd jobs, so the migration remediation is now complete.
Result `20260831T110001Z`: completed, failures [], **38 files / 2,271,638,322 bytes uploaded**.

**My correction to Fred's (b) recommendation, which is the transferable part:** Fred reasoned from
the Aug-30 receipt that nothing was at risk. The receipt was accurate about 13:08Z on Aug 30 and
silent about everything after. `find app/data/nflverse_usage/raw -type f | wc -l` → **1622** against
the receipt's 1584 — 38 files, 2.1G, in exactly one copy. **We both reasoned from an artifact's
summary; only one of us measured the thing it summarised.** Every artifact has an as-of, and the
as-of is part of the claim.

### The gate's verdict was a function of which tree it ran in — now closed
Three faces, one defect. Third one is new and is the worst:

**Every land has been gated against `engine_b_v1`, not the models production serves.** Same commit,
two trees: land worktree → v2 bundles **NONE**, falls back to v1 for every position; trunk → QB
`engine_b_v2_qb`, RB `engine_b_v2_rb`, WR `engine_b_v2_wr`, TE `engine_b_v3_te`. Mechanism:
`v2_manifest.json` and every served run dir are gitignored while the **v1 pickles are tracked**, so
`_load_v2_bundles()` fails the manifest read, logs an error nobody reads, returns `{}`, and
`engine_b_service.py:137` — `self._v2_bundles.get(position) or self._v1_bundle` — silently
downgrades. v1 is the build the repo documents as not beating a naive baseline.
Fixed by sharing `app/data/models`: **177 passed/7 skipped → 184 passed/0 skipped**, bundles
NONE → v2/v3. (REPORTED BY Greg: the gitignored-manifest instance. I measured the resolution.)

**⛔ STILL OPEN, David's call, NOT mine to make:** `engine_b_service.py:137` **fails open**. A missing
manifest should arguably be a hard error, not a silent downgrade to a superseded model. Serving code,
model lane, changes production behaviour.

### The QB-1 frozen boundary had drifted for three days
Pin `307200148e2c251e…` held from 2026-07-02 through the **parent of `99c238d6`** (hashed the parent
directly). `99c238d6` = DG-028, 2026-08-28, custodially registering `engine_b:v1_fallback` — a
legitimate change whose own `approved_by` says "not a new approval". The pin was never updated with
it. **Nothing published by QB-1 is affected**; only a re-run would have refused. Re-pinned in BOTH
places on David's word — and the double entry CAUGHT ME when I edited the runner alone. Good guard.

### The roster screen threw away 20 scores it already had
`roster_auditor.py:643-645` joined on `gsis_id`, which Sleeper returns null for on **all 27** of
David's players. Same server, same minute: `/api/players/12527` → 75.3 ACTIVE_B while
`/api/roster/audit` → null PRE_MODEL; endpoint reported `dropped_player_count: 0` while doing it.
Fix reads the already-adjudicated universe row, so it **inherits** the 8-game refusal rather than
recomputing it — a naive key swap would have printed Wilson **77.6** and Allen **31.2**, two invented
numbers. Live: 24 of 27, three correct blanks (Wilson 7 games, Allen 4, Dell no 2025 season).
Also retired **instance five of the constants class**: `signal_completeness` was `4 ÷ 17` = 0.2353
for everyone, and would have dragged a *"do not use for dynasty decisions"* caveat under every
restored score. Real values now 1.0 / 0.9412 / 0.8333.

### Corrections I owe the record
- **My own bug, published because it biased toward the null:** I reported "19 of 79 sells are
  rookies". My expression was `(r[3] or 9) <= 1`, and `0 or 9` is `9` — every rookie silently became
  a ten-year veteran, making the age finding look *weaker*. Greg's 49 is right. A truthiness bug that
  argues against your own thesis is the most dangerous kind, because nobody re-checks it.
- **`~/dynasty-genius` is NOT a stale clone** — it is the MAIN WORKTREE of the same repository, with
  the product dir as a linked worktree of it. It is behind only because it sits on
  `feature/qb-context-annotations`. I told two lanes otherwise this morning. (REPORTED BY Greg.)
- **The GitHub token does NOT leave the machine.** `backup.sh:29` is `jq 'del(.env)'`, and I checked
  the committed artifacts rather than the script's intent: all three backed-up settings files have no
  env block and no token. `git grep` across all three repos returns two hits, **neither matching the
  live token by sha** — one a deliberate fixture, one a function name. Plaintext in one mode-600 file;
  worth moving to the keychain, not worth an emergency rotation.

### Age bias — independently reproduced, not taken on report
Ran it myself off `universe_market_divergence_latest.json`: BUY n=85 mean age **27.41**, SELL n=79
**23.84**, gap **+3.57y**, `corr(age, model_minus_market_delta)` = **+0.3921** over n=333, signal
counts identical to Greg's and Fred's. **Three independent reproductions.**
The dead end, so nobody re-walks it: `universe_pvo_runtime.json` has `divergence` **null on all
12,226 rows** and no `market_overlay` — the served object carries no market data at all.

### Ops state at 09:00
15 dynasty labels; 06:15 nflverse, 06:30 league-transaction, 06:45 ff-playerids, 07:00 vintage-sync
all fired clean; 09:00 chain fired 09:00:09. Machine on **AC** (the memory's bold "PLUG IT IN" was
stale). API restarted 08:18 on David's word to serve the roster fix; cold 0.74s / warm 0.58s at
load 1.61. **`launchctl runs` resets at LABEL LOAD, not just boot** — so the recorded "require
runs>=1" Tuesday-check remedy is itself insufficient. Use the receipt.

**Mine and unstarted:** `dynasty-model-pvo-refresh` is genuinely failing (exit 1, 08-29), deduped at
`[2,1]`, and today's reboot zeroed its count — Fred's boot-scoped fix will unmask it, which is
correct. I have not touched it.

### ADDENDUM 10:50 — four more lands, one self-inflicted incident, three corrections

| commit | repo | what |
|---|---|---|
| `11021d9` | dg-build | worktrees share `app/data/models` |
| `10562f9` | dg-build | DG-102 — the land gate can see the frontend |
| `5f06a4a8` | product | `--preflight` for run_market_divergence_refresh |
| `8cc44eef` | product | tie-aware midranks |

**⛔ THE GATE WAS VALIDATING A DIFFERENT MODEL THAN PRODUCTION SHIPS.** Same commit, two trees: land
worktree → v2 bundles **NONE**, falls back to v1 for every position; trunk → QB `engine_b_v2_qb`,
RB `engine_b_v2_rb`, WR `engine_b_v2_wr`, TE `engine_b_v3_te`. `v2_manifest.json` and every served
run dir are gitignored while the **v1 pickles are tracked**, so `_load_v2_bundles()` failed the
manifest read, logged an error nobody reads, returned `{}`, and `engine_b_service.py:137` —
`self._v2_bundles.get(position) or self._v1_bundle` — silently downgraded. v1 is the build the repo
documents as not beating a naive baseline. Fixed by sharing `app/data/models`; `share_path` already
handles the mixed tracked/ignored tree so no run-id list is needed. **177 passed/7 skipped → 184/0.**
⚠ **STILL OPEN, David's, not mine to take:** `:137` FAILS OPEN. A missing manifest should arguably be
a hard error, not a silent downgrade to a superseded model. Serving code, model lane.

**DG-102 CLOSED.** `dg-land.sh` now runs `npm run gate` always-on (5.6s) and **REFUSES the land if
npm cannot be resolved** rather than skipping — npm is nvm-managed and absent from a minimal PATH.
Harness 21 → **32 checks**, proving fire, pass, and the npm-absent refusal. Scope stated in-code: no
Playwright, so this is not visual or a11y coverage.
⚠ Consequence: those 7 frozen-boundary tests now RUN on every land. They pass only because the F25
pin was corrected this morning. The first time someone touches `model_registry.json` the gate will
REFUSE the land — that is the alarm finally being connected, not a new breakage.

**TIE-AWARE MIDRANKS.** 23 tie groups, 64 players given a fabricated ordering. The clamp pins 11 of
89 TEs at exactly DVS 100.0 and they received **eleven different percentiles** — Kelce 90.9 below
Fannin 92.0, separated by nothing but sort order. Average rank now; untied populations arithmetically
unchanged and the three pre-existing contract tests pass unmodified. The correct pattern already
existed at `market_overlay_service.py:46-53`.
⚠ **NAMING TRAP, worth everyone's time:** this value reaches artifact and API as
**`valuation.xvar_percentile_position`**, NOT `dvs_pct` (`universe_pvo_batch.py:99`). Grepping a
served payload for `dvs_pct` returns nothing and looks like a dead field. It nearly made me report
the defect as not reaching David.

**⛔ I DESTROYED TWO TRUNK FILES AND RESTORED THEM.** Building the gate fix, a loop wrote into a
directory it had ALREADY symlinked, so `rm -rf`/`ln -s` followed the link into the real trunk and
replaced `app/data/models/head_a/{v3_manifest.json,runs}` with symlinks pointing at themselves.
That is what failed the 09:00 chain's `run_pvo_refresh` (`[Errno 62] Too many levels of symbolic
links`). Restored from `gs://…/runs/20260830T141500Z/` on David's word and **verified by sha256
against `model_registry.json`** (`9e1b0b7f…`), then confirmed byte-identical in today's backup
staging. Product never affected — serving reads the engine_b manifest, not head_a.
**Rule: never `ln -s` into a tree you have already symlinked; check every ANCESTOR with `-L`, not
just the leaf.** `dg-work.sh:share_path` already does this correctly — read it before writing your own.

**THREE CORRECTIONS I OWE THE RECORD:**
1. **`run_pvo_refresh`'s flags are NOT broken.** I said they were. They redirect fine; the decisive
   flag is `--runtime-dir`, defaulting to `app/data/valuation_runtime`, and I missed it after my own
   grep showed unidentified args. `--preflight` existed and would have shown me. Fred was right.
2. **Ledger ruling 4 is GENUINE.** I challenged it as possibly an agent quoting its own shell output.
   Verified from `queue-operation` records: `11:45:56.026Z enqueue`, bare payload, then
   `absorbed_mid_turn`. David typed it. My searches were sound and my wording hedged, but the
   impression I left was false and I corrected it to him plainly.
3. **`team_postures` DOES carry `team_name`**, 9 of 12 populated — roster_ids 2, 5, 8 are genuinely
   unnamed in Sleeper. I told David the field was absent entirely. Data gap, not plumbing, and smaller.

**✅ THE DETECTION LAYER WORKS; DELIVERY IS WHAT FAILS.** The 10:30 alert reported my own damage,
naming the exact failing step of six, on schedule. Three days running it has been right and specific
— vintage-sync never firing (08-30), a pvo retry refusal (08-29), head_a today. **All three reached a
human only because somebody opened a text file.**
And one thing that is RIGHT BY DESIGN and must not be "fixed": `inspect_backup_marker`
(`system_capture_health_models.py:1077-1116`) makes an absent sentinel benign ON PURPOSE with the
26-hour `finished_at` law as the real guard — a sentinel can only ADD a reason, never suppress one,
because every healthy run violates sentinel-vs-marker for its whole duration. Predicted before the
event and confirmed: a backup in flight at 10:30 produced **zero** backup lines, and `grep -ci backup`
over the entire file's history is **0**. That is the difference between a silence that is a bug and a
silence that is a decision.

**Still mine, still unstarted, waiting on David:** League Pulse identity (30 cards, no player names,
producer computes a full `asset` and `map_card`'s `common` dict drops it — needs an assembler change
plus an additive OpenAPI regen) and DG-121's drift detector (confirmed LIVE: the served bundle was 1
commit behind HEAD and nothing compares them; I ran the ritual, it now matches).
**Also open and not mine:** 4.3GB of orphaned backup staging from runs that died 08-01 and 08-12 —
cleanup only sweeps the current run's subdir, so a hard-killed run leaks permanently.


## Greg — *(append here)*

## Lou — *(append here; David relays)*
