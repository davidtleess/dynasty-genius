# 2026-08-19 — DG-014 Agent Ledger

## 16:4x ET — Claude Consultant: DG-014 evidence complete — the decision is David's `[w#DG-014]`

- **Claimed** `DG-014` via `dg-work.sh DG-014 --from main`; worktree `~/dg-wt/DG-014`.
- **Ticket headline independently confirmed, having first doubted it.** I initially believed the
  deployed fit also included 2025, because 2025 rows are present in the matrix and are not in
  `HOLDOUT_SEASONS`. Measured: 2025 has 505 rows, **zero** outcomes in `avg_ppg_t1_t2`, and
  `training_eligible=0` throughout — its t+1/t+2 result has not occurred. Those rows cannot enter a
  fit. **The deployed models really were fit on 2018–2021 only.** My QB count reproduces the
  ticket's 169 exactly.

| | rows | seasons |
| :-- | --: | :-- |
| fitted | **1,492** | 2018–2021 |
| gradable, withheld | **744** | 2022–2023 |
| total available | 2,236 | — |

  Per position the withheld share is uniform: QB +56%, RB +48%, TE +50%, WR +49%.

- **What this means in plain terms.** The newest season the deployed model has ever trained on is
  **2021**. Four seasons have been played since — 2022, 2023, 2024, 2025 — and it has seen none of
  them. Half the gradable evidence in the repository is being withheld from the artifact David uses.
- **Interaction with the 2024 gap (DG-029, claimed by another lane).** Even a full refit would still
  skip 2024, because 2024 is absent from the feature matrix entirely while being present upstream in
  every `nflverse_usage.db` table (2016–2025). A refit recovers 2022–2023; it does not recover 2024.
- **Not resolved here, and deliberately so.** The ticket's "done" is *a decision, made deliberately
  rather than by omission*. Refitting changes the served model's behaviour and is David's call under
  his gates, not a lane's. **Evidence is complete; no model was retrained, no artifact written, no
  commit, no push.**
- **Recommendation, offered as a lane's reading and not a ruling:** evaluate on the holdout, then
  refit the final artifact on all gradable seasons with the hyperparameters the evaluation chose.
  That is the ordinary sequence and it is what `retrain_mode: refit_per_fold_fixed_alpha` already
  implies. Whatever is chosen, DG-015 now makes the answer visible on the card either way.
