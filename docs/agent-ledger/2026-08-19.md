# 2026-08-19 — DG-015 Agent Ledger

## 16:3x ET — Claude Consultant: CORRECTION to my own fix — it overstated the fit exactly as the bug did `[w#DG-015]`

- **What I got wrong.** My first version derived the deployed fit from every season present in the
  feature matrix, and published
  `"2018–2021, 2025 (deployed fit; 2022, 2023 held out for evaluation)"`. **The model never saw
  2025.** Measured while starting DG-014: 2025 has 505 rows, **zero** outcomes in `avg_ppg_t1_t2`,
  and `training_eligible=0` throughout — its t+1/t+2 result has not happened yet, so those rows
  cannot enter a fit no matter what the holdout says.
- **So I reproduced the defect I was repairing**, in the repair: a card claiming the model saw a
  season it did not. Found by measurement on the next ticket, not by review of this one.
- **Corrected statement:**
  `"2018–2021 (deployed fit; 2022, 2023 held out for evaluation; 2025 present but not yet gradable,
  so not fitted)"` — and the ungradable season is disclosed rather than silently dropped, because a
  reader who sees 2025 in the matrix and not in the fit deserves to be told why.
- **Structural fix, not just a value fix:** `deployed_fit_seasons` now takes *trainable* seasons —
  those carrying an outcome — and `seasons_from_training_data` splits the matrix by measuring
  outcome presence rather than assuming it. A season with no outcome can no longer be claimed as
  fitted by construction, and a contract test pins that.
- **This independently corroborates DG-014's headline**, which I had doubted an hour earlier: the
  deployed models really were fit on **2018–2021 only**.
- **Verification after correction:** **44 passed** under Python 3.14 (17 contracts here, up from 15,
  plus 27 pre-existing); `uvx ruff check` clean. Still 5 exact paths, still no commit or push.

## 16:0x ET — Claude Consultant: DG-015 READY_FOR_GATE — the card now states what the model actually saw `[w#DG-015]`

- **Authority:** David, verbatim — *"we need to build the product NOW"*, then *"switch me to a
  buildable ticket now"*. Claimed `DG-015` via `dg-work.sh DG-015 --from main`; worktree
  `~/dg-wt/DG-015`, branch `ticket/DG-015`, base `main@552733c`.
- **The defect, measured before any edit.** All four cards in `app/data/backtest/model_cards/`
  declare `"training_window": "2018–2022 (expanding; 4 folds)"`. `scripts/train_engine_b.py:66,187`
  fits on rows *excluding* `HOLDOUT_SEASONS = [2022, 2023]`. The training data
  (`engine_b_features_v2.csv`) carries feature seasons 2018, 2019, 2020, 2021, 2022, 2023, **2025**.
  So the deployed fit actually saw **2018–2021 and 2025**, and the published claim is wrong three
  ways at once: it claims 2022, which was held out; it omits 2025, which was used; and the dash
  asserts a contiguous span that does not exist — 2024 is absent from the data entirely.
- **Root cause, structural rather than a typo.** `generate_model_cards.py:325` rendered
  `training_window` from `sorted({y for f in folds for y in f.train_years})` — a true statement
  about the walk-forward EVALUATION, published under a name every reader takes to mean the deployed
  fit. And `HOLDOUT_SEASONS` existed in exactly one file, invisible to the generator, so nothing in
  the system was capable of noticing the two disagreed.
- **Fix.** New `src/dynasty_genius/eval/training_provenance.py` holds the holdout constant that both
  the trainer and the card generator now import, plus season rendering that cannot imply continuity
  it does not have (a dash only across three or more unbroken seasons; `2018–2021, 2025`, never
  `2018–2025`). `training_window` now states the deployed fit **and names what was withheld**;
  the fold span is preserved separately and honestly as a new `evaluation_window` field.
  `ModelCard.schema_version` 1.0.0 → 1.1.0; the new field carries a default, so old artifacts load.

| | statement |
| :-- | :-- |
| before | `training_window: "2018–2022 (expanding; 4 folds)"` |
| after | `training_window: "2018–2021, 2025 (deployed fit; 2022, 2023 held out for evaluation)"` |
| after | `evaluation_window: "2018–2022 (walk-forward evaluation; 4 folds)"` |

- **RED before GREEN.** 15 contracts written first; the run failed on
  `ModuleNotFoundError: training_provenance` — the intended reason — then passed. The last of them
  is a **binding test**: it fails if `train_engine_b.py` ever restates the holdout as a literal
  again, which is the only reason this drift was possible.
- **Verification:** **42 passed** under real Python 3.14 — the 15 new contracts plus the 27
  pre-existing model-card / trust-surface tests, proving the schema change is backward compatible.
  `uvx ruff check` clean (0 remaining). **No commit, push, merge, release, shared-data write or
  Studio access.**
- **Scope:** 5 paths — `training_provenance.py` (new), its contract test (new), and edits to
  `generate_model_cards.py`, `train_engine_b.py`, `model_card.py`. The 7 remaining untracked entries
  are `dg-work.sh` bootstrap artifacts and will never be staged; staging is by exact path.
- **Deliberately NOT done:** the shipped card artifacts are not regenerated. They live in shared
  `app/data/backtest/` and regenerating them writes shared data and needs a backtest run. The
  generator is now correct; **the cards will still carry the false string until someone regenerates
  them under David's gate.** Saying so plainly because a fixed generator is easy to mistake for a
  fixed artifact.
- **Next gate:** David's approval to stage the 5 exact paths.
