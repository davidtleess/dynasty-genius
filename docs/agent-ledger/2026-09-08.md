# Headshot identity hazard — 2026-09-08

The Sleeper NFL player dump fetched for DG-192 has swapped external ESPN IDs for Ryan Izzo (Sleeper 5094) and Tyler Conklin (5133). The Sept 4 DynastyProcess snapshot and Sept 6 NFL roster capture instead agree with independent CFBD historical college records: Izzo/Florida State is ESPN 3122920; Conklin/Central Michigan is ESPN 3915486. Do not overwrite those verified mappings with the current Sleeper dump.

The DG-192 photo acquisition excluded both disputed external fallbacks and successfully fetched both photos from their exact Sleeper CDN IDs. No shared identity data was changed. The raw capture, hashes, acquisition report, and independent review are in the private DG-192 handoff evidence.

## DG195: test caches need their own dependency wrapper

`dg-work.sh` links the complete frontend/node_modules directory to shared trunk. Vite writes temporary config modules into the nearest node_modules/.vite-temp (`vite/dist/node/chunks/node.js`); Vitest also caches under node_modules/.vite. A ticket worktree alone therefore does not isolate these tool outputs. DG195 replaced only its node_modules symlink with a local real directory whose package entries link read-only to the existing dependencies and whose .vite/.vite-temp directories are private. No packages installed or shared caches removed. Future ticket verification should use private tool caches as well as private source/data outputs.
