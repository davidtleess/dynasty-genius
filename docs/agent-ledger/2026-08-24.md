# 2026-08-24

## DG-040 — nflverse contracts upstream rename (lane ClaudeFable5-DG040-20260824, ticket/DG-040)

Layer 1. The scheduled 06:15 nflverse capture has failed on every run of its life (installed
2026-08-20, first run 08-21; 4-for-4 exit 1): the contracts upstream renamed `cols` to
`season_history` (same 13 fields) and added `contract_history` (11 fields) between 08-08 and
08-21, with `nflreadpy` unchanged at 0.1.5. The exact-shape gate at `normalize_rows` refused,
correctly. The 12 stat streams land daily before the failure; raw contracts blobs are retained
daily, so nothing is unrecoverable. Last normalized contracts vintage: 2026-08-08.

What landed here:
- `_CONTRACTS_COLUMNS` / `json_columns` / `nested_fields` now declare the measured 26-key
  shape exactly; the gate stays exact so the next drift fails as loudly as this one.
- `tests/fixtures/contracts_slice.json` regenerated from the retained 2026-08-24 raw blob by
  `docs/agent-ledger/evidence/2026-08-24/build_contracts_fixture_claude_v2.py` (196 rows,
  full provenance in the manifest, `--check` verified byte-identical).
- Contract tests updated to the new shape; both nested columns now covered by the digest,
  round-trip, refusal, and SQL-NULL tests; two DG-040 regression tests pin the incident
  (the retired shape refuses by name; the 62 null-in-both rows store as SQL NULL).

Commands of record:
- Census: streaming parse of the full 08-24 raw blob — one homogeneous 26-key top-level
  shape across 51,994 records; every nested entry exactly on its field set; 62 records null
  in both nested columns; zero integer/non-finite violations.
- RED: `.venv/bin/python3.14 -m pytest tests/contract/test_contracts_ingestion_red.py -q`
  → 60 failed / 48 passed against the old spec.
- GREEN: same → 108 passed. Full suite: `.venv/bin/python3.14 -m pytest tests -q`
  → **6323 passed, 40 skipped, 0 failed, zero collection errors** in 58.95s.

Deployment still required after landing (recorded in the ticket): trunk fast-forward, the
explicit `UsageStore.migrate_additive_columns` run against the live store (additive only;
legacy `cols` column keeps the 08-08 vintages), and one hand-run of the capture outside the
scheduled window to prove status ok end-to-end.
