## 21:34 ET - Codex (Phase 24 Regime A Design Review)

- Task: Review Claude Code's proposed Phase 24 Regime A drafted-class pick valuation design before spec writing.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up design; backend valuation only; frontend HOLD remains.
- Intended or completed write scope: Design review only; no production code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected current `src/dynasty_genius/trade_lab/draft_pick_valuation.py` API and `resources/prospect_cards.json` schema.
  - Verified `prospect_cards.json` is 82 rows: 80 scored/ranked 2026 cards plus 2 unscored 2027 watchlist rows; 67 scored rows currently have negative raw xVAR, so option-value flooring is necessary.
- Product alignment: Recommended a small dispatcher/wrapper around the existing curve path, with board pricing kept model-blind by consuming a normalized rank-to-xVAR data structure rather than importing scorers or PVO assemblers.
- Drift risks: Regime A should not be wired into production future-pick reconstruction until there is a clear current-year pick consumer; otherwise current unknown future picks could silently shift from historical expectation to a specific class without caller intent.
- Handoff / next step: Send design review findings to Claude Code pane `dynasty:1.1`.

## 22:18 ET - Codex (Regime A RED Contracts — Tasks 2-4)

- Task: Author RED contract tests for Regime A drafted-class pick valuation Tasks 2-4.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Regime A follow-up; backend valuation capability only; frontend HOLD remains.
- Intended or completed write scope: Add `tests/contract/test_regime_a_pick_valuation.py` only, plus this ledger entry. No production code.
- Files changed:
  - `tests/contract/test_regime_a_pick_valuation.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `.venv/bin/python3.14 -m pytest tests/contract/test_regime_a_pick_valuation.py -q` -> RED as expected during collection: missing `load_prospect_board` import from `draft_pick_valuation.py`.
  - `.venv/bin/ruff check --select I tests/contract/test_regime_a_pick_valuation.py` -> passed.
  - `.venv/bin/ruff check tests/contract/test_regime_a_pick_valuation.py` -> passed.
  - `.venv/bin/python3.14 -m pytest tests/contract/test_draft_pick_valuation.py -q` -> 20 passed.
- Product alignment: Tests lock `valuation_regime`, board resolutions, model-blind board loading, Option-A floor semantics, board round means, beyond-board unresolved behavior, empty-board curve parity, and tier-with-board curve routing.
- Drift risks: RED currently stops at the first missing API (`load_prospect_board`), so Claude implementation should re-run the file after each task to expose the later schema and dispatcher assertions.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Tasks 2-4 to green.

## 22:30 ET - Codex (PR #39 Regime A Review)

- Task: Review PR #39 (`feature/phase24-regime-a` -> `main`) implementation and PR scope for Regime A drafted-class prospect-board pick valuation.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Regime A follow-up; backend valuation capability only; frontend HOLD remains.
- Intended or completed write scope: Review only; local ledger entry only. No production code or PR files changed.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 39 --json ...` -> PR open, merge state `CLEAN`, CI `Python checks` success.
  - `gh pr diff 39 --name-only` -> expected six files only: curve artifact, Regime A spec, Regime A plan, curve build script method string, `draft_pick_valuation.py`, and Regime A contract tests.
  - `.venv/bin/python3.14 -m pytest tests/contract/test_regime_a_pick_valuation.py tests/contract/test_draft_pick_valuation.py tests/contract/test_pick_valuation_inference_only.py -q` -> 31 passed.
  - `.venv/bin/ruff check src/dynasty_genius/trade_lab/draft_pick_valuation.py scripts/build_draft_pick_value_curve.py tests/contract/test_regime_a_pick_valuation.py` -> passed.
  - `.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `load_prospect_board(2026)` smoke -> 80 ranks, min 1, max 80; `load_prospect_board(2027)` -> `{}`.
  - `git diff --check main...HEAD` -> passed.
- Product alignment: Dispatcher routes non-empty board + slot/round-only to Regime A; tier and empty-board paths remain curve-backed; board exact/round pricing uses Option-A flooring; beyond-board exact slots resolve unavailable rather than falling back to the curve; loader remains artifact/data-only and model-blind.
- Drift risks: None identified for the PR. v1 remains capability-only; future production callers must still intentionally load/pass the board before Regime A takes effect.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; Gemini governance review can proceed before merge.

## 22:34 ET - Codex (PR #39 Merge + Ledger Closeout)

- Task: David-authorized closeout of PR #39 and preservation of local Codex/Gemini ledger entries after merge.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Regime A follow-up closeout; backend valuation capability merged; frontend HOLD remains.
- Intended or completed write scope: Merge PR #39, then commit only the post-push review/sign-off ledger entries on a small docs closeout branch.
- Files changed:
  - `docs/agent-ledger/2026-05-26.md`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 39 --json ...` before merge -> merge state `CLEAN`, CI `Python checks` success.
  - `gh pr merge 39 --merge --delete-branch` -> PR #39 merged; merge commit `74d3f6b7796e9ffb04a3416ca5a80a97c8cc0db0`; feature branch deleted.
  - Local closeout branch created from updated `main`: `docs/phase24-regime-a-ledger-closeout`.
- Product alignment: PR #39 keeps Regime A capability-only, model-blind, fully caveated, and `decision_supported=False`; no market data enters training and frontend HOLD remains unchanged.
- Drift risks: None. The closeout branch is docs/ledger-only and intentionally does not flip `AGENT_SYNC.md`.
- Handoff / next step: Commit/push the ledger closeout PR, merge it if clean, then report PR #39 merge commit and closeout status to Claude Code pane `dynasty:1.1`.

## 22:40 ET - Codex (AGENT_SYNC Cleanup)

- Task: Clean up `AGENT_SYNC.md` after PR #39/PR #40 closeout.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: State-board cleanup only; Phase 24 quick-wins complete; frontend HOLD remains.
- Intended or completed write scope: Update stale sprint state in `AGENT_SYNC.md`; append ledger entry.
- Files changed:
  - `AGENT_SYNC.md`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Reviewed `AGENT_SYNC.md` diff.
  - `.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `git diff --check` -> passed.
- Product alignment: Records Regime A as merged, removes stale Phase 21/22 current objective text, marks frontend alignment PR #33 as merged, and clarifies that Phase 23 W5b remains deferred behind explicit David approval.
- Drift risks: This is documentation state only; no code/model/PVO/market/frontend behavior changes.
- Handoff / next step: Report cleanup summary to David.

## 22:53 ET - Codex (SF-QB Knob Calibration Spec Review)

- Task: Review `docs/superpowers/specs/2026-05-26-sf-qb-knob-calibration-design.md` on branch `feature/phase24-sf-qb-calibration` before writing-plans.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up design review; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No spec/code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected spec from `feature/phase24-sf-qb-calibration` without switching branches.
  - Checked existing `apply_sf_qb_ordering`, `_SF_QB_PROMOTE_SLOTS`, and curve build script integration points.
  - Checked `prospects_with_outcomes.csv` columns/classes for `season`, `pick`, `position`, and `pfr_player_name`; 2022-2025 first-36 skill boards are populated.
  - Checked `prospect_cards.json` carries `full_name`, `position`, `draft_class`, `nfl_draft_pick`, and class-rank fields for 2026.
- Product alignment: Calibration remains read-only, model/market separated, and gated before any curve-regeneration change.
- Drift risks: Spec needs a precise rounding rule and should make completed-draft/class-year handling explicit before planning.
- Handoff / next step: Send findings to Claude Code pane `dynasty:1.1`.

## 22:56 ET - Codex (SF-QB Knob Calibration Spec Re-Review)

- Task: Re-review Claude Code's patch to the SF-QB calibration spec after MEDIUM/LOW findings.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up design review; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No spec/code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected patched spec from `feature/phase24-sf-qb-calibration` without switching branches.
  - Confirmed §1 and §8 now lock `round_half_up(m)=math.floor(m+0.5)`, not Python banker rounding, with 0.5 and 2.5 tie tests.
  - Confirmed §2 and §8 now require complete Sleeper drafts and explicit `draft_class` from `draft.season` with fallback to `league.season`.
- Product alignment: Original MEDIUM and LOW intent is resolved, but one stale data-flow line still says `_recommend_k(promotions)` uses `round(median)`.
- Drift risks: Plan authors may copy the stale `_recommend_k` component summary unless it is patched to `round_half_up`.
- Handoff / next step: Send residual LOW to Claude Code pane `dynasty:1.1`.

## 22:58 ET - Codex (SF-QB Knob Calibration Spec Final Re-Clear)

- Task: Confirm residual §4 rounding wording patch on branch `feature/phase24-sf-qb-calibration`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up design review; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No spec/code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected patched spec from `feature/phase24-sf-qb-calibration` without switching branches.
  - Confirmed no stray `round(median)` wording remains; §1/§4/§8 consistently use `round_half_up`.
  - Confirmed completed-draft and explicit `draft_class` requirements remain present.
- Product alignment: Spec is now internally consistent and preserves read-only Sleeper, model/market separation, gated curve regen, and frontend HOLD.
- Drift risks: None identified.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; writing-plans can proceed.

## 23:03 ET - Codex (SF-QB Calibration RED Contracts)

- Task: Author RED contract tests for SF-QB calibration Tasks 2-6.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 SF-QB calibration follow-up; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Add failing contracts in `tests/test_calibrate_sf_qb_knob.py`; append ledger entry. No production script implementation.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -q` -> RED as expected during collection: `ModuleNotFoundError: No module named 'scripts.calibrate_sf_qb_knob'`.
  - `.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Contracts lock half-up K rounding, name normalization, completed-rookie-draft filtering, NFL-skill-rank construction, QB promotion math, seed/live board support, and monkeypatched artifact generation without live network.
- Drift risks: RED currently stops at the missing script module; later assertions will surface as Claude implements Tasks 2-6.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements to green.

## 23:12 ET - Codex (PR #41 SF-QB Calibration Review)

- Task: Review PR #41 (`feature/phase24-sf-qb-calibration` -> `main`) implementation and PR scope.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 SF-QB calibration follow-up; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No production code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 41 --json ...` / `gh pr diff 41 --name-only` -> PR contains six intended SF-QB files only; local `AGENT_SYNC.md` and ledger edits did not leak into PR #41.
  - `.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -q` -> 8 passed.
  - `.venv/bin/ruff check scripts/calibrate_sf_qb_knob.py tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `git diff --check origin/main...origin/feature/phase24-sf-qb-calibration` -> passed.
  - `gh pr checks 41` -> `Python checks` passed.
- Product alignment: Pure helpers, read-only Sleeper seam, K=0/no-curve-change outcome, and model/market separation are aligned.
- Drift risks: Found a MEDIUM artifact-provenance gap: spec requires `per_draft`, but script/artifact currently emit aggregate-only calibration evidence.
- Handoff / next step: Send finding to Claude Code pane `dynasty:1.1`; await patch before CLEAR.

## 23:16 ET - Codex (PR #41 SF-QB Calibration Re-Clear)

- Task: Re-review PR #41 after per-draft artifact provenance patch.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 SF-QB calibration follow-up; backend calibration script only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No production code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected PR #41 head `6402d74`.
  - Confirmed artifact now includes `per_draft` entries with `draft_class`, `source`, matched/unmatched counts, and promotions; aggregate matched count reconciles to the per-draft sum.
  - `.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -q` -> 8 passed.
  - `.venv/bin/ruff check scripts/calibrate_sf_qb_knob.py tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `gh pr checks 41 --watch` -> `Python checks` passed.
- Product alignment: Per-draft provenance closes the thin-corpus audit gap; K remains 0 and no curve change is included.
- Drift risks: None identified for PR #41.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; Gemini governance can proceed.

## 23:22 ET - Codex (PR #41 Merge + Shared-Tree Closeout)

- Task: David-authorized closeout of PR #41 and preservation of shared-tree SF-QB review/sign-off state.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 SF-QB calibration follow-up closeout; backend calibration script merged; frontend HOLD remains.
- Intended or completed write scope: Merge PR #41, then commit the parked ledger entries and legitimate `AGENT_SYNC.md` cleanup on a small docs closeout branch.
- Files changed:
  - `AGENT_SYNC.md`
  - `docs/agent-ledger/2026-05-26.md`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 41 --json ...` before merge -> merge state `CLEAN`, CI `Python checks` success.
  - `gh pr merge 41 --merge --delete-branch` -> PR #41 merged; merge commit `ebdc8e61eff327b47fce1ddca375c46ea1529c84`; feature branch deleted.
  - Reviewed `AGENT_SYNC.md` diff and classified it as legitimate Phase 24 state cleanup: it records PR #36/#39 merged state, removes stale current-sprint text, preserves frontend HOLD, and does not add a SF-QB PR-pending flip.
- Product alignment: PR #41 reports K=0 with per-draft provenance, leaves the pick-value curve unchanged, keeps Sleeper access read-only, and preserves model/market separation.
- Drift risks: Closeout branch is docs/state-only; no code/model/PVO/market/frontend behavior changes.
- Handoff / next step: Commit/push the docs closeout PR, wait for CI, merge it if clean, then report PR #41 merge commit and `AGENT_SYNC.md` handling to Claude Code pane `dynasty:1.1`.

## 23:48 ET - Codex (Phase 24 Follow-Up Research Consultation)

- Task: Review external mock-draft and dynasty ADP source research and give Claude Code a technical/governance verdict on B-first sequencing.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 follow-up scoping; design consultation only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No production code or artifact changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read `docs/strategies/Dynasty Genius — Phase 24 Follow-up Scoping- Mock-Draft & Dynasty ADP Sources.md`.
  - Inspected `scripts/calibrate_sf_qb_knob.py`, `src/dynasty_genius/adapters/fantasycalc_adapter.py`, and `src/dynasty_genius/adapters/market_source.py`.
  - Live-probed MFL ADP JSON shape: response root is `adp.player[]` with `id`, `rank`, `averagePick`, `minPick`, `maxPick`, `draftsSelectedIn`, `draftSelPct`, plus `timestamp`, `totalDrafts`, and `totalPicks`.
  - Live-probed that `IS_KEEPER=Rookie Only` returns `Invalid value for IS_KEEPER`; `ROOKIES=1` returned a rookie-shaped response, and `IS_KEEPER=N` returned a non-keeper/startup-shaped response. Adapter contract must verify/lock the correct MFL params before implementation.
- Product alignment: Recommended B-first, but with MFL aggregate as overlay-only and explicitly excluded from SF-QB calibration because QB-count and TE-premium are not filterable.
- Drift risks: Do not create a duplicate Sleeper draft-source abstraction over the existing calibration fetch path; any broader Sleeper corpus expansion should be a separate increment built around bring-your-own draft IDs.
- Handoff / next step: Send verdict to Claude Code pane `dynasty:1.1`; wait for David/Claude to choose the next spec scope.

## 00:05 ET - Claude Code (Phase 24 Follow-up — Team Convergence & Go-Forward Decision)

- Task: Synthesize the three-reviewer discussion of the mock/ADP research into one agreed go-forward plan; record David's decision.
- Reviewers: Claude Code (lead), Codex (technical/governance feasibility + live MFL probe — see entry above), Gemini (PM/governance read — `docs/agent-ledger/2026-05-26.md`, reframed from a sign-off to a governance-read input). All three converged.
- Decision (David, approved): **Build Follow-up B (dynasty rookie ADP ingestion) before Follow-up A (mock aggregation)**, in three increments:
  - **Increment 1 — `MflAdpMarketSource` overlay adapter only.** Mirrors existing FantasyCalc adapter patterns (3-stage cache, banned-field sanitizer, source timestamp/freshness caveats, normalized rows, market-leakage guard tests, captured live-response fixture). Binding source-metadata caveats `mfl_adp_format_blended_qb_count` + `mfl_adp_te_premium_unfiltered` (MFL ADP cannot filter SF QB-count or TE-premium). `decision_supported=False`, snapshot-before-parse, excluded from training CSVs. **Explicit non-goal: not used for SF-QB calibration** (a 1QB/SF blend would bias the QB-promotion axis). Params locked from a fresh live probe, NOT the research URL — Codex's probe found `IS_KEEPER=Rookie Only` invalid; `ROOKIES=1` returns the rookie shape, `IS_KEEPER=N` returns startup. Verified shape: `adp.player[]` (`id, rank, averagePick, minPick, maxPick, draftsSelectedIn, draftSelPct`) + `timestamp/totalDrafts/totalPicks`.
  - **Increment 2 (separate, later) — Sleeper calibration-corpus expansion** via BYO draft/league IDs feeding the existing `calibrate_sf_qb_knob.py` board path (`_collect_rookie_boards` / monkeypatchable `_fetch_league_rookie_drafts`). Extends calibration inputs only; not a new market-overlay abstraction.
  - **Increment A (later) — mock aggregation**, separately spec'd after B lands. Free path only (NFL.com authors + WalterFootball + Grinding-the-Mocks cross-check); never NFLMDDB/PFF as redistributable feeds.
- Governance: overlay/inference-only, never an Engine A/B training input; frontend Phase 12 HOLD and NOISE_BAND lock untouched; no model pkl/manifest/contract change.
- Handoff / next step: Begin Increment 1 via the cockpit motion — brainstorm → spec → writing-plans → TDD build. First spec input is a live MFL contract probe. No code until the spec is approved.

## 00:04 ET - Codex (Increment 1 MFL ADP Design Review)

- Task: Review Claude Code's pre-spec design for `MflAdpMarketSource` before Increment 1 spec drafting.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up scoping; design consultation only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No production code, model, PVO, market consumer, or frontend changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected `tests/test_market_leakage_gate.py`, `tests/contract/test_market_overlay_pvo.py`, `tests/test_market_overlay.py`, `src/dynasty_genius/adapters/market_source.py`, `src/dynasty_genius/adapters/fantasycalc_adapter.py`, `src/dynasty_genius/models/engine_a_contract.py`, and `src/dynasty_genius/sources/source_registry.py`.
  - Confirmed existing PVO market-overlay contract is FantasyCalc-specific and should not be touched while MFL is unwired.
  - Confirmed current `MarketSource.fetch()` returns `list[dict]`; MFL caveats should remain adapter-level unless a later consumer changes the shared interface.
- Product alignment: Design is sound as an overlay-only, non-calibration rookie ADP adapter if the spec locks season-scoped caches, source timestamp fallback behavior, source-registry/leakage coverage, and no PVO consumer wiring.
- Drift risks: Changing the shared `MarketSource.fetch()` signature or touching FantasyCalc PVO contracts would broaden Increment 1 beyond the approved adapter-only scope.
- Handoff / next step: Send technical verdict and MUST-fix clarifications to Claude Code pane `dynasty:1.1`.

## 00:10 ET - Gemini (PM Governance Review — MFL Rookie ADP Spec)

- Task: Review proposed MFL Rookie ADP Overlay spec (`docs/superpowers/specs/2026-05-27-mfl-rookie-adp-overlay-design.md`) and author the PM/governance consultation.
- Governance read:
  - `docs/governance/02-agent-operating-loop.md` (v1.0.0)
  - `docs/governance/00-product-constitution.md` (v1.0.0)
  - `docs/governance/01-north-star-architecture.md` (v1.0.0)
  - `docs/governance/03-code-hygiene-policy.md` (v1.0.0)
  - `AGENT_SYNC.md`
  - `docs/agent-ledger/2026-05-27.md`
- Active phase / surface: Phase 24 Follow-up B (MFL Rookie ADP) spec review; frontend HOLD remains; no code files touched.
- Intended or completed write scope: Append daily ledger steering consultation entry to `docs/agent-ledger/2026-05-27.md`.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read proposed spec `docs/superpowers/specs/2026-05-27-mfl-rookie-adp-overlay-design.md`.
  - Audited spec against leakage gates, banned-language, scope-fences, and format-blend caveats.
- Product alignment: Perfectly aligned with PM read-only and strategic coordination scope. Providing structured, defensive governance input on the Increment 1 specification.
- Drift risks: None.
- Handoff / next step: Present the spec consultation directly in the chat to David/Claude Code.

## 00:10 ET - Codex (MFL Rookie ADP Spec Review)

- Task: Review `docs/superpowers/specs/2026-05-27-mfl-rookie-adp-overlay-design.md` before writing-plans.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, brainstorming skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 spec review; design-only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No production code, model, PVO, market consumer, or frontend changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read the MFL Rookie ADP Overlay spec on branch `feature/mfl-rookie-adp-overlay`.
  - Re-checked the spec against the four Codex design-review must-fixes: `MarketSource.fetch()` rows-only/constructor season, season-scoped cache files, source-registry/leakage wiring without touching the FantasyCalc PVO contract, and defensive MFL `adp.timestamp` handling.
- Product alignment: Spec preserves overlay-only scope, `decision_supported=False`, not-for-calibration, no consumer wiring, no model-training leakage, NOISE_BAND lock, and frontend HOLD.
- Drift risks: No blocking gap found. Plan should make cache TTL vs source-publish-age semantics explicit and ensure leakage tests cover `draft_selection_pct` / `drafts_selected_in`, which are not caught by the existing broad `market_` regex.
- Handoff / next step: Send CLEAR with plan-level clarifications to Claude Code pane `dynasty:1.1`; writing-plans can proceed after David's final spec read.

## 08:09 ET - Codex (MFL Rookie ADP Task 2 RED Contracts)

- Task: Author Task 2 RED leakage-gate contracts for MFL rookie ADP overlay fields.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_market_leakage_gate.py`; append ledger entry. No production implementation.
- Files changed:
  - `tests/test_market_leakage_gate.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_market_leakage_gate.py -k mfl -v` -> RED as expected: `test_mfl_overlay_only_fields_are_prohibited` fails because `draft_selection_pct` is not yet in `PROHIBITED_COLUMNS`; regex documentation test passes.
  - `./.venv/bin/ruff check tests/test_market_leakage_gate.py` -> passed.
- Product alignment: Locks explicit fail-closed protection for MFL overlay-only fields that the broad leakage regex misses; no adapter, source registry, PVO, Engine, calibration, or frontend changes.
- Drift risks: RED is intentionally limited to `draft_selection_pct` / `drafts_selected_in`; Claude should add only those prohibited columns for Task 2.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 2 to green and commits test+implementation together.

## 08:13 ET - Codex (MFL Rookie ADP Task 3 RED Contracts)

- Task: Author Task 3 RED registry contracts for `mfl_rookie_adp`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_market_overlay.py`; append ledger entry. No source registry implementation.
- Files changed:
  - `tests/test_market_overlay.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_market_overlay.py -k mfl_rookie_adp -v` -> RED as expected: both MFL tests fail with `KeyError: 'mfl_rookie_adp'`.
  - `./.venv/bin/ruff check tests/test_market_overlay.py` -> passed.
- Product alignment: Locks that MFL rookie ADP must be a `market_overlay` source only, with `json_cache`, 24h freshness, and `use_cached` failure behavior; no adapter, PVO, Engine, calibration, or frontend changes.
- Drift risks: RED is intentionally limited to registry behavior; Claude should add the `SOURCE_REGISTRY` entry and extend the leakage-gate source set in the green step.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 3 to green and commits test+implementation together.

## 08:25 ET - Codex (MFL Rookie ADP Task 4 RED Contracts)

- Task: Author Task 4 RED adapter-scaffold contracts for `mfl_adp_adapter.py`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_mfl_adp_adapter.py`; append ledger entry. No adapter implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -q` -> RED as expected: all four scaffold tests fail with `ModuleNotFoundError: No module named 'src.dynasty_genius.adapters.mfl_adp_adapter'`.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock live-probed MFL URL params, list-vs-singleton normalization, season-scoped cache paths, and allowlist sanitizers; no source adapter implementation, PVO, Engine, calibration, or frontend changes.
- Drift risks: RED is intentionally limited to constants and pure helpers; Claude should create only the adapter scaffold needed for Task 4 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 4 to green and commits test+implementation together.

## 08:28 ET - Codex (MFL Rookie ADP Task 5 RED Contracts)

- Task: Author Task 5 RED freshness-clock contracts for MFL ADP source timestamp handling.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_mfl_adp_adapter.py`; append ledger entry. No adapter implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -k "publish_age or freshness" -v` -> RED as expected: all three selected tests fail because `_source_publish_age_hours` / `_freshness_caveats` are not implemented.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock the two-clock distinction: cache age and source publish age are separate, and missing/unparseable MFL `adp.timestamp` must be caveated.
- Drift risks: RED is intentionally limited to pure freshness helpers; Claude should add `_cache_age_hours`, `_source_publish_age_hours`, and `_freshness_caveats` only for Task 5 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 5 to green and commits test+implementation together.

## 08:30 ET - Codex (MFL Rookie ADP Task 6 RED Contracts)

- Task: Author Task 6 RED cache/degrade contracts for `fetch_adp_with_cache`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_mfl_adp_adapter.py`; append ledger entry. No adapter implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -k "adp_stage or live_refresh or wrong_season" -q` -> RED as expected: all five selected tests fail because `fetch_adp_with_cache` is not implemented.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock fresh-cache no-network behavior, stale serve with cache/source ages, cold fail, live refresh caching, and season-scoped cache isolation for MFL ADP; no PVO, Engine, calibration, consumer, or frontend changes.
- Drift risks: RED is intentionally limited to ADP fetch/cache behavior; Claude should add cache I/O, `_get_json`, and `fetch_adp_with_cache` only for Task 6 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 6 to green and commits test+implementation together.

## 08:32 ET - Codex (MFL Rookie ADP Task 7 RED Contracts)

- Task: Author Task 7 RED players-map cache/degrade contracts for `fetch_players_with_cache`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_mfl_adp_adapter.py`; append ledger entry. No adapter implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -k players -q` -> RED as expected: all three selected tests fail because `fetch_players_with_cache` is not implemented.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock independent MFL players-map fetching, cold-fail caveat behavior, season-scoped players cache, and MFL singleton response handling; no PVO, Engine, calibration, consumer, or frontend changes.
- Drift risks: RED is intentionally limited to players-map fetch/cache behavior; Claude should add `_rows_to_player_map` and `fetch_players_with_cache` only for Task 7 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 7 to green and commits test+implementation together.

## 08:34 ET - Codex (MFL Rookie ADP Task 8 RED Contracts)

- Task: Author Task 8 RED normalization contracts for `normalize_mfl_adp_entry`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only in `tests/test_mfl_adp_adapter.py`; append ledger entry. No adapter implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -k normalize -q` -> RED as expected: the two new normalization tests fail because `normalize_mfl_adp_entry` is not implemented; the existing `_as_list` normalize test passes.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock MFL id join, numeric coercion, unmatched identity behavior, `source="mfl_rookie_adp"`, `decision_supported=False`, and intrinsic format-blend caveats; no PVO, Engine, calibration, consumer, or frontend changes.
- Drift risks: RED is intentionally limited to row normalization; Claude should add `_as_int`, `_as_float`, and `normalize_mfl_adp_entry` only for Task 8 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 8 to green and commits test+implementation together.

## 08:36 ET - Codex (MFL Rookie ADP Task 9 RED Contracts)

- Task: Author Task 9 RED wrapper contracts for `MflAdpMarketSource`.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, TDD skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 cockpit build; test-driver step only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests in `tests/test_mfl_adp_adapter.py` and `tests/test_market_overlay.py`; append ledger entry. No wrapper implementation.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `tests/test_market_overlay.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py tests/test_market_overlay.py -k "market_source or mfl_adp_market_source" -q` -> RED as expected: the three new MFL wrapper tests fail because `MflAdpMarketSource` is not implemented; existing selected market-source tests pass.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py tests/test_market_overlay.py` -> passed.
- Product alignment: Contracts lock that the MFL wrapper is a `MarketSource`, defaults season through `_current_season()`, and returns normalized rows only with `decision_supported=False`; transient caveats stay out of the wrapper return.
- Drift risks: RED is intentionally limited to wrapper behavior; Claude should add `MflAdpMarketSource` to `market_source.py` only for Task 9 green.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 9 to green and then drives Task 10 full-suite/governance validation.

## 08:56 ET - Codex (PR #46 MFL Rookie ADP Overlay Review)

- Task: Final technical review of PR #46 (`feature/mfl-rookie-adp-overlay` -> `main`) before Gemini governance read.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, GitHub skill, requesting-code-review skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 1 PR review; overlay adapter only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No production code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 46 --json ...` -> merge state `CLEAN`, CI `Python checks` success, expected file list only.
  - `gh pr diff 46 --name-only` -> expected spec/plan/ledger, adapter/source/contract files, fixtures, and tests only.
  - Read `src/dynasty_genius/adapters/mfl_adp_adapter.py`, `market_source.py`, source registry, leakage contract, and MFL adapter tests.
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py tests/test_market_overlay.py tests/test_market_leakage_gate.py tests/test_source_registry.py -q` -> 75 passed, 2 skipped.
  - `./.venv/bin/ruff check src/dynasty_genius/adapters/mfl_adp_adapter.py src/dynasty_genius/adapters/market_source.py tests/test_mfl_adp_adapter.py tests/test_market_overlay.py tests/test_market_leakage_gate.py tests/test_source_registry.py` -> passed.
  - `./.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - Confirmed `tests/contract/test_market_overlay_pvo.py` and `app/cache/mfl_adp/` are untouched; `.gitignore` covers `app/cache/`.
- Product alignment: MarketSource contract remains rows-only with season constructor; MFL rows carry intrinsic caveats and `decision_supported=False`; adapter transient caveats remain on the adapter channel; cache files are season-scoped; MFL is market_overlay only, unwired, and explicitly not for SF-QB calibration.
- Drift risks: None identified. Minor non-blocking note: `market_source.py` docstring still says FantasyCalc is the only active implementation, but PR behavior and tests are correct.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; hold merge until Gemini governance read.

## 10:14 ET - Codex (Increment 2 SF-QB Calibration Corpus Design Review)

- Task: Review pre-spec design for Follow-up B Increment 2, expanding the SF-QB calibration corpus via curated Sleeper draft IDs.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, brainstorming skill, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 design consultation only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected `scripts/calibrate_sf_qb_knob.py`, `tests/test_calibrate_sf_qb_knob.py`, and `app/data/sleeper.py`.
  - Live-checked David's known draft metadata/picks as a shape sanity check: `/draft/{id}` includes `league_id`, `settings.rounds`, `settings.teams`, `type`, `season`, and `/picks` includes `pick_no`, `draft_slot`, metadata name/position.
- Product alignment: Recommended extending the existing calibration script rather than adding a new market abstraction; MFL remains barred from calibration; K application and curve regeneration remain David-gated.
- Drift risks: Spec should fail closed on malformed/auction drafts, cap BYO board picks to the first 36 overall, use Sleeper `scoring_settings` for PPR/TEP metadata, and de-duplicate curated draft IDs to avoid overweighting.
- Handoff / next step: Send verdict and must-fix clarifications to Claude Code pane `dynasty:1.1`.

## 11:13 ET - Codex (Increment 2 SF-QB Corpus Spec Review)

- Task: Review `docs/superpowers/specs/2026-05-27-sf-qb-knob-calibration-corpus-expansion-design.md` before writing-plans.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, brainstorming skill, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 spec review; design-only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read the Increment 2 spec on branch `feature/sf-qb-calibration-corpus-expansion`.
  - Checked the spec against the five Codex design-review must-fixes: first-36 cap, auction/malformed fail-closed handling, `scoring_settings` metadata, order-preserving de-dupe with duplicate provenance, and missing/fetch-failed rejection.
- Product alignment: Spec preserves existing-script extension, read-only Sleeper, no market abstraction, MFL barred from calibration, no K application or curve regeneration, no model artifacts, and frontend HOLD.
- Drift risks: No blocking gap found. Plan should clarify that `malformed_picks` is detected during BYO board construction (or by a helper that receives picks), not inside `gate_byo_draft(draft, league)` alone.
- Handoff / next step: Send CLEAR with implementation-boundary clarification to Claude Code pane `dynasty:1.1`; writing-plans can proceed after David's final read.

## 12:12 ET - Codex (Increment 2 SF-QB Corpus Spec Final Pass)

- Task: Re-read the Increment 2 SF-QB corpus expansion spec fresh before David approval, probing edge cases beyond the original five must-fixes.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 spec review; design-only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Re-read `docs/superpowers/specs/2026-05-27-sf-qb-knob-calibration-corpus-expansion-design.md`.
  - Inspected `scripts/calibrate_sf_qb_knob.py` and outcomes CSV season coverage (`2015-2025`).
  - Re-checked Sleeper draft metadata shape: draft objects include `league_id`, `season`, `settings.rounds`, `type`, and picks carry `pick_no`/`draft_slot`.
- Product alignment: Existing-script extension remains right; no market abstraction, no MFL aggregate, no K application, no curve regeneration.
- Drift risks: Found spec edits needed before final approval: BYO drafts with unavailable rank maps should be rejected, draft class parsing must be explicit, BYO IDs that duplicate David-chain drafts must not double-count, malformed-picks handling should move out of `gate_byo_draft`, and artifact provenance should state capped/excluded pick counts deterministically.
- Handoff / next step: Send findings to Claude Code pane `dynasty:1.1`; await patched spec before CLEAR-FINAL.

## 12:19 ET - Codex (Increment 2 SF-QB Corpus Spec CLEAR-FINAL)

- Task: Re-review Claude Code's patched Increment 2 SF-QB calibration corpus expansion spec after the six final-pass edits were folded in.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, brainstorming skill, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 spec review; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No spec/code changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Re-read `docs/superpowers/specs/2026-05-27-sf-qb-knob-calibration-corpus-expansion-design.md`.
  - Confirmed `rank_map_unavailable`, `invalid_draft_class`, `duplicate_existing_draft`, `_build_byo_board` ownership of `malformed_picks`, first-36 cap provenance, and defensive `malformed_draft_settings` handling are all specified with contract-test intent.
- Product alignment: Spec remains read-only Sleeper, diagnostic-only, no MFL calibration input, no K application or curve regeneration, no model/PVO/frontend changes, and no market-derived training leakage.
- Drift risks: None remaining at spec level; implementation should preserve the rejected-list reason set and the capped-board matched/unmatched invariant.
- Handoff / next step: Send CLEAR-FINAL to Claude Code pane `dynasty:1.1`; David can approve for writing-plans.

## 13:04 ET - Codex (Increment 2 SF-QB Corpus Task 1 RED)

- Task: Author Task 1 RED contracts for curated BYO draft-ID input loading.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only; no production script or resource changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k byo_draft_ids -v` -> RED as expected: three failures because `scripts.calibrate_sf_qb_knob` has no `_BYO_PATH`.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Tests lock order-preserving BYO draft-ID de-duplication and missing/empty-file no-op behavior; no live network, no K application, no curve regeneration, no MFL calibration input.
- Drift risks: RED stops at missing `_BYO_PATH`; after Claude adds the path, `_load_byo_draft_ids` behavior assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 1 to green and commits test+implementation.

## 13:11 ET - Codex (Increment 2 SF-QB Corpus Task 2 RED)

- Task: Author Task 2 RED contracts for pure SF/12-team format gate helpers.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only; no production script changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k "superflex or twelve_team or format_metadata" -v` -> RED as expected: missing `is_superflex`, `is_twelve_team`, and `league_format_metadata` attributes.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Tests lock exact `SUPER_FLEX` token detection, defensive 12-team coercion, and metadata-only `scoring_settings` extraction for PPR/TE premium.
- Drift risks: RED stops at missing helper APIs; after Claude adds them, the scoring metadata shape and coercion assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 2 to green and commits test+implementation.

## 13:16 ET - Codex (Increment 2 SF-QB Corpus Task 3 RED)

- Task: Author Task 3 RED contracts for `gate_byo_draft`, limited to draft+league gates.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only; no production script changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k gate_byo_draft -v` -> RED as expected: missing `gate_byo_draft`.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Tests lock complete-rookie SF/12-team acceptance plus fail-closed reason precedence for not complete, malformed rounds, startup-sized rounds, auction drafts, non-SF leagues, and non-12-team leagues. Pick-level malformed handling remains out of this helper by design.
- Drift risks: RED stops at missing `gate_byo_draft`; after Claude adds it, reason precedence assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 3 to green and commits test+implementation.

## 13:18 ET - Codex (Increment 2 SF-QB Corpus Task 4 RED)

- Task: Author Task 4 RED contracts for `_build_byo_board` pick-level board construction.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only; no production script changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k build_byo_board -v` -> RED as expected: missing `_build_byo_board`.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Tests lock draft-class fail-closed parsing, league-season fallback, whole-draft malformed pick rejection, first-36 cap, sorted picks, and cap provenance without touching live Sleeper or calibration math.
- Drift risks: RED stops at missing `_build_byo_board`; after Claude adds it, the board shape and cap assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 4 to green and commits test+implementation.

## 13:21 ET - Codex (Increment 2 SF-QB Corpus Task 5 RED)

- Task: Author Task 5 RED contract for `_collect_byo_boards` / `_fetch_byo_drafts` seam.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing test only; no production script changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k collect_byo_boards -v` -> RED as expected: missing `_fetch_byo_drafts`.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Test locks the monkeypatchable read-only Sleeper seam, fail-closed fetch behavior, cross-source draft-ID de-duplication, and non-SF/missing-league rejection without live network access.
- Drift risks: RED stops at missing `_fetch_byo_drafts`; after Claude adds it, the accepted-board and rejection-reason assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 5 to green and commits test+implementation.

## 13:24 ET - Codex (Increment 2 SF-QB Corpus Task 6 RED)

- Task: Author Task 6 RED contracts for `main()` BYO integration, rank-map exclusion, duplicate provenance, and per-draft metadata.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, test-driven-development skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 cockpit build; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Add failing tests only; no production script changes.
- Files changed:
  - `tests/test_calibrate_sf_qb_knob.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -k "main_includes_byo or main_byo_dupe" -v` -> RED as expected: current `main()` ignores BYO boards and does not emit `rejected`.
  - `./.venv/bin/ruff check --select I tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/ruff check tests/test_calibrate_sf_qb_knob.py` -> passed.
- Product alignment: Tests lock BYO inclusion, rank-map-unavailable exclusion from per-draft math, within-file duplicate provenance, cap/format metadata propagation, and matched-count reconciliation without applying K or regenerating the curve.
- Drift risks: RED currently shows `per_draft` empty and `rejected` absent; after Claude rewrites `main()`, these assertions should become active.
- Handoff / next step: Send RED status to Claude Code pane `dynasty:1.1`; Claude implements Task 6 to green, then owns Task 7 full-suite/governance/ledger.

## 13:36 ET - Codex (PR #48 SF-QB Corpus Expansion Review)

- Task: Review PR #48 (`feature/sf-qb-calibration-corpus-expansion` -> `main`) implementation and PR scope.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, GitHub skill, requesting-code-review skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 PR review; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No PR code/spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 48 --json ...` -> PR head `7f7c0e5`, merge state `CLEAN`, CI `Python checks` success.
  - `gh pr diff 48 --name-only` -> expected six files only: spec, plan, empty BYO resource, calibration script, calibration tests, ledger.
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -q` -> 23 passed.
  - `./.venv/bin/ruff check scripts/calibrate_sf_qb_knob.py tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `git diff --check origin/main...HEAD` -> passed.
- Product alignment: Scope is diagnostic-only and does not touch the curve artifact, valuation module, model/PVO/frontend code, or MFL calibration path.
- Drift risks: Found one MEDIUM implementation-order issue: `_collect_byo_boards` fetches picks before applying `gate_byo_draft`, so an already-rejectable draft can be mislabeled `fetch_failed` if the picks call fails.
- Handoff / next step: Send MEDIUM finding to Claude Code pane `dynasty:1.1`; await patch before CLEAR.

## 13:48 ET - Codex (PR #48 SF-QB Corpus Expansion Re-Clear)

- Task: Re-review PR #48 after the `_collect_byo_boards` gate-before-picks patch.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, GitHub skill, requesting-code-review skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 PR review; backend calibration corpus expansion only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No PR code/spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected PR #48 head `9a94310`; `_collect_byo_boards` now calls `gate_byo_draft` before `get_draft_picks`.
  - Confirmed regression test `test_collect_byo_boards_gates_before_fetching_picks` asserts a non-SF draft rejects as `not_superflex` and never fetches picks.
  - `./.venv/bin/python3.14 -m pytest tests/test_calibrate_sf_qb_knob.py -q` -> 24 passed.
  - `./.venv/bin/ruff check scripts/calibrate_sf_qb_knob.py tests/test_calibrate_sf_qb_knob.py` -> passed.
  - `./.venv/bin/python3.14 scripts/validate_governance.py` -> passed.
  - `gh pr checks 48` at review time -> CI still pending on `Python checks`.
- Product alignment: The patched collector preserves fail-closed behavior without mislabeling hard-gate rejects; scope remains diagnostic-only, read-only Sleeper, no MFL calibration input, no K application/curve regeneration, and no model/PVO/frontend change.
- Drift risks: None remaining in implementation review; final merge should wait for CI success and Gemini governance read.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; hold for CI/Gemini before merge.

## 15:32 ET - Codex (Follow-up B Next-Move Sequencing Input)

- Task: Provide technical sequencing input after Follow-up B Increments 1 and 2 shipped.
- Governance read: Prior session bootstrap remains in force for this same working session: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 follow-up scoping; recommendation only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or state-board changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks: No tests run; consultation-only.
- Product alignment: Recommended next sequencing that preserves diagnostic-only calibration gates, keeps market overlays out of training, and avoids prematurely expanding the source/licensing surface.
- Drift risks: Consumer wiring of MFL requires careful boundary definition so overlay rows stay `decision_supported=False` and do not become training/PVO inputs by accident; mock aggregation requires a separate source/license spec.
- Handoff / next step: Send sequencing recommendation to Claude Code pane `dynasty:1.1` for David's decision.

## 18:22 ET - Codex (PR #50 SF-QB Curated Corpus Review)

- Task: Review PR #50, which curates six real SF rookie drafts into the calibration corpus and commits the run-of-record artifact.
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment C data/artifact review; diagnostic calibration artifact only; frontend HOLD remains.
- Intended or completed write scope: Review only; append ledger entry. No PR files changed.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `gh pr view 50 --json ...` -> PR head `7b11f742`, merge state `CLEAN`, CI `Python checks` success.
  - `gh pr diff 50 --name-only` -> exactly two PR files: `resources/sf_rookie_draft_ids.json` and `app/data/backtest/phase24/sf_qb_knob_calibration_20260527T221751Z.json`.
  - Artifact `jq` check -> `recommended_k=0`, `median_raw=-2.0`, `n_drafts=12`, `n_qbs_matched=50`, `n_qbs_unmatched=7`, `rejected` length 0, six BYO per-draft IDs matching the curated resource file.
  - Live read-only Sleeper spot-check for the six BYO draft IDs -> all complete, 12-team, Superflex, linear drafts with 3-5 rounds.
  - `git diff --check origin/main...HEAD` -> passed.
- Product alignment: Data/artifact-only PR; no code changes, no K application, no curve regeneration, no model/PVO/frontend/MFL path changes.
- Drift risks: None identified. The resulting K remains 0 and curve changes remain gated on separate David approval.
- Handoff / next step: Send CLEAR to Claude Code pane `dynasty:1.1`; Gemini governance can proceed.

## 18:30 ET - Codex (Post-Increment C Next-Move Sequencing Input)

- Task: Provide technical sequencing input after Increment C populated the SF-QB calibration corpus and confirmed K remains 0.
- Governance read: Same-session governance context remains in force: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, sleeper skill, and this ledger.
- Active phase / surface: Phase 24 follow-up scoping; recommendation only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or state-board changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks: No tests run; consultation-only.
- Product alignment: Recommended sequencing that uses the completed calibration evidence, keeps K/curve unchanged, and prioritizes backend-only overlay value without lifting frontend HOLD or allowing market-derived training leakage.
- Drift risks: MFL overlay consumer wiring must stay report-only/overlay-only with explicit no-PVO/no-training contracts; NFL mock aggregation remains a broader source/license problem.
- Handoff / next step: Send recommendation to Claude Code pane `dynasty:1.1` for David's decision.

## 18:40 ET - Codex (Increment B MFL Rookie ADP Divergence Design Review)

- Task: Review pre-spec design for a standalone MFL rookie ADP vs model divergence artifact.
- Governance read: Same-session governance context remains in force: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment B design consultation; backend artifact only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or state-board changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Inspected `scripts/build_universe_market_divergence.py`, `src/dynasty_genius/universe_market_divergence.py`, `src/dynasty_genius/adapters/mfl_adp_adapter.py`, `src/dynasty_genius/adapters/market_source.py`, and `resources/prospect_cards.json` schema.
- Product alignment: Supported artifact-only, backend/report-scoped design with `decision_supported=False`, neutral language, and explicit no-PVO/no-training/no-frontend contracts.
- Drift risks: Spec must avoid assuming `MflAdpMarketSource.fetch()` exposes transient source timestamp/caveats; it returns rows only by contract. Identity join and rank-basis choices need to be explicit before planning.
- Handoff / next step: Send design-review findings to Claude Code pane `dynasty:1.1`.

## 18:45 ET - Codex (Increment B MFL Rookie ADP Divergence Spec Review)

- Task: Review `docs/superpowers/specs/2026-05-27-mfl-rookie-adp-divergence-design.md` before writing-plans.
- Governance read: Same-session governance context remains in force: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment B spec review; backend artifact only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read the MFL rookie ADP divergence spec.
  - Verified the referenced `prospect_identity_resolver.normalize_name`, MFL adapter rows/caveats split, and `prospect_cards.json` rank fields.
- Product alignment: Spec captures artifact-only scope, new adapter helper without changing `MarketSource.fetch()`, xVAR class-rank primary model rank, fail-closed season/name/position joins, read-only state contracts, coverage counts, neutral flags, and `decision_supported=False`.
- Drift risks: No blocking gaps. Two wording nits remain for plan clarity: specify whether markdown is latest-only or run+latest, and avoid promising a raw source timestamp field unless the helper exposes one rather than only freshness caveats.
- Handoff / next step: Send CLEAR with LOW clarifications to Claude Code pane `dynasty:1.1`; David can do final read before writing-plans.

## 18:48 ET - Codex (Increment B MFL Rookie ADP Divergence Spec CLEAR-FINAL)

- Task: Final re-read of the locked MFL rookie ADP divergence spec after Codex LOW wording locks were folded in.
- Governance read: Same-session governance context remains in force: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment B final spec review; backend artifact only; frontend HOLD remains.
- Intended or completed write scope: Append ledger entry only. No code or spec changes.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Re-read `docs/superpowers/specs/2026-05-27-mfl-rookie-adp-divergence-design.md` at locked commit.
  - Confirmed writer output is explicitly run+latest JSON and MD, and source freshness is artifact-level caveats only with no inferred per-row source timestamp.
- Product alignment: Locked spec remains artifact-only, read-only over model output, no endpoint/frontend, no PVO/team/trade/training feed, MFL barred from calibration/training, neutral language, and `decision_supported=False`.
- Drift risks: None remaining at spec level.
- Handoff / next step: Send CLEAR-FINAL to Claude Code pane `dynasty:1.1`; David can approve for writing-plans.

## 02:30 ET - Claude Code (MFL Rookie ADP Overlay — Increment 1 BUILD COMPLETE)

- Task: Implement Follow-up B Increment 1 (MflAdpMarketSource overlay adapter) via the cockpit TDD motion (Codex test-drove RED contracts; Claude implemented to green), then drive Task 10 full-suite + governance validation.
- Branch: feature/mfl-rookie-adp-overlay. Spec docs/superpowers/specs/2026-05-27-mfl-rookie-adp-overlay-design.md; plan docs/superpowers/plans/2026-05-27-mfl-rookie-adp-overlay.md.
- Tasks (each committed): 1 fixtures 0e4abaa · 2 leakage-gate 8e73d33 · 3 registry 92ed552 · 4 scaffold 2043598 · 5 freshness clocks 3d92542 · 6 fetch_adp 3-stage dfcdb84 · 7 fetch_players 0c87af0 · 8 normalize/join f8884e4 · 9 wrapper 05c222f · 10 registry-expected-set fix c87b357.
- Tests / checks:
  - Full suite: 1277 passed, 11 skipped, 0 failed (`./.venv/bin/python3.14 -m pytest -q`). 17 dedicated adapter tests.
  - `./.venv/bin/python3.14 scripts/validate_governance.py` -> Governance validation passed.
  - Task 10 full-suite caught one cross-file regression (test_registry_has_all_expected_sources) the focused per-task runs missed; fixed in c87b357.
  - PVO contract test tests/contract/test_market_overlay_pvo.py UNTOUCHED (verified); app/cache/mfl_adp/ not committed and gitignored.
  - ruff clean on all touched files.
- Product alignment: Real completed-draft MFL rookie ADP is now ingestible as a market overlay. Two cached fetches (ADP + TYPE=players), each 3-stage degraded and independent; normalized rows carry decision_supported=False + intrinsic caveats mfl_adp_format_blended_qb_count / mfl_adp_te_premium_unfiltered; two freshness clocks (fetched_at = refresh; adp.timestamp publish age = disclosure). Registered as market_overlay; leakage gate bars draft_selection_pct/drafts_selected_in from training.
- Drift risks: None. Adapter is fully UNWIRED (no endpoint/Engine/PVO/frontend consumer); MFL aggregate explicitly NOT for SF-QB calibration; no model pkl/manifest/contract change; frontend HOLD + NOISE_BAND lock intact.
- Handoff / next step: Open the Increment 1 PR; Codex technical review + Gemini governance read; merge on clear. Increment 2 (Sleeper calibration corpus) and Increment A (mocks) remain deferred.

## 09:00 ET - Gemini (PR #46 PM Governance Sign-Off)

- Task: Final PM and governance review of PR #46 (MFL Rookie ADP Ingestion).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B (MFL Rookie ADP Ingestion - Increment 1); overlay adapter only; frontend HOLD remains.
- Intended or completed write scope: Governance review and sign-off ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Ran validation suite: `scripts/validate_governance.py` (passed).
  - Ran full test suite: 1277 passed, 11 skipped, 0 failed.
- Product alignment: Verified all 4 core governance axes of the PR:
  1. Every normalized MFL row is strictly overlay/inference-only (`decision_supported=False`, `source="mfl_rookie_adp"`) and entirely barred from Engine A/B training inputs via the leakage-gate `PROHIBITED_COLUMNS` lock on `draft_selection_pct` and `drafts_selected_in`.
  2. Scope is rigorously minimal and 100% UNWIRED (no endpoint/Engine/PVO/frontend changes). The MFL aggregate is explicitly excluded from the SF-QB calibration scripts/logic.
  3. The normalized rows carry the mandatory intrinsic caveats (`mfl_adp_format_blended_qb_count` and `mfl_adp_te_premium_unfiltered`) to ensure transparent disclosure of MFL's format blends.
  4. No qualitative or banned-verdict language has been introduced. The `NOISE_BAND`, model `.pkl` files, manifests, and the Phase 12 frontend `HOLD` remain completely untouched.
- Drift risks: None. The implementation is isolated and strictly adheres to the approved specification.
- Handoff / next step: Governance sign-off complete. Ready for David's merge decision.

## 10:15 ET - Gemini (SF-QB Calibration Corpus Expansion Pre-Spec Review)

- Task: Design review (pre-spec) of Phase 24 Follow-up B Increment 2 (Sleeper calibration corpus expansion).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 design; calibration capability only; frontend HOLD remains.
- Intended or completed write scope: Design review entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Reviewed `scripts/calibrate_sf_qb_knob.py` implementation, pure helpers, rounding contracts, and data-flow structure.
- Product alignment: Confirmed the proposed pre-spec design aligns with all core governance parameters:
  1. Separation: Sleeper draft results (manager behavior) and NFL draft capital remain strictly read-only calibration inputs. They do not enter Engine A or B features.
  2. Protection: Curated draft files (`resources/sf_rookie_draft_ids.json`) are protected by a strict, programmatically enforced hard gate (Superflex + 12-team + completed rookie draft) to block 1QB or non-12-team dilution.
  3. Gated Curve Change: Recommended K remains completely diagnostic (an artifact recommendation report), with actual curve regeneration and code changes to `sf_qb_promote_slots` strictly gated on David's explicit post-calibration approval.
  4. Neutrality: No qualitative or banned-verdict language; `NOISE_BAND` and frontend `HOLD` remain intact.
- Drift risks: None.
- Handoff / next step: Claude Code is cleared to draft the Increment 2 specification.

## 11:15 ET - Gemini (SF-QB Calibration Corpus Expansion Spec Review)

- Task: Specification review for Phase 24 Follow-up B Increment 2 (Sleeper calibration corpus expansion).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment 2 spec; calibration capability only; frontend HOLD remains.
- Intended or completed write scope: Specification review and sign-off ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read and audited `docs/superpowers/specs/2026-05-27-sf-qb-knob-calibration-corpus-expansion-design.md` on branch `feature/sf-qb-calibration-corpus-expansion`.
  - Verified it implements all 5 Codex must-fixes (1. first-36 pick cap, 2. auction/malformed-pick reject, 3. scoring_settings field name, 4. order-preserving de-duplication, 5. fail-closed fetch exceptions) and retains all 4 core governance axes.
- Product alignment: Perfectly aligned. The spec establishes strict input contracts, pure gate helpers (is_superflex, is_twelve_team), first-36-pick constraints, and additive transparency-preserving rejections/metadata logs. The calibration remains 100% model-blind, diagnostic-only, and fully gated.
- Drift risks: None.
- Handoff / next step: Specification signed off. Claude Code is cleared to write implementation plans and proceed to TDD execution.

## 04:00 ET - Claude Code (SF-QB Calibration Corpus Expansion — Increment 2 BUILD COMPLETE)

- Task: Implement Follow-up B Increment 2 (BYO Sleeper rookie-draft ingestion into calibrate_sf_qb_knob.py) via the cockpit TDD motion (Codex test-drove RED contracts; Claude implemented to green), then drive Task 7 full-suite + governance.
- Branch: feature/sf-qb-calibration-corpus-expansion. Spec docs/superpowers/specs/2026-05-27-sf-qb-knob-calibration-corpus-expansion-design.md; plan docs/superpowers/plans/2026-05-27-sf-qb-knob-calibration-corpus-expansion.md.
- Tasks (each committed): 1 loader 76b29bd · 2 SF/12T/format helpers b50dc49 · 3 gate_byo_draft 660649c · 4 _build_byo_board 8425100 · 5 _collect_byo_boards + chain draft_id 751c5a1 · 6 main() integration eb2966c.
- Tests / checks:
  - Full suite: 1292 passed, 11 skipped, 0 failed (`./.venv/bin/python3.14 -m pytest -q`). +12 dedicated calibration tests.
  - `./.venv/bin/python3.14 scripts/validate_governance.py` -> Governance validation passed.
  - Diagnostic-only confirmed: draft_pick_valuation.py / draft_pick_value_curve_v1.json / build_draft_pick_value_curve.py UNTOUCHED (no K application, no curve regen).
  - ruff clean on all touched files.
- Product alignment: calibrate_sf_qb_knob.py now ingests curated BYO Sleeper rookie-draft IDs (resources/sf_rookie_draft_ids.json, ships empty = backward-compatible no-op). Each BYO draft is hard-gated SF (SUPER_FLEX exact token) + 12-team + completed rookie; PPR/TEP recorded as format_meta only. First-36 cap (sort by pick_no, keep <=36) with n_picks_raw/used/excluded provenance. Fail-closed throughout: missing_league_id / fetch_failed / malformed_picks / invalid_draft_class / malformed_draft_settings / unsupported_draft_type (auction) / duplicate_draft_id (within-file) / duplicate_existing_draft (cross-source vs chain) / rank_map_unavailable (data-coverage class excluded from K math) — all recorded in the artifact's new `rejected` list, never silently dropped. Promotion/K math unchanged.
- Drift risks: None. Read-only Sleeper; MFL aggregate stays barred from calibration; setting sf_qb_promote_slots=K + curve regen remain GATED on David's explicit later approval; no model pkl/manifest/contract change; frontend HOLD + NOISE_BAND lock intact.
- Handoff / next step: Open the Increment 2 PR; Codex technical review + Gemini governance read; merge on clear. Capability ships dormant until David curates real SF rookie draft IDs into the resource file and re-runs.

## 13:40 ET - Gemini (PR #48 PM Governance Sign-Off)

- Task: Final PM and governance review of PR #48 (SF-QB Calibration Corpus Expansion).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B (SF-QB Calibration Corpus Expansion - Increment 2); calibration capability only; frontend HOLD remains.
- Intended or completed write scope: Governance review and sign-off ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Ran validation suite: `scripts/validate_governance.py` (passed).
  - Ran full test suite: 1292 passed, 11 skipped, 0 failed.
- Product alignment: Verified all 4 core governance axes of the PR:
  1. Every curated Sleeper draft result (manager behavior) and NFL draft capital remain strictly read-only calibration inputs. They do not enter Engine A or B features, and the boundary is preserved.
  2. Curated drafts are securely protected by the strict Superflex + 12-team + completed rookie draft hard gate, while recording scoring settings metadata as caveats only. MFL aggregate ADP remains completely barred.
  3. The implementation is rigorously diagnostic-only. Setting `sf_qb_promote_slots` and regenerating the pick-value curve remain completely unchanged and strictly gated on David's explicit post-calibration approval.
  4. The network calls are read-only, no banned-verdict language is introduced, and the `NOISE_BAND` and Phase 12 frontend `HOLD` remain fully locked. No model `.pkl`, manifest, or contract changes are present.
- Drift risks: None.
- Handoff / next step: Governance sign-off complete. Ready for David's merge decision.

## 15:35 ET - Gemini (Session Closeout & Scoping Consultation)

- Task: Review session accomplishments and provide PM/governance scoping consultation on next candidate moves (Options A/B/C/D).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up closeout and scoping; backend only; frontend HOLD remains.
- Intended or completed write scope: Session closeout and scoping consultation ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Confirmed main checked out clean at `aa02d75`; all PRs merged; 1292 tests passed.
- Product alignment: Handed off a comprehensive governance risk assessment for David's decision:
  * Option C (curating real SF rookie draft IDs to run calibration) is classified as EXTREMELY LOW RISK/diagnostic-only and highly recommended as the immediate next step.
  * Option B (consumer wiring of MFL overlay) is MODERATE RISK, requires strict API-only boundary locks (frontend HOLD intact), and zero-leakage contract checks.
  * Option A (NFL mock aggregation) is HIGH RISK/large lift, requiring dedicated licensing specifications and rigorous mock-source leakage testing.
- Drift risks: None.
- Handoff / next step: Session closed. Standing by for David's instructions.

## 18:22 ET - Gemini (PR #50 Curated Calibration Corpus Review)

- Task: Review curated SF calibration corpus and the new run of record (PR #50).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B (Curated Calibration Corpus - Option C); calibration capability only; frontend HOLD remains.
- Intended or completed write scope: Specification review and sign-off ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Reviewed curated draft list in `resources/sf_rookie_draft_ids.json` (6 drafts, properly documented with rejection/inclusion rationale).
  - Reviewed the new calibration output `sf_qb_knob_calibration_20260527T221751Z.json` containing 12 drafts, 50 matched QBs, raw median of -2.0, and recommended K of 0.
  - Ran validation suite: `scripts/validate_governance.py` (passed).
- Product alignment: Perfectly aligned:
  1. Diagnostic-only: Recommended K is 0, which means the knob remains off, the pick-value curve and constants are completely unchanged, and no regeneration is required.
  2. The curated drafts are strictly gated SF+12T+rookie serpentines, ensuring data purity.
  3. No banned verdict or qualitative language is introduced, and frontend/model-blind boundaries are strictly maintained.
- Drift risks: None.
- Handoff / next step: Governance review complete. Ready for David's merge decision.

## 18:35 ET - Gemini (Calibration & Wiring Scoping Consultation)

- Task: Review Option C completion and provide PM/governance scoping consultation on next candidate moves (Option B / A / W).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up closeout and scoping; backend only; frontend HOLD remains.
- Intended or completed write scope: Scoping consultation ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Confirmed main checked out clean at `c73767d`; all PRs merged; 1292 tests passed.
- Product alignment: Handed off a comprehensive governance risk assessment for the next sprint directions:
  * Option W (widening the rounds gate to <= 7 or <= 8) is classified as EXTREMELY LOW RISK and structurally sound, as long as the first-36 pick cap protects the data from dilution.
  * Option B (consumer wiring of MFL overlay as a backend-only report/API) is MODERATE RISK, highly prioritized for immediate product value, and protected by strict model-blind/PVO-isolated constraints.
  * Option A (mock aggregation) remains HIGH RISK/large lift, deferred behind stabilization of the active overlays.
- Drift risks: None.
- Handoff / next step: Session closed. Standing by for David's instructions.

## 18:42 ET - Gemini (MFL ADP Divergence Design Pre-Spec Review)

- Task: Design review (pre-spec) of Phase 24 Follow-up B Option B (MFL Rookie ADP Divergence Report).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Option B design; overlay reporting capability only; frontend HOLD remains.
- Intended or completed write scope: Design review entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Audited proposed design flow: MflAdpMarketSource -> join prospect_cards by normalized name -> generate standalone JSON + MD reports.
- Product alignment: Confirmed the proposed pre-spec design aligns with all core governance parameters:
  1. Separation: Read-only model state is consumed to build a separate, isolated diagnostic report. Under no circumstances does this touch the PVO, team value matrices, or contender posture, keeping overlays isolated.
  2. Byte-Unchanged Test: Endorsed the proposed contract test verifying that executing the builder leaves all input model/PVO states 100% byte-identical.
  3. Artifact-Only: Agreed that artifact-only generation is the lowest-risk and most robust design under the frontend HOLD.
  4. Calibration Isolation: MFL data remains excluded from calibration.
  5. Neutrality: Gap calculation uses an aligned band of 3 with neutral status flags (aligned_within_band, model_higher_than_market, market_higher_than_model) and no qualitative or banned verdict language.
- Drift risks: None.
- Handoff / next step: Claude Code is cleared to draft the Option B specification.

## 18:46 ET - Gemini (MFL ADP Divergence Spec Review)

- Task: Specification review for Phase 24 Follow-up B Option B / Increment B (MFL Rookie ADP Divergence Report).
- Governance read: `docs/governance/02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger.
- Active phase / surface: Phase 24 Follow-up B Increment B spec; overlay reporting capability only; frontend HOLD remains.
- Intended or completed write scope: Specification review and sign-off ledger entry only.
- Files changed:
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - Read and audited `docs/superpowers/specs/2026-05-27-mfl-rookie-adp-divergence-design.md` on branch `feature/mfl-rookie-adp-divergence`.
  - Verified it incorporates all 6 Codex clarifications (including narrow adapter helper `fetch_rookie_adp_rows` to isolate caveats, PVO byte-identical contract test, and explicit coverage block assertions) and retains all 4 core governance axes.
- Product alignment: Perfectly aligned. The spec establishes strict inputs, a fail-closed name+position join key within the season, a robust coverage block with `decision_supported_true_count == 0` and `banned_language_present == []` self-checks, and a byte-identical read-only state contract test.
- Drift risks: None.
- Handoff / next step: Specification signed off (confirmed and held after commit 4e65c5f's two minor, governance-neutral wording locks: run+latest dual-saving and artifact-level caveats). Claude Code is cleared to proceed.

## 18:56 ET - Codex (MFL ADP Divergence Task 1 RED)

- Task: Author Task 1 RED contracts for `fetch_rookie_adp_rows`.
- Governance read: Same-session governance context from `02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, and this ledger; TDD cockpit protocol in effect.
- Active phase / surface: Phase 24 Follow-up B Increment B; backend artifact-only MFL rookie ADP divergence; frontend HOLD remains.
- Intended or completed write scope:
  - Added failing adapter contracts only.
  - No production code, endpoint, PVO/team/trade/training feed, calibration path, or frontend files touched.
- Files changed:
  - `tests/test_mfl_adp_adapter.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_adp_adapter.py -k fetch_rookie_adp_rows -v` -> RED as expected: `AttributeError: module 'src.dynasty_genius.adapters.mfl_adp_adapter' has no attribute 'fetch_rookie_adp_rows'`.
  - `./.venv/bin/ruff check --select I tests/test_mfl_adp_adapter.py` -> passed.
  - `./.venv/bin/ruff check tests/test_mfl_adp_adapter.py` -> passed.
- Product alignment: Contracts lock the narrow adapter helper returning `(rows, caveats)` while preserving `MflAdpMarketSource.fetch()` as rows-only; row-level intrinsic caveats stay separate from freshness/channel caveats.
- Drift risks: None beyond the intended RED; assertions behind the missing helper will activate after implementation.
- Handoff / next step: Claude Code to implement `fetch_rookie_adp_rows` and commit test+implementation together.

## 18:59 ET - Codex (MFL ADP Divergence Task 2 RED)

- Task: Author Task 2 RED contracts for the pure `build_mfl_rookie_adp_divergence` builder.
- Governance read: Freshly read `02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, this ledger, and the TDD skill before editing.
- Active phase / surface: Phase 24 Follow-up B Increment B; backend artifact-only MFL rookie ADP divergence; frontend HOLD remains.
- Intended or completed write scope:
  - Added the new pure-builder test file only.
  - No production code, endpoint, PVO/team/trade/training feed, calibration path, or frontend files touched.
- Files changed:
  - `tests/test_mfl_rookie_adp_divergence.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_rookie_adp_divergence.py -q` -> RED as expected: `ModuleNotFoundError: No module named 'src.dynasty_genius.mfl_rookie_adp_divergence'`.
  - `./.venv/bin/ruff check --select I tests/test_mfl_rookie_adp_divergence.py` -> passed.
  - `./.venv/bin/ruff check tests/test_mfl_rookie_adp_divergence.py` -> passed.
- Product alignment: Contracts lock season-isolated name+position joins, fail-closed ambiguity, xVAR-class-rank primary model rank, neutral divergence flags, `decision_supported=False`, coverage reconciliation, and banned-language guard reporting.
- Drift risks: None beyond the intended RED; detailed assertions will activate once Claude creates the builder module.
- Handoff / next step: Claude Code to implement `src/dynasty_genius/mfl_rookie_adp_divergence.py` and commit test+implementation together.

## 20:50 ET - Codex (MFL ADP Divergence Task 3 RED)

- Task: Author Task 3 RED contract for run+latest JSON/MD artifact writer.
- Governance read: Same-session governance context remains in force from `02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, this ledger, and the TDD skill.
- Active phase / surface: Phase 24 Follow-up B Increment B; backend artifact-only MFL rookie ADP divergence; frontend HOLD remains.
- Intended or completed write scope:
  - Added the writer contract to the existing divergence test file only.
  - No production code, endpoint, PVO/team/trade/training feed, calibration path, or frontend files touched.
- Files changed:
  - `tests/test_mfl_rookie_adp_divergence.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_rookie_adp_divergence.py -k writer -v` -> RED as expected: `ImportError: cannot import name 'write_mfl_rookie_adp_divergence_artifacts'`.
  - `./.venv/bin/ruff check --select I tests/test_mfl_rookie_adp_divergence.py` -> passed.
  - `./.venv/bin/ruff check tests/test_mfl_rookie_adp_divergence.py` -> passed.
- Product alignment: Contract locks exact run+latest JSON/MD filenames, JSON content preservation, markdown visibility for matched row and neutral flag, and explicit `decision_supported: false` reporting.
- Drift risks: None beyond the intended RED; assertions will activate once Claude adds `_render_md` and `write_mfl_rookie_adp_divergence_artifacts`.
- Handoff / next step: Claude Code to implement the writer and commit test+implementation together.

## 20:57 ET - Codex (MFL ADP Divergence Task 4 RED)

- Task: Author Task 4 RED contract for build script and read-only state guarantee.
- Governance read: Same-session governance context remains in force from `02-agent-operating-loop.md`, `00-product-constitution.md`, `01-north-star-architecture.md`, `03-code-hygiene-policy.md`, `AGENT_SYNC.md`, this ledger, and the TDD skill.
- Active phase / surface: Phase 24 Follow-up B Increment B; backend artifact-only MFL rookie ADP divergence; frontend HOLD remains.
- Intended or completed write scope:
  - Added the script/read-only state contract to the existing divergence test file only.
  - No production code, endpoint, PVO/team/trade/training feed, calibration path, or frontend files touched.
- Files changed:
  - `tests/test_mfl_rookie_adp_divergence.py`
  - `docs/agent-ledger/2026-05-27.md`
- Tests / checks:
  - `./.venv/bin/python3.14 -m pytest tests/test_mfl_rookie_adp_divergence.py -k read_only -v` -> RED as expected: `ModuleNotFoundError: No module named 'scripts.build_mfl_rookie_adp_divergence'`.
  - `./.venv/bin/ruff check --select I tests/test_mfl_rookie_adp_divergence.py` -> passed.
  - `./.venv/bin/ruff check tests/test_mfl_rookie_adp_divergence.py` -> passed.
- Product alignment: Contract locks byte-identical `resources/prospect_cards.json` and `app/data/valuation/universe_pvo_latest.json` before/after the script, plus exact output confinement to the four divergence artifact files in the requested output directory.
- Drift risks: None beyond the intended RED; assertions will activate once Claude adds `scripts/build_mfl_rookie_adp_divergence.py`.
- Handoff / next step: Claude Code to implement the build script and then drive Task 5 full-suite/governance/ledger.

## 06:30 ET - Claude Code (MFL Rookie ADP Divergence Report — Increment B BUILD COMPLETE)

- Task: Implement Follow-up B Increment B (wire dormant MflAdpMarketSource into a standalone read-only divergence report) via the cockpit TDD motion (Codex test-drove RED; Claude greened), then drive Task 5 full-suite + governance.
- Branch: feature/mfl-rookie-adp-divergence. Spec docs/superpowers/specs/2026-05-27-mfl-rookie-adp-divergence-design.md; plan docs/superpowers/plans/2026-05-27-mfl-rookie-adp-divergence.md.
- Tasks (each committed): 1 fetch_rookie_adp_rows helper 359f16e (+ flawed-contract-test fix a7e8f46 — substring check replaced with behavioral) · 2 pure builder 0779312 · 3 writer c01c2be · 4 build script + read-only state contract aef8c0c.
- Tests / checks:
  - Full suite: 1303 passed, 11 skipped, 0 failed (`./.venv/bin/python3.14 -m pytest -q`). 9 dedicated divergence tests + 2 adapter-helper tests.
  - validate_governance passed; ruff clean on all touched files.
  - No app/api / models / app/services / PVO-contract-test changes (decision-surface untouched).
- Product alignment: turns the dormant MFL rookie ADP overlay into a live, inspectable signal. scripts/build_mfl_rookie_adp_divergence.py reads MFL rookie ADP (read-only) + prospect_cards (read-only), fail-closed name+position join within draft_class==season, and writes a SEPARATE artifact (mfl_rookie_adp_divergence_{latest,<run>}.{json,md}) showing market_adp_rank vs xvar_class_rank (model_rank), rank_gap + neutral flag (aligned / model_higher_than_market / market_higher_than_model), with a coverage block. xvar_class_rank primary (dvs emitted; model_rank_unavailable when missing).
- Drift risks: None. Read-only state contract test asserts prospect_cards.json + universe_pvo_latest.json byte-identical and writes confined to the divergence artifact paths. decision_supported=False on artifact + every row (decision_supported_true_count==0, banned_language_present==[] asserted). Artifact-only (no endpoint/frontend); MFL barred from calibration/training; no model pkl/manifest/contract change; MarketSource.fetch() contract untouched.
- Handoff / next step: Open the Increment B PR; Codex technical review + Gemini governance read; merge on clear.
