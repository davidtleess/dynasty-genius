# 2026-08-25 — Dynasty Genius Agent Ledger

## Morning — lane ClaudeFable5 (DG-029 · DG-004 · DG-021) `[w#DG-029]` `[w#DG-004]` `[w#DG-021]`

**David's rulings this morning:** all future tickets land on `main` (`feature/outcome-loop-week1`
retired as a landing base; the trunk switches to `main` after today's 09:00–10:15 producer window),
and DG-029 → DG-004 → DG-021 in that order. DG-023 handed to David's parallel session, worktree
prepped on `main` with its pushed half-fix cherry-picked (`f2e09abd`, its 7 contract tests green).

### DG-029 — landed on `main` as merge `849f3eaf` (gate: 6037 passed / 0 failed)

**The answer, re-measured at close, not relayed: 2024 is absent from the training table BY
DESIGN — the same `apply_inference_partition` mechanism as the runtime, not a second hole.**
`training_eligible = feature_season < (inference_season - 1)` (`feature_assembly.py:89`, applied
`:325`) makes 2024 the in-between season of the 2-year outcome horizon. Proof a gap cannot fake:
2025 sits in the same table with 505 rows, zero outcomes, zero eligible. What landed is the 08-19
Judge-implementer lane's contract test (8 tests, pins the rule with exact surviving sets),
cherry-picked onto `main`; its worktree-local duplicate of the 08-19 ledger was deliberately NOT
re-planted (DG-032). **This close also resolves a contradiction inside the 08-19 trunk ledger:**
the Consultant lane's "gap, not a design choice" claim (grounded only in 2024 existing upstream)
is wrong — upstream presence is consistent with a downstream partition dropping it.

### DG-004 — repo-root leakage report was a stale one-off artifact; deleted

Untracked (`git ls-files` no match), writer `models/leakage.py:54` has zero production callers,
`adp_sleeper` not in the current training matrix. Live systemic guards named in the ticket close:
the `training-csv-market-leakage` pre-commit hook and the feature gate's temporal-leakage check.
Removed with `rm` (untracked, so no commit carries it; the ticket close is the record).

### DG-021 — a row with no prior now says so

**The defect:** the dead-window else-arm (`pvo_assembler.py`, prior `:459-467`) ran precisely
because NO Engine A result existed, yet stamped `dvs_engine="A"` and appended "Engine A prospect
score used as prior" — spec 3.4's "'A' as provenance marker" put a false self-description on 114
served player cards, and the player API served such rows as `modeled` with `degradation=None`
beside a null score.

**The fix, two surfaces, both TDD'd (RED watched: 3 failed for the expected reasons, then GREEN):**
1. `pvo_assembler.py` else-arm: `dvs_engine` stays `None` (the field names the engine that
   produced the score; none did) and the caveat now reads "…no dynasty value score available (no
   Engine A prior, no reliable Engine B projection)". The TRUE prior claim in the elif-arm — where
   an Engine A prior genuinely is used — survives, and a test pins that it must.
2. `app/api/routes/players.py`: a `modeled` row with a null `dynasty_value_score` now always
   carries a degradation notice instead of `degradation=None`.

**Disclosed contract-test changes:** `test_phase15_blend.py` (spec 5.10) and
`test_phase14_dvs.py` (spec 5.5) both pinned the false behavior and were amended with disclosure
comments. New pins in `tests/contract/test_dg021_no_prior_says_so.py` (6 tests: both caveat
directions, route honesty, and the serving surface both ways). No API schema change — the
`degradation` field and plain-string `model_status` already existed, so no OpenAPI regen needed.

**Not yet true in production:** the live serving artifact still carries the 114 false rows until
the universe producer regenerates it from the fixed code — that is the deployment step after this
lands and the trunk is updated (post-window), and the row count at regen is the acceptance check.

## 11:5x ET — lane ClaudeFable5: session closeout — deployment done, everything confirmed `[w#DG-021]` `[w#DG-023]`

**The "not yet true in production" above is now resolved, twice over.** On David's "go ahead",
post-window at 10:20–10:21 EDT: the trunk switched to `main@b291107f` (the three dirty files on
main-deleted paths archived in `dg-build/preserved/2026-08-25-trunk-switch/` AND `stash@{0}`
before the switch — preserved twice, destroyed nowhere; dirty count 47→25, remainder carried
over), and `run_pvo_refresh.py` was hand-run with launchd's exact invocation → `status ok`,
exit 0. Acceptance on `universe_pvo_runtime.json` (12,225 rows, same path both times): false
rows **114 → 0**, old caveat **114 → 0**, honest no-score caveat on **exactly 114** rows, all
`dvs_engine=None`. **Production-confirmed at 11:30** by the scheduled run from launchd's own
trigger: exit 0, `status ok`, vintage `15:30:02Z`, same three numbers. Nothing about DG-021
remains open.

**Trunk state at close:** `main@b4662707`, level with origin (fast-forwarded after the parallel
session landed DG-023; overlap 0 of 25 dirty paths). Every scheduled producer now runs from
current `main`. Today's in-window producers (09:00–10:15) ran from the OLD code by design — the
switch waited for the window; tomorrow's full cycle is the first all-from-main day.

**Closed today, all with measured acceptance:** DG-029 (`849f3eaf`), DG-004 (no commit — untracked
artifact deleted, ticket is the record), DG-021 (`b291107f` + deployment), DG-023 (`b4662707`,
parallel session — whose lane also proved the cherry-picked producer half was INERT and fixed the
reader, the actual surface). DG-041 filed, not fixed (permanently-red inputs gate, scoped out of
DG-023 by David). Session boundary: this entry closes the lane; sequencing of DG-022 / DG-041 /
DG-020 / SR-11 (D4 = 08-26) is David's word.

## 17:0x ET — lane ClaudeFable5-DG041: the inputs gate stops crying wolf `[w#DG-041]`

**DG-041 fixed at the cause.** `load_participation`'s own first line is
`max_season = get_current_season(roster=True) - 1` — the source refuses the current roster season
*by construction*, so asking for it guaranteed a ValueError, a recorded fallback, and
`inputs_degraded` on every scheduled run, forever. The fix is the ticket's cheapest shape: a
per-stream source CEILING beside the existing floor in `_STREAM_LOADERS`
(`scripts/run_feature_refresh.py`), expressed as the client's own formula so it cannot rot as
seasons roll. The window now never requests a season the source cannot serve: no refusal, no
fallback, and `fallback_used` goes back to meaning *something happened*.

**TDD, RED watched (3 failures, each for the expected reason — double-request after refusal,
`degraded is True`, uncapped forward-year ask):** new
`tests/contract/test_dg041_stream_source_ceiling.py` pins (1) a capped stream is asked ONCE with
the capped window while uncapped streams still get the full window, (2) the health gate reads the
resulting provenance as `inputs_live` — the whole ticket in one assertion, (3) the ceiling tracks
`get_current_season(roster=True) - 1` in a 2031 geometry, not a hardcoded 2025. DISCLOSED TEST
CHANGE (DG-031 precedent) in `test_feature_refresh_source_isolation_red.py`: the fake provider
gains the real module's `get_current_season`, and participation's CH1 expectations change from
"refused then stepped back" to "capped up front — no fallback to record". Full suite in the
worktree: **6,070 passed, 0 failed, zero collection errors** (6,067 at DG-023's land + 3 new).

**Blast radius, measured and disclosed, not discovered later:** the frames are byte-identical
(participation already served the capped window via the fallback) — what changes in the C4 hash
inputs is `stream_provenance.participation`: `fallback_used true→false`, `error_type
"ValueError"→null`. So the FIRST run on this code will see a moved `source_hash` and regenerate
the candidate once, then settle. That is the honest cost of the provenance telling the truth.

**Deployment sequencing (deliberate):** landed on `origin/main` today, but the trunk is NOT
pulled yet — tomorrow ~09:00 is DG-023's first scheduled production run and it stays
single-variable on `main@a61f0fbe`. The trunk pulls post-window on 08-26; DG-041 is live from
08-27 with seven scheduled runs before the 09-04 freeze. Acceptance in production: the 08-27
`feature_refresh_latest_report.json` shows participation `fallback_used=false`, and
`/api/health`'s inputs line reads `inputs_live` on a healthy day.
## 19:1x ET — lane ClaudeFable5-DG022: the 08-19 WIP rebased, finished, and proven on the real surface `[w#DG-022]`

**DG-022 resumed exactly where its author's handoff said to.** The 08-19 WIP (origin/ticket/DG-022
@ `86e9d218`, "implementation complete, blocked at real-surface QA and DG-031 sequencing") was
cherry-picked onto current `main` in a freshly recreated worktree. Both handoff blockers had
dissolved: DG-031 landed via PR #159, and the QA is now run. Conflicts went the mandated ways —
the 08-19 ledger add/add kept the trunk's canonical file (the WIP draft survives on the branch),
and the four generated OpenAPI seams were REGENERATED from the rebased contract, never
hand-merged: regen came back byte-identical for `openapi.json`/`types.gen.ts`/`zod.gen.ts` and
corrected `index.ts`, whose WIP side predated main's Coverage/ModelScoreboard/BackupHealth types
— the exact silent-revert the regen-trap memory warns about, caught by the regen.

**Analysis fan-out (6 agents, adversarially verified) + hand-verification found and fixed four
things the WIP could not have known:** (1) its compact inspector variant minted a duplicate
"2026 model evaluation" region landmark beside the card's — axe `landmark-unique`, fixed
TDD (compact renders no landmark; card's pinned); (2) its API tests' hermeticity rule now had a
third seam — without patching `_load_frozen_prediction_membership`, DG-021's API tests silently
read the production capture DB (disclosed patch); (3) `with sqlite3.connect(...)` leaked the
handle — `contextlib.closing` now, the scorer's idiom; (4) the reachable classifier arm
captured+null-projection had no pin — pinned. The predicted `visualCraftAuditBaseline.json`
mis-merge hazard did NOT occur (hunk verified on the right block; audit test green).

**Real-surface QA — the gate the WIP never earned — PASSED.** Live app on the rebased tree,
real Tank Dell (9502): `not_in_frozen_prediction_cohort` / `non_model_route_at_freeze`, frozen
capture 2026-08-05, desktop+mobile+mid-scroll captures, axe scoped to the new component clean,
its elements inside a 390px viewport. The spec now derives coverage text from the LIVE API
(218 of 272 at QA time — card must equal API), replacing the WIP's dated "221 of 274".
Endpoint cost with the coverage join: ~130ms steady over repeated hits — measured, acceptable.
**Pre-existing base defects found by the whole-page gates, NOT this ticket's:** two axe
violations (.dg-two-lane__divergence contrast; dl.dg-two-lane__facts markup) and 390px page
overflow to 776px — proven independent by hiding every DG-022 element and re-measuring (still
776). Filed as DG-043 *(this sentence first landed saying "DG-042", written minutes before the
parallel session's PPG ticket claimed that number — corrected in place 20:2x same day; the
closeout section below records the collision)*. Evidence: `dg-build/preserved/2026-08-25-dg022-qa/`.

**Suites on the finished tree:** backend 6,087 passed / 0 failed / zero collection errors
(6,085 baseline + 1 new pin + 1 dist-conditional unskip, accounted); frontend 73 files /
302 tests; typecheck, build, ruff-on-touched-files, `git diff --check` all clean. The 08-19
`runs/` record moved off the landing tree (preserved in dg-build + on the ticket branch).

## 20:0x ET — lane ClaudeFable5: evening closeout — seven tickets closed today, trunk pull held for the morning `[w#DG-022]` `[w#DG-041]`

**The day, whole:** seven tickets closed with measured acceptance — morning: DG-029 (`849f3eaf`),
DG-004 (untracked artifact, ticket is the record), DG-021 (`b291107f`, deployed 10:21,
production-confirmed 11:38), DG-023 (`b4662707`, parallel session); evening: DG-041 (`b797ee1f`),
DG-042 (`c2b11f0a`, parallel session — PPG "all games" guard), DG-022 (`20807368`, the 08-19 WIP
resumed, hardened, and proven on the real Tank Dell surface). Filed today: DG-041 (closed same
day) and DG-043 (the pre-existing player-card two-lane defects DG-022's whole-page QA found —
this ledger's earlier "Filed as DG-042" reference predates the ID collision with the parallel
session's PPG ticket; the a11y filing is DG-043).

**Trunk state at close, deliberate:** `main@a61f0fbe`, THREE merges behind `origin/main` — held
so tomorrow's ~09:00 producer cycle runs DG-023's first scheduled confirmation single-variable.
Working tree: same 25 carried dirty files as this morning, nothing added tonight (verified at
close). No locks, no temp worktrees, no dev server left running. This entry is committed from a
throwaway detached worktree because the trunk must not move tonight.

**Tomorrow (2026-08-26), in order:**
1. Hands off 09:00–10:15 ET (producer window).
2. Verify DG-023's first scheduled run: `feature_refresh_latest_report.json` + the health
   reader's words (participation described, not "EMPTY"; gate wording per DG-023).
3. Post-window: `git pull --ff-only` in the trunk — brings DG-041 + DG-042 + DG-022 together
   (only DG-041 touches a producer; overlap with the 25 carried dirty paths should be checked
   before pulling, as always).
4. Restart whatever serves the API so DG-022's frozen-prediction lane goes live.
5. SR-11's scheduled slot (D4).
6. DG-041's production acceptance is **08-27**, not tomorrow: expect ONE source_hash move
   (provenance echo, frames identical), then `fallback_used=false` and `inputs_live`.

**Still David-gated:** stale-branch cleanup (`origin/feature/outcome-loop-week1`,
`origin/ticket/{DG-021,DG-022,DG-023,DG-029}` — all landed or superseded), DG-043's post-freeze
status, the three unreviewed preserved trunk-switch files, DG-020's stale lane, DG-017/DG-032.
