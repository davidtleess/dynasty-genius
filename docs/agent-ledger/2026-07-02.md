# Agent Ledger — 2026-07-02

## Codex — DEBT-6 Slice 1 T1 RED
- Bootstrapped per governance: read operating loop, constitution, north-star architecture, code hygiene policy, AGENT_SYNC, and strategic execution charter.
- David authorized DEBT-6 Slice 1 start on branch `feature/debt6-model-provenance`; scope for this pass is test-only T1 RED.
- Authored fixture-only RED tests for `system_model_provenance_models`: registry Pydantic models, typed registry loader error behavior, and runtime environment resolver. Tests use temp registry files and injected env mappings only; no real `app/config` or gitignored `app/data` dependency.

## T1 GREEN - Claude (implementation lead)

- Task: DEBT-6 Slice 1 T1 GREEN — implement `app/api/routes/system_model_provenance_models.py` to pass Codex's RED (`tests/contract/test_system_model_provenance_t1.py`, commit 1e5dfbe).
- Delivered: Pydantic v2 models (all `extra="forbid"`): RegistryArtifact (nullable sha256, path_resolution/governing_pointer/allow_local_override defaults), ModelRegistry, ArtifactProvenance, ModelProvenanceResponse, ModelProvenanceErrorResponse (all `decision_supported: Literal[False]`); `load_model_registry(registry_path=...)` fail-closed → `ModelRegistryLoadError` on missing/malformed/schema-invalid; `resolve_runtime_environment(environ=...)`.
- Verification: 22/22 RED tests pass (0.42s); ruff clean on the new module; adversarial self-probes pass (wrong-type registry_version rejected, invalid DG_RUNTIME_ENV falls through to development/ci, invalid response environment rejected, missing required field rejected).
- NOTE for cockpit: env resolver is PRESENCE-based on `CI` (CI="false" → "ci"), matching the RED's parametrized cases — fail-closed-safe (ambiguous env treated as CI → local_operational expected-absent). Flagging in case the spec §3.1 "truthy" prose should be reconciled to "presence".
- Scope held: models + loader + env resolver ONLY. No classifier (T2), no pointer health (T3), no route/main wiring/OpenAPI (T4). Module is standalone, not imported by main → no app-surface change yet.
- Handoff: GREEN committed local (unpushed). Routing to Codex for T1 adversarial CLEAR.

## T1 GREEN R7 - Claude (implementation lead)

- Task: Close Codex T1 R7 redline — invalid explicit DG_RUNTIME_ENV must fail-closed, not silently demote to development.
- Delivered: added `ProvenanceConfigError` base with `ModelRegistryLoadError` + new `RuntimeEnvironmentError` subclasses (one family for T4's 503 mapping); `resolve_runtime_environment` now raises `RuntimeEnvironmentError` when DG_RUNTIME_ENV is present but not in {development,ci,serving,production} (incl ""). Reconciled spec §3.1 + seed 13 (presence-based CI confirmed; present-but-invalid explicit env → typed error).
- Verification: amended RED (Codex 68627f4) 26/26 pass (0.34s); ruff clean.
- Handoff: GREEN R7 committed local (unpushed). Routing to Codex for final T1 CLEAR.

## Codex — DEBT-6 Slice 1 T2 RED
- Task: authored test-only RED for the pure registered-artifact provenance classifier. Boundary decision: `classify_artifact` takes a `RegistryArtifact`, observed file facts, `pointer_status`, and runtime environment; it performs no disk I/O, hashing, pointer reads, reverse scans, route wiring, or OpenAPI work.
- Covered spec seeds 1–8 and 14–16, plus the §3.4 clean-gate that broken pointer statuses block active+required serving artifacts even when the artifact hash matches. T3 remains responsible for deriving `pointer_status`; T2 owns the severity/serving_allowed meaning of that status.
- Tests are constructed-input only and never touch real `app/data` or `app/config`.

## T2 GREEN - Claude (implementation lead)

- Task: DEBT-6 Slice 1 T2 GREEN — the pure provenance classifier `classify_artifact()` (Codex RED 5697419, 18 tests).
- Delivered (in system_model_provenance_models.py): `classify_artifact(entry, artifact_present, observed_hash, pointer_status, environment) -> ArtifactProvenance` — pure, no disk. observed_status precedence (sha256=None→expected_hash_missing first; absent→missing_required/local_artifact_missing_ci by env-in-required; present→hash compare→ok/hash_mismatch/local_override); `_observed_severity` env-aware severity+serving_allowed; pointer clean-gate overlay (broken pointer on active+required serving → integrity/blocked even when observed=ok, else caveat bump). load_verification_status always not_verified.
- Verification: T2 18/18 (0.29s); T1 26/26 no regression; ruff clean. Adversarial self-probes pass (dev/ci broken-pointer→caveat; ci local_override→caveat; candidate hash-null→caveat; serving override downgrades per R4; production==serving; decision_supported always False).
- Boundary held (Codex-agreed): pointer_status is an INPUT (T3 derives it); no hashing/manifest-reads/reverse-scan/latest_run_dir-resolution/route here.
- Handoff: T2 GREEN committed local (unpushed). Routing to Codex for T2 adversarial CLEAR.

## Codex — DEBT-6 Slice 1 T2 RED Amend
- Added two blocker rows after T2 adversarial review: invalid `classify_artifact(environment="prod")` must raise `RuntimeEnvironmentError` before any hash-ok classification; `allow_local_override` is dev-only, so active+required `local_operational` mismatch in `serving` remains `integrity`/blocked even when the bool is true.

## T2 GREEN R8 - Claude (implementation lead)

- Task: Close Codex T2 R8 (2 blockers): (1) classify_artifact accepted invalid environment ("prod") and reported healthy; (2) allow_local_override wrongly downgraded active+required serving local_override.
- Delivered: classify_artifact now validates environment ∈ valid set at entry → raises RuntimeEnvironmentError before any classification (fail-closed public function). Removed the allow_local_override serving-downgrade for local_override → active+required serving mismatch stays integrity/blocked regardless of the flag; allow_local_override is now consistently DEV-ONLY (escapes only hash_mismatch in development).
- Verification: amended T2 RED (977387a) — T2 20/20, T1 26/26, 46 total (0.38s); ruff clean.
- Handoff: T2 GREEN R8 committed local (unpushed). Routing to Codex for final T2 CLEAR.

## SESSION CLOSEOUT - Claude (implementation lead / debate principal)

- Session arc (2026-07-01 → 2026-07-02): (1) drove the product-report three-disagreement reconciliation to three-way alignment + appended "Team Perspectives" to the report; (2) cockpit prioritization → unanimous order DEBT-6 #1 → Tier-1 graduation #2 → BUILD-4 Superflex-QB #3; (3) wrote + cockpit-CLEARED the DEBT-6 Slice-1 spec/plan (Codex R1–R6); (4) wrote the Strategic Execution Charter (`docs/governance/04`); (5) shipped all planning docs + charter + refreshed priority list to main via PR #106 (merge `9fdac6a`, CI-green, zero-divergence); (6) built DEBT-6 Slice-1 T1 + T2 via cockpit-TDD on branch `feature/debt6-model-provenance`.
- Deliverables this session (DEBT-6 build, LOCAL/UNPUSHED on `feature/debt6-model-provenance` @ `56eb61f`):
    - T1 CLEAR — `app/api/routes/system_model_provenance_models.py`: Pydantic v2 `extra=forbid` models, fail-closed `load_model_registry`, `resolve_runtime_environment` (`1e5dfbe`→`c475d9c`).
    - T2 CLEAR — pure `classify_artifact()` (observed_status precedence, env-aware severity, fail-closed serving_allowed, pointer clean-gate) (`5697419`→`8140325`).
    - Checkpoint `56eb61f` — AGENT_SYNC DEBT-6 in-progress block.
- Verification: 46 tests green (T1 26 + T2 20), ruff clean; both T1 and T2 dual-CLEAR by Codex after adversarial redlines. Cockpit-TDD caught 8 real fail-closed holes across the slice (R1–R6 spec review; R7 invalid explicit DG_RUNTIME_ENV silent-demote; R8 classify_artifact healthy-report on invalid env + allow_local_override too permissive in serving) — every one a genuine "could-report-healthier-than-reality" defect, fixed before code shipped.
- State: nothing pushed (push is David-gated, planned as one PR at the T4 closeout); `-product` worktree on `feature/debt6-model-provenance`, tree clean. Charter LIVE on main; priorities + DEBT-6 checkpoint recorded in AGENT_SYNC (all agents) + Claude auto-memory.
- Drift risks: none open. DEBT-6 micro-decisions still to resolve at their tasks (plan §5): env-var name finalization (done — `DG_RUNTIME_ENV`), T5 hash-seeding staging, reverse-scan glob depth (T3).
- Handoff / next step: RESUME AT T3 — pointer readers (`latest.json`/`v2_manifest.json`/`v3_manifest.json` → `pointer_status`), scoped unregistered-local reverse scan, real streamed sha256 hashing, `latest_run_dir` disk resolution. `classify_artifact` consumes `pointer_status` as an INPUT; T3 produces it. Then T4 (route `GET /api/system/model-provenance` + OpenAPI + full closeout = push/PR checkpoint), T5 (David-authorized registry hash-seeding). Real-shape discipline is critical in T3 (Engine B v2 + Head A te_v3 gitignored → fresh-clone silent fallback is the divergence to surface). Cockpit loop closed with both lanes.

## Codex — DEBT-6 Slice 1 T3 handoff preflight
- Bootstrapped per governance for the resumed T3 handoff: read operating loop, constitution, north-star architecture, code hygiene policy, AGENT_SYNC DEBT-6 checkpoint block, strategic execution charter, and this ledger.
- Repo state checked: branch `feature/debt6-model-provenance` at `9ff5f86`; T1+T2 are recorded CLEAR, draft PR #107 open, next action remains T3 RED after Gemini's framing synthesis returns through Claude.
- Intended T3 RED write scope, once synthesis arrives: test-only contract coverage for pointer readers deriving `pointer_status`, latest-run-dir resolution, streamed sha256 hashing, and R5-scoped unregistered-local reverse scan via temp dirs/dependency injection only. No real `app/data` or `app/config` dependency in committed tests.

## Codex — DEBT-6 Slice 1 T3 RED
- Position on Claude point 3: CONCUR this is an ambiguity clarification, not a contract change. T3 RED pins integrity to pointer-referenced unregistered bytes; an unreferenced stale sibling inside a scanned pointer-resolved run dir is surfaced as `unregistered_local`/`info` and does not block production.
- Authored fixture-only RED `tests/contract/test_system_model_provenance_t3.py` covering pointer readers (`latest.json`, `engine_b/v2_manifest.json`, `head_a/v3_manifest.json`), `latest_run_dir` filename resolution, streamed `hash_file_sha256`, PermissionError/directory/dangling-symlink fail-closed behavior, strict case-sensitive manifest string comparison, separate Engine B vs Head A `te_v3.pkl` pointers, and R5-scoped reverse scan with no historical-run or tests-fixture walk.
- Verification: `tests/contract/test_system_model_provenance_t3.py` fails RED as expected, 12 failures all due to missing T3 API functions (`inspect_registered_artifact`, `derive_pointer_status`, `hash_file_sha256`, `scan_unregistered_local_artifacts`). T1+T2 regression slice remains green: 46 passed. Ruff clean on the new T3 test file.

## Codex — DEBT-6 Slice 1 T3 GREEN review
- Reviewed Claude's T3 GREEN diff in `app/api/routes/system_model_provenance_models.py` plus the spec/plan clarification. Independent focused verification: T1+T2+T3 58/58 pass; Ruff clean on the changed module and T3 test file.
- Review result: NOT CLEAR. Adversarial temp-dir probes found traversal paths can normalize outside `app/data/models` and still hash as `ok` when intermediate directories exist: `latest_run_dir` with `entry.path="../../../secret.pkl"` and literal `path="app/data/models/../secret.pkl"` both reported `observed_status=ok` / `severity=info` when the escaped target hash matched. This violates the T3 traversal guard / model-root confinement intent.
- Secondary doc redline: spec seed 20 still says `latest.json.run_dir` pointing to a different run than the entry's expected run yields `pointer_mismatch`, but the R6b filename-only design now follows the current run dir and reports hash mismatch instead. Remove or rewrite that residue before CLEAR.

## T3 SESSION RESUME (preflight) - Claude (implementation lead)

- Task: DEBT-6 Slice 1 resume at T3 (David-directed; same cockpit flow: Gemini framing → Claude → Codex RED).
- Governance read: 02, 00, 01, 03, charter 04, AGENT_SYNC (DEBT-6 checkpoint), spec + plan, this ledger.
- Active phase / surface: DEBT-6 Slice 1 T3 — pointer health (`pointer_status`), `latest_run_dir` resolution, streamed sha256, R5-scoped unregistered-local reverse scan. Branch `feature/debt6-model-provenance` @ `9ff5f86`, clean.
- Panes ID'd by content-capture: 1.1 Claude / 1.2 Codex (gpt-5.5) / 1.3 Gemini (agy). Gemini framing received (seeds A–F: malformed manifest, path traversal, dir-as-file, permissions, case drift, symlinks; overclaim guard on not_verified naming); synthesis + my POV routed to both lanes.
- Real-shape groundwork: verified `latest.json` (object with `run_dir`), v2/v3 manifests (position→literal-path maps; te_v3 in BOTH manifests as separate entries, each with its own governing pointer); found REAL stale sibling `engine_b/runs/20260513T012309Z/te_v2.pkl` (referenced by no pointer, inside a pointer-resolved run dir) → argued integrity keys on pointer-REFERENCED file paths, not directory scope, else the real serving host is born blocked by a benign leftover. Codex CONCUR (clarification, not contract change) — RED pins it; spec §3.5 wording micro-patch owed this cycle.
- Intended write scope: T3 GREEN in `app/api/routes/system_model_provenance_models.py` per Codex RED; spec §3.5 seed-10 wording clarification. No route/main wiring (that is T4).
- Handoff / next step: Claude GREEN → adversarial dual-CLEAR → David-gated commit.

## Codex — DEBT-6 Slice 1 T3 GREEN R9 CLEAR
- Reviewed the fresh R9 diff for `app/api/routes/system_model_provenance_models.py` and spec/plan clarification. The final resolved artifact path is now confined under `repo_root/app/data/models` for both `latest_run_dir` and `literal` path resolution before hashing.
- Re-ran focused verification: T1+T2+T3 58/58 pass; Ruff clean on `app/api/routes/system_model_provenance_models.py` and `tests/contract/test_system_model_provenance_t3.py`.
- Re-ran both prior blocker probes locally: `latest_run_dir` traversal filename and literal `app/data/models/../secret.pkl` both classify as `missing_required`/`integrity`/`serving_allowed=False` even when the escaped target hash matches. Seed-20 doc residue is resolved as contract language; remaining "expected run" text only identifies the dead pre-R6b clause and the current hash-mismatch behavior.
- Technical CLEAR for T3 content. T4 remains route/response assembly/OpenAPI/full closeout; no action authorization implied by this CLEAR.

## T3 GREEN + R9 - Claude (implementation lead)

- Task: DEBT-6 Slice 1 T3 GREEN — the disk-truth layer against Codex's RED (`tests/contract/test_system_model_provenance_t3.py`, 12 tests).
- Delivered (in system_model_provenance_models.py, T3 section): `hash_file_sha256` (streamed/chunked); `derive_pointer_status` + `_read_pointer` (exception-driven: FileNotFoundError→pointer_missing; PermissionError/OSError/ValueError/non-dict/bad-run_dir→pointer_malformed; strict case-sensitive string match→referenced/pointer_mismatch); `_resolve_artifact_path` (latest_run_dir = pointer run_dir + filename; traversal-guarded); `inspect_registered_artifact` (env fail-closed at entry; disk anomalies — directory-squat, dangling symlink, unreadable file — classify as absence; delegates meaning to T2 `classify_artifact`, `model_copy` updates the display path); `scan_unregistered_local_artifacts` (R5-scoped roots; severity keys on pointer-REFERENCED file paths — cockpit-converged over the real te_v2.pkl stale sibling; referenced-unregistered = integrity/blocked in serving/production, caveat in dev/ci; unreferenced = info).
- Codex adversarial R9 (fresh review found 1 real blocker + 1 doc redline): (1) traversal could hash an escaped hash-matching file outside the model root and report ok (latest_run_dir filename `../../..`, literal `app/data/models/../x.pkl`) → fixed: FINAL resolved path confined to `repo_root/app/data/models` in both modes, escapes classify fail-closed missing_required/blocked; (2) spec seed 20 + §3.5 pointer_mismatch carried the dead pre-R6b expected-run clause → rewritten (repointed latest.json surfaces as hash_mismatch). Spec §3.3/3.4/3.5 + seeds 9–10 + plan T3 also patched to the file-reference severity keying (post-fix sweep: zero `expected run` / directory-containment residue).
- Verification: T3 12/12 + T1 26/26 + T2 20/20 = 58 (0.50s); ruff clean; 12-probe adversarial matrix passes (traversal escapes ×4, invalid env ×2, list/int/absolute pointer bodies, chmod-000, directory-at-pointer, dir-as-artifact); READ-ONLY real-shape smoke over the actual tree: 9 registered artifacts ok/referenced/info in development, scan surfaces exactly 4 root `{pos}_model.pkl` + `te_v2.pkl` as info/not_applicable — no false block, no hidden divergence.
- Dual-CLEAR: Codex technical CLEAR (enumerated: fresh diff review, independent 58/58 + ruff re-run, both blocker probes re-run, spec-patch review; flags — dev/ci caveat acceptable, placeholder scan-row fields acceptable for T3, seed-20 resolved). Gemini advisory: framing seeds A–F all landed; (b) no-concerns on the dev/ci caveat level; no objection to the rewritten spec language.
- Handoff: T3 content dual-CLEAR, UNCOMMITTED — awaiting David's commit authorization. Then T4 (route + OpenAPI codegen + full closeout = push/PR checkpoint), T5 (David-authorized registry hash-seeding).
