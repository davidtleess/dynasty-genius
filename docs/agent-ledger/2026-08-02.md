# Agent Ledger — 2026-08-02

## 00:30 ET - Claude (injury capture LIVE — four more defects, all found by running it)

**Layer served: 1 (ingest).** Continues the 2026-08-01 session past midnight; the CFBD and
injuries-contract record is in `docs/agent-ledger/2026-08-01.md`.

**David's words this stretch:** *"commit both and run the live injury capture"*, then
*"agreed - go"* on the era/numeric fix plan, then *"agreed on all fronts"* on the closing
recommendation.

**COMMITTED (local, unpushed at the time of writing):**
- `b132c2e` — CFBD live-probe defects: the adapter had been calling `/stats/team/season`, which is
  CFBD's **Swagger docs page** (HTTP 200, `text/html`), and `sacksAllowed` was never a CFBD field.
  Both pre-existing since Stage 2 and both the root cause of `qb_sack_rate_final` = 0/126 dark.
  Plus the shared `cfbd_http` bounded-retry policy.
- `b550d81` — the nflverse injury stream contract (64 files incl. the whole
  `docs/agent-ledger/evidence/2026-08-01/` tree). Codex **INJURIES CONTRACT CLEAR**, 8 enumerated
  checks, after two NOT CLEAR rounds totalling eight rows — all accepted, none challenged.

**THE CAPTURE IS LIVE.** `app/data/nflverse_usage.db` now holds **34,812 injury rows**:

| Seasons | Era | Rows |
| :-- | :-- | --: |
| 2020-2024 | `revisioned` | 28,744 |
| 2025 | `single_observation` | 6,068 |

34,812 distinct `row_key` — conserved. Identity 28,395 canonical / 6,417 source_only. All six
seasons `ok`. The revision pair that motivated the whole grain design survives in production:
Cade Stover 2024 wk15 **Questionable 03:34Z → Out 14:17Z**.

**FOUR DEFECTS, none of which the offline contract could have caught.** Getting from "contract
CLEAR" to "data stored" took four rounds:

1. **2025 is a different era.** nflverse swapped `date_modified` for `season_type` — same 16
   columns, one traded for the other — and 2025 carries **zero duplicate groups** on the
   five-column key, so that era *cannot express a revision at all*. Now two `StreamEra`s detected
   from the **column set, never the year**; an unrecognised shape **refuses** rather than being
   mapped onto whichever era looks closest.
2. **The 2020 file types `season`/`week` as Float64** while 2021+ types them Int32, so the store
   held `'2020.0'` and `'1.0'`. `season == 2020` missed the entire season. Integral values are now
   normalized; a genuinely fractional one **refuses** rather than being coerced.
3. **`publish_export` inferred dtypes from a 100-row window** — the injury stream's early rows carry
   a null `report_primary_injury`, polars inferred `Null`, and the first real value (`"Ankle"`)
   raised `ComputeError` mid-export. The module's stated principle is *declared, never inferred*;
   that held at cast time but **not at construction time**. Now explicitly Utf8-typed at construction.
4. **Era columns had nowhere to land.** The table was created from the default era's columns, so
   2025's `season_type` was declared by its era, produced by normalization, and then **silently
   dropped at insert** — `row.get()` means no error, just missing data. `stored_columns` now unions
   every era. **Found only because a verification query failed on a column that was not there.**

**A FIFTH, FLAGGED AND DELIBERATELY NOT FIXED:** the store's content hash is computed over the
**normalized rows**, not over the stored schema, so widening the schema does not invalidate it. The
store correctly saw "same content" and skipped the rewrite, leaving the newly-added `season_type`
column NULL for all 6,068 rows. Real, reproduced, and a named follow-up rather than a quiet patch.

**PRODUCTION-STORE ACTIONS TAKEN, disclosed rather than buried:**
- Two **additive** `ALTER TABLE ADD COLUMN` migrations (`source_era`, then `season_type`). No data
  dropped by either.
- One **DELETE of the injuries table contents and its six capture records** to force the rewrite the
  content hash had skipped. Scoped to the injuries stream only; the other four streams were verified
  intact (`ngs_passing` 5,933 · `ngs_rushing` 6,059 · `ngs_receiving` 14,731 ·
  `player_snap_count` 253,106) before and after. The data was captured minutes earlier in this same
  session and is free to rebuild from a public source.

- **Files changed (UNCOMMITTED at time of writing):** `src/dynasty_genius/nflverse_usage.py`
  (`StreamEra`, era resolution, numeric normalization, unioned `stored_columns`, typed export
  construction), `tests/contract/test_nflverse_injuries_red.py` (44 rows, up from 30).
- **Tests / checks:** nflverse slice **84 passed** / 4,190 deselected; Ruff clean on `src app`.
  `verify_sprint_closeout.py` **ENFORCE PASS** was run before both commits.
- **Product alignment:** `05` foundation-first — layer-1 ingestion. `00` untouched: no player claim,
  no model input, no promotion. `01` §Source Adapter Rules — raw snapshot, canonical identity,
  completeness flags, stale/era state exposed rather than smoothed.
- **Drift risks:** the four fixes change code Codex **cleared**, and are live-verified but
  **unreviewed** — routed for challenge before the commit. The content-hash gap remains open.
- **Handoff / next step:** Codex review of the four fixes → commit + push all three bodies together
  (one CI run) → **STOP**. Contracts and depth-chart adapters are deliberately NOT started tonight:
  four defects surfaced in one hour in code I had just written and self-verified, and none were
  caught by my own review. Named follow-ups: the content-hash-vs-schema gap; the three withheld NGS
  paths; `docs/data-inventory.md` (Codex's record, still uncommitted); the refreshed CFBD curated CSV
  (published, unpromoted, no consumer); the two parked framings. **New thread opened on David's word:
  "guards that didn't guard"** — see `docs/agent-ledger/evidence/2026-08-02/`.

## Codex — post-live injuries review + guard-framing challenge

**Layer served:** 1 (ingest) for the code review; governance/process for the framing review.

Independently reconciled the live injury store and ready-marker-selected Parquet: 34,812 rows and
distinct keys; 28,744 `revisioned`; 6,068 `single_observation`; 2025 `season_type` 5,783 `REG` /
285 `POST`; six `ok` captures; typed integer season/week. Raw snapshots confirm two 16-column
eras and the reported duplicate-key distribution. Reproduced the focused census (`84 passed, 4190
deselected`) and clean Ruff result.

The four-fix body is **NOT CLEAR**: six blocking and two material rows. Probes proved that additive
unknown columns are accepted/dropped despite exact-era claims; float/int semantic duplicates
normalize to identical coordinates but receive different row keys; schema widening is skipped by
the rows-only content hash and leaves new columns null; the changed artifact remains labelled v3;
the production ALTER/delete recovery has no reproducible migration/raw-replay implementation; and
an empty export still has zero columns. Wrong numeric types are deferred past normalization, and
the new stored-column test never stores or reads back. Evidence:
`nflverse_injuries_postlive_fixes_review_codex_v1.md`.

**Miss accounting:** my earlier offline CLEAR reviewed one synthetic 2024 shape and the declaration,
not every retained source era through capture → store → export; it also accepted structural
assertions where a schema-transition counterexample was required.

The new framing receives a six-row **CHALLENGE**. Eight examples do not establish “nearly every”
of 44 findings; guard sensitivity is the broader primitive; binding `02` already contains
break-attempt/positive-control substance; registry growth is a subtype of proxy/claim boundary
mismatch; cost should be one durable counterexample per high-consequence mechanism; and external
ingestion still needs representative schema-era execution because fixture mutation cannot discover
an era the fixture inventory omitted. Evidence:
`framing_guards_that_did_not_guard_codex_challenge_v1.md`.

## Codex — both tooling answers for David

David asked both (1) for an independent assessment of Claude's proposed Hypothesis/mutmut/schema
tooling and (2) what Codex's own integration-review lane needs. Durable answer:
`docs/agent-ledger/evidence/2026-08-02/tooling_answers_codex_v1.md`.

**Testing-tool answer.** Hypothesis is useful for metamorphic/stateful falsification, but the new
demonstrator explicitly constructs int/float/text variants; Hypothesis varies and shrinks values,
not the missing invariant. It reproduces the known defect (`1 failed, 1 passed`, minimal week 1 /
season 2015). The package is local but undeclared, while CI installs only `requirements.txt` before
pytest. Mutmut 3.7.0 is a reasonable targeted pilot, not evidence that persistence boundaries are
covered: it does not mutate module-level `SCHEMA_VERSION`, and the existing structural membership
test may kill union-loop mutants while still never storing. The newly written v2-style
`--paths-to-mutate` command is not mutmut 3's documented interface. Broad Pandera/Great Expectations
adoption is not justified; a curated-dataframe pilot may be reconsidered later.

**Codex-lane answer.** The highest-value missing capability is a pinned authoritative
source-contract bundle plus real schema-era replay fixtures: CFBD OpenAPI/version/hash and sanitized
endpoint/stat-catalog responses; nflverse column+dtype fingerprints and minimal fixtures per era;
PFF/PlayerProfiler redacted headers and representative rows per export vintage. Pair them with an
end-to-end temporary capture → normalize → store → export harness and cheap live fingerprint
preflight. Also make Hypothesis a reproducible CI test dependency and freeze review targets using
existing git worktrees/SHAs. No new MCP, skill, DuckDB, schema framework, or browser extension is
currently needed.

## 01:00 ET - Claude (Codex NOT CLEAR accepted — parked, nothing committed or pushed)

**Closes the 00:30 entry's handoff differently than it planned.** The plan was: Codex review →
commit + push all three bodies → stop. Codex returned **NOT CLEAR (6 blocking + 2 material)** on the
four post-live fixes and a **6-row CHALLENGE** on the guards framing. Both accepted in full.
**Nothing was committed or pushed.**

**WHY NOT WORKED THROUGH TONIGHT.** Every row is the same class of defect this session kept
producing. Three hours ago I wrote four fixes, verified them against live data, declared them sound,
and Codex found eight more problems inside them. Writing further code into that at 01:00 is the
mechanism that produced the defects, not the cure. The rows get a disposition with a clear head.

**CODEX'S MISS ACCOUNTING — the most consequential line of the exchange, recorded rather than
absorbed:** their earlier `INJURIES CONTRACT CLEAR` reviewed **one synthetic 2024 shape** and the
declaration — not every retained source era through capture → store → export — and accepted
structural assertions where a schema-transition counterexample was required.

**That means `b550d81`, already committed, carries a contract both lanes now know was
under-reviewed.** It is committed, local, and **will not be pushed** until that is resolved. Better
to hold an uncomfortable known-weak commit locally than push it because it happens to be green.

**THE EIGHT ITEM-1 ROWS** (blocking unless noted): era matching is not exact-column-set matching, so
an added provider column is accepted and silently dropped, and `next()` has no ambiguous-match
refusal · integer normalization writes `row` while blank/key logic still reads `record`, so
float/int semantic duplicates normalize identically yet receive **different row_keys** · the
content-hash/schema defect reproduces exactly, and the manual DELETE was **recovery, not repair** ·
artifact semantics changed but `SCHEMA_VERSION` stayed v3 and the header now describes a 16-vs-17
column difference the raw evidence contradicts (it is 16-vs-16, swapped) · production depends on a
manual `ALTER`+`DELETE` while the error text promises a raw-replay rebuild that **does not exist** ·
an empty stream still exports a zero-column Parquet · *(material)* bool/non-numeric values pass
normalization · *(material)* the new stored-columns test is another structural proxy that never
stores or reads back.

**Four I already accept without re-verification** because they name things I knew and did not close:
the rows-only content hash, the missing migration/replay path, the stale v3 label and false header,
and the framing's overclaim. **Two I intend to verify myself first** rather than accept on sight:
the float/int row-key divergence (if true, my fix cured the storage symptom and left the *keying*
defect), and the zero-column Parquet (I suspect it predates this change and is pre-existing, not a
regression).

**ITEM 2 — the guards framing takes a 6-row challenge and its central overclaim falls.** "Nearly
every" of 44 findings is not established by eight instances, and *"a feature bug announces itself"*
is false as written. Codex's reframes adopted in principle: the primitive is **guard-sensitivity
evidence**, not one manual technique; `02` §Falsification #1/#5/#6 plus Verify-the-verifier already
contain the substance, so **operationalize before amending governance**; registry growth is a
**subtype of proxy/claim boundary mismatch**; cost bounds to one durable counterexample per
high-consequence mechanism, **never by sabotaging production, paid or external state**; and positive
controls **cannot discover an omitted source era** — external ingestion needs representative
schema-era execution end to end.

- **Files changed:** this entry only. The four fixes remain **uncommitted**.
- **Tests / checks:** nflverse slice 84 passed / Ruff clean at the time of the NOT CLEAR; that census
  is now superseded as evidence of correctness by Codex's rows.
- **Product alignment:** unchanged — nothing promoted, no model touched, no consumer wired.
- **Drift risks:** the live store holds 34,812 injury rows produced partly by a **not-cleared code
  path**. Codex independently reconciled the DATA as coherent; the defect is in the path, not the
  rows, so there is nothing to roll back — but no consumer may be pointed at it until the path
  clears.
- **Handoff / next step:** **STOP.** Next session, in order: disposition the six blocking + two
  material Item-1 rows (verifying the two named above rather than accepting them sight-unread) →
  corrected post-live state for re-review → disposition the six framing rows → only then commit and
  push all three bodies in one CI run. Unchanged follow-ups: the three withheld NGS paths,
  `docs/data-inventory.md` (Codex's, uncommitted), the refreshed CFBD curated CSV (published,
  unpromoted), and the two parked framings.

## 01:40 ET - Claude (David authorizes the full tooling program — recorded, NOT started)

**David's word:** *"im authorizing all recommendations and sending this auth to both panes"*, following
Codex's two-part tooling answer (`tooling_answers_codex_v1.md`) and my ACK of their corrections.

**AUTHORIZED — the durable list, so it survives this session:**

1. **Pinned authoritative provider-contract bundle** *(Codex's item 1; the highest-value item and
   NOT one of mine)* — CFBD OpenAPI with version + hash, plus the stat catalog or sanitized real
   endpoint responses; nflverse source column + dtype fingerprints and minimal fixtures **per era**;
   PFF/PlayerProfiler redacted header manifests and representative rows per vintage.
2. **`hypothesis` declared reproducibly in a CI-installed requirements file.** It is currently local
   only; `.github/workflows/ci.yml:29` installs `requirements.txt` alone, so the new property test
   **cannot be committed until this lands** or clean CI fails.
3. **Frozen review targets** via SHA / patch / dedicated git worktree. No install — git already
   provides it.
4. **Targeted `mutmut` pilot** in an **isolated worktree**, scoped to one high-consequence module,
   with coverage **measured rather than claimed**.
5. **Schema-era replay harness** over archived REAL fixtures — column + dtype fingerprints driven
   through capture → normalize → store → export — plus a cheap **live fingerprint preflight**.
   Ordinary pytest suffices; no new framework.

**Explicitly declined by Codex and NOT authorized-by-default:** another MCP, an agent skill, DuckDB,
Pandera/Great Expectations broad adoption, a browser extension. Pandera stays a possible
one-adapter curated-dataframe pilot only.

**TWO CORRECTIONS TO MY OWN EARLIER CLAIMS, verified against the repo before accepting:**
- I told David the Hypothesis demonstrator **found the row-key defect unaided. It did not.** My test
  constructs `as_int` / `as_float` / `as_text` explicitly — **I encoded the type axis**; Hypothesis
  varied values within it and shrank. It confirmed the failure held across the generated range,
  minimized to `week=1, season=2015`, and reported it as structural. Real, useful, and materially
  weaker than what I claimed.
- I told him the cost was **zero**. It is not: `hypothesis` is absent from `requirements.txt`.
- `requirements-dev.txt` corrected: mutmut 3 does **not** mutate code outside functions, so crediting
  it with the module-level `SCHEMA_VERSION` bump was wrong; and my `--paths-to-mutate` line was a
  v2-style invocation **I never ran**. Both errors recorded in the file rather than silently edited,
  and the command removed rather than guessed twice.

**WHY THE BIGGEST ITEM IS NOT A TEST FRAMEWORK.** Both CFBD defects existed because someone reasoned
about an external API **from memory**. The official CFBD Swagger lists `/stats/season` and
`/stats/player/season` and **never** `/stats/team/season`. A pinned contract catches that before a
paid run and before months of silently-empty `sack_rate`. **No mutation or property tool can find a
route that does not exist.** Codex's caveat sharpens it: `sacksOpponent` is a *dynamic* stat name, so
an OpenAPI pin alone is insufficient — that needs the stat catalog or a captured real response.

- **Files changed:** `requirements-dev.txt` (corrected), this entry. Nothing installed.
- **Product alignment:** `01` §Source Adapter Rules and Reproducibility — pinning a provider contract
  is the direct expression of "the same versioned inputs produce the same outputs".
- **Drift risks:** **both lanes now hold the same authorization**, so uncoordinated starts could
  collide on the same files. Ownership split proposed to Codex before any work begins.
- **Handoff / next step:** **NOT STARTED TONIGHT, deliberately.** The authorization is durable and
  does not expire; the four NOT CLEAR fixes are still open and remain the first task. Order next
  session: disposition the 6 blocking + 2 material rows → corrected state for re-review → the 6
  framing rows → then the tooling program, provider-contract bundle first.

## 03:30 ET - Codex (authorized provider-contract bundle + frozen review target ready)

**Layer served: 1 (source contracts).** David authorized the complete tooling recommendation set.
Claude and Codex claimed exact, non-overlapping paths before editing: Codex owns the provider
contract bundle and frozen-review helper; Claude owns nflverse fingerprints/replay, Hypothesis/CI,
and the isolated mutmut pilot. Codex did not touch Claude's requirements, CI, nflverse, injury, or
property-test files.

**Provider evidence built:**

- `docs/provider-contracts/cfbd/openapi.json` — the complete public OpenAPI document extracted from
  CFBD's official `swagger-ui-init.js`, pinned at API **5.21.0** with source and content hashes.
- `docs/provider-contracts/cfbd/team-stat-catalog.json` — value-free dynamic `statName` catalog from
  126 raw QB team-response files / 116 unique payloads in successful run
  `20260802T024342156864Z`. It proves `passAttempts` + `sacksOpponent` and excludes
  `sacksAllowed`; no stat values or player rows are committed.
- `pff/header-manifest.json` — 149 paid payloads / 12 exact schemas; and
  `playerprofiler/header-manifest.json` — 56 private export files / 16 schemas across all five
  streams. Both publish only header hashes, widths, vintages and first-200-row value-kind profile
  hashes/counts. No row, identifier, filename, absolute path, or complete paid header enters Git.
- Rebuild helper: `scripts/build_provider_contract_bundle.py`. It refuses if the generated PFF
  schema set diverges from the governed private catalog.

**The bundle caught drift during its own construction.** Codex initially carried a stale WEPA path
into the generator. The current official schema refused it: the provider route is
`/wepa/players/passing`. The current official API version is 5.21.0, newer than the 5.17.0 observed
in the earlier browsing pass. This is the exact from-memory failure the bundle exists to stop.

**Frozen target:** `scripts/freeze_review_target.py` creates a binary-safe patch from an explicit
scope, includes untracked files, records the base SHA + patch SHA + post-patch file hashes, refuses
private paid-data paths, and verifies a reviewer-created detached worktree. The final target is:

- manifest: `docs/agent-ledger/evidence/2026-08-02/review-targets/provider-contract-bundle-codex-v2/manifest.json`
- patch SHA-256: `accb67037d8ed44e1cf1ffe364b5090abb443a07cb81107dad38ca7023f4c25d`
- base: `c9b1c23749f35b6dc66dd5a0e6e214ffabfbcc3a`
- materialized worktree: `/private/tmp/provider-review-v2.WLq0EP/tree`

**Verification:** exact target hash verification PASS (10 files); focused contract **9 passed** in
both the live worktree and the detached target; Ruff clean; `git diff --check` clean; fixed-time
rebuild reproduced every generated JSON byte-for-byte (README is hand-authored and intentionally
not generated).

- **Files changed:** new `docs/provider-contracts/**`,
  `scripts/build_provider_contract_bundle.py`, `scripts/freeze_review_target.py`,
  `tests/contract/test_provider_contract_bundle.py`; this append and lane-suffixed evidence only.
- **Product alignment:** source adapter rules and reproducibility. No paid API call, ingestion-store
  mutation, feature promotion, model work, consumer wiring, commit, or push.
- **Drift risks:** OpenAPI cannot enumerate dynamic stat-name values; the captured value-free
  catalog is deliberately the second half of the CFBD contract. Paid schemas are hash-verifiable on
  machines holding the private exports but remain redacted in Git.
- **Handoff / next step:** independent Claude audit of the frozen v2 target. Commit/push remain
  separate authority.
