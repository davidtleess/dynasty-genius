# 2026-08-22 — Dynasty Genius Agent Ledger

## 07:00–08:00 ET — Claude Code: SR-06 PROVEN IN PRODUCTION · SR-07 shipped · SR-08 premise falsified `[w#SR-07]`

**Layer: 1 (ingest).** **Tier 0** — code only, no persisted output; rollback is `git revert` +
next scheduled run. No seat claimed (`tty` = not a tty, no tmux server).

### ✅ SR-06 PROVEN LIVE — first scheduled run with yesterday's change

06:15 fired with `seasons: [2023, 2024, 2025, 2026]`. **`depth_charts` 2026 landed: 459,221 rows,
155 daily snapshots, latest `2026-08-22T07:26:29Z`.** Exactly ONE ledger row was written today
(`depth_charts / 2026 / ok`) — every finished season returned `unchanged`, every unpublished 2026
stream was recorded `not_yet_available`. Without Move 1 the run would have died at `ngs_passing`
2026 and none of it would exist. `contracts` still refuses the upstream shape change (known).

### 🔴 SR-08 PREMISE FALSIFIED — there is no 13-day gap

The ticket says recover a 13-day transaction gap. Measured: **every season is COMPLETE** against
Sleeper's own reported totals — 2023 243/466, 2024 323/524, 2025 299/450, 2026 73/132, stored
counts identical. `max(created_at)` for 2026 is `2026-08-21T18:53:15`, already current; a real
transaction landed last night and the 06:30 job captured it unprompted. `legs_with_activity=[1]`
— it was a quiet August, not a gap. "Store last advanced 08-07" described WRITE TIME, not missing
data — the same error class as the nflverse false alarm on 08-21. **SR-08 step 1 ("recover
first") is a no-op.** Its adjudication still stands and is better evidenced: the counts matching
proves re-reading recovers history, so `daily_control.py:235-236` is the wrong declaration.

### 🩹 INCIDENT — I corrupted the production status marker, then repaired it

Ran SR-07's verification command **as the spec writes it**. `--db-path` redirected the store to
scratch, but `raw_root` — which carries BOTH the status marker and the raw snapshot dir — was
never passed by the runner, so it resolved to production. This **overwrote
`transaction_capture_status_latest.json`** (the source's declared `success_marker`) with a
league-0 result and left two junk raw files. The store itself was never touched.

**Repaired:** junk deleted (verified 0 remaining), marker regenerated by re-running the real
capture (idempotent by design). Store verified unchanged: 938 rows, 2026 current to 08-21.
**Cause is a real defect, not just my error — any agent following the ticket does the same.**
The spec's verification block now carries a ⚠️ note recording it.

### ✅ SR-07 SHIPPED — and the ticket's prescribed fix was insufficient

Step 1 says match the house pattern `return 0 if status == "ok" else 1`. **That would not have
satisfied SR-07's own verification.** Measured: `--league-id 0` (a league that does not exist)
returns `status: "ok"` with `managers_total: 0`, zero transactions, all 18 legs empty. The
self-assessment reports that the mechanics ran, not that anything was reached. **Step 3 — define
what a partial/empty capture means — is load-bearing, not garnish.**

- `capture_is_healthy()` in both runners. Transaction rule: `status == "ok"` AND `chain_complete`
  is not False AND every league contacted reported ≥1 manager (handles both status shapes —
  chain nests `chain_coverage.by_season`, single-season is top-level).
- **Discriminator is managers, never transaction count.** A real league has managers in a week
  with no moves (12/12/12/11 across the chain); transaction count would have flagged the
  legitimate 08-05..08-21 quiet stretch as failure. Pinned by
  `test_a_quiet_league_with_zero_transactions_is_STILL_healthy`.
- nflverse rule is just `status == "ok"` — a failing stream raises, so "ok" means all 13 reached.
  **`not_yet_available` skips are HEALTHY** and pinned as such: counting them as failure would
  exit non-zero every morning until week 1 and poison SR-09/SR-11 from the day they land.
- **Isolation (David's word, 2026-08-22):** `resolve_raw_root()` — a redirected database takes its
  paperwork with it. `--raw-root` overrides. Default path unchanged, so the 06:30 job is untouched.

### Verification — run, not assumed
```
SR-07 verification cmd  -> EXIT=1                       (was 0)
production marker sha256 -> 39b5da751a5f812bd65574d9 IDENTICAL before/after
production raw count     -> 40 IDENTICAL before/after; probe artifacts in scratch
nflverse --summary       -> EXIT=0   (read-only path preserved)
real transaction capture -> EXIT=0   (healthy path preserved)
pytest tests -q          -> 17 failed, 6292 passed, 12 skipped, ZERO collection errors
                            failures unchanged at 17 (baseline: governed_cadence + openapi drift,
                            other lanes, untouched). Passes 6276 -> 6292 = +16, exactly the new tests.
ruff check src app + touched files -> All checks passed
```
TDD throughout: all 16 tests written first, watched fail with `AttributeError` before any
production line existed.

### Open
1. `contracts` schema break unfixed; export marker held at 08-08. Filed 08-21, still filed.
2. **SR-08 needs re-scoping** before execution — step 1 is a no-op, the adjudication is not.
3. Manager/team-name reconciliation: David asked for it 08-22, **deferred to post-freeze
   alongside SR-18 League Activity, on his word.** Raw history is captured daily and backed up
   (`league_runtime/runs` is `required[30]` in the backup manifest), 37 snapshots, zero gaps, so
   nothing is being lost by waiting and the change log is reconstructable retroactively.

---

## 12:20–15:00 ET — Claude Code: HARDWARE MIGRATION INCIDENT · scheduler repaired · first verified backup on the new Mac

**Layer: 1 (ops).** **Tier 2** — launchd state changed on David's explicit word. No seat claimed
(`tty` = not a tty, no tmux server). Session opened to continue the season sprint; the sprint was
preempted by a live incident and SR-11 (D4) was **not started**.

### CONTEXT DISCOVERED MID-SESSION

David migrated from a **MacBook Air to a new MacBook Pro (M5 Pro, arm64, 48GB) today**. He stated
it mid-session: *"this is my new MAIN machine ... this machine is now the heartbeat."* Nothing in
the repo recorded the migration; the incident was found by inspection, not by any alert.

### 🔴 THE MORNING WAS LOST AND NOTHING SAID SO

`sysctl -n kern.boottime` → **2026-08-22 09:04:39**. `who` → console login **10:48:52**. Thirteen
scheduled slots fell in that window. LaunchAgents do not exist while logged out and launchd does
**not** replay an elapsed calendar slot. Control: `pvo-refresh` holds a missed 09:30 and a live
11:30 and first spawned at **11:30:00.467**; `what-changed` (09:45) and `backup` (10:15) sat at
`runs = 0, last exit code = (never exited)`. **Filed as `~/dg-build/tickets/DG-035`** on David's
instruction (*"Scope it as a new ticket, decide after SR-11"*).

### 🔴 ROOT CAUSE — MIGRATED INTEL BINARIES, NO ROSETTA

The Air was **Intel**. Migration Assistant carried an **x86_64 `.venv`** onto Apple Silicon.
Verified in the system log (via `/usr/bin/log`, not zsh's `log` builtin, which silently shadows it
and returns nothing):

```
10:57:05.466  dg-mail-carrier                     posix_spawn(.../.venv/bin/python3.14) EBADARCH
11:30:00.466  dynasty-market-divergence-refresh   86: Bad CPU type in executable
11:30:00.467  dynasty-model-pvo-refresh           86: Bad CPU type in executable
```

`/Library/Apple/usr/libexec/oah` absent → **no Rosetta**. Corroborated: the cached pre-commit
`ruff` is `Mach-O 64-bit executable x86_64`. Zoom's updater failed identically in the same
millisecond — systemic, not project-specific. David rebuilt the venv at 12:13 (`pyvenv.cfg` now
3.14.7, `file` → arm64), so the interpreter is healthy.

### 🔴 THE STICKY PART — launchd's PENALTY BOX

The three jobs that attempted a spawn during the broken window were left
`properties = penalty box | inferred program | managed LWCR`, `watching = 0`, `state = spawn
scheduled`. **`kickstart` does not clear it** (tried 13:10; blocked 2 min, `runs` stayed 1, zero
launchd log rows). Cleared with `bootout` + `bootstrap` on David's word ("Clear all three now").

**PROVEN REPAIRED, unattended:** at **14:00:01 / 14:00:02** `market-divergence-refresh` and
`pvo-refresh` fired on their own 14:00 slot, `runs = 1`, `last exit code = 0`, fresh markers.

**This failure class writes NOTHING to disk** — it lives only in launchd's in-memory state and dies
at reboot. Their stdout logs still read Aug 21. **SR-11 cannot detect it from logs, markers or exit
codes.**

### RECOVERY — 6 of 7 producers re-run

fc-snapshot 13:08:25 · league-capture 13:08:47 · feature-refresh 13:09:28 · pvo-refresh 13:13:20
(manual; launchd refused) · what-changed 13:15:46 · market-divergence 13:16:02 (manual). All exit 0.
market-divergence self-reported `freshness_basis: runtime_verified_pvo_and_fresh_fc_forward_capture`,
`aborted_reason: null` — the chain confirming dependency order held from the inside.

### 🩹 DAMAGE I CAUSED — feature-refresh published DEGRADED

`feature_refresh_latest_report.json` reads `status: ok` but its own `stream_provenance` carries
`participation: {status: loaded_empty, error_type: ValueError, fallback_used: true}` and
`pbp / player_stats / snap_counts: {fallback_used: true, effective_season: 2025}`. The evaluator
returns `feature_refresh: inputs_degraded`, rollup `('degraded','daily_diagnostics')`. **The report
is overwrite-in-place and gitignored, so the prior provenance is destroyed** and "was it already
degraded?" is now unanswerable. Treat today's Engine B runtime as provenance-degraded.

### 🔴 BACKUP — TWO SEPARATE NEW DEFECTS, BOTH FIXED

**(1) `auth_unavailable`.** launchd's `PATH=/usr/bin:/bin:/usr/sbin:/sbin` makes the gcloud wrapper
resolve `python3` → `/usr/bin/python3` (**3.9**); gcloud refuses to load. Credentials were never the
problem. Reproduced with `env -i PATH=... gcloud auth print-access-token` → exit 1; cleared by
`CLOUDSDK_PYTHON`. Fixed in `ops/launchd/com.davidleess.dynasty-backup-irreplaceable.plist`.

**(2) `upload_verification_mismatch` — Gatekeeper.** The ~1GB DBs upload as **parallel composite
objects** (`component_count = 23`), which carry **crc32c only, no MD5**, so the restore drill
depends entirely on crc32c. gcloud runs its interpreter with **`-S`**, so a `google-crc32c` wheel in
any venv is invisible to it (I installed one on a wrong hypothesis and reverted it); it shells out
to `gcloud-crc32c`. That binary was **quarantined** by macOS on this morning's fresh SDK install, so
Gatekeeper blocked it and gcloud computed the destination hash as `AAAAAA==` — the hash of nothing.
Every large object "mismatched" **after a full 3.2GB upload**. David cleared it with
`xattr -d com.apple.quarantine`.

**✅ FIRST VERIFIED BACKUP ON THIS HARDWARE:** run `20260822T180700Z`, exit 0,
`status: completed`, **`sha256_verified: true`**, 648 files / 3,212,209,843 bytes, 42 min.
`latest.json` advanced, `verified: true`. Prior to this the new Mac had **never** produced one
(`runs = 0`).

⚠️ The failed attempt left **649 orphaned objects** at `runs/20260822T173058Z` that `latest.json`
never referenced. Append-only, harmless, costs storage. Deletion is David's call; not touched.

### ADVERSARIAL PASS — five auditors, and it caught me repeatedly

Run on David's instruction (*"adversarial subagent passes have caught several errors by the core
agent in each session"*). It **refuted**: my "transient brew-upgrade window" story (`/opt/homebrew`
was created 11:47, **after** both failures — a fresh install, not an upgrade); my array-form
`StartCalendarInterval` hypothesis (coincidence; the penalty box was the mechanism); "seven jobs"
(thirteen slots); "login ~11:00" (10:48:52, inferred from a directory mtime, which proves nothing);
"nothing was harmed" (feature-refresh, above); my `AutoWake.plist` corroboration (that path is
meaningless on macOS 26 — the conclusion survives on one command, not two); and my "ruff missing"
framing (`requirements-dev.txt:5` supplies ruff via pre-commit, never the venv). It also **ran the
fidelity test I should have run and didn't**: ATTACH + full-row EXCEPT against the pre-migration
copies in `backup_staging/20260812T141500Z` — 610,178 rows of `model_forward_capture_raw` and
22,889 of `fc_forward_capture_raw` byte-identical both directions.

Confirmed and left standing: launchd `Weekday 2` **is** Tuesday, so `realized-outcome-scoring`,
`roster-capacity-audit` and `league-opportunity-map` are weekly and **on schedule**, not stale.

### SHIPPED

`tests/contract/test_backup_irreplaceable_ops_scheduler.py` (**6 tests, TDD — RED observed for the
right reason, then GREEN**) + the plist fix. While patching I wrote `--` inside an XML comment,
reproducing **the exact SR-02 defect class**; `plutil -lint` passed it while `plistlib` raised
`ExpatError`. Caught by the test, fixed. All 12 plists parse under plistlib.

Suite: **6,297 passed / 17 failed / 12 skipped** — +5 vs the 6,292 baseline, exactly the new tests;
the 17 failures are unchanged and belong to other lanes.

### OTHER MACHINE STATE

- `pre-commit` cache removed (`~/.cache/pre-commit`, 286MB) — its `ruff` was x86_64, killing the
  lint gate across **all 15 worktrees** via the shared git dir. Will rebuild arm64. It also held 4
  stale patch files from late May; deleted with it. Working tree verified intact afterward (59 =
  57 other-lane + 2 mine).
- **`.venv/bin/python` trap is GONE** — now → `python3.14` (3.14.7), not system 3.9.6. The season
  brief's "4+ scripts call the broken path" hazard is **retired** by the rebuild.
- `pmset -g sched` → **empty**; no scheduled wake exists. Needs David's sudo. Note it does **not**
  address DG-035: a wake does not create a login session.
- **The Air is still powered on.** Split-brain risk into the shared GCS bucket from 06:15 tomorrow.
  Bootout + move-aside commands handed to David; unresolved at close.

### NOT DONE

**SR-11 (D4) NOT STARTED.** Deferred deliberately: it is Tier 2, 1.0d, and its own spec asks for
fresh context and a clean board. Today's evidence materially changes its requirements — see the
AGENT_SYNC block.

### ADDENDUM — the offsite archive has a 14-DAY HORIZON, and the 365-day tier is empty

Measured at close (`gcloud storage buckets describe gs://dynasty-genius-backup-dtl`):

```
rule = [{action: Delete, condition: {age: 14,  matchesPrefix: ['dynasty-genius/runs/']}},
        {action: Delete, condition: {age: 365, matchesPrefix: ['dynasty-genius/archive/']}}]
```

`gcloud storage ls .../dynasty-genius/` returns exactly `latest.json` and `runs/`. **`archive/` has
never existed** — nothing has ever been written to the 365-day tier. Runs present: 16, `20260808`
through `20260822`, unbroken daily, exactly the 14-day window. The policy is working as designed.

**This does NOT lose history.** Each run is a FULL backup of the current stores (648 files, 3.2 GB),
not an incremental, so June data still lives inside today's `model_forward_capture.db`. What is
bounded is **rollback depth**: any of the last 14 days, and nothing before.

**The consequence worth naming:** *rollback window is 14 days; detection latency is currently
unbounded.* Every failure in today's incident was silent. A corruption introduced and unnoticed for
15 days is unrecoverable — the last clean copy has aged out. This is not an argument for longer
retention; 14 days is reasonable. It is the sharpest available argument for **SR-11**: the backup's
value is capped by how fast a problem is detected.

Not filed as a ticket. Recorded so the next lane does not have to rediscover it.

### CLOSE-OUT STATE

- **The Air is QUIET** — all 14 agents booted out, all 18 plists moved to
  `~/Library/LaunchAgents/disabled-20260822/`, `pmset repeat cancel` run. `launchctl list | grep
  davidleess` returns nothing. Its `app/data` is untouched and remains a fallback copy.
  Split-brain risk into the shared GCS bucket is CLOSED.
- **Orphaned run `20260822T173058Z`:** David ruled LEAVE IT. It is a complete 649-object copy whose
  only fault was a client-side hash check broken by Gatekeeper, no pointer references it, and the
  lifecycle policy deletes it automatically on **2026-09-05**. Deleting by hand would have meant a
  recursive delete against the only offsite copy, for ~$0.07/month.
- ✅ **`pmset repeat` IS SET** — David ran `sudo pmset repeat wakeorpoweron MTWRFSU 06:00:00` in a
  real terminal on the MacBook Pro (the `!` prefix cannot: no TTY, so sudo has nowhere to prompt).
  Verified from session: `pmset -g sched` → `wakepoweron at 6:00AM every day`, 58 bytes, was 0 all
  day. Battery sleep still 45.
  **🔧 CORRECTION — how to verify a wake schedule on macOS 26.** BOTH file paths cited today are
  wrong: `/Library/Preferences/com.apple.AutoWake.plist` (mine) and
  `/Library/Preferences/SystemConfiguration/com.apple.AutoWake.xml` (the auditor's replacement).
  **Neither exists even with the schedule LIVE.** `pmset -g sched` is the only reliable check; a
  future lane verifying by file path would wrongly conclude the wake is missing.
  Note this does NOT address DG-035: a wake does not create a login session.
- **⚠️ `~/dg-build` HAS NO GIT REMOTE.** All 35 tickets exist on one machine. Its `dg-cockpit` mirror
  is stale at DG-034 (last commit 08-19, 47 files uncommitted) and `dg-build` is absent from
  `backup_manifest.json` (whose `ALLOWED_ROOTS` covers only paths inside the product repo).
  **It is not backed up anywhere.** The memory index claiming "dg-build backed up" is WRONG.
