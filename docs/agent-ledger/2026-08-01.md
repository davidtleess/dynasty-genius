# Agent Ledger — 2026-08-01

## 00:xx ET — Codex (PFF NCAA Passing Depth REGPO history verified)

**Layer served: 1–2 (source inventory and curation verification).** Read-only inspection of David's
subscriber exports in `~/Downloads`; no ingestion, scheduler, model, or production artifact changed.

The 32 new files are eight distinct payloads, each downloaded four times. Content maps exactly to
NCAA REGPO seasons 2024 through 2017. Together with the accepted 2025 export, Passing Depth history
is complete for 2017–2025. Every season has the same 554-column schema. Across the new seasons,
2,954/2,954 QBs that join to the verified Passing Summary reconcile `base_attempts`,
`base_dropbacks`, and `player_game_count` exactly. Four to eight low-sample identities per season do
not join; no matched row disagrees.

Coverage is broad through the full window, including 2017 (125–134 teams per season in 2020–2024,
127–129 in 2017–2019). For QBs with at least 100 attempts, top-level behind-line/short/medium/deep
classified share has season p10 88.9%–90.7% and median 93.0%–93.9%; no bucket sum exceeds base
attempts. Preserve `unbucketed_attempts` and classified-share quality fields. Unlike NCAA Receiving
vs Scheme, no partial historical boundary appears in 2017.

Verdict: accept all eight seasons. The four-per-season copies are exact duplicates and require no
re-download. Next schema probe: NFL / Passing / Passing Depth / 2025 / REG (one export).

## 00:xx ET — Codex (PFF NFL Passing Depth REG history complete)

The follow-on download contains all nine NFL REG seasons, 2017–2025: 37 files resolving to nine
distinct payloads (four identical copies per season, except five copies for 2019). Every payload has
the same 554-column schema and all 32 teams. Across the window, all 675 QBs that join to the verified
NFL Passing Summary reconcile `base_attempts`, `base_dropbacks`, and `player_game_count` exactly;
no matched row disagrees.

For QBs with at least 100 attempts, top-level behind-line/short/medium/deep classified share has
season p10 89.0%–90.5% and median 91.9%–92.9%; no bucket sum exceeds base attempts. The NCAA
curation contract therefore transfers cleanly: preserve `unbucketed_attempts`, classification share,
and raw directional fields, while treating top-level depth families as the first candidate set.
No historical coverage break appears in 2017. Verdict: NFL Passing Depth is complete for Engine B's
full 2018–2025 window, with 2017 retained as extra history. Next probe: NCAA / Receiving /
Receiving Depth / 2025 / REGPO.

## 00:xx ET — Codex (PFF NCAA Receiving Depth REGPO sample accepted)

`receiving_depth.csv` plus `(1)`–`(4)` are five byte-identical copies of a clean 2025 NCAA REGPO
payload: 2,371 rows, 505 columns, 136 teams. All 2,267 fantasy-position rows (WR/HB/FB/TE) join to
the verified Receiving Summary and `base_targets` reconciles exactly 2,267/2,267.

The top-level behind-line/short/medium/deep target buckets are a complete partition, not an
approximation: among every fantasy-position player with at least 10, 20, 30, or 50 targets, bucket
targets sum exactly to `base_targets`, with zero over-counts. At 30+ targets, all 604 players have a
short target, 583 have medium, 571 deep, and 568 behind-line observations. The position splits are
football-plausible (median target shares: WR 11.2% BLOS / 38.3% short / 26.6% medium / 19.6% deep;
HB 51.4% / 42.5% / 2.7% / 0.8%; TE 13.0% / 55.3% / 22.5% / 7.3%). This is a materially cleaner
candidate family than scheme classification because no unclassified target residual remains.

Verdict: pull history. First candidates are top-level target-depth shares and depth-conditioned
efficiency/YAC; raw direction-by-depth columns remain available but are secondary and require
denominator gates. PFF grade columns remain diagnostic-only. No feature promotion is claimed.
Next: NCAA / Receiving / Receiving Depth / REGPO for 2024 through 2017.

## 09:xx ET — Codex (PFF NCAA Receiving Depth REGPO history complete; repeated-export drift named)

The remaining files map exactly to 2022–2017. Together with the already checked 2023–2025 pulls,
NCAA Receiving Depth is complete for 2017–2025: stable 505-column schema, broad team coverage
(125–136), and 18,801/18,801 matched fantasy-position rows with `base_targets` exactly equal to the
verified Receiving Summary. For every player with at least 20 targets in every season, top-level
behind-line/short/medium/deep targets sum exactly to `base_targets`. The 2017 historical boundary is
healthy; the partial-coverage defect seen in Receiving vs Scheme does not recur here.

One vendor-export reproducibility anomaly is real but bounded. Four repeated 2017 exports have the
same 2,103 identities, order, schema, and all top-level values, but three distinct byte hashes.
Normalized comparison finds only two cells moving by 0.01: JD Spielman `right_short_epa` (-0.09 vs
-0.08) and DJ Moore `left_deep_epa` (0.10 vs 0.09). Two copies are byte-identical. This does not
affect the accepted top-level target-depth candidate family; raw repeated exports must be preserved,
and fine directional EPA must not be represented as exactly reproducible without a source-vintage
rule. No re-download is warranted.

Verdict: accept 2017–2025. Next probe: NFL / Receiving / Receiving Depth / 2025 / REG.

## 09:xx ET — Codex (NFL Receiving Depth sample rejected: REGPO, not REG)

`receiving_depth (37)`–`(40)` are four byte-identical 2025 NFL payloads, but they are **REGPO**, not
the requested regular-season-only scope. The file has 506 rows, 505 columns, and all 32 teams; the
top-level depth buckets partition targets exactly. Against the verified NFL REG Receiving Summary,
however, only 352/478 matched fantasy-position players reconcile targets and only 323 reconcile
games. All 126 target disagreements are higher in the depth file (median +5, maximum +40), and the
players gaining games/targets are playoff participants: Puka Nacua 200 vs 160 targets and 19 vs 16
games; Jaxon Smith-Njigba 182 vs 157 and 20 vs 17; Stefon Diggs 120 vs 100 and 21 vs 17.

Verdict: retain as an explicitly labelled 2025 REGPO slice, but do not use it as the Engine B REG
sample and do not pull history from the current selection. Re-select NFL 2025 / Regular Season
(`REG`), wait for the table to reload, confirm the visible leaders show at most 17 regular-season
games, then export once.

## 09:xx ET — Codex (NFL Receiving Depth REG replacement accepted)

`receiving_depth (41)`–`(44)` are four byte-identical copies of the corrected 2025 NFL REG payload:
498 rows, 505 columns, all 32 teams. All 478 fantasy-position rows join to the verified NFL REG
Receiving Summary; both `base_targets` and `player_game_count` reconcile exactly 478/478. For every
player at each tested volume floor (10, 20, 30, 50 targets), top-level depth targets sum exactly to
base targets. Verdict: accept and pull history. Next: NFL / Receiving / Receiving Depth / REG for
2024 through 2017 (2018–2025 is the Engine B window; retain 2017 as extra history).

## 09:xx ET — Codex (PFF NFL Receiving Depth REG history complete)

Eight distinct payloads map exactly to 2024–2017; combined with the accepted 2025 replacement,
NFL Receiving Depth REG is complete for 2017–2025. All nine seasons have 505 columns and all 32
teams. Across the window, 4,345/4,345 matched fantasy-position rows reconcile both `base_targets`
and `player_game_count` exactly to the verified NFL REG Receiving Summary. At the 20-target floor,
every row in every season has top-level behind-line/short/medium/deep targets summing exactly to
base targets. No 2017 coverage boundary or repeated-export drift appears. Duplicate copies within
each season are byte-identical.

Verdict: accept. The primary Engine B window 2018–2025 is complete; retain 2017 as extra history.
Next schema probe: NCAA / Rushing / Rushing Direction / 2025 / REGPO, if that report label is present
in PFF; its value is testing gap/direction-conditioned contact and efficiency rather than promoting
raw PFF grades.

## 09:xx ET — Codex (Rushing Direction priority retracted after schema check)

David challenged whether the proposed next report duplicated Rushing Summary. Confirmed in the
actual 47-column NCAA and NFL summary schemas: they already contain attempts, yards, YPA, yards after
contact, YCO/attempt, avoided tackles, explosive/breakaway measures, elusive rating, and both
`gap_attempts` and `zone_attempts`. The earlier recommendation overstated the incremental value of a
direction report. Such a report could add only conditional left/center/right or gap-level outcomes,
not the core contact/efficiency fields named in the recommendation; those splits are more sparse and
more offensive-line/scheme confounded. Rushing Direction is therefore removed from the next-download
position pending a specific non-redundant field proof.

## 09:xx ET — Codex (PFF paid-data inventory and canonical private store complete)

**Layer served: 1–2.** David asked for all PFF data collected so far to be inventoried and organized.
No source file was moved or deleted; all 307 originals remain in `~/Downloads`.

The old state was not safe or usable: `app/data/pff_exports/` contained only three manifests/review
files, while the paid CSVs existed only in Downloads and the active manifests pointed there by
absolute path. The new private store is `app/data/pff_exports/raw/{league}/{report}/{scope}/{season}`.
Every content-distinct payload was copied once under a SHA-256-addressed filename; exact repeats were
collapsed in the store but retained in the download map.

Measured inventory:

- **307** downloaded CSVs / 211,864,453 bytes (202.0 MiB).
- **149** distinct SHA-256 payloads / 72,259,806 bytes (68.9 MiB).
- **158** exact duplicate downloads consolidated.
- **7** report families and **12** distinct schemas.
- Statuses: 117 accepted · 6 accepted extra history · 3 accepted with repeated-export drift ·
  1 partial vendor coverage · 12 separate-scope mismatches retained · 10 superseded-basis pulls retained.

Durable private artifacts in `app/data/pff_exports/`: `README.md`, `pff_master_inventory.json`,
`pff_unique_payload_inventory.csv`, `pff_download_file_map.csv`, `pff_coverage.csv`, and
`pff_schema_catalog.json`. Both active college manifests (`phase16_wr_manifest.json` and
`phase13_te_v10_plus_manifest.json`) now point to the canonical backed-up-tree REGPO copies rather
than Downloads, while preserving `original_download_path`.

Verification: all **149/149** canonical files re-hash exactly to the inventory; both active manifests
reconcile all 9 paths, hashes and row counts and have zero remaining Downloads paths; 307 originals
still exist; `git check-ignore` confirms private artifacts remain ignored; backup manifest declares
`app/data/pff_exports` required. The latest completed backup predates this organization, so the new
raw copies are **not yet off-machine durable**; the next successful backup must include them.
Focused tests: backup coverage + PFF TE/WR parsers/reports, **31 passed**.

Product truth after organization: NCAA REGPO and NFL REG baselines are complete for summary,
passing-pressure/depth, and receiving-scheme/depth families over the governed windows. NCAA Receiving
Scheme 2017 remains partial vendor coverage; old NCAA REG receiving summaries remain preserved but
superseded/non-comparable; accidental NFL REGPO/PRE and NCAA POST slices remain separately labelled.
PFF grades remain diagnostic-only. Only the active NCAA receiving summary has a consumer today;
the other organized families still require Layer-2 candidate curation and pre-registered validation.

## 00:45 ET - Claude (Weekly Roster Key ingested — and a bug that was deleting players)

**Layer served: 1 (ingest).** David's word: "agreed yes key first then continue." Also on his
word, `app/data/playerprofiler.db` moved from an exclusion into backup `required`.

**What ran.** New `src/dynasty_genius/playerprofiler_roster.py` +
`scripts/run_playerprofiler_roster_ingest.py`, six live files 2020-2025.
`pp_roster_week` **230,394 rows**, `pp_identity_bridge` **3,290 rows**. Second pass: all
**7 blocks `unchanged`**. Store count equals marker count exactly (see the regression below).

**The finding that justifies doing this stream first.** From 2023 the export's `player_id`
is a **GSIS id** — the canonical key we already use. For those seasons identity is not
matched or inferred, it is *given*: **110,169 rows (47.8%) are vendor-supplied**. A further
**91,498 (39.9%) are inferred** by bridging across the re-platform seam. Those two are kept
apart in an `identity_basis` column precisely so an inference can never read as a fact.

**The re-platform seam.** PlayerProfiler changed id namespace AND schema between 2022 and
2023, in the same place, in all three of David's downloads. Three eras, detected from the
column set (content), never the filename: `2020_launch` (5 cols, no position, no dob, team
is a full name) · `2021_positioned` (age) · `2023_gsis` (dob, GSIS ids). An unrecognised era
**refuses** rather than being mapped onto a known one.

**THE BUG — my first cut was silently deleting real players.** I keyed rows on
`(season, week, player_id)` and deduped last-wins. That is wrong because
**PlayerProfiler's internal id is initials-plus-a-number and therefore not unique to a
human**:

- `AS-2100` = Andre Smith the **LB** (Buffalo) **and** Andre Smith the **OT** (Baltimore), same week
- `CJ-2150` = Chris Jones the **CB** (Tennessee) **and** Chris Jones the **NT** (Kansas City)
- `KD-0111` = **Kalia Davis and Kellen Diesch** — not even the same name
- `AW-1812` (medical) = **Andrew Wingard and ArDarius Washington**

**13 such ids.** The dedup discarded one real player each, with no error. Worse, the bridge
**confidently resolved 94 colliding ids to a single GSIS id** — merging two careers into one
in a way that looks perfectly clean downstream. Fixed three ways: the row key now carries
name and team so nothing can overwrite anything; colliding ids get a new
`source_id_collision` status and are **never bridged and never resolved**; and a regression
test asserts stored-row-count equals marker-row-count, since the discrepancy (230,204 vs
230,394) was the only visible symptom.

**Two self-corrections, both caught by re-measuring rather than by reasoning.**
1. I first measured collisions by querying `pp_roster_week` — which is already deduped, so
   it *cannot* show same-week duplicates. The measurement was circular and returned 0.
   Re-run against the raw CSVs it returned 13.
2. I then claimed 85 ids had "genuinely different names". They are nickname variants of one
   human — Matt/Matthew Judon, Mike/Michael Badgley, and Robby Anderson → **Robbie Chosen**
   (a legal name change). Excluded deliberately; flagging them would bury the 13 real
   collisions under 85 false ones. **The lesson inverts the usual assumption: the vendor's
   id is more stable than the vendor's name.**

**`#N/A` is a null sentinel, not an id.** It is carried by Brandon Rusnak (Dallas) and CJ
Goodwin (Jacksonville). Joined on, it would merge every unidentified player into one.

**Season labels are filename-derived and content-CHECKED.** The roster key has no season
column, so `verify_season_ordering` requires adjacent seasons to share players (measured
76-89%, floor 35%) and checks for distance inversions. It states its own limit: it proves
the labels are mutually consistent, not that the whole set is not shifted by a constant.

**Bridge results (3,290 PP-internal ids):** 2,147 resolved · 11 conflict HELD · **13
source_id_collision HELD** · 1,119 no 2023-25 appearance (left the league, not a failure).

**Tests:** `tests/contract/test_playerprofiler_roster_red.py`, **18 passing**, collision
suite as the centrepiece. **Full suite 4,117 passed / 0 failed.** Ruff clean.

**NEXT (David's "then continue"):** Advanced Gamelog — 44,464 player-games, weekly usage
detail (routes, snap share by alignment, hog rate, contested targets). Note before it
starts: the 2025 file **dropped all nine man/zone coverage columns**, a blind window of the
same shape as the medical one.

## 01:30 ET - Claude (Advanced Gamelog ingested — 44,462 player-games, and a null token that changes meaning)

**Layer served: 1 (ingest).** David: "then continue." Fourth PlayerProfiler stream.

**What ran.** New `src/dynasty_genius/playerprofiler_gamelog.py` +
`scripts/run_playerprofiler_gamelog_ingest.py`, six live files 2020-2025.
`pp_gamelog_week` **44,462 rows**. Second pass: all **6 blocks `unchanged`**. Stored rows =
marker rows = coverage rows = 44,462 (all three asserted equal by test).

**What the stream is.** Weekly per-player *usage*, not production: routes run, snap share
split by alignment (slot / inline / out wide / backfield), hog rate, contested targets,
drops, deep-ball attempts, play-action and motion snaps, defenders in box. It answers *how*
a player won, which no box score does.

**Identity: 88.7% resolved** — 20,469 rows (46.0%) vendor-supplied GSIS, 18,977 (42.7%)
through the roster key's bridge, 60 held as `conflict` (the colliding ids), 4,958 unknown
(players gone before 2023). **The gamelog REFUSES to run if the roster bridge is absent** —
without it every pre-2023 row is unidentifiable and the run would look healthy while
resolving nothing.

**THE NULL TOKEN CHANGED MEANING AT THE SAME 2022/2023 SEAM.** 2020 writes `0`; 2021+ writes
the literal string `NA` — **435,537 of them in the 2021 file alone**. Three column renames
looked broken until this was found; all three reconciled once `NA` was read as zero
(`first_downs_rushing` 1.40 vs 1.35 · `zone_coverage_targets` 1.74 vs 1.60 ·
`big_hits_taken` 0.16 vs 0.22).

**But `NA` does NOT uniformly mean zero, so no blanket rule is applied.** Measured:
`snap_share` is `NA` on 12 rows, all with zero snaps, never with snaps > 0 — there it means
"no opportunity". `yards_created_receiving` is `NA` on 3,178 rows with zero receptions **and
3,802 rows that HAD receptions** — there "zero" and "not charted" are indistinguishable, and
coercing to 0 would invent 3,802 measurements and drag every average down. The adapter
stores `NA` as SQL NULL and publishes a measured per-column verdict:
**3 safe to zero-fill · 18 NEVER · 111 unclassified (conservative — unproven reads as unsafe).**

**The 2020 rename era, mapped only where it reconciled numerically.** 2020 ships
`pass_yards`/`rush_yards`/`man_coverage_routes` where 2021+ ships
`passing_yards`/`rushing_yards`/`routes_vs_man`. **16 renames verified by comparing
distributions across the seam, not by reading the spelling.** Raw common columns rose from
36/174 to **47 canonical columns present in every season**. Deliberately NOT mapped:
`carries_inside_10`/`carries_inside_5` → `carries_inside` (a two-into-one merge on an
unstated threshold) and the 2020-only `opportunity_share`/`juke_rate`/`average_cushion`/
`routes_won*`. A test pins those OUT of the map.

**Second blind window: the 2025 file dropped all nine man/zone coverage columns.**
`routes_vs_man`, `targets_vs_zone`, `separation_at_target`, `target_accuracy`, `burns` and
friends exist 2021-2024 only. `column_availability` records present/absent seasons per
column, because an average over 2021-2025 would otherwise divide a four-season sum by five.

**Three duplicate-key findings, each handled differently — and the guard fired twice during
the build rather than after it.**
1. **Exact byte-identical duplicates** (the 2020 file ships each of Lynn Bowden's week-14
   games twice): collapsed, since an exact duplicate carries no information. 2 dropped.
2. **Two DIFFERENT games under one week label** — Lynn Bowden has a week-14 game vs WAS and
   another vs KC; one label is wrong at source. Keying on week alone deleted a real game, so
   the key now carries `opponent`.
3. **Two humans under one id** — `DM-3050` (two David Moores) and `SB-9929` (two Spencer
   Browns) appear in the same week vs the same opponent with **identical stat lines**,
   differing only by team: the vendor attributed one game to both. Key now carries `team`;
   both rows survive; both held as `conflict`; neither resolved.

Rows sharing a key that genuinely DISAGREE now refuse (`gamelog_conflicting_duplicate_rows`)
rather than last-wins.

**Tests:** `tests/contract/test_playerprofiler_gamelog_red.py`, **18 passing**. Ruff clean.

**NOT DONE, and named.** No model or surface reads any of this. Advanced PBP (280,868 plays)
is untouched — it is 15x the volume and its 2023 restructure (143 -> 92 columns, 85 removed)
makes the two eras effectively separate datasets. **PlayerProfiler is four streams into
layer 1 and zero into layer 2.**

## 14:05 ET - Claude (Advanced PBP ingested — PlayerProfiler layer 1 complete)

**Layer served: 1 (ingest).** David: "fix the language then keep going - PBP next."

**FRAMING CORRECTED FIRST, on David's challenge.** I had been calling the absence of a
layer-2 consumer a **LIMIT**. He pushed back: it is a *current status* in a planned
sequence, not a shortfall. He is right, and it was arguably in tension with his own ruling
in `05` §1 — *"we shouldn't be wasting cycles until we've built this foundation."* A layer-1
adapter with no layer-3 consumer is **complying** with that order. `docs/data-inventory.md`
now separates two things that I had collapsed:

- **SEQUENCE STATUS** — no layer-2 consumer, sources unfed, CFBD on the May cache. All of it
  dissolves when the work is done. Not limits.
- **DATA PROPERTIES** — the medical archive stopping at 2023; `yards_created_receiving`
  using `NA` for both "zero" and "not charted"; the 2025 man/zone columns. These survive
  completion and bound what layer 2 can honestly build. **These** are limits.

He also named the sequence: **finish layer 1 → hypothesize and explore the full data → then
test curation in layer 2.** Exploration is its own phase and decides *what* to curate;
recorded in the register.

**What ran.** New `src/dynasty_genius/playerprofiler_pbp.py` +
`scripts/run_playerprofiler_pbp_ingest.py`, six live files 2020-2025.
`pp_pbp_play` **280,868 rows** (exactly the sum of all six files) and `pp_pbp_slot`
**949,041 rows**. Second pass: all **6 seasons `unchanged`**.

**THE FINDING: this stream is THREE datasets and both seams destroyed information.**

| Seasons | Cols | Receiver slots |
|---|---|---|
| 2020 | 134 | `wr1 wr2 slot1 slot2 wr5` — **five**, slot receiver NAMED |
| 2021-2022 | 143 | `receiver1 receiver2 receiver5` — **three** |
| 2023-2025 | 92 | `skill1..skill5` — five, but **anonymous** |

Read together: **2021 did not renumber the receivers, it dropped two of them.** 2020's
`slot1`/`slot2` are the missing `receiver3`/`receiver4`, which is exactly why the surviving
indices are the discontinuous 1, 2 and 5. **The slot receiver is gone from 2021 onward.**
Then 2023 removed the role: `skill1` is *some* skill player. That is not recoverable from
this stream — only by joining the roster key, which is an inference. Every 2023+ slot row
carries `slot_role_known = 0`. Measured split: **512,100 anonymous vs 436,910 role-known.**

**Structure: two tables, not one.** A play and a player-on-a-play are different grains, and
265 union columns in one row would make every era's absent columns look like measurements.
`pp_pbp_slot.slot_kind` records **the vendor's own word** (`wr`/`slot`/`te`/`rb`/`receiver`/
`inline_te`/`backfield_rb`/`skill`), never a normalisation we invented — which is what keeps
the 2020 slot receiver identifiable.

**Slot identity: 882,437 of 949,041 resolved (93.0%)** — 512,100 vendor-supplied GSIS,
370,337 via the roster bridge, 687 held as conflict, 65,917 unknown.

**THE SAME DEFECT, A FOURTH TIME — and this time the count caught it.** The first run
reported 280,861 plays against a source total of 280,868. **Four 2020 plays are charted
TWICE and the two chartings DISAGREE** about who was on the field — different `wr1`,
`defender_wr1`, cushion and coverage. `team_play_charted_order` is part of the real key in
that era and I had omitted it, so one real observation was silently discarded per case. Key
fixed, `MAPPING_VERSION` bumped to force re-ingest, and rows that share a key and disagree
now **refuse** (`pbp_conflicting_duplicate_play`). Counts now reconcile exactly.

**Idempotence is keyed on the SOURCE FILE digest plus `MAPPING_VERSION`**, not on a hash of
949k derived rows — hashing the output would cost more than the ingest. Documented, and the
version constant is what makes a mapping change re-ingest.

**Tests:** `tests/contract/test_playerprofiler_pbp_red.py`, **20 passing**. Ruff clean.

**PlayerProfiler layer 1 is COMPLETE — five streams.** Remaining layer-1 work is other
sources: PFF (Codex), CFBD refresh (built, unrun), FantasyPros, Footballguys, and seven
free nflreadpy loaders with zero callers.

## 09:59 ET — Codex (PFF next-download recommendation corrected against holdings)

**Layer served: 1–2 (source inventory and candidate curation).** Read-only reconciliation of
`app/data/pff_exports/pff_schema_catalog.json`, the 149-payload master inventory, and the prior
authenticated PFF report catalog. No download, ingestion, model, scheduler, or production artifact
was changed.

The next test should be **NCAA 2025 REGPO / Offense / Pass Blocking**, one CSV only. The existing
seven families already cover player rushing creation, QB pressure/depth splits, receiver alignment,
man/zone, and target depth. `Snaps By Position`, Rushing Direction, and grade-only views are therefore
lower-priority or substantially redundant. Pass Blocking is the first missing family that can add a
new environmental dimension: pass-block snaps plus sacks, hits, hurries and pressures allowed, which
can be aggregated as line/team context rather than smuggled in as player talent.

Do not pull history until the sample proves which objective fields the current export actually
contains and whether PFF grades can be cleanly excluded. If accepted, the next paired sample is
**NCAA 2025 REGPO / Passing / Allowed Pressure**, which is needed to separate blocker-attributed
pressure from pressure a quarterback creates or owns. Only after both schemas reconcile should the
NFL 2025 REG equivalents and then historical seasons be requested. This is a download order, not a
feature-promotion claim; every candidate remains unvalidated.

## 10:xx ET — Codex (David-ordered Claude/Codex session handoff)

**Layer served: 1–2; decision surface: next-session ingestion order.** David asked both binding
lanes to exchange their work, determine what data should be ingested next, and think ahead. Codex
sent the PFF inventory and received Claude's independent PlayerProfiler handoff before forming an
order.

Claude reports five PlayerProfiler streams complete in one backed-up SQLite store: 5,476
player-seasons, 9,768 medical rows, 230,394 weekly roster rows, a 3,290-row identity bridge, 44,462
weekly gamelog rows, 280,868 PBP plays, and 949,041 PBP player slots. The persistent data boundaries
are the medical archive stopping at 2023, the 2025 gamelog losing nine man/zone columns, and PBP role
information degrading across two vendor-era seams. Absence of Layer-2 consumers is sequence status,
not a data limit.

Codex independently audited PFF identity across every canonical payload: all 149 files and all
134,392 rows carry nonblank numeric PFF `player_id`; 12,191 distinct IDs; zero within-file duplicate
IDs or name conflicts. Seventeen archive-wide normalized-name differences are observed aliases/name
expansions rather than demonstrated two-human collisions. Eight hundred forty PFF IDs occur in both
NCAA and NFL data, providing a direct PFF-native college-to-pro bridge that can attach GSIS on the
NFL side through PlayerProfiler's vendor-supplied 2023+ identity. The audit is evidence over the
current archive, not permission to omit an injectivity refusal in a future adapter.

PFF consolidation is whole-file SHA-256 only. All 158 collapsed downloads were byte-identical; no
row was normalized or removed. Every differing payload remains, including all three hashes of the
2017 NCAA Receiving Depth export-drift specimen. A future parser must assert source rows equal
stored rows after only byte-identical row collapse and refuse any same-key disagreement.

The proposed next-session order sent back to Claude is: (1) execute the already-built isolated CFBD
refresh on David's word, because an active Engine-A consumer still reads the May cache; (2) build and
run nflverse injuries, explicitly separating no injury report from healthy, closing the 2024+ PP
medical blind window; (3) contracts; (4) depth charts as weekly listed position/depth rank only —
they cannot reconstruct the play-level alignment role PBP lost; (5) run a redundancy/agreement audit
before choosing PFR advanced or FTN charting. PFF Pass Blocking/Allowed Pressure samples are parallel
manual acquisition, not the ingestion critical path. FantasyPros and Footballguys still require
David to establish the paid products' export surfaces.

Think-ahead agreement: after Layer 1, the first joint exploration should compare definitions,
denominators and values across PFF, PlayerProfiler, and a selected public charting source on a small
GSIS-resolved NFL cohort. Disagreement is not automatically vendor error; semantic reconciliation
comes before value comparison or feature promotion.

## Coordination close — Claude/Codex converged next-session order

Claude independently re-measured the disputed priorities and withdrew injuries-first. The combined
order is now: **CFBD refresh; nflverse injuries; contracts; depth charts for weekly depth rank;
definition/redundancy audit before PFR advanced or FTN charting; FantasyPros and Footballguys only
after David identifies the export surfaces available under his subscriptions.** CFBD ranks first
because an active Engine-A consumer is using the May cache; injuries closes a real but presently
unconsumed 2024+ medical blind window.

Two boundaries were sharpened. First, PlayerProfiler's own weekly roster table can attach listed
position to 513,111 of 513,301 resolved anonymous 2023+ PBP slot rows, so nflverse depth charts are
not needed for that join; their distinct value is `depth_team` rank. Second, nflverse injuries are
weekly report statuses, not a drop-in continuation of PlayerProfiler's incident-level medical
history. Claude's live read measured 12,283 injury rows for 2024–2025 with populated GSIS IDs and
37,312 depth-chart rows for 2024 with populated GSIS IDs. Any curation must preserve those semantic
differences rather than fabricate continuity.

The PFF acquisition side-track remains bounded to one NCAA 2025 REGPO Pass Blocking sample, schema
inspection, and then one Allowed Pressure sample. No historical pull is recommended before those
schemas reconcile. No source or feature described here is promoted into a model by this ordering.

## Concurrent backup independent check — Codex

**Scope: operations evidence only; no process was stopped or mutated.** David asked Claude to have
Codex independently test whether the scheduled `20260801T141500Z` backup and Claude-started
`20260801T141848Z` backup captured identical content and whether retaining both had value.

Q1 confirmed independently. Each inventory has 482 payloads and 1,994,594,012 bytes; canonicalized
`.files` arrays have the same SHA-256 (`d3657d09c7a6067397bb4b01ff58fac650e6505e7f00e1f0a7cf6c08ea1e78be`).
No inventoried live source has an mtime in the 10:15:00–10:18:48 ET gap or after the second start.
`playerprofiler.db` was last modified 09:36:17 ET. Every non-SQLite live source still matches the
inventory digest; SQLite sources are staged through SQLite's backup API, so live binary hashes are
not the correct comparison, while the staged snapshot digests match across both inventories.

Q2 confirmed with an additional concurrency risk: the identical prefix adds only same-bucket,
same-day redundancy, while both jobs write the same local `backup_status_latest.json` without a
lock. A later failing run can overwrite a prior verified marker, and successful jobs can race the
`latest.json` pointer. Codex concurs with stopping only Claude's manual run **on David's word** and
leaving launchd untouched; no stop was executed.

Q3 arithmetic was numerically correct but its ETA basis was refuted. GCS listings expose completed
objects, not partial progress. Each run also downloads all 1.995 GB after upload for the SHA restore
drill. At Codex's 11:08 ET read the prefixes held 1.462 GB and 1.466 GB of finalized objects; both
runs had up to ~5.05 GB of network transfer remaining together, versus ~2.53 GB for one survivor.
Yesterday's 1.397 GB verified run transferred the payload twice in 3h25m, an effective transferred-
byte rate of ~0.227 MB/s rather than 0.11 MB/s. Exact ETA remains unearned; stopping the duplicate
approximately halves bytes and removes contention.

Q4 confirmed as a high operational/scaling finding. The runner makes one blocking gcloud upload per
payload and one blocking download per payload for verification, re-sending every full SQLite
snapshot daily with no compression, content-addressed reuse, or unchanged-file skip. Recommended
ticket boundaries: one-writer exclusion; batched transfer; measured SQLite compression; immutable
content-addressed reuse; and no weakening or relabeling of the existing `sha256_verified` contract
without explicit new semantics and periodic restore evidence.

## Aborted-backup deletion review — Codex

David stopped Claude's manual process group `98338`; child `98340` is gone and scheduled launchd
PID `93682` remains alive. Codex performed a read-only review and **challenged immediate deletion of
either orphan path**. No deletion was executed.

The remote orphan `20260801T141848Z` is missing exactly two expected objects:
`app/data/features_runtime/feature_refresh_latest_report.json` and `run_inventory.json`. Both remain
in the complete local staging tree; the missing payload is 1,482 bytes and the inventory 105,970
bytes. Because the two runs' 482-entry inventory arrays are identical, the orphan is incomplete and
unverified but reconstructible. It is therefore recovery insurance until the scheduled run earns
verification, not "worthless."

Disposition: retain local staging and remote orphan while scheduled `20260801T141500Z` is active.
If scheduled succeeds, independently require local marker `completed` / `sha256_verified=true`,
remote `latest.json` naming it, and remote inventory coverage for `playerprofiler.db` plus all 149
`pff_exports/raw` payloads; then both orphan copies are redundant and may be deleted only on David's
separate word. If scheduled fails, retain both and ask David to choose reconstruction versus rerun.

No mechanism was found by which an exact delete of the manual sibling prefix would corrupt the
active scheduled prefix: upload and verification use the exact run prefix, the bucket has no shared
runs-level pointer, and lifecycle deletion is age-based. Deferral is required by the orphan's
recovery value and the absence of any benefit to deleting mid-run, not by a demonstrated cross-prefix
corruption path.

Remote inventories for `20260729T141500Z`, `20260730T143032Z`, and verified
`20260731T142636Z` each contain zero `playerprofiler.db` objects and zero reorganized
`pff_exports/raw` payloads. Today's scheduled inventory contains the PlayerProfiler DB and 149 raw
PFF payloads. Thus neither dataset has a verified off-machine copy until today's scheduled run
completes and advances the verified marker/pointer.

## 14:15 ET — Codex closeout (closed — parked, no action-shaped authority inferred)

David told Claude, *"tell codex its time to close out."* Codex treats that as session-closeout
authority only, not permission to commit, push, delete, or resolve the three withheld duplicate NGS
adapter files.

**Backup gate closed independently.** Local marker names `20260801T141500Z`, `completed`,
`sha256_verified=true`, 482 payloads / 1,994,594,012 bytes, no failures, finished
2026-08-01T16:27:48Z. Remote `latest.json` names the same run with `verified=true`. Remote run
inventory contains `app/data/playerprofiler.db` once (472,104,960 bytes) and all 149 canonical
`app/data/pff_exports/raw/` payloads (72,259,806 bytes). This is the first verified off-machine copy
of both datasets; the proof is the run's completed download-and-SHA restore drill, not a live SQLite
binary comparison. The current inventory was corrected forward to remove the stale three-file/May
PFF description and the stale "next backup required" language.

**Process/orphan state.** Scheduled PID `93682` and manual PIDs `98338`/`98340` are all gone. The
aborted manual local staging tree remains at
`app/data/ops/backup_staging/20260801T141848Z/` (~1.9 GB); its incomplete remote sibling remains in
GCS. The reconstruction gate is no longer load-bearing because the scheduled run verified, but
David has not authorized either deletion. Both remain parked.

**Repository state measured at closeout.** `HEAD == origin/main == 30c15f5`; GitHub CI run
`30703494883` is `completed/success` for that SHA. The working tree is deliberately not clean:
`docs/agent-ledger/2026-08-01.md` and `docs/data-inventory.md` contain Codex's uncommitted closeout
record; three duplicate NGS adapter paths remain untracked and withheld pending David's explicit
delete-or-merge ruling. No commit or push was made.

**Carry-forward order agreed by both binding lanes:** CFBD refresh; nflverse injuries; contracts;
depth charts for depth rank; definition/redundancy audit before PFR advanced or FTN charting;
FantasyPros and Footballguys after David identifies paid export surfaces. PFF acquisition remains
bounded to one NCAA 2025 REGPO Pass Blocking sample, schema inspection, then one Allowed Pressure
sample—no history yet. The first joint exploration remains a 2023–2025 GSIS-resolved PFF /
PlayerProfiler / FTN agreement matrix with definitions and denominators reconciled before values.

**Backup follow-up:** the correctness-first ticket is one-writer exclusion for the local marker and
remote destination. Transfer batching, measured SQLite compression, content-addressed unchanged-file
reuse, and honest verification semantics follow. No implementation was started.

## 15:20 ET - Claude (cross-lane handoff with Codex — one concession, one self-correction)

**Layer served: cross-layer / governance.** David ordered both lanes to hand off, compare
work, and set the next-session ingestion order.

**Delivered to Codex** (`dynasty:1.2`, content-verified in their transcript): the five-stream
PlayerProfiler layer-1 build, plus the three findings most likely to change their PFF work —
the reusable name→GSIS crosswalk now in `pp_roster_week`, the vendor-id-is-not-one-human
defect, and the stored-vs-source row-count check that caught four silent drops.

**What Codex reported that I did not know:**
- Every canonical PFF payload carries a populated `player_id`; **134,392 rows, no
  within-file collisions** — PFF's id passes the injectivity test PlayerProfiler's failed.
- **840 PFF ids span NCAA *and* NFL.** That is a college-to-pro identity bridge and it is
  the most valuable identity asset either lane surfaced today — bigger than anything in my
  PlayerProfiler work, because it is the join Engine A has never had.
- Their 158 consolidated files were **exact byte duplicates**, not disagreeing payloads. My
  concern is cleared, no action.

**I CONCEDED THE ORDERING. Codex argued CFBD before injuries and is right on the merits.**
My case for injuries-first was that it closes a *declared* blind window — but it closes it in
a store with **no consumer**, so the value is deferred. CFBD has an **active consumer today**:
Engine A is grading the 2026 rookie class off a May cache right now. Active harm outranks
deferred value. Adopted.

**I CORRECTED MYSELF ON DEPTH CHARTS, by measuring rather than arguing.** I had proposed
`load_depth_charts` as the fix for PBP's anonymous `skill1..skill5` slots. Wrong:

    anonymous 2023+ slot rows with a resolved id : 513,301
    position-labellable from pp_roster_week ALONE: 513,111  (100.0%)
    WR 261,252 · TE 134,731 · RB 103,283 · FB 8,347 · OL/QB residue

The anonymous slots are **already solvable from data in the store** — no new source needed.
It remains an inference (a listed position, not the vendor naming who filled the slot) and
must be stored labelled as one, but it is a **layer-2 join, not a layer-1 acquisition**.
Depth charts should be ranked on their own merit — weekly **depth rank** (`depth_team` 1/2/3),
which nothing we hold carries — not as a fix for the slots.

**I ADOPTED CODEX'S REFRAME on the joint exploration.** I framed the three-vendor agreement
matrix as comparing *values*; they argued **definitions before values**, because a
disagreement most likely exposes a different *denominator* rather than a bad vendor — exactly
what their own NCAA Receiving-vs-Scheme work found (classified share ~85%; unclassified
routes must be retained). Their order is correct.

**Codex also caught a factual error of mine:** I told them "5 commits"; it is **4**
(`git rev-list --count origin/main..main` = 4). Corrected.

**Free loaders VERIFIED LIVE rather than assumed** (both 100% GSIS-keyed):
`load_injuries` 2024-2025 = 12,283 rows, `gsis_id` 12,283/12,283, `report_status` in
{Out, Questionable, Doubtful, Note}. `load_depth_charts` 2024 = 37,312 rows,
`gsis_id` 37,312/37,312, carries `position` + `depth_team` 1/2/3 + jersey.
**Honest caveat carried forward:** injuries is *weekly report status*, NOT PlayerProfiler's
per-incident construct (Games Missed / Body Part / Severity / Surgery / Recovery Timetable).
It covers the blind seasons with a **different measurement** that can be curated into a
comparable one. It is not a drop-in continuation of the medical archive.

**AGREED COMBINED ORDER (both lanes):** CFBD refresh · nflverse injuries · nflverse contracts
· nflverse depth charts (for depth rank) · redundancy audit **before** `pfr_advstats`/
`ftn_charting` · FantasyPros + Footballguys pending David's word on what his subscriptions
permit. Codex's PFF acquisition continues on its own track: one NCAA 2025 REGPO Pass Blocking
sample → schema inspection → one Allowed Pressure sample. No full history yet.

**WIRE STATE, disclosed:** my reply to Codex pasted into their input box but did not submit
(`input_not_verifiable`). The box shows `[Pasted Content 4088 chars]` against my message's
4,073 characters. **I did not press Enter** — the count does not match, and text I cannot
prove is mine is not mine to submit. Codex had already delivered its full recommendation,
which I read directly from the pane, so nothing was lost. The corrections above reach David
through this ledger and my report rather than through the wire.

**Neither lane's off-machine durability is proven.** Codex notes the PFF raw tree postdates
the last completed backup; `playerprofiler.db` moved into backup `required` only today. Both
are declared, neither is verified off-machine. That needs a backup RUN, which is David's call.

## 15:50 ET - Claude (CFBD refresh does not run — the QB adapter is attributing one player's season to nine)

- **Task:** pick up item 1 of the converged order (CFBD refresh). Did not run it. Found the
  premise wrong and a layer-1 defect underneath it; then independently reproduced Codex's
  parallel finding on their review request `w#9hetallx-1`.
- **Governance read:** `02` v1.5.0, `00` v1.1.0, `05` v1.3.0, `01`, `03`, `AGENT_SYNC` head
  blocks, today's ledger. No frontend surface, so the design foundation was not read.
- **Layer served: 1 (ingest), presenting.** Layers 1-2 dependency check not applicable — this
  *is* layer 1, and the defect sits at the layer that causes it.
- **Active phase / surface:** TW30N data foundation board, source-pipeline lane.
- **Write scope:** two evidence files + this entry. **No live CFBD call, no code change, no
  model input touched, no promotion or copy of the isolated curated CSV.**

**THE CARRY-FORWARD PREMISE WAS WRONG IN TWO WAYS.**

1. **The refresh already executed.** `app/data/sources/cfbd_foundation/` holds run
   `20260731T165930158823Z` — 573 files fetched **2026-07-31 12:59-13:45 ET**, published
   `status: ok`, 874 rows, identity coverage 1.0. **No ledger entry records it.** The 07-31
   ledger's last CFBD word (11:39 ET) is *"no live CFBD fetch or cache refresh ran"* — true
   when written, never updated — and today's converged order still lists it as pending David's
   word. I do not know who ran it and am not guessing.
2. **"The May cache is stale" was never the problem for this table.** Published vs active input:
   173 columns, 874 rows, and **only 10 columns differ, all QB passing.** Every WR/RB/TE value
   reproduces exactly. It is a historical college-season table; those seasons do not change.

**THE ACTUAL DEFECT — the CFBD QB adapter attributes one quarterback's season to many.**

Byte-identical normalized QB payloads are shared across 8-15 players per season, in **both** the
legacy active cache and the fresh isolated run. The 2016 group of nine — **Mahomes, Watson,
Trubisky, Kizer, Beathard, Dobbs, Peterman, Kaaya, Kelly** — every one carrying
`completion_pct 0.00594 · YPA 6.2 · TD:INT 1.0909 · all_purpose_yards 2923 · pass_attempts 374`.
The 2020 group of ten puts **Lawrence, Fields, Mac Jones, Trask, Mills, Book, Mond, Ehlinger**
on one line. In the curated table these collapse to one value per draft class: 2015 → 7.5,
2017 → 6.2, 2021 → 6.4, 2023 → 6.7.

**Two further defects Codex's packet did not name:**
- **`completion_pct` is out of range for 62/62 populated rows** — min 0.0, max 0.01, distinct
  `{0.0, 0.00572, 0.00576, 0.0058, 0.00594, 0.00618, 0.01}`. A completion percentage cannot be
  0.006 on any scale. A **computation** defect independent of the collision, surviving its fix.
  `sack_rate` is **0/126 — fully dark** — and the run still published `ok`.
- **The `raw/` snapshot is not raw for 2 of 4 families:** `tpa_*` (404) is a bare float scalar,
  `qb_stats_*` (126) is the post-normalization 11-key dict; only `player_*` (28) and
  `sp_ratings_*` (14) keep the API shape. Violates `01` §Source Adapter Rules — and it is
  **why the mechanism cannot be closed from disk: no artifact records which player CFBD
  actually returned.** The evidence was discarded before it was written.

**Root-cause status, proven vs inferred.** *Proven from disk:* cross-player identical vectors in
both vintages; `completion_pct` invalid 62/62; `sack_rate` 0/126 published `ok`;
`_validate_curated` (`cfbd_foundation_refresh.py:97-139`) has no identity, collision, range, or
retention gate; and **the defect is in the ACTIVE input Engine A reads today** — the isolated run
redistributes and partly drops the bad payload, it does not introduce it. *Inferred, strongly
supported by code but NOT proven:* fuzzy `playerName` (`cfbd_qb_adapter.py:129-133`) plus
`_first_stat` (`:58-68`) taking the first `statType=="YPA"` record with no player/team/season
binding, and `_request_json` (`:40-48`) swallowing every exception into `[]` — which also explains
why the larger live run produced *fewer* values than the cache.

**CROSS-LANE.** Codex opened the same blocker independently (`w#9hetallx-1`). I reproduced every
figure of theirs keyed on `gsis_id` (unique 874/874, row order verified identical): **251 changed
cells / 44 distinct players / 100% QB / 0 non-QB**; payload groups 14/10/10/10/9/8 (isolated) and
15/10/10/9/9/8 (legacy); their 2016 cohort exact. The 44 is driven by the four `_source` columns,
the 15 by the three value columns — both right, different denominators, not a disagreement.
Likewise their "2016" is the college-season filename year while the CSV `season` column reads 2017
(draft year) for the same nine players.

**Proposed contract boundary for Codex's RED (Claude owns the GREEN):** G1 response-identity
binding · G2 no silent exception swallowing · G3 cross-player collision gate · G4 semantic range
gate · G5 feature-retention/coverage gate · G6 raw fidelity. Full text in the evidence file.

- **Files changed:** `docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_claude_v1.md`
  (new), `docs/agent-ledger/evidence/2026-08-01/msg_codex_cfbd_confirm_claude_v1.txt` (new),
  this entry. All uncommitted.
- **Tests / checks:** none run — no code changed. All findings are measurements over committed
  or gitignored data artifacts, rerunnable from the paths cited in the evidence file.
- **Product alignment:** `05` foundation-first — this is a layer-1 hole found before spending
  cycles above it. Per `05` §3, a root-layer conclusion is **not a licence to fix**: the repair
  is proposed, not opened. `00` §Evidence Hierarchy — no player claim is made from these values;
  they are the thing under suspicion.
- **Drift risks:** ~~the three `qb_*_final` columns are in `run_phase20_bakeoff.py:60-62` at
  importance 39.5 in `phase20_bakeoff_20260524T183807Z_db568d44.json`, so a past bakeoff consumed
  a feature where nine QBs share one line.~~ **[STRUCK — MY ERROR, corrected 16:20 ET. 39.5 is
  `coverage_pct`, NOT importance; all four CFBD QB fields are in `dropped_features` and were
  dropped before any fit; `passing_candidates: []`, `model_pkl_changed: false`,
  `QB_model.pkl` dated Apr 30 predates the run. NO model consumed them and none was contaminated.
  Caught by Codex `w#cfbdblast1`; full correction in §7 of
  `docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_claude_v1.md`.]** The
  actual drift risk is narrower and stands: the bad values live in the cache and the training CSV
  as a **future-candidate hazard** — a later run with better coverage would fit on them. **Recorded,
  not opened** — model remediation needs David's word. Second risk: the unrecorded 07-31 live run
  means the ledger is not a complete account of paid API activity.
- **Handoff / next step:** CONFIRM sent to Codex (`dynasty:1.2`), content-verified queued in
  their pane; awaiting CONCUR/CHALLENGE on the G1-G6 boundary. **David's word needed on:** whether
  the adapter repair opens now; and separately, nothing is to be promoted, re-fetched, or
  model-touched. Item 2 of the order (nflverse injuries) is untouched and available.

## 15:40 ET — Codex (CFBD resume check found an adapter/curation blocker)

- **Task:** Resume the agreed CFBD-first ingestion order without overwriting the parked working
  tree or spending another paid fetch blindly.
- **Governance read:** `02` v1.5.0, `00` v1.1.0, `05` v1.3.0 (§1 David-verbatim; §2 onward pending),
  `01` v1.0.0, `03` v1.1.0, current `AGENT_SYNC.md`, and today's ledger. Used the `cfbd` and
  `cockpit-messaging` skills.
- **Layer:** 1–2 (ingest/curate). The dependency check was the July 31 manifest/raw snapshot plus
  the active-input/consumer read. Result: the presenting problem is a foundation defect, not an
  Engine A or frontend symptom. Conclusion: hold upper-layer work and repair the source contract;
  this conclusion is not authority to change model inputs.
- **Finding:** a successful isolated run already exists at
  `app/data/sources/cfbd_foundation/`, contrary to the stale board entry. It published 572 raw
  files / 874 curated rows / 100% identity as `ok`, but differs from the active 874-row input in
  251 cells across 44 QBs. Per-year QB cache files repeat identical normalized payloads across
  different players (largest identical groups 5–14 in every season checked). The active input
  includes impossible repeated values such as `completion_pct=0.00594`; the isolated refresh moves
  those payloads among players or drops them. The wrapper's current quality gate cannot detect it.
- **Active-consumer check:** Engine A training/bake-off scripts still read
  `app/data/training/prospects_with_outcomes_v3.csv`; nothing reads the isolated curated path.
  Re-executing the wrapper alone does not resolve the May-era active input.
- **Write scope:** evidence record only; no code, API call, source artifact, scheduler, model, or
  active input changed. Full finding:
  `docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_codex_v1.md`.
- **Checks:** CFBD preflight ready; manifest/status/raw counts; SHA and keyed CSV comparison;
  per-year QB payload uniqueness census; source-read audit. No live fetch run.
- **Drift risks:** `AGENT_SYNC.md` and `docs/data-inventory.md` still say the wrapper was never
  executed. They must be corrected only after the technical disposition is settled; no stale text
  was silently rewritten during the finding pass.
- **Handoff / next step:** route to Claude as source-pipeline implementer for independent
  reproduction and contract disposition. Codex owns the failing RED/review lane. Injuries remains
  next only after this CFBD foundation blocker is resolved or David explicitly reorders.

## 15:43 ET - Gemini (Operations & Telemetry)

**Layer served: cross-layer / telemetry (ingestion/curation awareness).**

Processed Codex integration review awareness copy [w#6j85qabh-1] regarding the CFBD foundation blocker.

### Preflight State
- **Docs read & versions:**
  - `docs/governance/02-agent-operating-loop.md` (v1.5.0)
  - `docs/governance/00-product-constitution.md` (v1.1.0)
  - `docs/governance/05-layer-doctrine.md` (v1.3.0)
  - `docs/governance/01-north-star-architecture.md` (v1.0.0)
  - `AGENT_SYNC.md` (current)
- **Current task:** Process Codex integration review awareness copy of `docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_codex_v1.md`.
- **Intended write scope:** Append-only preflight and awareness entry to today's ledger via `scripts/gemini_ledger_append.py`.
- **Layers 1–2 dependency check:** Telemetry/awareness only; no active curation or modeling changes proposed.
- **Drift/Validation:** N/A.

### Telemetry & Operational State Review
- **Observed CFBD Run State:** CFBD isolated run `20260731T165930158823Z` exists at `app/data/sources/cfbd_foundation/` with `status=ok`, but contains 251 cell differences across 44 QBs compared to active inputs.
- **Data Quality Defect:** Stored normalized QB files repeat identical statistics (e.g. Mahomes, Watson, Trubisky, Dobbs all sharing the same stats in 2016) in both legacy cache and the new isolated run. The active curated values include `completion_pct=0.00594` (0.594%), which is mathematically invalid for a qualifying QB.
- **Curation Gate Defect:** Curation quality gate lacks checks for cross-player collisions, player-response identity, or semantic range validation, allowing invalid states to publish `status=ok`.
- **Active Consumer Isolation:** Verified that active Engine A training/bake-off runs read `app/data/training/prospects_with_outcomes_v3.csv` and do not consume the isolated curated CSV, isolating the defect from model inputs.
- **Rushing Hypothesis Status:** The QB rushing hypothesis (H2) remains **UNDER TEST** as registered in `docs/validation/2026-07-21-qb-1-study-registration.md` and has not been cited or assumed.

## 15:49 ET - Codex (CFBD RED boundary opened)

- **Layer served:** Layer 1 ingest and Layer 2 curation contract; no Layer 3 model change.
- **Independent reproduction received:** Claude confirmed the 251-cell / 44-QB diff, repeated
  same-season payloads, absent publication gates, and active-consumer isolation in
  `docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_claude_v1.md`. Claude
  additionally established that all 62 populated completion values are mis-scaled, sack rate is
  0/126 populated, and two staged `raw/` families contain normalized/scalar outputs rather than
  source responses.
- **Provider contract check:** current public CFBD Swagger metadata confirms
  `/stats/player/season` has no player-name or player-ID filter; no data endpoint was called.
- **Disposition:** concur with Claude's G1-G6 repair boundary with two corrections: a duplicate
  vector is a fail-closed audit alarm rather than proof by itself, and `[0,1]` would not reject
  `0.00594`. Add G7 enforcing endpoint-supported request parameters and response-side player-ID
  binding. Codex is opening the failing RED; Claude holds the GREEN until that artifact is routed.
- **Still prohibited/out of scope:** no paid refresh, no isolated-to-active CSV copy/promotion, no
  model remediation. QB rushing remains **UNDER TEST** and is not a premise of this work.

## 15:54 ET - Codex (CFBD G1-G7 failing RED ready)

- **Layer served / write scope:** Layer 1 ingest plus Layer 2 publication contract. Added only
  `tests/contract/test_cfbd_qb_ingest_red.py` and durable evidence; production code and data are
  unchanged.
- **RED:** 11/11 focused tests fail at the intended missing boundaries: response-ID binding, PCT
  scale, supported endpoint parameters, request-error distinction, source-response persistence,
  collision refusal, completion plausibility, dark-family refusal, raw-fidelity checks for both
  normalized QB dicts and TPA scalars, and material feature-coverage regression.
- **Baseline isolation:** the pre-existing adapter/wrapper suites remain **29 passed, 2 skipped**.
  Ruff and diff-check pass. No integration/live API test ran.
- **Handoff:** full contract and failure census are in
  `docs/agent-ledger/evidence/2026-08-01/cfbd_qb_ingest_red_codex_v1.md`. Claude owns the GREEN;
  internal implementation is unconstrained so long as G1-G7 observable behavior passes.
- **Held boundaries:** no active-input promotion/copy, paid refresh, consumer wiring, bakeoff
  rerun, or model remediation. QB rushing remains **UNDER TEST**.

## 15:57 ET - Codex (mandatory CFBD blast-radius correction)

- **Correction:** the earlier cross-lane statement “importance 39.5” was a field-reading error.
  In the Phase 20 artifact, 39.5 is **coverage percentage**. All four CFBD QB fields are listed in
  `dropped_features`; only draft capital and final college age were available; ridge and GBT were
  skipped as `enriched_features_equal_baseline`; no candidate passed; and the artifact records
  `model_pkl_changed=false` / `latest_json_changed=false`.
- **Promoted-model check:** `QB_model.pkl` predates the bakeoff (April 30 vs May 24), its model card
  identifies Engine B v2 training data, and no inspected model artifact contains the four CFBD
  feature names.
- **Correct blast radius:** the defect spans adapter request identity/scale/error semantics, raw
  capture, and curation/publication gates. Its proven impact is the CFBD cache and prospect training
  CSV plus latent future-use risk. The inspected bakeoff did **not** fit these fields and no promoted
  model was contaminated; current evidence does not justify model remediation.
- **Durable evidence:** `docs/agent-ledger/evidence/2026-08-01/cfbd_blast_radius_correction_codex_v1.md`.
  This correction supersedes every earlier “importance 39.5” / “bakeoff consumed” sentence; those
  historical messages remain preserved as sent but must not be repeated.

## 16:02 ET - Codex (CFBD GREEN held for David's explicit word)

- Claude independently ran the Codex RED and reproduced exactly **11 intended failures**, then
  began independently checking the cited **29 passed, 2 skipped** pre-existing baseline.
- Cross-lane technical disposition is closed: G1-G6 stand, with G7 added for provider-supported
  parameters and response-ID binding. The blast-radius correction is absorbed by both lanes.
- Production implementation has **not** opened. Claude correctly held the GREEN because a review
  lane cannot authorize an action. David's explicit word is required to repair the adapter/raw
  snapshot/publication path.
- Proposed authorization remains narrowly bounded: production code plus tests for G1-G7 only;
  no paid/live CFBD call, refresh, active-input copy/promotion, consumer wiring, bakeoff rerun, or
  model change.

## 16:03 ET - Codex (CONCUR on David-gated GREEN)

- Claude independently verified the RED at **11 failures** and the pre-existing baseline at
  **29 passed, 2 skipped**, accepted G1-G7 without modification, and held production work.
- Codex concurs: review clearance settles the technical contract but does not authorize the source
  implementation. The RED remains unchanged pending David's explicit word.
- A hold reply was drafted but **not routed**: the helper refused because David was actively typing
  in Claude's input. Codex did not submit or overwrite that text. David then explicitly authorized
  continuation (`if u guys can fix this - youre approved to continue`), superseding the draft and
  opening the bounded G1-G7 GREEN. No API, refresh, promotion, bakeoff, or model action was opened.

## 16:04 ET - Codex (ACK authorized GREEN; independent review hold)

- David's word received verbatim: `if u guys can fix this - youre approved to continue`.
- Codex reads the authorization as exactly Claude's stated scope: the CFBD QB adapter and
  foundation publication wrapper against G1-G7, with no live/paid refresh, data copy/promotion,
  consumer wiring, bakeoff rerun, PFF replacement, or model work.
- Codex is holding the RED unchanged and will not edit Claude's production files. Independent audit
  begins only after GREEN READY and covers the production diff, contract/baseline/full-suite tests,
  Ruff/diff hygiene, and divergence report.

## 16:07 ET - Codex (G6 two-file scope feasibility challenge)

- Before production edits landed, Codex identified that the direct G6 RED passes `cache_dir` only
  to `scripts.build_w2b_cfbd.compute_qb_cfbd_features`; neither authorized production file has that
  path on the direct call.
- A clean raw-before-normalization implementation therefore appears to require the builder to pass
  or persist raw response data. Hidden environment/global/call-stack coupling is rejected.
- The delivery helper refused while Claude was active, so the challenge was **not routed** and its
  unsent draft was deleted. Claude independently identified and disclosed the same builder-file
  expansion to David before handoff. No production file or RED was changed by Codex.

## 16:15 ET - Codex (blocking GREEN review before handoff)

- Source/diff review found five unresolved contract gaps: unresolved player identity can still
  accept a lone unattributed row; non-list JSON schema failures still become empty data; collision
  grouping ignores season; raw responses are persisted only after normalization succeeds; and the
  wrapper's array-only raw gate rejects scalar TPA/games-count files still emitted by its builder.
- The last item guarantees the next live refresh would refuse its own staged output. It is a release
  blocker, not a deferred cleanup. No live call is needed to prove it; the writer and validator are
  contradictory in the current diff.
- The five mechanisms were independently encoded in
  `tests/contract/test_cfbd_qb_ingest_green_review.py`; all **5 failed in 0.99s**, exactly as
  predicted. The first send attempt was not delivered because Claude's closeout was active; Codex
  will resend and positively verify delivery when the pane is idle. Codex made no production edit
  and will not CLEAR until all five are closed or counter-evidence falsifies them, followed by tests
  against the final edit state.

## 16:27 ET - Codex (G6 fidelity follow-up during repair)

- Claude independently reproduced all five review failures and concurred on their merits.
- The first cache-family repair wrapped derived TPA/games-count scalars in fabricated one-object
  arrays. That makes the array-only validator pass but does not preserve the provider response
  unchanged, so it does not satisfy G6 raw fidelity.
- Added `test_g6_games_cache_preserves_the_unmodified_provider_response`. Updated review census:
  **5 passed, 1 failed**; the failing row proves a two-object provider games response is replaced
  by a synthetic `gamesCount` record. Follow-up delivery pending pane availability.
- Claude accepted that finding and changed the games path to retain the provider list. Codex then
  checked the parallel TPA path: it still wraps the derived scalar with `_stat_record()` and loses
  the `/stats/season` response. Added a TPA fidelity row; updated census **6 passed, 1 failed**.
- Claude accepted the TPA row and added a receiving-adapter raw sink; all seven fidelity/identity
  rows then passed. Final source audit found that TPA and games transport exceptions still become
  the same persisted empty array as valid no-data. Added two exact G2 rows: updated review census
  **7 passed, 2 failed**. The full suite already underway did not collect those rows and is stale
  for final-clearance purposes.
- After G2/G6 went green, final identity review found `_resolve_player_id()` still falls back to an
  exact-name match on the wrong team when no requested-team match exists. Added a provider-confirmed
  mismatch row. Added its operational companion because the builder passes training aliases such as
  `Florida St.` without using its existing `Florida State` normalization. Review census **9 passed,
  2 failed**. These are the remaining G1 player/team-binding edges, not new gates.

## 17:08 ET - Codex (CFBD GREEN CLEAR after final independent audit)

- Final two G1 rows closed: wrong-team exact-name matches refuse; builder canonicalizes college
  aliases before strict provider-team resolution. Review suite is **11/11 passed**.
- Independent final CFBD slice: **129 passed, 2 skipped**. Independent full repository suite with
  the two standing nflreadpy collection exclusions: **4,173 passed, 12 skipped, 9 xfailed, zero
  failed** in 429.26s. Ruff on every CFBD production/test file in the diff and `git diff --check`
  are clean.
- Verified both pre-existing test edits directly: no value assertion was removed or weakened;
  identity was added to fixtures, HTTP failure now asserts a typed raise, and genuine no-data plus
  legacy scalar-file read semantics remain pinned.
- Accepted the fail-loud behavior in two untouched receiving-adapter callers as G2's intended blast
  radius. Accepted the production expansion to four files as necessary for G2/G6, with the earlier
  two-file sentence recorded as inaccurate.
- Disposition: **GREEN CLEAR, uncommitted**. Durable audit:
  `docs/agent-ledger/evidence/2026-08-01/cfbd_green_clear_codex_v1.md`. No live refresh, data
  promotion/copy, consumer wiring, bakeoff, model action, commit, or push occurred.

## 17:30 ET - Claude (CFBD QB ingest repair — GREEN against G1-G7, after nine reviewer findings)

- **Task:** implement the Claude-owned GREEN for the CFBD QB ingest defect, against Codex's
  G1-G7 RED contract.
- **David's word, verbatim:** *"if u guys can fix this - youre approved to continue"*. That
  authorized the fix. It did **not** authorize commit, push, paid refresh, CSV promotion,
  consumer wiring, bakeoff rerun, or model work — none of which happened.
- **Governance read:** `02` v1.5.0, `00` v1.1.0, `05` v1.3.0, `01`, `03`, `AGENT_SYNC`, today's
  ledger. No visual surface, so the design foundation was not read.
- **Layer served: 1 (ingest), presenting.** Layers 1-2 dependency check not applicable — the
  work is at layer 1 and repairs the layer that causes the defect.

**WHAT THE REPAIR DOES.** `/player/search` resolves the player; every family is then bound to
that identity locally (`playerId` for stats, `id` for PPA, `athleteId` for WEPA). No
`playerName` reaches a stats endpoint. **Unresolved identity yields nothing** — not a
best-guess row. Transport failures raise instead of returning empty. `completion_pct` is
normalized by magnitude, fixing the double-division that produced 0.00594. Four publication
gates (collision, plausibility, zero-coverage, coverage-retention) and raw-snapshot fidelity
across the QB, tpa and games families.

**NINE REVIEWER FINDINGS ACROSS THREE ROUNDS — all Codex's, all closed, none challenged.**
My own adversarial self-probe passed 21/21 and missed every one of them; that is the entry
that matters here.

1. **Unresolved identity accepted a lone unattributed row.** My stated ambiguity policy, which
   I had flagged for challenge. *"There was only one row, so it must be his"* is a smaller copy
   of the very defect under repair.
2. Non-list payload became empty data (a provider schema change read as an empty season).
3. Collision grouping ignored season, so cross-season coincidence would false-alarm.
4. Raw snapshot discarded when normalization raised — a paid response bought twice.
5. **The validator contradicted the builder's own writers.** I had disclosed this as a "named
   follow-up"; Codex correctly refused that framing and called it a release blocker, because
   writer and validator contradicted each other inside one diff.
6. My first games fix stored a derived `{statName, statValue}` record — array-shaped, so it
   satisfied my own gate while carrying no captured response. **Compliance theatre**, caught by
   a test demanding the verbatim payload.
7. tpa remained a derivative; fixed at the bridge Codex named — `fetch_team_pass_attempts`
   gained a `raw_sink` capturing `/stats/season` before the scalar is derived.
8. **G2 hole in tpa:** a timeout produced an empty `raw_sink` persisted as `[]`, byte-identical
   to a valid empty response.
9. **G2 hole in games:** same collapse, and the negative-cache write would have suppressed the
   retry permanently.

**Rows 8-9 are the original defect reappearing inside the code written to fix it** — a failure
made indistinguishable from an answer. Recorded rather than smoothed.

**Files changed:** `src/dynasty_genius/adapters/cfbd_qb_adapter.py`,
`src/dynasty_genius/adapters/cfbd_receiving_adapter.py`,
`src/dynasty_genius/capture/cfbd_foundation_refresh.py`, `scripts/build_w2b_cfbd.py`, plus test
updates in `tests/test_cfbd_qb_adapter.py`, `tests/test_cfbd_receiving_adapter.py`,
`tests/test_w2b_cfbd.py`. Diff stat over source+tests: 833 insertions / 151 deletions.

**Tests / checks (post-final-edit):** Codex review checks **9/9**; Codex RED **11/11**;
six-suite CFBD slice **127 passed, 2 skipped**; **full suite 4,171 passed / 12 skipped /
9 xfailed / 0 failed** (419.69s, standing `--ignore` of the two nflreadpy collection-error
files); `scripts/verify_sprint_closeout.py` **ENFORCE PASS** (python-suite, ruff,
standalone-scripts); Ruff clean on `src app` and every touched file. Two earlier full-suite runs
were **voided and killed** — Codex correctly noted they predated the final rows.

**Product alignment:** `05` foundation-first — a layer-1 repair before spending cycles above it.
`00` §Evidence Hierarchy — no player claim is made from these values. `01` §Source Adapter Rules
— raw snapshot before parsing, now actually honored for QB, tpa and games.

**Drift risks / disclosures:**
- **Scope grew from 2 files to 5.** Each widening was driven by the RED contract, but the scope
  sentence I gave David was wrong twice. Disclosed to him and to Codex both times.
- **Two pre-existing tests edited.** `test_fetch_returns_none_on_http_error` asserted the G2
  defect as a requirement; four `test_w2b_cfbd.py` round-trips used the old scalar API. **No
  assertion weakened** — every expected value still asserted. A GREEN author editing his own
  constraints warrants independent verification; Codex was asked to check the diff, not my
  description.
- **Behaviour change past this diff:** `fetch_team_pass_attempts` now raises where it returned
  `None`, affecting two untouched callers (`scripts/build_college_features.py:312`,
  `scripts/enrich_te_prospects_cfbd_2026.py:189`). Flagged to Codex.
- One incidental lint fix: dead `final_team_yds` assignment (F841), required by `03`
  clean-on-touch. Why it was unused is **not** investigated — WR curation, out of scope.
- Eight pre-existing Ruff findings remain in `tests/` files this work did not touch; CI gates
  `src app`, which is clean.

**Handoff / next step:** GREEN round-3 census delivered to Codex (`dynasty:1.2`,
content-verified); **awaiting their GREEN CLEAR or NOT CLEAR.** Nothing committed —
David's word covered the fix; **commit and push are separate words.** Still unopened threads:
PFF-NCAA-passing as a replacement source, and `enriched_features_equal_baseline` as a
model-layer defect signal.

## 18:05 ET - Claude (CFBD QB ingest repair — Codex GREEN CLEAR; held uncommitted)

**Closes the 17:30 entry.** Codex issued an enumerated **GREEN CLEAR** on the CFBD G1-G7 tree.
Durable reviewer artifact: `docs/agent-ledger/evidence/2026-08-01/cfbd_green_clear_codex_v1.md`.

**Two more findings landed after the 17:30 entry was written, making ELEVEN across four rounds:**

10. **`_resolve_player_id` fell back to name-only matches when no row matched the requested
    team.** `if narrowed: matches = narrowed` is a preference, not a narrowing — an
    identically-named quarterback at another school resolved for the requested player and his
    whole season was bound with full confidence. **My bug, and the same defect family as the
    original, one layer down.** Team filter is now unconditional when `college_team` is supplied.
11. **The builder passed raw training aliases** (`Florida St.`) where the provider returns
    `Florida State`, so strict binding would have refused correctly and darkened valid rows.
    `normalize_college_name` is now applied before resolution. **Exposure measured, not assumed:
    69 distinct QB colleges in the live training table, 15 rewritten by the map** — Colorado St.,
    Florida St., Fresno St., Iowa St., Kansas St., Miami (FL), Michigan St., Mississippi St. and
    others. Roughly a fifth. Zero QB rows have a blank college.

Also fixed: `load_team_games_count`'s docstring still promised `None` on API failure after the
G2 fail-loud change.

**FINAL VERIFICATION — post-final-edit, both lanes independently:**

| Check | Claude | Codex |
| :-- | :-- | :-- |
| Full suite (standing 2-file `--ignore`) | 4,173 passed / 12 skipped / 9 xfailed / 0 failed (392.05s) | 4,173 / 12 / 9 / 0 (429.26s) |
| Six-suite CFBD slice | 129 passed, 2 skipped | 129 passed, 2 skipped |
| Codex review suite | 11/11 | 11/11 |
| Codex RED (G1-G7) | 11/11 | — |
| Ruff | clean (`src app` + every touched file) | clean |
| `verify_sprint_closeout.py` | **ENFORCE PASS** (suite, ruff, standalone-scripts) | — |
| `git diff --check` | — | clean |

Separate runs, identical counts. **Three earlier full-suite runs were killed and voided** —
Codex correctly flagged each as predating the then-current findings.

**Codex explicitly accepted, on the record:** both pre-existing test edits weaken nothing (QB
expected values still asserted; the receiving-adapter change moved only the unsafe
HTTP-failure expectation from `None` to a typed raise; the w2b cache tests preserve
438.0 / 14 / None while separating raw-writer contracts from legacy-file readability); the
out-of-diff caller blast radius; and the four-file production scope as a **named divergence, not
hidden scope**.

**CALIBRATION, recorded per `02` §Falsification #6.** Across four rounds my self-probes passed
21/21, 17/17, 9/9 and 5/5 and caught **one** defect of my own. Codex's review found **eleven**
after my first GREEN claim — including two (rows 8-9) that were the original defect reappearing
inside the code written to repair it, and one (row 6) where my fix satisfied my own gate while
carrying no captured response. On this surface the implementer's evidence was not a substitute
for the independent reviewer's, and this ledger says so rather than rounding it off.

- **Files changed:** four production (`cfbd_qb_adapter.py`, `cfbd_receiving_adapter.py`,
  `cfbd_foundation_refresh.py`, `build_w2b_cfbd.py`) plus three test files. **Uncommitted.**
- **Tests / checks:** as tabled above.
- **Product alignment:** `05` foundation-first layer-1 repair; `01` §Source Adapter Rules now
  honored (raw snapshot before parsing) for the QB, tpa and games families; `00` unaffected —
  no player claim is made from these values.
- **Drift risks:** unchanged from 17:30 and all reviewer-accepted — scope grew 2 -> 4 production
  files; two pre-existing tests edited; `fetch_team_pass_attempts` now raises for two untouched
  callers.
- **Handoff / next step:** **GREEN is CLEAR and HELD UNCOMMITTED.** David's word covered the fix
  only; **commit and push are separate words.** On a commit word, the post-commit divergence
  audit request goes to Codex per `02` §Closing the loop. Boundaries intact: no live/paid
  refresh, no CSV promotion or copy, no consumer wiring, no bakeoff rerun, no model work.
  Unopened threads unchanged: PFF-NCAA-passing as a replacement source, and
  `enriched_features_equal_baseline` as a model-layer defect signal.
