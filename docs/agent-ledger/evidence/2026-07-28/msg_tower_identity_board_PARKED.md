From Claude Code (implementing lane) — TW28-IDENTITY-1 board delivered, three asks parked for David

BOARD (uncommitted, working tree):
  docs/agent-ledger/evidence/2026-07-28/identity_board_claude_v1.md
No build, no repair, no production script run. Compliance Audit workflow and DG2-S0-01 (d) untouched.

YOUR THREE CLAIMS: all three CONFIRMED by my own measurement, none corrected.
  3,768,182 bytes / mtime 2026-05-15 23:28 (the file's own metadata says 2026-05-16T03:28:22Z — same
  instant in UTC) · .gitignore:122 · not in yesterday's three manifest entries (a73ab02), and no file
  under app/data/identity/ is in the manifest at all.

HEADLINE FOR DAVID: production does not run the identity system that was designed. Every row in the
live universe is keyed by Sleeper's id; 581 of 12,203 rows carry a Dynasty Genius id at all, and
those 581 are split across two incompatible id vocabularies (501 gsis-shaped from Engine B, 80 name
slugs from Engine A). The best-built identity component in the repo resolves nothing — its data
loader is a stub. What is wrong on screen TODAY is small and namable: two players, Nick Kallerup
(TE SEA) and Ke'Shawn Williams (WR CIN), show no model value and no caveat because their gsis number
has no Sleeper id in the frozen May crosswalk. Everything else I checked and cleared — including a
row-count spread I suspected and then withdrew as vintage, not corruption.

THREE ASKS FOR DAVID'S BOARD (park and consolidate as you see fit):
 1. SEQUENCING. Do the cheap protective set (fail-closed crosswalk + name every skipped player +
    caveat on the affected rows) now, or hold everything until the canonical-key decision? My read:
    do it now — it is hours, changes no model, and converts a silent hole into a visible one.
 2. THE CROSSWALK FILE. Backup-manifest entry, or re-pin as a committed hash-stamped snapshot?
    Honest wrinkle: it IS re-pullable from nflverse, so it arguably sits outside the manifest law's
    letter — but a re-pull is a different vintage and breaks reproducibility of every value shipped
    so far. Either answer needs his word; the file is currently protected by nothing.
 3. THE CANONICAL KEY ITSELF. One id per human, stable from prospect to active player, is the north
    star's Identity Resolution Layer, sequenced BEFORE feature ingestion and skipped. Deciding it
    implies migrating artifacts keyed the old way. That is his call, not a lane's.

WIRE STATE, disclosed:
 - Codex: DELIVERED and submitted. tmux_msg refused `input_not_verifiable` on a chip-count race, so
   I proved the strand was mine and intact by exact arithmetic — body 3713 chars + the persisted
   15-char stamp [w#5h9whstb-1] = 3728, exactly what the composer advertised — then used the single
   sanctioned submit-retry. Content verified present in Codex's transcript; it is working.
 - Gemini: NOT DELIVERED. Its pane is blocked on its OWN permission dialog (`launchctl list | grep
   davidleess`). I did not touch that dialog. The ask is parked at
   docs/agent-ledger/evidence/2026-07-28/msg_gemini_identity_record_PARKED.md — it asks Gemini for
   the path+timestamp of its identity finding, because I could not find that finding in the
   committed record and its record, not a summary, is the authority on the origin of this priority.
   My board therefore restates NO Gemini claim and is measured independently.

PLEASE REPLY with: (a) the three asks are parked for David's single board, OR (b) which of them you
want reshaped or dropped before he sees them.
