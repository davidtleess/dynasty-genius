From Claude (write lane) — ROUND 8 COMPLETE: your four R7 smallest corrections implemented; find concrete defects [w#qb1-exec-1]

AUTHORITY: David's word in the round-8 record ("one more bounded round - open round 8 per your sanctioned mechanism, claude implements your four R7 smallest corrections, execution only on your clear") + his standing sequencing word: "run it through codex when the suite is green." The suite is green (census below, measured at these exact pins). Scope: exactly your three round-8 files, nothing wider.

ROUND-8 PINS (post Ruff import-sort safe-fix; verified byte-identical on disk this session):
- src/dynasty_genius/eval/qb_validation/execution.py 913225f529a0c63a30b4ee6b025c3d1ac408a5d6107be15b8a65dc80d83e9f37
- scripts/run_qb1_study.py ef7a8244d5b141eaa7280f37b269531089f5eeeb6649b230b09fcbe50bde86eb
- tests/contract/test_qb1_green_correction_contracts.py 513ed1bd255c9c62368bcb68f8cacfbfdc3284aa5c9df3a0c8a36affd46b9b58
- UNCHANGED: status.py 67651821… · __init__.py d8876020… · execution RED 5d3bc660… · program RED 7e950792… · ratchet 25c4ffde… · reinforcement db351f8c… · frozen wire pair b3247ec8…/fd924eb1…

THE FOUR IMPLEMENTATIONS, item by item against your smallest corrections:
1. R7-G1-STATUS-TOTALITY — the gate now reconstructs the canonical evaluation payload (using production's own contrast_status mapping) and INVOKES the shipped evaluate_power_and_status itself, requiring EXACT equality of the emitted status (plus H5 ni_met and flags) against the recomputation, with the below-floor special case honored; registered fold totals enforced; required evidence fields enforced (adjusted_p on substantive statuses; H5 adjusted_p_ni + one_sided_lower_95). A published status that the shipped total function would not itself produce refuses.
2. R7-G2-H5-RECONCILIATION — every margin leaf is closed: computed = all produced fields non-null; unavailable = named state + all three null outputs; production converts present-but-null leaves to honest unavailable rather than shipping shells. Per-missing-contrast exclusion flags, and EXACT two-sided reconciliation: evaluable rows + structured excluded rows == fold-metric presence, both directions; every exclusion row carries test_season + a closed-vocabulary reason.
3. R7-G3-F13-SEMANTICS — boundary rows bound to the trailing window via the shared ARCHETYPE_WINDOW_SEASONS constant (identity-pinned with the runner), positive-integer qualifying games, the registered abs(yards−400) ≤ qualifying_games relation enforced per row, and gate/flip booleans RECOMPUTED from the row's own fields rather than trusted (out-of-boundary seasons contribute identically to all three shifted classifications, so flips are decidable from boundary rows alone — the property the recomputation rests on).
4. R7-G4-CASE-FOLD-LANE — the admissible case-lane set is computed from the case's own fold: h5 lanes admitted ONLY when the case's fold is a registered H5 fold; a reported H5 lane on any other fold refuses.

TWO PRODUCTION-TRUTH CORRECTIONS, disclosed rather than smoothed (both caught by the G4 end-to-end arbiter and recorded in the 2026-08-15 ledger): (a) the excluded_folds structured shape my first draft assumed did not match what production emits — corrected to the emitted structure; (b) production emitted present-but-null margin leaves where my draft required absence — resolved by the honest-unavailable conversion in item 2. Both are the write-from-incomplete-measurement class; the arbiter existed to catch exactly this.

ORACLE + MUTANTS: 12 new R8 tests, every one publishing through the public runner — including ALL NINE of your round-7 probe rows as named mutants, plus positive controls proving the honest paths still publish ok (flagged exclusion with reconciled counts · state="unavailable" null cell · recomputed flip on a genuine boundary row).

CENSUS (measured at these exact pins): correction contracts 91/91 (79 + 12 R8) · frozen bundle + reinforcement 555/555 · YOUR round-7 probe now FAILS 9/9 · all carried probes r2–r6 FAIL 23/23 · FULL suite 6,093 passed / 15 failed / 12 skipped in 12:17, preceded by touched-file sanity 121/121 + strict compile clean — the 15 verified BY NAME (the standing untracked test_governed_cadence_inputs_red.py alone reproduces exactly 15); ZERO tracked failures · Ruff clean. Hygiene disclosure, same as rounds 6–7: the chain's tail masked pytest's exit code; the tally is pytest's own summary line and the 15 were re-verified by the named re-run. Provenance disclosure: the full-suite tally was measured in the authoring session immediately before its context handoff (ledgered in the 07:4x addendum, David's word "wait for the suite then record the tally"); this fresh session re-verified all pins byte-identical before routing. You re-run independently during review regardless.

STRUCTURED STATE: findings 7-1..7-4 resolved in round 8 via the verbs; loop verdict CLEAR_ELIGIBLE with zero reasons. Your CLEAR terminates the cycle.

ON YOUR CLEAR: reviewer-clear + round-close via the verbs, then DAVID'S STANDING TRIGGER FIRES THE STUDY (.venv/bin/python3.14 scripts/run_qb1_study.py — deterministic, seed 20260716, atomic publication under the frozen root). The registered readout then goes to David for HIS ruling. ON A BLOCKER: no further round exists on any standing word — the run re-parks for DAVID directly (the Judge's one ruling is spent). H2 QB rushing remains UNDER TEST with no result until execution AND that ruling. No push.

PLEASE REPLY with: (a) CLEAR with enumerated checks, OR (b) concrete defects with evidence.
