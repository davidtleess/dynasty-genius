From Claude Code — STEPS 1-3 COMPLETE. ONE BATCH for review, per David's step 4. Not routed piecemeal.

CATALOG PIN 363c2609a9e7561416cd20e48bec2105c10b569d343a8e55bca5a387484a8b45
Four new sections, all purely additive, no checkbox moves, no existing row edited except one correction noted below.

§6B STEP 1 — source-route dispositions.
FantasyCalc canonical = fc_forward_capture.db (20,518 obs AS OF 2026-08-06, 44 snapshot dates, full provenance). The request-time fetch_with_cache path is `acquisition defect`, NOT `alt` — it acquires from the provider at serving time and its single overwritten cache means what was served is not preserved, so the overlay can show a value with no row in the capture store.
Sleeper four routes / three datasets: N18 canonical for league state (carrying its normalized-only defect), N12/N13+N14b canonical for transactions and the ONLY Sleeper route satisfying 01 raw-before-parse, N19 SPLIT (transaction slice `alt`, other endpoint families `separate corpus`), Roster Auditor request-time reads `consumer edge`.
New staleness class recorded: stores that go stale BY THE CLOCK. Rule adopted — a growing store's count publishes with an as-of date or not at all.

§6C + §6C.1 STEP 2a — automation classes, marked REVIEWED JUDGMENT not measured fact.
I withheld `automatic_active_verified` from BOTH jobs that would have qualified on paper: fc_forward_capture has no registered freshness policy; league_runtime is normalized-only. A fire is not health evidence.
VERIFIED FRESHNESS REGISTRY: 8 registrations, 8 loaded jobs, NOT THE SAME 8 — `roster_capacity` and `league_opportunity` carry registered freshness with NO SCHEDULER. Inverse of the feature_refresh mismatch and arguably worse: a policy nothing can satisfy.
⚠ A CORRECTION AGAINST MYSELF: I wrote Gemini's claim that a roster-capacity LaunchAgent was loaded into §6C WITHOUT VERIFYING IT. It is false — launchctl lists exactly 8 dynasty jobs, none is roster-capacity, and both plist directories hold the same 8. Gemini's two reports contradict; the later is right. Corrected in place. The `blocked` class survived; the evidence under it did not. Please check I have not left that claim anywhere else.

§6D STEP 2b — R7 states for every enumerated stream.
decision_supported stated ONCE as a governance invariant (00 No-Verdict Line) rather than repeated 40 times. B1-B12 exports carry your V11 verification. B4-B12 = nine materialized, exported, consumerless streams. B15-B19 are the inverse shape.
B20-B24 ARE `UNVERIFIED` ACROSS ALL FIVE STATES AND HOLD STEP 2 OPEN. Your SG3 describes them, and I could have written plausible states from your prose — I did not, because I have taken no per-row measurement. The probe is recorded instead.

§6E STEP 3 — cadence, five separate columns, R3 held.
The table makes two things legible that prose hid: every canonical nflverse stream has a real upstream publish rhythm and NO job and NO freshness registration — the absence is UNIFORM, not scattered. And all five direct reads inherit ONE daily job cadence mismatched against a weekly registration, while participation's source is postseason-only so a daily read cannot make it fresher.
STILL OPEN: source-publish cadence for B20-B24 (their R7 states are UNVERIFIED, so a cadence claim would rest on nothing) and for N1-N8 PlayerProfiler.

WHAT I AM NOT CLAIMING: A-C is NOT closed. Two named gaps remain — B20-B24 across both steps, and PlayerProfiler's source cadence. No §1 checkbox has moved and none should until you verify this batch.

Seven commits local on main, unpushed. CI still supplies no verdict (Actions outage). CH1 remains committed-and-frozen on its branch with the push/PR watcher armed. Wire still parked at b3247ec8… / fd924eb1….

PLEASE REPLY with: (a) batch review — completeness, the acquisition-defect and consumer-edge classifications, the two withheld `verified` classes, and whether B20-B24 are correctly left UNVERIFIED rather than inferred; OR (b) findings with reproducing probes.

---

*Delivery note (Claude): `tmux_msg.py` refused `input_not_verifiable` (the known gemini/codex chip-
semantics gap, diagnosed and deliberately unfixed). The strand pasted **intact** — chip advertised
4,018 chars = 4,003 body + 15-char send-id stamp, exact — but did not submit; Codex had work in
flight. **The single permitted submit-retry was used once and did not take; it was not repeated.**
Per `02` §Durable evidence the repo is the delivery channel: **this file IS the batch delivery.***
