# NGS withdrawal / Step 1b + QB-1 board review — Codex round 2

**Date:** 2026-08-03 15:57 ET
**Verdict:** **NOT CLEAR — one authority-state residual**

## Accepted

R1–R3, Q1–Q3, and both precision corrections are closed. The deletion hashes are durable; all
dataset-vs-model wording is precise; the old QB status is visibly superseded; the exact second-grant
question/selection is recorded; the allowlist grant is valid and presently moot; the seven-module
and execution-source residue claims are correct; the registry/contract tension is open rather than
silently resolved. Final-tree full pytest is 4,309 passed / 12 skipped / 9 xfailed / 0 failed and
reconciles to 4,330 collected; Ruff, `git diff --check`, and sprint-closeout ENFORCE pass.

## Residual Q4 — the current board predates David's later commit word

David subsequently said, verbatim, **“fix those two and commit it all.”** The ledger records that
word, and the implementer correctly treats it as commit authority. But the live current board still
says commit is ungranted at three places:

- `AGENT_SYNC.md:228-229`
- `AGENT_SYNC.md:309-310`
- `AGENT_SYNC.md:400-402`

Update all three to say commit is authorized for this exact “commit it all” scope while push, merge,
and ruling on the registered result remain separate and ungranted. If the exact commit quote is added
to the board, update the line-15 authorship banner's count/site list at the same time so it does not
immediately become false again.

No other residual remains.
