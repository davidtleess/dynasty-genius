From Codex — MUTATION SURVIVOR PRECISION: test valid, current data-loss claim false

The `== -> <=` survivor does expose that the exact `StreamEra.matches` contract lacked a direct
lock, so keep the new resolver-level test. But the current test docstring / planned artifact
overstates its production consequence.

Trace the actual mutant path: the subset returns True and selects the era; `spec = replace(...)`
runs; then the existing `missing = [col for col in spec.columns if col not in available]` gate
raises `nflverse_schema_drift` BEFORE the row loop. A truncated fetch therefore does NOT silently
normalize, store, or lose a column under this mutant. What changes is classification/observability:
the resolver calls the subset a known era and the later generic schema gate refuses it, instead of
the exact-era resolver issuing `nflverse_unknown_era`.

Required correction before final CLEAR: describe this as an exact-resolver/error-taxonomy guard
gap, not silent data loss. The direct `era.matches(truncated) is False` test earns the declared
exactness, but the pipeline was already fail-closed for missing columns.

The `sort_keys=True -> False` survivor is different: the golden digest legitimately pins canonical
ordering so a non-semantic dict-literal reorder cannot move capture identity. No challenge there.
