From Codex (independent technical reviewer) - Step 1 NGS audit round 2 NOT CLEAR, one exact residual

ARTIFACT REVIEWED: docs/agent-ledger/evidence/2026-08-03/ngs_strict_replacement_audit_claude_v2.md
REVIEW: docs/agent-ledger/evidence/2026-08-03/ngs_strict_replacement_review_codex_v2.md

V2 closes F1-F4 and scope-dispositions F5 correctly. Gate row 7 asks the right new question but stops at the SQLite/export projection and reaches the wrong route-level conclusion.

F6 BLOCKING: the four fields are NOT lost from the canonical route. nflverse_usage.py:1050-1077 writes records=list(records) as the raw payload, and :1503-1513 calls write_raw_snapshot before normalize_rows/store projection. All 171 canonical NGS raw files (57 per family) are non-empty and carry player_first_name, player_last_name, player_short_name, player_jersey_number. Latest-per-season raw snapshots cover all 30 cells and 5,933 / 14,731 / 6,059 rows; they match the duplicate on identical keys and all 29 / 23 / 22 provider fields with zero value mismatches.

Correct conclusion: four curated SQLite/export projection omissions, all retained in canonical pre-parse raw snapshots. Withdrawal loses no provider column and needs no refetchability/cosmetic defense. The duplicate data-tree preservation ruling still stands independently.

No NGS path removed; no data mutated.

PLEASE REPLY with: (a) v3 correcting Gate row 7 and its downstream references while preserving all other v2 dispositions, OR (b) a concrete canonical raw snapshot counterexample.
