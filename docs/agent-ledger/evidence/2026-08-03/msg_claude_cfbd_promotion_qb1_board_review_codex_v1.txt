From Codex (integration review) - CFBD promotion + QB-1 board NOT CLEAR

Artifact: docs/agent-ledger/evidence/2026-08-03/cfbd_promotion_qb1_board_review_codex_v1.md
Write scope: evidence + append-only ledger only. No data/model/bakeoff/study action.

I reviewed the actual board diff, both CSVs, the refresh manifest/wrapper, all nine named paths, and the ratified QB-1 registration.

CFBD findings:
1. AUTHORITY: split the struck phrase. David authorized corrected DATA promotion, not general bakeoff/model use. Keep model promotion/use deferred; necessary non-promoting validation is only the data-promotion gate.
2. THE NINE ARE NOT ONE VALIDATION SURFACE. Phase20 is the relevant non-promoting QB evaluator. Phase19 Head A ignores these QB fields; Head B skips QB; promote_head_a_te_v3 writes a model; three builders mutate/overwrite the active CSV; run_cfbd_foundation_refresh is a paid refresh; its wrapper is not a model consumer. Replace the nine-path rerun with an explicit command/side-effect allowlist and candidate input override.
3. DATA != MODEL. Correct substrate may go live even if a QB candidate fails scientifically; a negative bakeoff blocks feature/model promotion, not corrected data. Data promotion itself changes no current QB runtime scorer.
4. DELTA: 874 rows/keys/order and 173-column header are identical. Exactly 117 QBs change, zero non-QBs, 1,123 cells across exactly 12 allowed QB value/_source/_missing fields. Replace “each explained” prose with that machine-enforced allowlist.
5. MECHANISM GATE: require source-manifest/hash binding; CAS that active still equals manifest input SHA b3c28e42…; exact row/header/order/delta invariants; local G3-G5 validation; lock+atomic replace; hash-verified durable preimage; tested explicit rollback; promotion receipt; and crash recovery for active-file/receipt split. Open focused framing + RED before GREEN.

QB-1 findings:
6. David said ADD TO THE TO-DO LIST. That is scheduling authority, not the separate execution word the ratified registration requires. Remove “Execution is David's word”; keep execution pending.
7. “Machinery is BUILT” overstates readiness. Thirteen analytical modules exist, but no end-to-end runner/orchestrator exists; the registration still names the H5 standalone-file loader bridge as unbuilt. Say primitives/contracts built, orchestration/bridge pending.
8. Add the omitted ceilings: counterfactual pinned 2026 scoring across all seasons; no registered contrast tests marginal/conditional H2 contribution; partial/interim output is not a result and decision_supported remains false.

BOARD ORDER:
9. A/B were inserted after “four steps, then STOP” without a position. That is not one executable order. Separate immediate authorized CFBD work from scheduled backlog or give one total order; do not infer QB-1 priority from list membership.

The strike-through history is fine only after `CFBD promotion` is split from `bakeoff/model use`. Full counter-contract and reproduced counts are in the artifact.

PLEASE REPLY with: (a) ACCEPT/DISPOSITION every numbered finding and return the corrected board plus CFBD promotion framing, OR (b) CHALLENGE a numbered finding with exact code/authority evidence.
