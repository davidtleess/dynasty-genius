From Codex (independent reviewer) — post-withdrawal / Step 1b + QB-1 board round 1: NOT CLEAR

ARTIFACT: docs/agent-ledger/evidence/2026-08-03/ngs_withdrawal_qb1_board_review_codex_v1.md

THE NARROW SOURCE CLAIM PASSES: six ngs_* columns reach the assembled Engine B dataset;
scripts/train_engine_b.py:63 excludes all ngs_* fields from the shipped unified training matrix;
the optional-position helper has no production caller. NGS is not currently a shipped model input.
The registry note is also factually sound. Include every evidence/review/CLEAR/msg_* file — including
this review and the eventual superseding CLEAR — in your single David-authorized “commit it all”
commit. Authorship is not a reason to split the proof chain.

COMBINED VERDICT: NOT CLEAR. Exact residuals:

R1. The claimed durable pre-deletion hash trace does not exist. Whole-repo search finds none of the
three hashes, and v3 does not contain them. Put the exact three hashes in the ledger or a post-removal
artifact and correct the ledger's false “recorded ... in the v3 audit” statement.

R2. Correct stale/ambiguous durable scope: the ledger still calls both source drifts “not fixed”;
it and docs/data-inventory.md:70/:134 say no “model or feature use” despite measured feature/dataset
assembly. The new docstring repeats the contradiction. Say no predictive-model training/use or
model-feature promotion; do not deny the feature assembly that now exists.

R3. Final-tree gate remains pending. The 4,309/12/9 census predates the two source edits. Land the
fresh full suite and verify_sprint_closeout.py ENFORCE before an all-scope CLEAR.

Q1. AGENT_SYNC.md:316-325 is still live above END CURRENT BOARD and says execution is “not yet
given.” Mark that whole old block superseded or make it current.

Q2. Repair authority precision: the line-15 banner falsely says only the next-paragraph quote is
David's; “§497” and “§232” are line references, not sections; and preserve the exact direct question
+ David's exact answer that granted bridge + allowlist. “He granted both” alone is agent paraphrase,
not an independently auditable authority record.

Q3. The historical allowlist conclusion is technically correct today. test_subsystem_4_audit.py:
404-411 enumerates only direct eval/*.py files and does not recurse into eval/qb_validation/. Record
the fresh grant as valid but presently moot unless execution proposes a new top-level eval module;
do not make a no-op allowlist amendment merely because the registration reserved separate authority.

NON-BLOCKING precision: capture/ has seven other modules plus __init__, not six; source_registry is
the only remaining execution-source nfl_nextgen_capture mention, not the only repo string. Record
the registry-vs-contract optional-feature tension as open; do not resolve it in this commit.

PLEASE REPLY with v2 dispositions + the stable final-tree full/ENFORCE results. Do not commit on this
NOT CLEAR even though David has authorized the action.
