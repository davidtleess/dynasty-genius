# NGS withdrawal / Step 1b + QB-1 board — Codex CLEAR

**Date:** 2026-08-03
**Reviewer:** Codex, independent reviewing lane
**Verdict:** **CLEAR**

## Cleared scope

- Removal of the three authorized, formerly untracked duplicate NGS code/test paths.
- Step 1b truth repair in `docs/data-inventory.md`.
- The two David-authorized source-note corrections in
  `src/dynasty_genius/sources/source_registry.py` and
  `src/dynasty_genius/nflverse_usage.py`.
- The current-board records for NGS withdrawal, QB-1 execution, the H5 loader bridge, the presently
  moot `eval/` allowlist authority, and the narrowly scoped commit word.
- The 2026-08-03 ledger and complete evidence chain from both lanes.

## Evidence accepted

- The three pre-deletion SHA-256 values are now durable and explicitly correct the earlier false v3
  attribution.
- The retained eight-file duplicate data tree remains untouched; deletion still requires a separate
  David retention ruling.
- Strict replacement is already independently cleared: unique canonical registry route, exact
  30-cell/key/shared-payload reconciliation, canonical identity/provenance, all provider columns
  retained in pre-parse raw snapshots, live canonical readers, and no production caller of the
  withdrawn route.
- NGS reaches feature refresh and Engine B dataset assembly, but is not a shipped model input:
  `train_engine_b.py` excludes `ngs_*` by name and the optional-position helper has zero production
  callers. Durable text now distinguishes dataset assembly from predictive-model use/promotion.
- The registry-vs-contract optional-feature tension is recorded open and was not silently resolved.
- QB-1 execution and the H5 bridge are authorized; the `eval/` allowlist grant is valid but presently
  moot because the exact-set audit does not recurse into `eval/qb_validation/`.
- No D3/D4 end-to-end QB-1 runner exists; the study has not run and has no result. QB rushing remains
  **UNDER TEST**. All target, inference, marginal-claim, `decision_supported=False`, and separate
  result-ruling ceilings remain intact.
- The board's authority inventory is exact: five distinct David words inside the handoff/execution
  block and six across all live content when the separate standing wall is included.
- Commit is authorized only for the 2026-08-03 Layer-1 change set and evidence chain under David's
  “fix those two and commit it all” word. A future QB-1 study commit is separate. Push, merge, and
  ruling on any registered result remain ungranted.

## Final gates

- Full unfiltered pytest: **4,309 passed / 12 skipped / 9 xfailed / 0 failed**.
- Census reconciliation: **4,309 + 12 + 9 = 4,330 collected**, zero collection errors.
- `verify_sprint_closeout.py`: **ENFORCE verdict PASS**.
- `ruff check src app`: **All checks passed**.
- `git diff --check`: clean.
- Post-gate changes were confined to `AGENT_SYNC.md` prose and received literal review; no executable
  gate rerun was required.

## Verdict boundary

**CLEAR for the authorized commit.** This is not push, merge, data-tree deletion, QB-1 execution
output, football/model promotion, or result-ruling authority. Route the resulting commit for
post-commit divergence verification before any further action.
