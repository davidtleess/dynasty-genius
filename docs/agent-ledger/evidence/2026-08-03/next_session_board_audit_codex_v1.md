# Codex audit — AGENT_SYNC next-session board

**Recorded:** 2026-08-03 11:12 EDT

**Artifact reviewed:** uncommitted `AGENT_SYNC.md` 74-line board insertion

**Verdict:** NOT CLEAR — four precise corrections; the substantive order and gates otherwise match
the accepted plan.

## 1. David's authority is understated, not overstated

David's exact words are action-shaped: `withdraw the duplicate NGS route - write the board into
AGENT_SYNC`. Read together with his immediately preceding `we'll do the NGS paths next session`,
this authorizes **next-session execution** of the withdrawal after the strict-replacement gate. It
does not authorize removal during the current closeout, and it does not authorize deletion of the
gitignored duplicate data tree. It also does not require a third, later “remove them” word.

The board should state that temporal/target boundary explicitly so next session does not either
execute early or stall waiting for authority David already supplied.

## 2. The heading misattributes agent-authored content

`NEXT-SESSION BOARD — LAYER 1 COMPLETION (David-worded, 2026-08-03)` describes the entire 74-line
board as David-worded. Only the quoted ruling is David's words. The four-step order, gates,
consumer-disposition vocabulary, measurements, and deferrals are agent-authored and lane-converged.
Rename the heading to distinguish the David ruling from the agent-authored plan.

## 3. “Layer 1 completion” overstates the finish line

The board itself records six zero-caller loaders, schedules only contracts and depth charts, defers
four others, and ends with a remeasurement/STOP. That is Layer 1 **continuation**, not completion.
The heading should not imply that completing this one board closes the entire layer.

## 4. CFBD active-input path is factually wrong

The board says `active Engine A input remains the 810-file May cache`. Engine A scripts read
`app/data/training/prospects_with_outcomes_v3.csv`; the active CSV still reflects features derived
from the defective May cache, but the cache is not the direct Engine A input. Replace the claim
with that exact boundary and retain that the fresh isolated curated path is unpromoted/unread.

## Checks that are clear

- David's withdrawal ruling and supersession are quoted accurately.
- The NGS gate, counts, caller paths, and preservation of duplicate data match disk/code evidence.
- Inventory repair, contracts/depth ordering, consumer-disposition vocabulary, hypothesis boundary,
  measured open state, and deferrals match the accepted plan.
- The board is placed below the standing Studio wall and above TW30N.
- The diff is append-only: 74 insertions, zero deletions.
