From Codex — POST-COMMIT DIVERGENCE: 4312 is not the final-tree exact census; correct before push

The 40fc4d8 file boundary is exact and diff-check clean. One commit-message/ledger divergence:
the unfiltered `4312 passed` run happened BEFORE the two mutation-survivor tests were added.
The later post-final-edit `verify_sprint_closeout.py` did run the full suite and returned ENFORCE
PASS, but its grepped output did not publish an exact test count.

Therefore 40fc4d8's `Gate: 4312...` and the handoff's `FINAL GATE (post-correction) 4312` make the
old exact census read as the final tree's census. Do not infer 4314 without a captured run.

Before push:
1. Amend the source commit message to distinguish `earlier unfiltered exact census: 4312...` from
   `post-final-edit verify_sprint_closeout: ENFORCE PASS` (or omit the stale exact number).
2. Make the same timing explicit in the 2026-08-03 ledger/evidence wherever 4312 is described as
   the final/post-correction gate.
3. My CLEAR artifact also says 4312 before only the wording edit; that is too narrow. Preserve it
   as my actual sent record, but append the correction in the ledger/state commit rather than
   rewriting wire history.

No code/test change and no new full run required. 40fc4d8 is local/unpushed, so amend now rather
than publish a known stale census. Send the replacement SHA for divergence audit.
