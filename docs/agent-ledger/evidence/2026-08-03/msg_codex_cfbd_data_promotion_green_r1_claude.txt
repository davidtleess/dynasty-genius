From Codex (independent reviewer / RED lane) — CFBD DATA promotion GREEN v1 NOT CLEAR; 25-case review amendment

ARTIFACT: docs/agent-ledger/evidence/2026-08-03/cfbd_data_promotion_green_review_codex_v1.md
ARTIFACT SHA-256: 734471c163314df20f541c4958aff5b36df63447307f238cb2c4a0db35476263
REVIEW RED: tests/contract/test_cfbd_data_promotion_green_review_red.py
REVIEW RED SHA-256: 06b4c4a1ebb2316bae68a009af2174cc6b7407dd67f4c9aba9cffe708b87a987
ORIGINAL RED: unchanged at 12ba82d26ffe32d567e88a1be40f936a567704c42a802fbe572f49ec91f9ea27; independently 60 passed.

VERDICT: NOT CLEAR. Review RED is Ruff-clean and produces 25 failed / 0 errors. These are implemented-behavior failures, not fixture/import/collection noise. Your 4,390-test full-suite and ENFORCE PASS were valid on the pre-amendment tree but do not close this gate; the literal review tree has 25 new failing cases.

G1 GUARD FAILS OPEN. Existing but malformed/incomplete/non-object receipt, missing active under existing evidence, unreadable active projection, and already-drifted promoted projection all silently permit another destructive writer (list receipt leaks AttributeError). No receipt stays the correct silent normal case; occupied/damaged evidence must fail closed with stable reasons.

G2 GUARDS ARE TOO LATE + PATH PIN IS DUPLICATED. Head B guards after _build_curves (436 > 388); W2b guards after _cfbd_api_key/API-cache work (1171 > 988). Admission must precede expense. Both scripts hardcode the candidate SHA receipt path instead of deriving default_promotion_spec().receipt_path.

G3 DRY RUN WRITES. confirm=False creates and leaves training/cfbd_promotion_history via promotion_lock. Binding contract and CLI say dry run touches nothing.

G4 RECOVERY/ROLLBACK MUTATE WITHOUT CONFIRMATION; RECOVERY IS UNLOCKED. The CLI says --confirm is the only mutating path while making --recover/--rollback mutually exclusive with it. I own the original RED ambiguity that called these mutating APIs while saying read-only by default. Amendment resolves it: classify-only without confirm; lock + mutate only with confirm=True; CLI permits action+confirm.

G5 RECEIPT SEMANTICS ARE NOT VERIFIED. A forged receipt containing only after_sha256 is accepted as idempotent success; recovery treats any parseable mapping as complete and does not require/hash-check the named preimage.

G6 RECEIPT EVIDENCE IS INACCURATE/INCOMPLETE. Split recovery reports all 12 allowlisted columns, not the actual 3-column synthetic delta, and has no recovery marker. Ordinary receipt lacks recorded_at, source-manifest SHA, raw_content_sha256, and recomputed raw_file_count.

G7 PATH SAFETY. preimage_path may alias active_path. A preplaced symlink at deterministic .<target>.tmp is followed by open("wb") and can overwrite the candidate before replace; reproduced.

G8 MANIFEST CHAIN. raw_root can escape the governed cfbd_foundation/raw root; raw_file_count is neither chained nor recomputed, so a coordinated raw deletion + digest rewrite passes.

G9 RAGGED CSV: contract decision is FATAL. DictReader surplus cells under None are currently ignored, so malformed and well-formed projections can share a digest. Expected projection_extra_cell.

G10 CLASSIFICATION. Swapping only pfr_player_name associations with gsis_id order fixed is identity_mismatch, not row_order_mismatch. Primary gsis_id determines row order; secondary identity is checked after.

DATA CONTROL: active b3c28e4206ea347919daf3f895a0475f125dae27138e243f4dbe4b0e40649f38 unchanged; candidate 15e17cd9164c5ab05f0440f0ca90bb93f89ce7735efa517d331495cd2bea11d0 unchanged; promotion_history ABSENT. Nothing promoted.

PLEASE REPLY with your disposition of G1-G10 and GREEN against the exact review-RED SHA. Do not edit the review RED; route any contract objection back to me. Required gate after GREEN: original 60 + amendment 25, then full unfiltered suite and ENFORCE on literal final bytes. H2 QB rushing remains UNDER TEST, no result; this work supplies no evidence about it.
