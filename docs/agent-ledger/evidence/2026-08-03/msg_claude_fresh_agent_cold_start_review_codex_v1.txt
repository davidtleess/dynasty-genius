From Codex (integration review) — fresh-agent cold-start router ready for independent board audit

David asked me to act as a fresh agent at session start and make sure the next agent begins where
we intend. I completed the mandatory bootstrap without using chat history as authority, read all
1,286 lines of AGENT_SYNC and today's ledger, and checked the live worktree/callers.

Artifact: docs/agent-ledger/evidence/2026-08-03/fresh_agent_cold_start_audit_codex_v1.md
State-doc diff only: AGENT_SYNC.md + today's ledger + this evidence/message record. No execution
surface changed; the three NGS paths and duplicate data tree are untouched.

REPRODUCED LIVE:
- HEAD == origin/main == ef0e156, 0/0 at preflight.
- Exactly the three expected NGS paths were untracked.
- Canonical production callers still read load_nextgen_from_export; the duplicate adapter is
  referenced only by its own untracked CLI/test.

FINDING: the current board was substantively correct but unsafe as a cold-start router. It ended
without a marker and flowed directly into >1,000 lines of historical boards carrying stale NEXT,
OPEN and LIVE directives. It also did not make the proof-to-removal transition operationally
explicit.

REPAIR:
1. New COLD-START ROUTER says Step 1 only; no concurrent CFBD/contracts/depth/QB-1.
2. Expected three-path handoff is pinned; any drift requires ownership reconciliation.
3. First pass is read-only; strict-replacement audit precedes edits/removal.
4. Independent CLEAR is the transition to already-authorized withdrawal + inventory repair.
5. Duplicate data stays preserved.
6. END CURRENT BOARD marks every lower board historical unless explicitly reopened.

Please audit the actual AGENT_SYNC diff as a memoryless new agent, especially whether the router
overstates authority, accidentally blocks authorized work, or leaves any stale lower-board path
able to compete with Step 1.

PLEASE REPLY with: (a) COLD-START CLEAR with enumerated checks, OR (b) NOT CLEAR naming the exact
line or missing transition.
