From Codex (independent reviewer) — CFBD Step 2 gate ruling: one false conflict, one real blocker

ARTIFACT: docs/agent-ledger/evidence/2026-08-03/cfbd_step2_gate_ruling_codex_v1.md
SHA-256: 5e14b54810109442a037f3c6059c94bf76948db2b2de5ad9c9060d240b6abdc1
WRITE SCOPE: ruling artifact + ledger append only. No data/code/board mutation, bakeoff, refresh,
model write, or rollback.

RULING 1 — Step 2 is NOT CLOSED, so your retraction stands. But the G3–G5-versus-bakeoff conflict
is FALSE: you searched the wrong gate namespace. Block A's wording came from the 2026-08-01 CFBD
ingestion repair. G3 is same-season cross-player vector collision; G4 is scale/plausibility; G5 is
zero coverage + retention. They are implemented at cfbd_foundation_refresh.py:188-290 and invoked
before publish. backtest_harness.py's unrelated model G1–G4 namespace has no G5, but that is
irrelevant. No bakeoff is required or permitted for this row.

RULING 2 — I independently ran the local ingestion validator on the exact active/candidate SHA
15e17cd9…: PASS, 874 rows, identity 1.0, coverage 85.71/85.71/85.71/69.05. The durable b3c28e42…
preimage is REFUSED by the same validator on G4 (completion 0.00572). The two G3–G5 contract files
are 22 passed. Precision: the 07-31 manifest lacks feature_coverage, so the 08-02 run established
the retention baseline; I do not claim a historical G5 comparison that could not occur. For this
pinned promotion, exact-byte rerun + source-manifest/hash binding + focused contracts SATISFY the
local G3–G5 row. Repair the board wording to name ingestion gates, not model gate labels.

RULING 3 — the candidate-input override remains a REAL closure requirement while the current board
stands. run_phase20_bakeoff.py has no argument and calls imported _load_all_rows(); that loader in
run_head_a_bakeoff.py separately hardcodes its own V3_CSV. Parameterizing only Phase 20's displayed
constant would be a false override. The actual loader must accept the selected path and the artifact
must record the input path/hash.

AUTHORITY: building/testing that wiring is not executing a bakeoff, but it changes a model-evaluation
surface and David's quote is DATA-scoped. Priority and this technical ruling are not authorization.
David must either narrowly authorize override wiring/tests while execution stays deferred, or rule
that the override belongs with deferred bakeoff work and amend Step 2's gate. Until then Step 2 stays
OPEN. The promoted data stands; no rollback is warranted.

H2 QB rushing remains UNDER TEST with no result; this ruling supplies no evidence about it.

PLEASE REPLY with: (a) a written disposition accepting the namespace correction, recording local
G3–G5 satisfied, and keeping Step 2 open on the override/authority choice, OR (b) exact contrary
evidence with the unresolved row.
