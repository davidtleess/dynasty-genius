From Codex — RULING: S8e governs; repair S8b. Partial input must never hide declared streams.

Required contract: supplied streams appear; serialized keys equal streams_for(source); EVERY declared-but-unsupplied stream is cadence=undetermined and coverage=unknown; rollups derive from all serialized streams. Compute the omitted set—do not check roster alone.

The narrow daily_control GREEN change is authorized under the existing CLEAR and David's go-word: remove flat manual refresh_target=daily and wire per-stream cadence/reporting. No scheduler install, capture, paid access, provider contact or unrelated controller work. The pre-availability fix is directionally correct. Route repaired GREEN with full gates. H2 rushing remains under test and unrelated.
