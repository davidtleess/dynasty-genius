From Claude (write lane) — ⭐⭐ R22 rerun COMPLETED: run_status=ok — the FIRST completed registered QB-1 execution; readout UNREAD, routed untouched to David for his ruling [w#qb1-exec-1]

# QB-1 Round-22 CLEAR-authorized registered rerun — terminal report (Claude v1)

Date: 2026-08-16 (America/New_York)
Run: `f8f7551c-a145-46e2-b9b4-dec427f313ba`

## 1. Exact one-run accounting

- Fired ONCE on the machinery resume wake (WIRE-07081ab5) after durable
  verification (revision 135, Round-22 CLEAR `2ffffdd3…`, fired pins =
  reviewed pins, zero processes, prior artifact/receipt pins verified).
- **Exactly one runner process:** PID 87628 (wrapper 87626), 23:04:29 →
  23:40:27 ET (~36 minutes). Zero runner processes remain. Your CLEAR wire
  and one duplicate machinery wake were matched to this same process
  (`[w#9r350m9b-1]` matched). No second run. No repair. No input mutation.

## 2. Terminal state — COMPLETED

- Runner exit code **0**; stdout receipt (4-key summary):
  `run_status` = **ok** · `failure_reason` = null ·
  `decision_supported` = **false**.
- Stdout receipt: `qb1_registered_rerun_r22_stdout_claude_v1.txt` SHA-256
  **`61ae2059f48c3d895e4d2c83cf4ede79ba705d7afd3856aafdb77f43ef80a643`**
  (207 bytes).
- **Registered terminal artifact:**
  `app/data/backtest/qb_validation/qb_validation_report.json`
  SHA-256 **`9a63234b06860525736315a8c94c11c817fc6e57f538e7ff23d336e3937bf968`**
  — **271,330 bytes** (the published registered report, vs the 296-byte
  failure envelopes of every prior run), written atomically 23:40:27 ET.

## 3. The untouched-readout law, honored

**I have not opened, read, parsed, summarized, or interpreted the registered
report.** The only fields I have seen are the CLI's 4-key stdout summary
above. No comparison value, status, fold row, case panel, sensitivity panel,
or disclosure has been read by this lane. The readout goes to David EXACTLY
as the runner published it, for HIS ruling under the frozen registration's
inference contract and status vocabulary.

## 4. Standing

- The five formerly-refusing walls (label placeholders → pbp parse →
  season_summary aggregates → F34 draft capital → exclusion-row vocabulary)
  are all closed under review; this execution passed the full publication
  gate (`decision_supported=false` per the No-Verdict law).
- **H2 QB rushing remains UNDER TEST** — a completed execution is NOT a
  result ruling. Registration Addendum A: only execution PLUS David's ruling
  on the registered readout lifts the status, per the registered contrasts'
  own vocabulary and ceilings (regular-season PPG in the veteran cohort —
  never "dynasty value").
- Working tree: all Round 13–22 code remains uncommitted; no commit, no
  push, no publication beyond the atomic artifact the runner itself wrote.

## 5. Requested next steps

1. Codex: record the passed real-surface receipt and the durable close per
   the sanctioned machinery.
2. David reads the registered readout at the artifact path above (or asks
   either lane to render it VERBATIM — no summarization by me until he has
   ruled, and any rendering he requests will be the exact registered
   content).

PLEASE REPLY with: (a) passed-receipt recorded + the durable close state, OR (b) a named correction to the accounting above.
