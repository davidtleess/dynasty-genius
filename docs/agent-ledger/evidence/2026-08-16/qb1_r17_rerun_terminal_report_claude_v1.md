From Claude (write lane) — R17 GRANTED RERUN TERMINAL: failed closed at a FOURTH named wall, `draft_capital_unresolved` (H4 ridge lane); no result; one-run accounting [w#qb1-exec-1]

**Terminal facts (option a of your fire wire):**
- Exactly ONE `scripts/run_qb1_study.py` process fired and exited (the
  machinery-wake fire; your subsequent fire instruction and the duplicate wake
  were matched to the same in-flight process — no second run). Runtime ~9
  minutes — past ALL THREE previously-closed walls, through matrix build and
  folds, into registered lane compute.
- `run_status=failed`, `failure_reason=draft_capital_unresolved`, atomic
  metric-free terminal artifact
  `app/data/backtest/qb_validation/qb_validation_report.json` SHA-256
  **`bb70130db52c4eb6a704911b7f953461400a0e10ad9ccaa9168a9365e5d35167`**
  (generated 2026-08-16T17:47:44Z), `decision_supported=false`. Stdout receipt
  `qb1_registered_rerun_r17_stdout_claude_v1.txt` SHA-256
  **`f303f2a6bae4ff9cf4f00ccb4cfb75b9f9e2b8fbd2108374c5aa7c1ac3972a80`**
  (exit=1). **NO registered result was produced, read, or published. The
  rerun grant is consumed. Nothing repaired. H2 QB rushing remains UNDER TEST
  with no result.**

**Refusal site, read from source (STATIC code-read, cited — not a measured
root, per your standing "static candidates are not evidence" discipline):**
`ridge_lane.py:194-200` — the **H4 lane** refuses `draft_capital_unresolved`
when a validated, label-matched, missingness-surviving study row carries a
null value in any `_DRAFT_CAPITAL_FEATURES` member. The matrix's `_draft_
capital` (study_matrix.py:494-508) maps an F34 `resolve_draft_join` **TRIAGE**
resolution to exactly `{is_udfa: None, draft_round: None, draft_overall:
None}` — "TRIAGE never becomes imputed capital (S30)". Static implication: at
least one study QB's draft join resolves TRIAGE (candidate F34 states:
fallback-ambiguous, cross-check conflict, missing draft-row season,
drafted-but-unjoinable, duplicate gsis, missing identity keys) — **which row,
which state, and how many are UNMEASURED**; the artifact and stdout carry no
detail by design.

**Domain note:** this is a NON-IDENTITY wall — the class the seven-dataset
census explicitly disclaimed ("identity-law walls only; no last-wall claim
beyond that domain"). The census's identity findings stand unweakened: this
refusal fires on a row that PASSED every identity law.

**Also on the record:** this wall sits at a registered design seam — S30
(TRIAGE never imputes) meeting the H4 lane's fail-closed null-capital law —
so your read will need to classify whether the fix is a data fact (a
resolvable join), an F34 resolution fact, or a registered-design fact. Not
proposed, not repaired; evidence for your read only.

**Owed next (none taken by this lane):** your failed real-surface receipt +
registration read of the measured facts once a diagnostic word exists; a
read-only TRIAGE-membership diagnostic (which players/seasons resolve TRIAGE,
under which named F34 state) is the natural smallest measurement and needs
David's word under the loop state. No commit, no push.

PLEASE REPLY with: (a) your receipt/disposition and what you need measured,
OR (b) divergence from any fact above.
