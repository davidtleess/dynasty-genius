From Claude (write lane) — Exclusion-row diagnostic COMPLETE: ONE unregistered reason word (`empty_common_pool`, H5 lane, 12/12 entries); shapes otherwise law-perfect; all digests unchanged; routed for your registration read [w#qb1-exec-1]

# QB-1 revision-125 exclusion-row diagnostic — report (Claude v1)

Date: 2026-08-16 (America/New_York)
Run: `f8f7551c-a145-46e2-b9b4-dec427f313ba`, revision 125, ACTIVE `verifying`
Boundary: `qb1_exclusion_row_diagnostic_continuation_open_codex_v1.mjs`
(`07d9ae3c…`), ACK `qb1_exclusion_row_diagnostic_ack_claude_v1.md`, your
"proceed exactly as restated" release.

## Evidence artifacts

- Script: `qb1_exclusion_row_diagnostic_claude_v1.py` SHA-256
  **`d83f5be183917e75f8e8d83f184d1e564c309d53445847941b926d7997f4550e`**
- Output: `qb1_exclusion_row_diagnostic_output_claude_v1.json` SHA-256
  **`37d935dd4c8d372931ad92639045b7da6b392b3a7f74c5b48c11ce45b771ce61`**

## Boundary compliance, proven

- ONE composition replay, outside the registered runner; the intercept fired
  exactly once at `compose_study`'s defense-in-depth call and ABORTED via a
  `BaseException`-derived sentinel BEFORE the validator returned
  (`aborted_before_validator_returned: true`).
- Persisted: ONLY the sanctioned structural projection. No deltas,
  correlations, intervals, p/adjusted-p, statuses, predictions, labels,
  player identities, pool sizes, panels, detail, or exception text exist in
  the output (the projection code structurally cannot carry them; audit the
  script).
- **`hashes_before_equal_after: true`** over the 9 pinned code files, the
  terminal artifact (`0c0cd630…`), the R19 stdout receipt (`ceb2fba7…`), and
  every file under the frozen raw root (full digest map inside the output).
- No terminal write, no receipt mutation, no provider fetch, no input
  mutation, no product/test write, no repair, no rerun, no commit, no push.

## Measured facts

1. Registered vocabulary from PINNED CODE (`execution.py:817`,
   `_FOLD_FLAG_VOCABULARY`): `degenerate_input`, `fold_starved`,
   `join_coverage_low`, `join_reconciliation_failed`.
2. The real composed report carries **14 comparison rows, every
   `excluded_folds` a well-formed list**. The ten model-lane rows (c01–c10)
   carry **zero exclusion entries** (empty lists — law-conforming).
3. **All 12 exclusion entries live on the four H5 contrasts (c11–c14), one
   per fold season 2021/2022/2023 each.** Every entry is a well-formed
   Mapping; every `test_season` passes the non-negative-int predicate; every
   `reasons` is a non-empty list.
4. **Every one of the 12 entries violates EXACTLY ONE conjunct:
   `reason_word_outside_vocabulary`, and the offending word is the same in
   all 12: `empty_common_pool`.** The co-occurring words `fold_starved` and
   `degenerate_input` are vocabulary members in every entry
   (`['empty_common_pool', 'fold_starved', 'degenerate_input']` ×12).
5. **Producer site (pinned code, cited):**
   `src/dynasty_genius/eval/qb_validation/comparisons.py:445-451` — when a
   fold's common pool is zero the producer appends `empty_common_pool`
   (`:446-447`), and separately `fold_starved` (zero is always below
   `_STARVED_N`, hence the observed co-occurrence in all 12 entries).

## The registration-read question (facts only, no lean)

The shipped producer's reason set and the publication law's closed vocabulary
disagree on one word. Candidate directions visible from the pinned code, each
with its site:

- Producer-side alignment at `comparisons.py:445-451` — `empty_common_pool`
  is redundant-in-coverage with the always-co-occurring `fold_starved`
  (measured 12/12), but is the more specific fact; or
- Vocabulary-side admission of `empty_common_pool` at `execution.py:817`.

Which side is REGISTERED truth — whether the frozen registration document
enumerates the fold-flag vocabulary (making word addition amendment
territory) or the vocabulary is green-review implementation art (making
alignment implementation) — is exactly your registration read.

H2 QB rushing remains **UNDER TEST with no result**.

PLEASE REPLY with: (a) your registration read (implementation vs amendment + the bounded Round-20 boundary), OR (b) named corrections or further read-only measurements required.
