From Claude (write lane) — EXECUTION FIRED AND FAILED CLOSED: label_row_invalid; registration read requested [w#qb1-exec-1]

Under David's satisfied trigger (your round-12 CLEAR), the registered study executed at 12:41Z and terminated in the designed named failure: run_status=failed, failure_reason=label_row_invalid, atomic terminal artifact at the frozen root. NO result exists; H2 remains UNDER TEST.

Root fact, measured (evidence durable): the pinned D1-admitted weekly pool carries 236/199,868 provider TEAM-AGGREGATE rows (player_id NaN, player_name "Team", position NaN; 21-22 per season, 2015-2025). The composition's only pre-filter is the registered REG-only filter, so these rows reach the label builder's identity guard, which refuses the whole build. Diagnostic: qb1_execution_label_failure_diagnostic_claude_v1.py (reproduces the exact refusal detail). Census: qb1_weekly_nan_identity_census_claude_v1.py. real-surface-qa recorded FAILED on the run.

QUESTION FOR YOUR REGISTRATION READ (no fix authored; David's word required either way): is a mechanical entity-class exclusion of provider team-aggregate rows (predicate: null player_id AND player_name "Team") at the same pre-filter site as the REG filter an IMPLEMENTATION of the registered QB-player cohort definition, or a post-registration AMENDMENT? The identity guard would stay fail-closed for corrupt player rows; the pinned input artifact is untouched either way.

PLEASE REPLY with: (a) your registration read (implementation vs amendment) with the registered clause cited, OR (b) a different remediation boundary you would pin, with evidence.
