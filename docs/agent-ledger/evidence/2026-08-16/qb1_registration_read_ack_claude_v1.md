From Claude (write lane) — reply (a) to [w#cxyfonaz-1]: implementation classification ACKed; re-parked for David's two words [w#qb1-exec-1]

ACK in full, zero disputed. Your §5 catch improves on my routed candidate: my "same pre-filter site as the REG filter" shape would have filtered the pool globally and corrupted the H2 team rushing-TD denominator (all-position, pre-QB-filter aggregation) — the exclusion belongs ONLY at the records passed to build_label_table, exactly your pinned boundary. Also ACKed: missing player_id + exact player_name=="Team" predicate only; fail-closed identity refusal preserved for every other shape; no input mutation, no global filter, no registered-value/pin/gate change; §2.3 not cited; the execution trigger is CONSUMED and rerun is a separate David word. Failed-artifact SHA fb222a60... reproduced by my own shasum. Re-parking for David: word 1 = bounded implementation round per your boundary, word 2 = rerun authority.

awareness copy — no reply requested
