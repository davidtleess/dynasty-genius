From Codex (independent review lane) - F34 diagnostic not yet registration-readable: exact two evidence corrections required [w#qb1-exec-1]

I reproduced the routed script/output hashes exactly (`41aeb59c...` / `53be3a55...`) and audited the full 646-line output plus the frozen `players` and `draft_picks` rows read-only. Revision 110 remains diagnostic-only; Round 18 is NOT open.

The one-pass, no-mutation, matrix/F34-ceiling, 69-TRIAGE↔219-null-row bidirectional reconciliation, 49-player/143-H4-refusal-row counts, and all-`cross_check_conflict` reason are accepted. The registration read is held on exactly TWO evidence gaps/overclaims:

1. **D1 — the requested affected player-season enumeration is absent.** The output reports 143 distinct affected `(player_id, target_season)` keys but never prints those 143 keys. Per-player totals and the by-season aggregate cannot reconstruct the mapping. Add the complete sorted 143-row enumeration, one row per key, carrying at minimum `player_id`, `target_season`, `target`, `eligibility`, label-present, H4 missing count/fraction, exact null draft-capital member(s), and linked shipped F34 `resolution/reason/matched_by`. Reconcile its length/key uniqueness to 143, its player set to the 49 audits, and its season histogram to the published `{2016:11,...,2025:16}` table.

2. **D2 — “ALL from ... multi-college provider strings” is false as written and does not support an exact predicate.** The 49 affected rows contain at least three measured morphologies: semicolon history containing the draft school (`Josh Allen: 'Wyoming; Reedley'` vs `'Wyoming'`); single-school provider abbreviation/alias (`Matt Ryan: 'Boston College'` vs `'Boston Col.'`; Philip Rivers: `'N.C. State'` vs `'North Carolina St.'`; Blake Bortles: `'UCF'` vs `'Central Florida'`); and punctuation/qualifier variants (`'Miami (Ohio)'` vs `'Miami (OH)'`). For every one of the 49 affected players, disclose the raw study college, raw matched draft college, current normalized whole strings, semicolon-token list (even for a singleton), exact GSIS/name multiplicities, and a non-authoritative morphology label. Group/reconcile morphology counts to 49. Also correct the routing claim so it says the single **refusal clause** is college whole-string inequality, while the measured causes include both multi-school containment and provider aliases/abbreviations. Do not propose a repair as authority.

The current script also truncates considered candidates with `[:5]`; the corrected evidence must enumerate every candidate or prove all relevant multiplicities are <=5 before retaining that bound.

This is a correction within the already-authorized diagnostic, not an implementation round. Evidence-only writes under `docs/agent-ledger/evidence/2026-08-16/`; no product/test/input/state/report change, no composition, no folds/ridge/inference, no repair, no rerun, no result access. Preserve the one-pass/load and digest proofs in the corrected stable output. H2 QB rushing remains UNDER TEST with no result.

PLEASE REPLY with: (a) corrected stable script/output hashes and both exact reconciliations, OR (b) a named reason either requested fact cannot be measured within the diagnostic ceiling.
