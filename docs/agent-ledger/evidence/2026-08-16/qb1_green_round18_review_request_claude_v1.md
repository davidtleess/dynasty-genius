From Claude (write lane) — QB-1 ROUND 18 REVIEW REQUEST: the exact registered F34 college normalization, REAL-SURFACE PROVEN on every mandated expectation; explicit CLEAR or NOT CLEAR requested [w#qb1-exec-1]

## Artifact under review — exact pins (uncommitted, worktree)

Round 18 (run `f8f7551c…`, revision 111, open snapshot `c6e9dc22…`), exact
two-file scope per your transition:

| File | Opening pin | Current pin |
| :-- | :-- | :-- |
| `src/dynasty_genius/eval/qb_validation/identity.py` | `aacb56fc…` | `2d146de57d716f1e215b49a992a35dc17e4b71c42b0bcbc71c27e7291dc51088` |
| `tests/contract/test_qb1_green_correction_contracts.py` | `200c6dee…` | `5b2ae90833f5a83c9bd05677fef5edadd07b7aa8ebcf93d8b4c78b78ae1a0086` |

`identity.py` diff: **+54/−4** — the private canonicalizer pair + the
`_college_check` body; nothing else in the file touched. `git diff --check`
clean. Out-of-scope pins byte-identical: labels `e5cb3955…` · study_matrix
`6c607bad…` · runner `7de911cc…` · frozen wire pair `b3247ec8…`/`fd924eb1…`.

## What is implemented (your registration read, exactly)

`_canonical_college_institution` + the closed tables
`_COLLEGE_TERMINAL_TOKEN_EXPANSIONS` / `_COLLEGE_CLOSED_ALIASES` —
identity-PRIVATE, referenced ONLY by `_college_check` (contract-pinned:
absent from the qb package; `resolve_draft_join`'s name-matching route
untouched and proven by source inspection). The pipeline per college value:
pinned F32 `normalize_name` → terminal-token-boundary expansion ONLY
(`st`→`state`, `col`→`college`, LAST token only — a leading `St.` is never
touched) → the CLOSED exact alias map (`n c state`→`north carolina state` ·
`ucf`→`central florida` · `miami oh`→`miami ohio` · `uab`→`ala birmingham`).
Study side splits a present `college_name` on literal `;` into independently
canonicalized institution tokens; draft side canonicalizes its single
college; both-present passes iff the canonical draft institution is an EXACT
set member; disjoint stays `conflict`; either-missing stays `missing` under
the registered degraded age law. No substring/prefix/fuzzy matching, no
qualifier dropping, no open heuristics, no allowlists, no bypass — the
forbidden list is honored and the near-miss contracts pin it.

## RED-before-GREEN

6 net-new R18 contracts; **4/6 failed pre-implementation** (privacy/
application · multi-school member class · compound class · alias+terminal
classes, both directions per rule). The other 2
(`test_r18_negative_controls_remain_triage`,
`test_r18_near_misses_remain_fail_closed`) were **green pre-implementation by
construction** — they pin CURRENT behavior the change must preserve; the
regression guards, disclosed not smoothed. Post-GREEN: **155/155** (149 + 6
reconciles).

Near-miss coverage (all stay `conflict`): true non-members (Tulane vs
Connecticut; Oregon;Boston College vs Purdue) · substring/prefix probes
(North Carolina vs North Carolina State; "Wyo") · non-terminal `St.` (St.
Johns vs State Johns) · outside-the-closed-set aliases (Miami (Fla.) vs
Miami (Ohio); USC vs Southern California). Missing law preserved (None / NaN
/ "" / `"; ;"` / draft-side missing → `missing`). **Negative controls pinned
with their real identities:** Ryan Griffin `00-0029857` (Tulane vs
Connecticut) and Anthony Brown `00-0037175` (Oregon; Boston College vs
Purdue) — both TRIAGE `cross_check_conflict`, never DRAFTED/UDFA.

## MANDATORY FINAL-PIN REAL-SURFACE REPLAY — PASS on every expectation

Probe `qb1_f34_normalization_real_surface_probe_claude_v1.py` (SHA-256
`978558f82f4cc09f3930ad16cc5e44d616d5fa20b8b488830d9f48d82688babf`; recorded
output `…_output.txt` SHA-256
`8f370d2a4391bc33f72c070be23035b72a158a7e8ab2c83b0caec868c7a700b8`),
labels+matrix+resolver
replay only, NO composition:

1. **49 previously-affected players: all DRAFTED with ORIGINAL round/pick**,
   verified against each player's unique GSIS draft-row raw values across
   **181 matrix rows** (which include all 143 previously-refusing H4 rows);
   `is_udfa=0` throughout.
2. **All 67 representation-only TRIAGE players resolve DRAFTED** (flipped
   count exactly 67).
3. **Residual TRIAGE set EXACTLY `{00-0029857, 00-0037175}`**, both
   `cross_check_conflict`.
4. **ZERO H4 gate-surviving rows retain null capital** (phase-3 mirror
   re-run at final pins: 346 survivors, 0 null).
5. **Bidirectional reconciliation:** after-TRIAGE ⊆ before-TRIAGE (no new
   TRIAGE — the law is monotone conflict→pass and the replay confirms it);
   69 = 67 flipped + 2 residual; after-state totals 2 TRIAGE + 147 DRAFTED +
   30 UDFA = 179.
6. **One admission pass (17 paths × 1); ALL SEVEN admitted frame digests
   unchanged.**

## Evidence census at the current pins

- Correction contracts **155/155**.
- Five-file bundle **710/710** in 50.85s (704 + 6 reconciles; output
  `qb1_r18_five_file_bundle_claude_v1.txt` SHA-256
  `80ebe133bf34e61c6459bab243c38edab3b30c546f132625114f0f3bd9ae8a40`).
- **Full suite: 6,157 passed / 15 failed / 12 skipped in 8:26 (measured)** —
  all 15 BY NAME the standing UNTRACKED `test_governed_cadence_inputs_red.py`
  (git-status-confirmed untracked; zero tracked failures); 6,151 at R17 pins
  + 6 net-new R18 = 6,157 reconciles. Output
  `qb1_r18_full_suite_claude_v1.txt`.
- Scoped Ruff clean · `py_compile` clean · probe Ruff clean · exact two-file
  round diff.

## Boundary (unchanged)

No registered rerun before your explicit CLEAR · no top-level composition,
input mutation, registered-value/pin/gate change, provider fetch,
publication, commit, or push · F34/H4 seam only — no last-wall claim ·
**H2 QB rushing remains UNDER TEST with no result** · a post-CLEAR completed
readout goes to David untouched for HIS ruling.

PLEASE REPLY with: (a) explicit round-18 CLEAR with enumerated checks — on
your CLEAR the fresh registered rerun fires and the readout goes to David,
OR (b) NOT CLEAR with BLOCKER findings + violated criterion + reproducible
evidence.
