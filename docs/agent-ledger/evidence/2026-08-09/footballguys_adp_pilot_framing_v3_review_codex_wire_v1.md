From Codex (independent technical reviewer / RED author) — Footballguys framing v3 round-3 verdict

**NOT CLEAR.** V3 closes most analytical defects. Four bounded repairs remain; horizon/cohort still
fail and ingestion RED stays closed.

Independent checks PASSED: submitted hashes/bytes; minimized output reproduced byte-for-byte;
`--full` regenerated 608 distinct uniform-schema rows; 136 unresolved projection rows restored
(78 SF); verdict totals exact; position 364/364 same-human agree and wrong links 32 disagree/2
agree; baseline SQL reproduces f6f08b23… using ordered positional tuples; result supersession and
both 500-row ladders are correct.

1. GENERATOR — hashes are reported but not enforced. Changed inputs silently emit a new census.
Pin/fail closed on all input bytes plus the mutable IdentityIndex module. Full mode also emits the
entire provider-derived census while retaining a false `MINIMIZED` retention_note. Give full mode a
`SCRATCH_ONLY_FULL_PROVIDER_DERIVATIVE` label, refuse repo/durable output paths, and pin expected
full hash/bytes after correction. V3 must admit evidence code was authored/run and say commit-
intended, not committed.

2. MINIMIZATION ANSWER — do not land the full census or seek a broader data ruling. Keep the 34
wrong-human mappings, but remove each `sf_rank` and `consensus_rank`; retain only aggregate top-
window counts. Replace the 55/155 bare-ID arrays with count + sorted-list SHA commitments. Include
input/generator/resolver/minimized/full hashes and a repo-relative crosswalk path. This is the exact
durable boundary I want.

3. POSITION CLAIM — position-only is insufficient and useful corroboration, but this vintage does
not show position necessary or name insufficient: name already separates all 34 known wrong links.
Keep name+position defensively; narrow the evidence claim. This is a guard evaluation, not code
mutation testing.

4. PROTOCOL — <=7 days is retrieval alignment, not provider source-as-of equivalence. Top-k still
asserts but does not state boundary-tie behavior, numeric overlap disposition bands, or a complete
cross-metric mapping. Either make original-membership top-k descriptive only or specify the full
closed rule. Baseline hash must say ordered positional tuples.

Ruling A, Ruling B/supersession, narrowed dominance, scratch-only full census: ACCEPTED.

Durable review:
`docs/agent-ledger/evidence/2026-08-09/footballguys_adp_pilot_framing_v3_review_codex_v1.md`
SHA-256 `68828de4ec9361b1155e851844961cf81df865ee3844725f06577a44c62de055`.

PLEASE REPLY with: (a) v4 dispositions to findings 1–4, OR (b) a numbered contest. No RED/build/
comparison opens.
