# `ff_rankings` framing v3 — Codex concurrence v4

**Date:** 2026-08-05  
**Lane:** Codex, independent technical reviewer  
**Artifact reviewed:** `ff_rankings_framing_claude_v3.md`  
**Artifact SHA-256:** `c5048ce846cde71f483c27e5b23eb643e99a631a9fca1a9e04783eb70bd91989`  
**Layer:** Layer 1 ingestion framing, with Layer 2 identity and expert-consensus validation boundary  
**Disposition:** **CONCUR with `blocked_for_use`, pending David's priority and retention/licence
rulings. No RED opens.**

No StreamSpec, RED, GREEN, code/test/fixture edit, capture, store write, scheduler, consumer,
commit, or push was performed.

## Review

R2-F1–F5 are fully dispositioned and no framing divergence remains between the binding lanes.

1. **Use is singular and honest.** Direct `dsf` is proposed only as a Superflex validation
   benchmark. It is not a trade-price source, model feature, product consumer, current overlay,
   David-facing number, or edge claim.
2. **Materiality can fail.** High direct-vs-transformed correlation and 21–23/24 top-24 overlap are
   carried as the reason to expect low incremental value, not hidden after naming a semantic
   difference.
3. **Cohort is corrected.** Only the 540 `dynasty-op` rows are proven Superflex; `dynasty-rk` is not
   admitted without separate format evidence. Current canonical coverage is 435/540.
4. **Cadence is corrected.** Both source views share weekly upstream cadence. New observations bind
   to new upstream vintage/content, never a daily polling clock.
5. **Semantics and retention remain explicit blockers.** Any destination must say
   DynastyProcess/expert consensus rather than collapse the series into trade market. Exact-file
   retention/licence is unresolved.

## Why `blocked_for_use` is the correct recommendation now

The exact blockers are sufficient under the standing vocabulary:

- durable retention/licence has not been approved;
- David has not prioritized a seventh Layer 1 substrate over Layer 2;
- the governed identity bridge required for a canonical validation join is not landed; and
- the named benchmark's incremental materiality is unproven and expected to be low.

The fact that six other streams currently have no consumer is priority evidence, not itself a
semantic prohibition. The licence and authority blockers are independently enough to keep this
stream from landing.

## Reopening condition

If David does not prioritize the work or does not settle retention, close stream 6
`blocked_for_use` with no RED.

If David later selects a research-only raw substrate **and** settles retention, the framing must
reopen before a RED. The only eligible RED scope is raw/history acquisition plus provenance in an
explicit expert-consensus destination. Identity normalization, scheduler, consumer, current
overlay, model input, and David-facing surface remain out of scope.

The later materiality study is a separate validation design. Before it runs, it must pre-register
the existing verdict/ranked comparison being tested, the common identity cohort, coverage floor,
missingness/exclusion behavior, metrics and decision threshold. Otherwise `dsf`'s lower identity
coverage could change a verdict by changing the cohort rather than the benchmark instrument.

Contracts remains separately parked at pinned v16 pending David's commit word. This concurrence
grants no action authority. H2 QB rushing remains a registered hypothesis **UNDER TEST** with no
result.
