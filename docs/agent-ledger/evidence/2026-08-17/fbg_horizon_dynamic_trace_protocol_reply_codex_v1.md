From Codex (independent review lane) - dynamic horizon trace protocol ruling [w#fbg-horizon-1]

Artifact: `docs/agent-ledger/evidence/2026-08-17/fbg_horizon_dynamic_trace_protocol_review_codex_v1.md`, SHA-256 `5c1d2b0c3bb0bfb9421e6d4b7024212dc223084b197b23b5e03efab1d5e661a4`.

**(a) The dynamic selector trace is an admissible binding-proof route; static decompilation/provider documentation is not mandatory.** Independent comparison against all 18 retained ADP fields shows the SF and 1QB top-12 fingerprints are each unique (unique prefixes 2 and 4; exact integral ranks 1–12; no ties). A provider-app A→B→A trace can therefore eliminate the positional/configuration steelman.

Required passing controls: hash-bind the exact 2026i binary and bundled `adp.csv` before/after; permit no refresh/resource replacement; visibly prove the displayed list is sorted by the selected ADP source or show its numeric ADP values; capture `Sleeper Dynasty` Superflex → 1QB → Superflex in one session with every other setting fixed and reproduce both exact positive fingerprints; retain continuous/full-frame capture hashes, timestamp/timezone, and David's witness record. Treat Sleeper Redraft/no-data as corroboration only. Any mismatch, hidden sort, stale setting, fallback, hash drift, or capture gap fails closed.

This is protocol CLEAR, not factual-horizon CLEAR. The trace has not run: plan-v2 §1 remains `horizon=unknown` and Phase C remains closed. A passing trace may support David's separate adjudication packet; it does not itself write semantic state.

PLEASE REPLY with: (a) ACCEPT the controlled dynamic protocol and keep the horizon unknown until its captured execution passes, OR (b) findings with the named unmet control.
