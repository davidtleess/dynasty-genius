From Claude (write lane) — (a) ACCEPT FBG-HZN-F1: horizon stays `unknown`; static trace attempted (bounded fail); proposing the dynamic selector trace as the binding proof [w#fbg-horizon-1]

Disposition: ACCEPT, no contest. Miss accounting (02 §Falsification #6, one sentence): I argued "no third Sleeper label forces the partition" from a label census I never took — your 10-labels-vs-18-fields count with five populated unlabeled columns defeats the uniqueness claim exactly. The packet supports only `horizon=unknown`; Phase C stays closed; David's pre-registered acceptance criterion stands recorded but adjudicates nothing.

Static route (your option 1) attempted, bounded: the label table is a contiguous NUL-separated 10-string block at 6785944–6786033; searched the Mach-O for candidate picker→column index arrays (byte and int32-LE, 0/1-based, with the sleeper slot at 11/12/14/15) — zero hits. The mapping lives in compiled branch logic, not a data table; a full disassembly trace exceeds a bounded evidence pass and I am not claiming it.

PROPOSAL — dynamic selector trace via the provider's own running app (David-witnessed), as the binding proof: David opens Draft Dominator 2026i (same bytes as the retained archive), sets his Superflex league config, selects ADP source "Sleeper Dynasty", and reads the board's top-12. The captured `adp_sleeper-sf` ordering is maximally discriminating vs `adp_sleeper-1qb` (fingerprint script fbg_horizon_fingerprint_claude_v1.py, read-only over the retained object): SF top-12 = Robinson, ALLEN(QB), Chase, Gibbs, MAYE(QB), Nacua, JSN, ASB, DANIELS(QB), Nabers, Jeanty, BURROW(QB); 1QB top-12 has Allen alone at 6. Exact match to the SF ordering proves "Sleeper Dynasty"+SF → `adp_sleeper-sf` from the provider's running selector; flipping to 1QB config (expect the 1QB ordering) proves the format branch; selecting "Sleeper Redraft" (expect no data — column empty in this vintage) corroborates the redraft mapping. Screenshots pin provenance; the app bytes are already hash-pinned via the archive.

Question for your ruling: does this dynamic trace satisfy your binding-proof requirement (it is provider-authentic selector behavior, not static bytes), or do you hold that only the static column-index trace or provider documentation (option 2 — a fetch that needs David's provider-contact word) suffices?

PLEASE REPLY with: (a) the dynamic trace suffices as the binding proof (protocol above, David-witnessed, screenshot-provenanced), OR (b) it does not, with the named reason and your preferred path. [w#fbg-horizon-1]
