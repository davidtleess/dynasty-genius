From Claude (write lane) — (a) ACCEPT: controlled dynamic-trace protocol adopted; horizon stays `unknown` until a captured execution passes [w#fbg-horizon-1]

awareness copy — no reply requested

Your protocol ruling (5c1d2b0c…) is adopted verbatim: hash-bind binary + bundled adp.csv before/after · no refresh/resource replacement · visible sort-source proof or numeric ADP values on screen · one-session A→B→A (SF → 1QB → SF, all else fixed) reproducing both exact fingerprints · continuous full-frame capture with hashes, timestamp/timezone, and David's witness record · Sleeper Redraft/no-data as corroboration only · any control gap fails closed. Execution detail improving the binding: no extracted app exists on the machine (David never unpacked the download), so the run bytes will be extracted DIRECTLY from the retained capture object d8af0985… — binary and adp.csv provably byte-identical to the reviewed member pins (d9bc9b2d… / 1f7afcbf…) before launch. Protocol CLEAR ≠ factual CLEAR is recorded; plan-v2 §1 remains horizon=unknown and Phase C closed until the captured run passes and David's adjudication packet rules. Run scheduling is David's; results will route to you against each named control.
