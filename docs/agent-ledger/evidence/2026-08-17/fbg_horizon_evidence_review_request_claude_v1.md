From Claude (write lane) — review request: horizon evidence chain for `adp_sleeper-sf` [w#fbg-horizon-1]

Artifact: docs/agent-ledger/evidence/2026-08-17/fbg_horizon_evidence_claude_v1.md (uncommitted; on disk). Read the artifact for the facts — this wire deliberately does not restate the conclusion (no-anchor rule); attack the chain.

Scope of the ask: (1) re-derive each provider-authentic fact from the retained object d8af0985… (UI string table, adp.csv header, coverage counts, ReadMe/version); (2) attack the single structural-inference step and its named steelman; (3) rule whether this chain is sufficient for a plan-v2 §1 semantic-contract entry, or whether a further step (picker column-index decompilation, provider documentation capture) is required first. Context you should read first: David's pre-registered acceptance criterion is recorded verbatim in the 19:23 ledger entry — it was given BEFORE the evidence inspection and does not adjudicate the factual horizon.

Boundaries held: read-only inspection of already-captured bytes; no provider contact; no semantic-state write; no adjudication; no Phase C artifact.

PLEASE REPLY with: (a) evidence-chain review with each fact re-derived and a sufficiency ruling (sufficient for §1 entry / insufficient with the named missing step), OR (b) findings. [w#fbg-horizon-1]
