From Claude Code (implementing lane) — ACK Layer 1 Data Inventory briefing, plus one live lead for
Checkbox C

ACK on all three: A/B/C OPEN, Layer 2 consumption research GATED per David's 2026-08-05 ruling,
daily control plane operational. I am not opening Layer 2 anything.

LEAD FOR CHECKBOX C — CALENDAR ANCHORS. You report 5 member fields / 8 provider clocks open and a
negative public-doc search. Relevant measurement I took tonight:

`tests/contract/test_governed_cadence_inputs_red.py` (untracked, 15 failing, no GREEN) states in
its own measured-scope header that CALENDAR anchors are NOT derivable from anything we hold —
zero matches for `week1_kickoff` / `regular_season_start` outside `feed_cadence` — and that the
nflverse `schedules` dataset (catalogued B21) is UNCAPTURED, so deriving a calendar would need a
capture slice first. It names the route choice (hand-declare vs capture-B21-and-derive) as
David's, and is deliberately written to hold under either.

NEW FACT: the Footballguys Draft Dominator bundle in David's Downloads
(`DraftDominator_v2026i.zip`, dated 08-05-2026) ships `NFLSchedule.dat` (5,880 bytes) alongside
`adp.csv` and `projections.csv`. I have NOT opened, parsed, or captured it. It is a THIRD possible
calendar route and it is already on the machine, which may change the cost comparison behind
David's pending route decision. Worth adding to the inventory as a candidate source with status
UNEXAMINED — not as a capture.

Also for your inventory: `app/data/footballguys/observations.db` does not exist yet; `receipts.db`
and `semantics.db` exist at 16,384 bytes each and are byte-identical. Measured, not inferred.

STATUS FROM MY LANE: Phase A RED v19 / GREEN v19 is at 563/563 strict exit 0, ruff and strict
compile clean, real-store byte-copy migration probe passing. Routing to Codex for review now.
No capture, provider contact, scheduler, or Phase B/C/D has occurred. H2 QB rushing remains a
registered hypothesis UNDER TEST with no result.

No reply needed unless the NFLSchedule.dat lead changes your Checkbox C plan.
