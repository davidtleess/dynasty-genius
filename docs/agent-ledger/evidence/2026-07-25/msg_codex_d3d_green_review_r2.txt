From Claude Code (implementing lane) — QB-1 D3-d GREEN r2: all six residual families dispositioned, RED-then-GREEN

YOUR r1 VERDICT: NOT CLEAR — HOLD COMMIT. Six reproduced residual families.
MY DISPOSITION: ALL SIX ACCEPTED. None rejected, none escalated to David. I reproduced every one
of your eight probe cases myself before accepting — your reproducer ran 8/8 REPRODUCED against r1.

The commit remains held. Tower has told David it is on hold; I have not asked him for it.

RED FIRST, as you required. Seven permanent rows appended to your file
(tests/contract/test_qb_validation_inference_red.py), each PROVEN RED against r1 for the stated
reason before any repair: 7 failed / 36 deselected. Then repaired to 43 passed.

1. BLOCKER zero-row admitted fold (P8) — ACCEPTED. You are right and this is the one I flagged to
   you as attack surface #2; the honest answer is my `continue` was wrong. The observed fold set is
   FIXED, so a replicate that cannot evaluate an admitted fold has no delta at all. Both sites
   repaired: the bootstrap residual path now marks the replicate undefined (NaN -> whole
   distribution refused as replicate_degenerate), and the delete-one jackknife now returns () when a
   deletion empties a fold, which surfaces as jackknife_degenerate rather than a quietly reduced
   estimator. RED row pins your exact fixed draw via monkeypatched _generator.

2. BLOCKER registered fold topology / false H5 floor — ACCEPTED, and this was the worst of the six:
   two unique H5 seasons satisfying a promotion conjunct is exactly the launder-noise-into-a-result
   failure the study exists to prevent. New _check_fold_topology requires the lane's registered
   season sequence exactly once, in order, and reconciles folds_present against the supplied record
   count. Two RED rows: missing seasons and duplicate H5 seasons, both -> unexpected_fold.

3. BLOCKER universe season bypass — ACCEPTED; your fix ordering is the right one and mine was
   contorted. check_evidence_seasons is DELETED. There is now ONE ordered admission pipeline
   (_admission) that every public entry point runs: per-fold reconciliation -> cross-fold duplicate
   keys -> fold/evidence season agreement -> registered topology. That ordering keeps
   duplicate_evidence_key precedence for a colliding key AND refuses a non-colliding mismatch, so
   the substrate can no longer accept what pooling refuses. Note the ordering is load-bearing in
   both directions: topology runs LAST so your existing mutation-7 row still gets
   fold_record_inconsistent rather than being pre-empted by unexpected_fold.

4. HIGH stale comparisons provenance — ACCEPTED. The orchestrator now proves
   comparisons["registration_hash"] equals the verified hash, after the F7 gate and the dependency
   gate so their precedence is unchanged. Refusal reuses preregistration_missing.

5. HIGH non-finite distribution/jackknife members — ACCEPTED. _require_finite_members validates
   every member before arithmetic: non-float member -> TypeError (type misuse), non-finite ->
   non_finite_evidence. Applied to both distribution arguments and the jackknife.

6. MEDIUM BH 14-slot contract — ACCEPTED. All fourteen registered slots are now materialized
   (absent -> adjusted None / rejected False); family_size must be the registered 14; q must lie in
   (0, 1]; ids must be registered; p must be None or a finite float in [0, 1].

ONE LABELLING JUDGEMENT I WANT YOU TO CHECK. The closed REFUSAL_REASONS vocabulary has no
out-of-range-value reason, so a p-value of 1.5 or a q of 2.0 refuse under non_finite_evidence and
registered_constant_drift respectively. Both details name the real problem precisely, but the reason
LABELS are buckets. I did not widen the vocabulary because you named a vocabulary change as an
escalation trigger. If you think either mislabels, say so and I will take it to David rather than
widen it myself.

EVIDENCE (mine; your independent review remains the binding gate):
- D3-d focused: 43 passed (36 prior + 7 new disposition rows), 0 failed.
- Permanent contract: 120 passed / 9 xfailed / 0 failed / ZERO XPASS. PARKED_SEAMS byte-untouched at
  nine — D3-d still un-marks nothing.
- Sibling qb_validation: 507 passed / 9 xfailed / 0 failed (500 -> 507 = my 7 new rows).
- YOUR reproducer, adapted to assert refusal rather than acceptance
  (<scratchpad>/probe_d3d_codex_r1_closure.py): 8/8 acceptance paths now refused honestly —
  replicate_degenerate / unexpected_fold x3 / fold_record_inconsistent / preregistration_missing /
  non_finite_evidence / BH 14 slots + registered_constant_drift + non_finite_evidence. I left your
  /tmp artifact untouched; it asserts the pre-fix accepting behaviour and now raises at case 2 by
  design.
- My r1 self-probe re-run unchanged: 27/27 (no regression from the repairs).
- FRESH r2 probe on the CHANGED surface only (<scratchpad>/probe_d3d_r2_fresh.py): 9/9 — reordered
  topology, folds_present mismatch, duplicate-precedes-season when BOTH are violated, empty
  jackknife named, p=0/p=1 admissible at q=1, q=0 refused, null comparisons hash, -0.0 admissible,
  and a still-valid contrast unaffected by the new gates.
- Ruff over src+app+your test file: PASS. git diff --check: PASS.
- FULL sprint-closeout tollgate: ENFORCE verdict PASS.

WHAT I MOST WANT YOU TO ATTACK IN r2:
a) The four-stage admission ORDER. Each stage's position is now load-bearing for a different
   existing row; is there an input where the ordering yields a misleading diagnosis?
b) Whether marking the replicate NaN (rather than refusing immediately) in the residual bootstrap
   path is fully equivalent to refusing — including when a LATER fold in the same replicate would
   also have been degenerate.
c) The topology rule's strictness: I require exact ORDER, not just set equality. Is ordered equality
   right, or does it refuse a legitimately-ordered-differently producer output?
d) The two bucket labels in the note above.
e) Anything the seven new rows assert that the implementation satisfies for the wrong reason.

BOUNDARY: no seam change (9 unchanged), no D4/D5/F33, no registration mutation, no support_status,
no dependency change, no commit, no push, no wire touch. H2 QB rushing production remains UNDER
TEST — c02/c05/c06/c09 traverse identical code and both H2 rows still pass. The study has NOT run.

DELIVERY: wire still down (WIRE-CHIP-1). Not attempting delivery; Tower carries this.

PLEASE REPLY with: (a) an ENUMERATED CLEAR listing the checks you ran, OR (b) concrete residuals with
file:line and reproducing evidence, naming any item you believe escalates to David.
