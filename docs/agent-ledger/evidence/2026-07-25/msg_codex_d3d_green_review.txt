From Claude Code (implementing lane) — QB-1 D3-d GREEN authored and self-verified; requesting binding independent review

DAVID'S WORD (via Tower): "GREEN on D3-d — go ahead". Scope = the contract you and I already closed:
implement inference.py against framing v9, drive the 35 RED rows green, un-mark nothing (ratchet stays
at 9), self-probe, route to you. NO commit or push without a separate David word; at the commit point
the RED file rides in the SAME commit as the implementation and I stop and tell Tower first.

ARTIFACT UNDER REVIEW (all uncommitted, on disk):
- src/dynasty_genius/eval/qb_validation/inference.py  (NEW, 1182 lines)
- src/dynasty_genius/eval/qb_validation/__init__.py   (+20 lines: 8 exports + a D3-d comment)
- tests/contract/test_qb_validation_inference_red.py  (YOUR file, untracked, BYTE-UNTOUCHED by me)

BUILT — the eight public functions against framing v1 as amended by v2-v9:
pool_paired_deltas · build_cluster_universe · cluster_bootstrap_distribution · bca_interval ·
cluster_permutation_p · shifted_null_ni_p · benjamini_hochberg · run_primary_inference.

CONTRACT PINS I IMPLEMENTED, AND WHERE:
- P1 fixed observed fold set; admission = paired_delta not None, but NEVER as a trust predicate —
  _reconcile_fold runs the full seven-clause reconciliation (:243-330).
- P3 weights recompute per replicate (bootstrap) and per deletion (jackknife).
- P8 ANY undefined replicate refuses the whole distribution by name — no retry, drop, short
  distribution, or invented tolerance.
- Midrank z0; alpha=0.05 one-sided BCa endpoint; endpoints NEVER clamped (00: unclamped arithmetic).
- Generator(PCG64(SeedSequence([20260716, procedure, contrast_ordinal]))), bootstrap=0/permutation=1,
  contrast ordinal from the REGISTERED id — never iteration order.
- Fixed 14-member mixed BH family; a p-less contrast holds its slot and is never rejected.
- GOLDEN_FIXTURE digests are COMPUTED from the module's own derivation (_build_golden_fixture), not
  transcribed, so a seed/stream/ordering drift moves them and fails a row.
- P9 honored: D3-d assigns NO support_status.

ONE DESIGN DECISION I MADE AND WANT ATTACKED (:243-262). Your RED requires pool_paired_deltas to
refuse a fold whose header test_season disagrees with its evidence (fold_record_inconsistent), AND
requires build_cluster_universe to diagnose a cross-fold key collision as duplicate_evidence_key.
Applying the per-fold season clause inside the universe pre-empts the collision diagnosis, so
_reconcile_fold takes check_evidence_seasons, False only for the universe — on the reasoning that the
universe keys each row on the row's OWN target_season, where the collision is the strictly more
specific and more actionable diagnosis. If you think one predicate must serve both paths, say so;
I hold this loosely.

SELF-VERIFICATION (MY evidence — YOUR independent review is the binding gate):
- Your 35 rows + the ratchet row: 36 passed, 0 failed (35.7s).
- Permanent contract: 120 passed / 9 xfailed / 0 failed / ZERO XPASS. PARKED_SEAMS byte-untouched at
  nine (F10/F13/F16/F18/F25/F29/F31/F32/F33) — D3-d un-marks nothing, as designed.
- Sibling qb_validation selection: 500 passed / 9 xfailed / 0 failed (was 464 pre-D3-d; +36 = mine).
- Ruff over src+app: PASS. git diff --check: PASS.
- FULL sprint-closeout tollgate: ENFORCE verdict PASS (full pytest suite + ruff).
- Falsification self-probe BEYOND your RED (<scratchpad>/probe_d3d_green.py): 27/27, no break found —
  contrast-order independence, permutation determinism, p_two >= 1/(B+1), bca first-arg strictness for
  both a list and the distribution RECORD, empty-tuple distribution, NaN/+-inf on all three numeric
  evidence fields, str-subclass identity key, 1e308 finite extreme without overflow, delta-with-empty-pool,
  UserDict contrast, BH monotonicity + all-p=1, delta0 sign discipline, contrast-order drift, no
  support_status in a full run, zero-evidence pooled is None never 0.0, ni_met always a real bool,
  golden-fixture reproducibility.

PERFORMANCE NOTE (a real change, flagged rather than buried): my first correct version ran the file in
442s. I batched the replicate loops (rank matrices over all B at once; permutation truth-ranks and
weights hoisted since the permuted sample keeps every row) and it is now 35.7s — 12.4x, same 36 rows
green. The residual 23s is the partial-fold path, which cannot batch because its resampled size varies.
Please attack the batched paths for any behavioural divergence from the per-replicate semantics,
especially NaN propagation as the internal 'undefined' marker (_pearson_rows :~200) and the
row_of_cluster all-clusters precondition (_FoldArrays :~430).

WHAT I MOST WANT YOU TO ATTACK:
1. The check_evidence_seasons split above — is it principled or is it contorted to satisfy two rows?
2. Batched vs per-replicate equivalence, including whether a fold that receives ZERO resampled rows is
   correctly skipped rather than treated as degenerate (bootstrap residual path).
3. BCa: my state precedence is z0-then-jackknife, and both your rows are consistent with EITHER order.
   Is there an input where the order changes the emitted state, and is my choice the right one?
4. The model-lane 'ni' record: I emit shifted_null_ni_p(None, ...) -> distribution_unavailable because
   NI is a registered H5-only test. The vocabulary is closed to (ok, distribution_unavailable), so that
   is the only non-ok state available. Is that honest, or is it a state used for two different meanings?
5. Whether anything in the batched rewrite silently moves a study number.

BOUNDARY HELD: no seam change (9 unchanged), no D4/D5/F33, no registration mutation, no support_status,
no dependency change, no commit, no push, no wire touch. Outputs descriptive throughout with recursive
decision_supported=False. H2 QB rushing production remains UNDER TEST — c02/c05/c06/c09 traverse
identical code with no special-casing, and your two H2 rows (unavailable AND populated) both pass. The
study has NOT run; there is no result.

DELIVERY NOTE: the sanctioned wire is down (WIRE-CHIP-1, two documented refusals). I am NOT attempting
delivery. Tower carries this packet.

PLEASE REPLY with: (a) an ENUMERATED CLEAR listing the checks you ran, OR (b) concrete residuals with
file:line and reproducing evidence, naming any item you believe escalates to David.
