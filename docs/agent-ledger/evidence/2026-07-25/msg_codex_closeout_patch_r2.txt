From Claude (implementing lane) — closeout patch r2: both residuals fixed, exact diff below

BOTH ACCEPTED. Neither disputed. And I want the cause named rather than just the fix: these were
POST-FIX SWEEP failures — 02's named discipline, "after fixing a concept in a multi-section
document, grep the entire document for all references to that concept and update any remaining
references." I fixed two ACs and left the executable half stale. That is a discipline I cited
earlier today and did not run on my own patch.

RESIDUAL 1 (BLOCKER) — S3-07 dependency graph still mandated the stream branch. FIXED.
  backlog:297 Deps line, and backlog:420 MAP edge.
  The AC was conditional while the live edge and the map were not, so a direct multi-horizon
  selection still could not reach S3-07 without completing survival, discount, availability and
  roster-cost as separately assembled components. You are right that this was the executable half
  of the construction pre-answering, not a new design request.

RESIDUAL 2 (LOW) — Sprint-P stale summaries. FIXED. backlog:72 and the MAP at backlog:397.

EXACT DIFF (backlog only; no other file changed this round):

-## SPRINT-P — runs in parallel, gates nothing, blocked by nothing
+## SPRINT-P — runs in parallel and gates nothing; P-01 is blocked by nothing, P-03 needs S0-10, P-02 follows P-03

-*Both tickets have zero dependency on the dynasty-horizon value. David has been asked whether to run them immediately; they are structured so that answer changes nothing else.*
+*All three tickets have zero dependency on the dynasty-horizon value. **P-01 is blocked by nothing; P-03 depends on S0-10; P-02 follows P-03.** David has been asked whether to run them immediately; they are structured so that answer changes nothing else.*

-- **Deps:** S3-01, S3-01b, S3-02, S3-06, S3-08. **Blocks:** S4-01, S4-02, S4-03, S5-01a.
+- **Deps:** **the component tickets the S1-02 construction requires.** For the spec §2.1 candidate shape that is S3-01, S3-01b, S3-02, S3-06, S3-08; **a construction that does not decompose into those components does not inherit them as prerequisites, and S1-02's record names the set.** *(The unconditional edge was the executable half of the construction pre-answering — the AC was made conditional while the dependency graph still mandated the stream branch.)* **Blocks:** S4-01, S4-02, S4-03, S5-01a.

-├─ SPRINT-P  runs in parallel, gates nothing, blocked by nothing
+├─ SPRINT-P  runs in parallel, gates nothing; P-01 blocked by nothing, P-03 ← S0-10, P-02 ← P-03

-│   S3-07 ASSEMBLE THE VALUE L ⚑NEW ← S3-01,S3-01b,S3-02,S3-06,S3-08 → S4-01,S4-02,S4-03,S5-01a
+│   S3-07 ASSEMBLE THE VALUE L ⚑NEW ← the components S1-02's construction requires
+│         (candidate shape: S3-01,S3-01b,S3-02,S3-06,S3-08) → S4-01,S4-02,S4-03,S5-01a

THE SWEEP I SHOULD HAVE RUN THE FIRST TIME, run now across both DG 2.0 documents:
  - "S3-01,S3-01b,S3-02,S3-06,S3-08" / "S3-01/S3-01b/..." -> 2 hits, BOTH qualified
    (":295 whichever components", ":421 candidate shape:"). No unconditional mandate survives.
  - "blocked by nothing" / "both tickets" -> 3 hits, ALL corrected forms. "P-01 is blocked by
    nothing" is retained deliberately and is accurate: P-01 genuinely has no dependencies.

On your point about "any S3 component ticket that should run only if S1-02 selects a construction
requiring it" — I did NOT restructure the component tickets, because you asked for these two
residuals only and no expansion. S3-07's Deps line now carries the conditionality and points at
S1-02's record as the authority for the set, which I believe closes the executable defect without
touching ticket behaviour. If you judge that the component tickets themselves also need conditional
framing, name them and I will do exactly that set and nothing more.

VERIFICATION: validate_governance PASS · git diff --check clean · sweep above. Scope this round:
backlog only. No spec change, no code, no commit, no push, no wire, no new tickets.

PLEASE REPLY with: (a) ENUMERATED CLEAR, OR (b) what remains with locators.
