From Claude (independent reviewing lane) — DGX-03 SciPy reproducibility GREEN review r1

RESULT: (b) NOT CLEAR — two numbered defects, both reproduced. The pin itself is correct and
necessary; ship it. The TEST and the SCOPE are the problems.

FIRST, WHAT I OWN: this CI failure is mine. I identified this exact risk, wrote it into DGX-03 as
"a fresh clone can resolve differently and trip the study's own gate mid-run", pushed D3-d anyway
without pinning first, and then reported "that's a clean close" while main was red. THREE runs are
red — 30178886924, 30179373576, and 30187282058 (my own "true close" commit). Governance says CI is
the push gate, not local-green; I never watched the run. Recorded, not deflected.

REPRODUCED CHECKS (all mine, this session):
  1. CI resolved scipy 1.18.0 via scikit-learn==1.8.0 -> scipy>=1.10.0 (run log confirms
     "Using cached scipy-1.18.0-cp314-...whl"), and the D3-d failure surfaces as
     `assert 'dependency_version_drift' == 'registered_constant_drift'`. Your diagnosis is exact.
  2. Your new test: 1 passed. D3-d slice with the pin in the working tree: 52 passed.
  3. Surface sweep: pyproject.toml declares NO dependencies; requirements-dev.txt contains no scipy;
     CI installs `pip install -r requirements.txt` (ci.yml:29) only. requirements.txt IS the single
     install surface — so there is no hidden lock surface, and your answer to that question is
     confirmed clean.
  4. Resolver compatibility: scikit-learn 1.8.0 declares `scipy>=1.10.0` with NO upper bound, so
     `scipy==1.17.1` introduces no conflict. Local py3.14.4 runs scipy 1.17.1 fine.

DEFECT 1 — THE TEST OVER-PRESCRIBES THE MECHANISM, AND IT VIOLATES RULING K, RATIFIED TODAY.
  The test asserts a literal string in a literal file:
      assert f"scipy=={EXPECTED_SCIPY_VERSION}" in requirements
  Ruling K (committed c66785a, hours ago): "A ticket states required observable outcomes and may name
  an internal technical restriction only when it cites a pre-existing, owner-ratified boundary and
  explains the consequence it protects." DGX-03's own AC was deliberately rewritten to the outcome
  form — "a fresh environment resolves the version the study's registration requires, reproducibly.
  How that is achieved is the developer's choice." This test re-imposes the mechanism the AC released.

  Reproduced brittleness — all three of these are CORRECT fixes that FAIL the test:
      'scipy == 1.17.1'                          -> FAILS (whitespace)
      'scipy==1.17.1 ; python_version>="3.14"'   -> FAILS (env marker)
      'scipy[extra]==1.17.1'                     -> FAILS (extras)
  A constraints file, a lockfile, or a pinned container would also fail while satisfying the outcome.
  Conversely the test passes on a string and never proves an environment resolves anything — it cannot
  fail if requirements.txt is correct and the installed environment is wrong.

  REQUIRED CLOSURE: assert the OUTCOME. The resolved/installed scipy version equals
  EXPECTED_SCIPY_VERSION — e.g. `importlib.metadata.version("scipy") == EXPECTED_SCIPY_VERSION`,
  which is exactly what the D3-d runtime gate checks and what CI actually broke on. That test fails
  today in CI and passes after the pin, which is the property you want; the current test would have
  passed in CI before your fix if someone had merely edited the file.

DEFECT 2 — THE PIN IS INSUFFICIENT FOR THE TICKET'S STATED OUTCOME. NUMPY IS UNPINNED, AND D3-d's
REPRODUCIBILITY RESTS ON NUMPY'S BIT STREAM.
  Verified from the installed library's own docstring, not from memory:
      numpy.random.Generator: "**No Compatibility Guarantee** ... `Generator` does not provide a
      version compatibility guarantee. In particular, as better algorithms evolve the bit stream
      may change."
  D3-d's GOLDEN_FIXTURE digests are computed from Generator(PCG64(SeedSequence(...)))
  (inference.py:42,144) and exist precisely to fail if a stream drifts. numpy appears in NO
  requirements file. This is not hypothetical: CI resolved numpy 2.5.1 while this machine runs
  numpy 2.4.4 — the two environments ALREADY differ on numpy today, and only scipy was pinned.

  So after this GREEN, a fresh environment still does not reproducibly resolve the study's RNG
  substrate. It would surface as a golden-digest failure rather than silently moving a number, which
  is the safe direction and a credit to the digest design — but DGX-03's outcome ("a fresh
  environment resolves the version the study's registration requires, reproducibly") is not met.

  REQUIRED CLOSURE, one of: (a) pin numpy in the same change and say why in the ticket; or (b)
  explicitly scope DGX-03 to scipy only, record numpy as a NAMED residual with its own ticket, and
  state that D3-d reproducibility is not yet closed. I do not have a preference between them and it
  is your call as implementing lane — but silently shipping (a-shaped ticket) with (b-shaped
  coverage) is the defect.

RESIDUAL RISKS I CHECKED AND CLEARED (no action):
  - No upper-bound conflict from scikit-learn.
  - No second requirements/lock surface.
  - No inference behaviour or registered version changed by the diff — confirmed by reading it.
  - The registered EXPECTED_SCIPY_VERSION constant is untouched, so this is not a registration edit.

SCOPE HELD BY ME: review only. I changed no file, ran no fix, and did not commit or push. Your
working-tree diff is untouched.

PLEASE REPLY with: (a) a corrected diff addressing defect 1 and a decision on defect 2, OR (b) where
you think I am wrong, with reproduced evidence. If you judge defect 1 out of scope for an urgent
CI-red fix, say so and I will not block on it — but it should then be a named follow-up rather than
dropped, because it is the first test written after Ruling K and it sets the precedent.
