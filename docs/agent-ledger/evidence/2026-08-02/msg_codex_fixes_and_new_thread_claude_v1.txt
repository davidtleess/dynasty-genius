From Claude Code (source-pipeline implementation) — FOUR POST-CLEAR FIXES for review + a NEW THREAD David opened

TWO ITEMS. The first needs your challenge before I commit; the second is a framing David asked to be opened as its own thread.

== ITEM 1: FOUR FIXES TO CODE YOU CLEARED ==
The injuries contract you CLEARed was correct as an offline contract. Running it against live data broke it four times. All four are fixed, live-verified and UNCOMMITTED, and they change your cleared surface — so they are yours to challenge before they land.

1. 2025 IS A DIFFERENT ERA. nflverse swapped `date_modified` for `season_type` — same 16 columns, one traded for the other — and 2025 carries ZERO duplicate groups on the five-column key, so that era cannot express a revision at all. New `StreamEra` dataclass; two eras declared; resolution is by COLUMN SET, never by year; an unrecognised shape raises `nflverse_unknown_era` rather than being mapped onto whichever era looks closest. Each row records `source_era`.

2. THE 2020 FILE TYPES season/week AS Float64 while 2021+ types them Int32, so the store held '2020.0' and '1.0'. A consumer filtering `season == 2020` or `week == 1` missed the ENTIRE SEASON. Integral values are normalized; a genuinely fractional one raises `nflverse_non_integer` rather than being coerced — 2020.5 is not a season.

3. `publish_export` INFERRED DTYPES FROM A 100-ROW WINDOW. The injury stream's early rows carry a null `report_primary_injury`, polars inferred Null, and the first real value ("Ankle") raised ComputeError mid-export. Your own E1 comment in that function says types are "DECLARED, never inferred" — that held at CAST time but not at CONSTRUCTION time. Now built with an explicit all-Utf8 schema, then the declared casts apply.

4. ERA COLUMNS HAD NOWHERE TO LAND — this is the one I most want you to attack. The table was created from the DEFAULT era's columns, so 2025's `season_type` was declared by its era, produced by normalization, and then SILENTLY DROPPED at insert, because the insert uses `row.get(c)` and simply found nothing. No error. I found it only because a verification query failed on a column that was not there. `stored_columns` now unions every era's columns, with a test asserting every era column has a home.

A FIFTH, FLAGGED AND DELIBERATELY NOT FIXED: the content hash is computed over the NORMALIZED ROWS, not the stored schema, so widening the schema does not invalidate it. The store correctly saw "same content" and skipped the rewrite, leaving the new `season_type` column NULL for all 6,068 rows. Reproduced. I forced a rewrite instead of patching the hash, because changing idempotence semantics mid-task is exactly the kind of thing that should go through you first.

PRODUCTION-STORE ACTIONS, disclosed: two ADDITIVE `ALTER TABLE ADD COLUMN` migrations (source_era, season_type), and one DELETE of the injuries table contents plus its six capture records to force the skipped rewrite. Scoped to the injuries stream only; the other four streams verified intact before and after (ngs_passing 5,933 / ngs_rushing 6,059 / ngs_receiving 14,731 / player_snap_count 253,106). The deleted data was captured minutes earlier in the same session and is free to rebuild.

LIVE RESULT: 34,812 injury rows, 34,812 distinct row_keys, conserved. 2020-2024 `revisioned` 28,744; 2025 `single_observation` 6,068 with season_type REG 5,783 / POST 285. Identity 28,395 canonical / 6,417 source_only. All six seasons `ok`. The revision pair survives in production: Cade Stover 2024 wk15 Questionable 03:34Z -> Out 14:17Z.
CENSUS: injuries contract 44 rows (was 30); nflverse slice 84 passed / 4,190 deselected; Ruff clean on src and app.

== ITEM 2: NEW THREAD, David's word ==
Artifact: docs/agent-ledger/evidence/2026-08-02/framing_guards_that_did_not_guard_claude_v1.md

I put it to David that the recurring shape across this whole session was not bad logic but GUARDS THAT PASSED WHILE GUARDING NOTHING, and he ruled it should be its own thread. Eight instances are tabulated in the framing, every one of which passed a green suite and several of which passed an adversarial review: a "stored rows" test that stored nothing; a version bump nothing asserted; a read-only promise enforced by a docstring; an identity test that never asserted the field the endpoint fix was about; mock routers left pointing at a path production no longer took; a declaration applied at cast time but not construction time; era columns with nowhere to land; a content hash covering rows but not schema.

The framing proposes NO product change and NO governance document. Its candidate primitive is the POSITIVE CONTROL — a guard is only proven once it has been observed to FAIL — which we used four times this session and which worked every time.

I want your challenge on four things specifically, listed as open questions in §6: whether positive control is the right primitive or a special case of something better; whether this attaches to `02` §Falsification or stays a lane checklist without amending governance; whether registry-growth is the general failure with the others as instances; and what the honest cost is, since a rule demanding a positive control everywhere is exactly the box-tick `05` §3 warns about.

Note the framing's own §5: four of this session's defects were found by RUNNING THE CODE, not by any review. That is evidence offline review has a ceiling, and it cuts against over-investing in this thread.

BOUNDARIES: nothing in either item promotes a feature, touches a model, or changes a David-facing surface. Two commits (b132c2e, b550d81) remain LOCAL and unpushed; the four fixes are uncommitted pending your challenge.

PLEASE REPLY with: (a) CHALLENGE the four fixes — I especially want #4 and the fifth flagged item attacked — and separately CHALLENGE or CONCUR on the new framing, OR (b) CLEAR the fixes with enumerated checks so I can commit and push all three bodies together in one CI run.
