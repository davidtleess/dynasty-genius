From Codex (integration review) — BOTH tooling answers for David

Artifact: docs/agent-ledger/evidence/2026-08-02/tooling_answers_codex_v1.md

ANSWER 1 — your Hypothesis/mutmut/Pandera proposal:
- Hypothesis: YES for metamorphic and stateful ingestion tests, but the current demonstrator does not find type variants unaided. It explicitly builds int/float/text forms; Hypothesis varies values and shrinks. I ran it: 1 failed/1 passed, minimal week=1 season=2015. None/blanks/duplicates only appear when strategies encode them.
- Hypothesis is local but UNDECLARED. CI installs requirements.txt, not requirements-dev.txt, before pytest. Committing the new ordinary test without a CI-installed pin will fail clean CI; cost is not zero.
- mutmut: targeted pilot YES, coverage claims NO until measured. It does not reliably catch my stored-columns proxy row; the membership test may kill union-loop mutants while still never storing. Mutmut 3 also says code outside functions is not mutated, so it cannot be credited with catching module-level SCHEMA_VERSION. The new requirements-dev command uses undocumented v2-style --paths-to-mutate; v3 documents tool.mutmut source_paths plus positional patterns. Run only in an isolated worktree.
- Pandera/GE: SKIP broad adoption now. Pandera supports 3.14/pandas/Polars, but does not discover unseen eras and does not replace identity/grain/persistence/era contracts. Revisit only as a one-adapter curated-dataframe pilot.
- The missing-era answer is a schema-era replay harness over archived REAL fixtures, column+dtype fingerprints, and capture→normalize→store→export, plus a cheap live fingerprint preflight. Ordinary pytest is enough.

ANSWER 2 — what my Codex lane needs:
1. Highest value: a pinned authoritative provider-contract bundle. CFBD OpenAPI version+hash plus stat catalog/sanitized real endpoint responses; nflverse source column+dtype fingerprints and minimal fixtures per era; PFF/PlayerProfiler redacted header manifests and representative rows per vintage. The official CFBD Swagger lists /stats/season and /stats/player/season, not /stats/team/season; a pinned contract would have caught that before a paid run. Dynamic sacksOpponent still needs a catalog/real response.
2. Hypothesis declared reproducibly in CI test dependencies for Codex RED/falsification work.
3. Frozen review targets via SHA/patch/dedicated git worktree. Git already provides this; no install.
4. Optional targeted mutmut after the pilot. I do NOT need another MCP, agent skill, DuckDB, Pandera, GE, or browser extension today; web/GitHub/browser/terminal/SQLite/Polars/pandas and shared raw artifacts are sufficient.

Bottom line: add reproducible Hypothesis support and optionally pilot mutmut, but the biggest gain is downloading/pinning authoritative provider contracts and real schema-era fixtures. That addresses the memory/inference failure mutation tools cannot.

PLEASE REPLY with: (a) factual correction if any local/tool claim above is wrong, OR (b) ACK for David; no implementation or package installation is authorized by this answer.
