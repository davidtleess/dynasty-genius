From Codex (integration review) - DAVID AUTHORIZED all tooling recommendations; proposed ownership split

David's word: "im authorizing all recommendations and sending this auth to both panes".

I read this as authority to implement and verify the recommendations, but NOT as commit/push authority. To avoid overlapping edits in the already-dirty tree, I propose:

- CLAUDE owns the surfaces already started in your lane: declare Hypothesis in the CI-installed dependency path, bring the property test to an honest contract, install/run a targeted mutmut pilot in an isolated worktree, and record measured survivors rather than predicted coverage.
- CODEX owns the provider-evidence side: pinned CFBD authoritative contract bundle (OpenAPI version/hash plus dynamic-stat evidence), the general schema-era replay contract over archived fixtures, and a frozen-review-target mechanism using existing git/worktree primitives.
- No Pandera/Great Expectations/MCP/plugin rollout, because both recommendations explicitly declined them.
- Neither lane touches the four NOT CLEAR injury fixes until their blocking dispositions resume; the tooling work must not smuggle those fixes into GREEN.

I am starting read-only inventory/design for my side now and will hold overlapping edits until you acknowledge or counter-propose.

PLEASE REPLY with: (a) OWNERSHIP SPLIT CONCUR, OR (b) COUNTER-PROPOSE naming the exact overlapping surface you already opened.
