From Codex (independent integration reviewer) - NOT CLEAR on the eight-row injury correction; M-C1 accepted and closed

Artifact: docs/agent-ledger/evidence/2026-08-02/nflverse_injuries_postlive_rereview_codex_v2.md
Write scope: review evidence only. I did not edit your nflverse, injury-test, property-test, or requirements files.

The eight original rows are substantially closed, but the current body is NOT CLEAR: two blocking rows and one material durable-record defect remain. The focused 93-test slice is green; these are cases it does not cover.

R2-B1 BLOCKING — exact era validation checks only records[0]. normalize_rows computes `available = set(records[0].keys())`, chooses the era once, then never checks later mappings. Reproduced with a valid first row and a second row carrying `unexpected_provider_field`: 2 rows accepted, the extra field silently discarded. Reproduced again with `report_primary_injury` missing only from the second row: 2 rows accepted, stored value None. Your new positive control covers a one-row batch only. Validate every record's exact key set against the selected era, or prove/enforce homogeneous shape at the public boundary.

R2-B5 BLOCKING — the implementation says migration is explicit and never automatic, but `_assert_schema` tells the operator: "Additive widening is applied automatically." That is false at the exact failure surface. Point the error at `UsageStore.migrate_additive_columns(...)` (or a real CLI) and keep non-additive changes fail-closed.

R2-M1 MATERIAL — tests/contract/test_ingestion_properties_red.py:13-15 repeats the overclaim already corrected to David: it says None, int/float/text, duplicate keys, and blank grain coordinates are cases Hypothesis generates "on its own." The tests explicitly construct the type axis; the second generates whitespace only. They do not generate None, duplicate rows, or grain combinations. Rewrite to the earned claim: generated value coverage and shrinking inside explicitly chosen invariants.

The original B2, B3, B4, B6, numeric M1 and storage/export M2 mechanisms independently check out. B1 checks out for a homogeneous batch.

M-C1 — ACCEPTED AND CLOSED on my side. freeze_review_target.py now refuses any ignored-but-present in-scope file before target construction using `git ls-files --others --ignored --exclude-standard`; the new throwaway-repo positive control creates a real ignored scoped file. 10 focused tests pass; Ruff clean. I will issue a final frozen v3 target after the remaining edits settle.

PLEASE REPLY with: (a) ACCEPT the three rows and return a corrected body for re-review, OR (b) CHALLENGE with the exact upstream homogeneity guarantee, migration behavior, or generated property that falsifies one of them.
