From Codex (independent integration review) — TOOLING NOT CLEAR, four remaining rows with two reproduced counterexamples

I reviewed the corrected generator, pinned bundle, replay suite, and preflight implementation rather than the summary. The prior T1-T6 dispositions materially improved them: 22/22 focused tests pass, Ruff is clean, and `--check` currently reports six shapes / 266 snapshots. Four rows remain.

R3-T1 BLOCKING — both generator and preflight still infer the column set from records[0].
`build_nflverse_era_fixtures.py` sets `columns = tuple(sorted(records[0].keys()))`; `_observed_shape` does the same. A provider field present only on a later row is added to the dtype map but omitted from `columns`. The preflight verdict compares the first-row columns and then iterates only those columns, so the later-row addition is ignored. This reintroduces the exact heterogeneous-batch failure just closed in normalize_rows, one layer earlier. Require every record in a snapshot/fetch to have one exact column set, with a typed/profile refusal naming row index + extra/missing, and lock both generator and preflight with `[{'a':1}, {'a':1,'late':'x'}]` plus the missing-column inverse.

R3-T2 BLOCKING — `--check` does not prove the pinned source set or all claimed fingerprint identity.
Reproduced in a temp fixture root: pin one `demo_2024_first.json`, add a second same-shape `demo_2024_added.json`, run `--check`. It exits 0 and prints `ok — 1 shapes match; 2 source snapshots hash-verified`, while the committed bundle names one source and live build names two. The loop checks only committed keys that remain live; added sources are never compared, and deleted sources are skipped by `if key in live_hashes`. Also, the fingerprint hashes columns + cumulative kinds, not the season-specific profiles the preflight treats as identity. Compare the complete deterministic contract payload (or explicit source-key/hash maps and per-season profiles) symmetrically. Add positive controls for added source, removed source, source-content mutation, and a per-season dtype change that leaves the era-wide union unchanged.

R3-T3 BLOCKING AS CLAIMED — archived witness rows do not cover the declared dtype variants.
I independently compared `fixture_rows_by_season` against `dtype_kinds_by_season`: 64 (stream, season, column, kind) variants have no witness row. Examples include injuries 2020 `practice_primary_injury=null`, injuries 2020 `practice_secondary_injury=str`, and many NGS null/float variants. `setdefault(season, records[:5])` preserves only the first snapshot's first five rows, so a later snapshot/type variant remains metadata and is never replayed. That contradicts the script's stated guarantee that “every pinned dtype variant has archived-real rows to replay.” Either select a deterministic minimal row cover per season over all observed `(column, kind)` pairs and assert zero uncovered variants, or narrow the contract honestly and explicitly lock only the load-bearing variants. Given the authorized recommendation was representative real-era replay, I recommend the cover.

R3-T4 BLOCKING — the preflight tests do not execute the preflight verdict.
The suite checks `_observed_shape`, dtype helpers, registry membership, and manually proves set inequality, but no test invokes the comparison/decision path in `main`; `_verdict` is nested and unreachable without a live loader. Thus a broken `_verdict`, wrong chosen season, or failure to bind the selected loader can leave all 10 tests green. Extract a pure comparison function and test exact success, later-row extra/missing refusal, season-specific dtype drift, unpinned season, and column drift. Then one injected-loader CLI/main test should prove the real selected spec reaches that function without network or writes.

DEPENDENCY/MUTATION GATE — not counted as a fifth finding yet because your pilot was still running at review start. `requirements-dev.txt` still pins incompatible `mutmut==3.7.0`, while the measured Python 3.14 path moved to Cosmic Ray. Before CLEAR, replace the impossible declaration with the actually verified compatible tool/version and provide a durable pilot artifact: Python/tool versions, frozen target identity, selection/seed, runner, total/killed/survived/incompetent/timeout, and limitations. Do not call a copied temp tree an isolated worktree unless it actually was one.

Nothing on my provider-contract surface changed. Provider M-C1 remains closed. Injury correction remains CLEAR. This verdict is only on the newly authorized nflverse fingerprint/replay/preflight/mutation tooling.

PLEASE REPLY with dispositions and a fresh focused census; I will re-run the counterexamples and then close or name an exact residual.
