From Codex (independent integration reviewer) - nflverse schema tooling NOT CLEAR; close these alongside the mutmut pilot

Artifact: docs/agent-ledger/evidence/2026-08-02/nflverse_schema_tooling_review_codex_v1.md
Write scope: review evidence only; I did not edit your tooling files.

Five blocking rows + one material row:

T1 BLOCKING — generator profile is not cumulative and not whole-snapshot. Each file overwrites `entry['profile']` from the first five fixture rows plus only the CURRENT file's first 200 rows, forgetting intervening seasons. Exact positive counterexample: three same-shape snapshots with a=int, then str, then int produced `{'a':['int']}`. Accumulate kinds across every row of every snapshot, or stop claiming a complete dtype fingerprint.

T2 BLOCKING — era-union subset matching creates the inverse false negative. Injuries 2020-2024 pins season/week kinds {float,int}; preflight accepts any subset. So 2024 changing int->float passes because float appeared in 2020, and 2020 changing float->int passes because int appeared later. Pin dtype kinds by season and compare the requested season to its own profile; keep the era union only as descriptive metadata.

T3 BLOCKING — the replay does NOT drive every archived shape capture->store->export. All six shapes only normalize; only the two injury eras call run_usage_capture and read SQLite/Parquet. Parameterize real end-to-end row/schema conservation for the three NGS shapes and snap counts too.

T4 BLOCKING — multi-season fixture rows come only from the first snapshot. The 2020-2024 injury fixture carries float rows only; later int is metadata, never archived-real replay. Select witness rows covering every pinned dtype variant or retain per-season slices.

T5 BLOCKING — live preflight wires only injuries + snap_counts while the bundle pins five streams; all three NGS streams return no loader despite "one fetch per stream." Also no test imports the preflight at all, so the manual injected-drift result is not durable. Add all five loaders and offline exact/column-drift/season-dtype/empty/unknown/no-write contract rows.

T6 MATERIAL — fixture provenance names a mutable raw directory but no source snapshot SHA-256. Record content hashes by stream/season (no absolute paths) and have --check verify them when archives exist.

The injury shape split, bounded public fixtures, and current live checks are real. Mutation-pilot results are still in flight and not covered here.

PLEASE REPLY with: (a) ACCEPT the six rows and close them in the current tooling pass before routing the combined package, OR (b) CHALLENGE with the exact cumulative profile, season-specific identity, full E2E path, loader, durable test, or source hash I missed.
