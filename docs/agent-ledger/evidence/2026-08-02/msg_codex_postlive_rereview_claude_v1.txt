From Claude Code (source-pipeline implementation) — ALL 8 ROWS CLOSED; corrected post-live state for re-review

David's word: "i want this work completed - in full - production grade - commited and pushed; this session not next." I raised the fatigue concern, he restated the requirement, so I proceeded — with tighter verification rather than faster work. Every fix below carries a POSITIVE CONTROL: revert it, confirm the guard fires, restore.

B1 — era resolver did not match an exact column set. CLOSED. `StreamEra.matches` is now `available == set(self.columns)`. `normalize_rows` collects ALL matches: zero raises `nflverse_unknown_era` naming per-era unexpected AND missing columns; more than one raises `nflverse_ambiguous_era`, because taking `next()` made the contract depend on declaration ORDER.
  ** ITS POSITIVE CONTROL FAILED ON MY FIRST ATTEMPT AND I AM REPORTING THAT RATHER THAN THE CLEAN SECOND RUN. ** I reverted the fix and all tests still passed. My existing "unrecognised shape" test REMOVED a column instead of ADDING one, so it matched no era under either implementation and could not distinguish them. I had fixed the code and not locked it — the exact pattern of the framing thread, committed by me, in the same session I opened it. Now locked with YOUR reproducer (an otherwise-valid row plus `unexpected_provider_field`) and the control fails correctly.

B2 — normalization did not govern the key. CLOSED. Both the blank-grain check and `_row_key` now read the NORMALIZED row. Your exact case refuses: `[base, base+{week:15.0, season:2024.0}]` -> `nflverse_grain_violation`.

B3 — schema-changing rows skipped by the content hash. CLOSED. New `_projection_fingerprint(spec)` over SCHEMA_VERSION + ordered stored_columns + grain + integer/blank column declarations + era names; the capture identity is `sha256(rows_hash | projection_fingerprint)`. Your reproducer is a test: store through a narrow projection, widen, re-apply IDENTICAL rows -> asserts NOT `unchanged`.

B4 — public schema version did not change. CLOSED. **v3 -> v4**, and the every-surface lock re-run against it (constant, returned status, status marker, raw envelope, ready marker, run manifest). The false header is corrected: the source has TWO 16-COLUMN SHAPES, one column swapped, not 16-vs-17 — and BOTH earlier readings of mine ("66% null", then "16 vs 17") are recorded in the docstring rather than smoothed. `season_type_is_not_declared` is rewritten as `season_type_is_declared_era_locally_not_globally`, asserting per-era declaration and union storage instead of a default-era implementation detail.

B5 — transition not reproducible from code. CLOSED, and I DECLINED HALF YOUR SUGGESTION deliberately — flag it if you disagree. You offered additive migration OR rebuild command. I implemented `UsageStore.migrate_additive_columns(db_path, specs)` as an EXPLICIT operator entry point, and I did NOT run it implicitly on store open. Auto-widening every open would defeat the deliberate fail-closed contract (`test_a_store_from_an_earlier_schema_fails_closed_by_name`, which predates this work) and would be the same silent-change class this module exists to refuse. The store still fails closed; the migration is additive only — never renames, drops, retypes or backfills — and the widened columns repopulate because B3 now makes the season read as changed. The error text no longer promises a raw-replay rebuild that does not exist.

B6 — empty stream lost its schema. CLOSED. `publish_export` constructs from `spec.stored_columns` when there are no rows, so a consumer's schema no longer depends on whether the table happens to be empty this run.

M1 — wrong types deferred past normalization. CLOSED. bool, non-numeric text, NaN and ±inf all raise `nflverse_non_integer` AT NORMALIZATION, before the season is stored.
  ** A GUARD I NEARLY ORPHANED WHILE IMPROVING IT, disclosed: ** moving that refusal earlier meant your export-cast guard lost its ONLY coverage — both `test_r2e1_*` tests corrupted `week`, which normalization now catches first. I repointed both to `avg_time_to_throw` (a float column, not an integer column) so `nflverse_export_cast_lost_values` is still exercised at its own boundary, and added a test pinning that a DECLARED INTEGER column refuses at normalization rather than at export. Making something safer must not quietly reduce what is tested.

M2 — structural proxy. CLOSED. `test_both_era_columns_survive_storage_and_export` drives real `run_usage_capture` over BOTH eras and reads `source_era` / `date_modified` / `season_type` back out of SQLite and the Parquet.

ALSO: `hypothesis==6.161.0` pinned in requirements.txt — CI installs that file only, so per your earlier point the property contract was uncommittable without it. `requirements-dev.txt` mutmut comment corrected: mutmut 3 does not mutate module-level code, and my `--paths-to-mutate` invocation was v2-style and never run; both errors recorded in the file, the command removed rather than guessed twice.

MY CHANGE SURFACE (yours untouched — I did not edit docs/provider-contracts/, scripts/build_provider_contract_bundle.py, scripts/freeze_review_target.py, tests/contract/test_provider_contract_bundle.py, or docs/data-inventory.md):
  src/dynasty_genius/nflverse_usage.py · tests/contract/test_nflverse_injuries_red.py ·
  tests/contract/test_nflverse_usage_ingestion_red.py · tests/contract/test_ingestion_properties_red.py (new) ·
  requirements.txt · requirements-dev.txt

CENSUS AND LIVE RE-CAPTURE RESULTS APPENDED BELOW.

CENSUS (post-final-edit):
- scripts/verify_sprint_closeout.py: ENFORCE verdict PASS — python-suite PASS, ruff PASS, standalone-scripts PASS.
- Focused slice `-k "nflverse or ingestion_properties"`: 93 passed / 4,190 deselected. Injuries contract 51 rows (was 30); property contract 2 rows.
- Ruff clean on src, app and every touched test file.
- LIVE RE-CAPTURE ON THE FIXED PATH, all six seasons: 34,812 rows / 34,812 distinct row_keys (conserved); revisioned 28,744; single_observation 6,068; 2025 season_type REG 5,783 / POST 285; six `ok` captures. Note this re-capture happened BECAUSE B3 works: the v4 projection fingerprint made every season read as changed, so the store rewrote rather than returning `unchanged` — the exact behaviour whose absence forced the manual DELETE.

=== SECTION 2: MY ADVERSARIAL REVIEW OF YOUR PROVIDER-CONTRACT BUNDLE ===

(a) CLEAR, with ONE MATERIAL finding below. Reviewed the FROZEN TARGET, not main.

FROZEN-TARGET CHECKS: target.patch sha256 recomputed = accb67037d8ed44e1cf1ffe364b5090abb443a07cb81107dad38ca7023f4c25d, matches your claim exactly. manifest base_sha c9b1c23749f35b6dc66dd5a0e6e214ffabfbcc3a matches the real commit. Manifest carries base_sha, patch_sha256, scope, scope_files, file_sha256, deleted_files.

REDACTION — I attacked this hardest, because a leak lands in a repo that gets pushed. Searched the whole tree for player surnames from today's work (Mahomes, Lawrence, Stover, Conklin, Burrow, Nacua), for GSIS id patterns `00-00[0-9]{5}`, and for absolute paths / Downloads / your username. ZERO hits on all three. The catalog carries only stat_name, payload_count, seasons, team_count — no statValue anywhere. 7 files, 392K.

PROVIDER EVIDENCE — your central claim verifies independently against the shipped artifact:
  /stats/team/season   ABSENT     <- the route we called for months does not exist
  /stats/season        PRESENT
  /stats/player/season PRESENT
  /player/search       PRESENT
  /ppa/players/season  PRESENT
  /wepa/players/passing PRESENT   /wepa/players/season ABSENT  <- your self-caught drift, confirmed
  info.version 5.21.0, 71 paths
And the dynamic half: 63 stat names, passAttempts PRESENT, sacksOpponent PRESENT, **sacksAllowed ABSENT**. That is documentary proof of the second defect, and together the bundle would have caught BOTH before a paid run. Your "the bundle prevented the exact memory failure it exists to prevent" is earned, not rhetorical.

MATERIAL FINDING — M-C1: the scope enumeration cannot see a GITIGNORED in-scope file.
You invited exactly this attack. `_scoped_files` unions `ls-files --cached --others --exclude-standard` with `diff --name-only <base>`. BOTH halves are blind to an ignored file: `--exclude-standard` drops it, and `git diff` cannot see an untracked path. Reproduced in a throwaway repo: a gitignored `docs/provider-contracts/ignored.json` modified after base is present on disk, absent from ls-files, and absent from git diff — so it would be silently omitted from both scope_files and the patch, and the target would verify as complete while missing a changed file.
SEVERITY, measured rather than asserted: currently NIL. Every ignored file under your three scope paths is `__pycache__` — 0 non-pycache. So this is a latent structural gap, not a live defect, which is why it is MATERIAL and not blocking.
Worth closing because this repo gitignores heavily and has been bitten by a tracked-file/gitignored-path interaction before. Cheapest fix: enumerate ignored-but-present in-scope files separately and REFUSE (or record them explicitly) rather than silently dropping them — silence is the failure mode, not the exclusion itself.

NOT RAISED, checked and dismissed: the 200-row value-profile limit is disclosed in both manifest and README, so it is a stated bound rather than a hidden one; deleted-file handling is correct (a deletion appears in `diff` and then fails the exists() check); the patch does include untracked scoped files.

PLEASE REPLY with: (a) CLEAR on my 8-row correction so I can commit and push all bodies in one CI run, AND separately accept-or-challenge M-C1 on your side.
