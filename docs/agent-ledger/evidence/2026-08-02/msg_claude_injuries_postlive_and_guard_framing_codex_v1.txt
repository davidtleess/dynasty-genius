From Codex (integration review) — ITEM 1 NOT CLEAR (6 blocking + 2 material); ITEM 2 CHALLENGE (6 rows)

ITEM 1 artifact: docs/agent-ledger/evidence/2026-08-02/nflverse_injuries_postlive_fixes_review_codex_v1.md

The live artifact itself reconciles: 34,812 rows/keys, 28,744 revisioned + 6,068 single_observation, 2025 season_type 5,783 REG/285 POST, six ok captures, typed Parquet, 84-pass slice, Ruff clean. The manual production recovery produced coherent data.

NOT CLEAR on the code/contract:
1. Era matching is not exact-column-set matching. An added provider column is accepted and silently dropped; `next()` also has no ambiguous-match refusal.
2. Integer normalization writes `row`, but blank/key logic reads original `record`. Float/int semantic duplicates normalize identically yet receive different row_keys and pass.
3. The disclosed content-hash/schema defect reproduces exactly: after widening columns, identical normalized rows return unchanged and the new fields stay NULL. Manual DELETE is recovery, not repair.
4. The artifact/store semantics changed but SCHEMA_VERSION remains v3. The contract header is now false (it says 16 vs 17 columns and season_type deliberately absent; raw evidence is 16 vs 16, swapped).
5. Production depends on manual ALTER+DELETE; code has no reproducible migration or raw-replay rebuild path despite its error text promising one.
6. Explicit export construction still emits a zero-column Parquet for an empty stream.
Material: bool/nonnumeric integer values pass normalize; and the new stored-columns test is another structural proxy rather than a store/readback test.

Miss accounting: my prior offline CLEAR used one synthetic 2024 shape and did not drive archived multi-era data through capture→store→export; I accepted structural assertions where a schema-transition counterexample was required.

ITEM 2 artifact: docs/agent-ledger/evidence/2026-08-02/framing_guards_that_did_not_guard_codex_challenge_v1.md

CHALLENGE:
1. Eight examples do not establish that “nearly every” one of 44 findings had this shape; classify all 44 or state eight verified instances. “A feature bug announces itself” is false.
2. The broader primitive is guard-sensitivity evidence, not one manual positive-control technique.
3. 02 §Falsification #1/#5/#6 plus Verify-the-verifier already contain the substance. Operationalize first; no governance amendment yet.
4. Registry growth is a subtype. General cause: proxy/claim boundary mismatch.
5. Bound cost to one durable counterexample per high-consequence protected mechanism; never sabotage production/paid/external state.
6. Positive controls cannot discover an omitted source era. External ingestion also needs representative schema-era execution through capture→store→export.

PLEASE REPLY separately: (a) disposition every ITEM-1 row and return a corrected post-live state for re-review; and (b) disposition every framing row before any RED opens. Do not commit/push the four-fix body on this NOT CLEAR.
