From Codex (reviewing lane) [w#l1-export-red-review-1] — (b) EXPORT RED NOT CLEAR at ca4cac7c; live-status RED also required

I recomputed candidate SHA-256 `ca4cac7cec6e20a1c18ae861685dae3609ba1f266c97deb435e8c63c8b63b8ee`, reproduced 3 failed / 2 passed, and reproduced Ruff clean. Three findings remain:

1. **Wrong production type.** The unresolved artifact's `season` is production TEXT, not Int64. `publish_export()` reads `season_ingested` from SQLite TEXT, and the Aug. 5 last-good unresolved Parquet measures `season: String`. The fixture's integer `2024` and expected Int64 would authorize an incompatible consumer change while fixing `snapshot_id`. Use `"2024"` and require String. The new snapshot-related columns must also exist on an empty artifact.
2. **The disclosed unseeded bound is not acceptable.** The successful-export test must prove the live combination that failed: a populated seasonal stream followed by a populated contracts snapshot. `assert "contracts" in json.dumps(manifest)` is only a substring check and the unseeded store makes the success path vacuous. Pin `snapshot.name == "contracts"`; seed it through the real `normalize_rows(..., season=None)` plus `apply_snapshot()` path (the existing contracts contract tests demonstrate the exact pattern); assert `manifest["files"]["contracts"]["rows"] > 0`; read the contracts Parquet; and assert `unresolved_identity.parquet` contains the contracts row with its non-null snapshot ID. A unit helper plus an empty end-to-end path is not enough for the exact production failure.
3. **The separately routed live-status residual is absent.** The canonical aggregate now says failed plus `last_success_at=null`, `age_days=null`, `freshness=unknown`, even though the explicit last-good ready marker proves the Aug. 5 success. Add a RED where a failed primary marker plus its explicitly configured prior ready marker reports failure and the prior success time/age together. Run outcome and last-good freshness are different axes. Use an explicit manifest field, never a source-name inference or directory scan.

The failure-injection patching defect visible in the earlier in-progress file has already been repaired in this candidate; it is not repeated as a finding. The existing marker-byte-preservation and cleanup REDs are correctly scoped.

Return the repaired export RED and status RED together, with their exact failure reasons and no GREEN. QB rushing remains a registered hypothesis under test with no result.
