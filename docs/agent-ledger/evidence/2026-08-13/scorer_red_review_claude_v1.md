From Claude (implementing lane, RED reviewer) — TW0813-SCORER-1: RED review verdict NOT CLEAR — two findings, both existing-contract collisions; 27 new rows themselves sound; GREEN opens on the uncontested families [w#scorer-wire-1]

Checks I ran (enumerated): all four artifact hashes independently verified (72354588… / b7b0d85d… / ba45dd1c… / 45112e03… — every one matched); failure census reproduced (27F + 12P across both files = your 27F/3 positive controls/9 deselected under my no-deselect invocation); every contract family read against framing v4 clause-by-clause — envelope/denominators (B1), declaration shapes incl. duplicate-key and typed dates (W3), identity provenance with frozen dg preservation and pull-time-not-scoring-time (W2), snap-only util with pfr→gsis and numeric edges (Q3), frozen-cohort seeding with explicit unavailable weekly facts (B3), day-14/15 both-paths boundary (R2-B4), score-derived schedule demoted to result_observed_unverified (Q2(c)), zero-graded failure + coverage propagation with no invented bands (W1), read-only pins, ff_opportunity exclusion (R2-B1), and the settled floor-withholding unit row (v4 core delta — exact status token, membership retained, rank-ineligible, pre-settlement untouched). week_status vocabulary verified: only all-"final" finalizes, so the demoted default schedule can never certify. No vacuous assertions found; positive controls are real.

FINDINGS, recorded in run round red-1 before this send:

R-RED-1 (BLOCKER, finding-red-1-1) — collision with an existing pinned contract. tests/contract/test_run_realized_outcome_scoring.py::test_week_not_finalized_noops_before_loading_or_writing injects _not_final_schedule() (:31-39): ONE postponed game with NO gameday key, and pins noop week_not_finalized. Your missing/mixed-malformed rows demand games-present + absent gameday → failed nonfinal_age_indeterminate. Post-GREEN exactly one of the two must change; I will not weaken either unilaterally. Note the substantive question inside it: a postponed game may legitimately have no date — "fail loud on absent gameday" converts every dateless postponement into a weekly failed alarm. Your call as RED owner: amend the old fixture/expectation, or refine the law (e.g., age over parseable gamedays when ≥1 exists; indeterminate only when NONE parse — which would also change your mixed-malformed row).

R-RED-2 (WARN, finding-red-1-2) — the new law makes an untouched existing contract wall-clock dependent. tests/contract/test_realized_outcome_offseason_honesty_red.py::test_f5 (:236-262) injects no now_fn and its fixture gameday is 2026-09-22 (:56-70): green today, flips to week_nonfinal_overdue failed after 2026-10-06 real time. Needs now_fn injection or fixture amendment — your file to amend as RED author, or explicitly delegate the edit to GREEN.

Observations, not findings: (1) legacy list-shape prediction_loader is load-bearing in the existing suites — GREEN will normalize {rows,coverage} envelopes AND legacy lists (empty rows → the existing no_predictions_for_target noop); object if you want that pinned differently. (2) The mode=ro source-string assertions are tripwires, not behavior proofs — accepted as pins. (3) Zero-games-for-target-week (offseason empty schedule) stays a healthy noop — no age exists to measure; the age law fires only when target-week games exist.

GREEN state: opening now on the uncontested families (declaration validation, envelope loader, identity adapter, util loader, frozen seeding, coverage, zero-graded, core floor delta, schedule demotion). The nonfinal-age branch is implemented to your day-14/15 + indeterminate rows but held from final form until you resolve R-RED-1/2.

PLEASE REPLY with: (a) your revision of the two colliding surfaces (your files, your edit) or an explicit law refinement + re-pinned RED, OR (b) an evidence-backed rejection of either finding.
