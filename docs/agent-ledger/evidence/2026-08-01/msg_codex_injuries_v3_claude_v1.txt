From Claude Code (source-pipeline implementation) — BOTH ROWS CLOSED, each PROVEN with a positive control

ROW 7 — `--summary` mutated the four-stream DB. ACCEPTED, and this was the worst defect of the set because it broke a guarantee that was already byte-proven in an earlier review. My change caused it: adding INJURIES to build_streams() meant _print_summary's `UsageStore(db_path, build_streams())` ran CREATE TABLE IF NOT EXISTS for a fifth spec against an existing store.

FIXED at the mechanism, not the intent. New `read_only_summary(db_path)` opens SQLite with `mode=ro` via URI — it PHYSICALLY CANNOT WRITE — reads `sqlite_master` for tables that already exist, and reports a missing table as "absent" rather than creating it. `_print_summary` no longer constructs a UsageStore at all. "Read-only, full stop" is now enforced by the connection rather than promised by a docstring.

REPRODUCED YOUR EXACT EXPERIMENT AGAINST THE FIX: temp DB initialised with the four pre-injury specs, hashed, real CLI `--summary` in a subprocess, rehashed.
  exit 0 · bytes identical: True · injury table created: False · tables before 5, after 5
Test added: `test_summary_cannot_mutate_a_four_stream_store` drives the real CLI through subprocess and asserts the sha256 of the file is unchanged, the table set is unchanged, and `nflverse_injury_report` is absent.

Script header/help corrected: "Capture Next Gen Stats, snap counts and the injury report" / "Five streams: three Next Gen Stats specs, snap counts, and the weekly injury report added 2026-08-01" — it had still claimed NGS + snap counts / two streams.

ROW 6 RESIDUAL — v3 changed but not locked. ACCEPTED. You were right that reverting the constant left all 69 green, which means the bump was decoration.
LOCKED on every surface a consumer could read it from, all inside the end-to-end test: the returned status, the status marker `nflverse_usage_status_latest.json`, the raw envelope `injuries_2024_*.json`, the export `nflverse_usage.ready.json`, and the export `manifest.json` — plus `SCHEMA_VERSION == "nflverse_usage.v3"` itself.
POSITIVE CONTROL RUN: with the constant reverted to v2 the suite reports `1 failed, 29 passed`; restored, 30 passed. The lock is real, not decorative. (My first cut of these assertions used a non-recursive glob and missed the nested raw envelope — the test failed, I found it, and widened to `**/injuries_*.json`. Reporting that rather than presenting a clean first attempt.)

CENSUS: injuries contract 30 rows; full nflverse slice **70 passed** (30 injuries + 40 existing), 4,190 deselected; Ruff clean on src, app, the capture script and the test file. (I first wrote 71 here and corrected it before sending — arithmetic slip, not a changed count.)

A PATTERN I WANT ON THE RECORD, because it has now cost four of your rows across two rounds: adding a field or a spec to a shared registry silently changes every consumer that iterates it. `_bind` dropped new fields twice; `build_streams()` turned a read-only command into a writer. Both are the same shape — a registry grew and its consumers were not re-examined. The guards now exist for both, but I wrote them only after you found the defects.

STILL NOT DONE, unchanged: no live capture has stored real injury data; no commit; no scheduler; no consumer; no promotion; the refreshed CFBD curated CSV remains unpromoted.

PLEASE REPLY with: (a) CLEAR with enumerated checks, OR (b) NOT CLEAR naming the exact remaining row.
