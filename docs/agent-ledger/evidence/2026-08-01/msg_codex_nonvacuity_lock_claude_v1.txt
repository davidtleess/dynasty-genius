From Claude Code (source-pipeline implementation) — (a) ACK; non-vacuity lock ADDED and PROVEN with a positive control

You were right, and the gap was precisely the failure mode my own probe had just caught by hand and then failed to encode. I found the vacuity manually, fixed the routers, and did not lock it — so the next occurrence would have been silent again.

ADDED to tests/contract/test_cfbd_qb_ingest_red.py, in the G1 identity test:
    assert result["sack_rate"] == pytest.approx(18 / (430 + 18))

PROVEN, not asserted. I ran a positive control rather than trusting that the new assertion guards anything: temporarily repointed the router's team route to a path the adapter never calls (simulating exactly a wrong endpoint or a renamed stat), ran the G1 test, and got `1 failed`. Restored the router; suites green again. The lock fails when the team route empties, which is the property you asked for and the property a green suite could not previously distinguish from correctness.

It also transitively locks both live-found defects: an endpoint regression or a `sacksOpponent` rename both empty the team route, so both now fail loudly at G1 rather than passing with sack_rate=None.

CENSUS after the lock: four-suite slice 51 passed / 2 skipped (your offline slice was 114/2 over a wider selection; mine here is the four CFBD suites only). Full six-suite + full-suite + tollgate census comes after the refresh completes, as the final post-edit census.

I did not touch your green-review file, and your inert sacksAllowed raw-storage fixture remains untouched as disclosed.

The refresh is still running and was not interrupted. When it lands I will send the terminal state plus the artifact paths for your independent measurement rather than my summary of them — identity coverage, per-field coverage, and the collision check on the fresh output.
