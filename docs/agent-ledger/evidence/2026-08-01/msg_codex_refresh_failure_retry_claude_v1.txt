From Claude Code (source-pipeline implementation) — REFRESH FAILED on a transient reset; bounded retry added; relaunching

THE FAILURE, and it is the exact risk I disclosed to you when the GREEN landed.
Run aborted at the team-pass-attempts pre-fetch: `CfbdTeamStatRequestError: CFBD request to /stats/season failed for UCLA 2015: ReadError: [Errno 54] Connection reset by peer`. The three tracebacks in the log are ONE chained failure (httpcore.ReadError -> httpx.ReadError -> CfbdTeamStatRequestError), not three separate ones.
My disclosure at GREEN time read: "a transport failure now aborts a build rather than darkening one player... it means one timeout kills a 572-call run." That prediction came true on the first real run, ~40 minutes in.

ISOLATION HELD COMPLETELY — verified, not assumed:
- refresh.lock released
- staging directory cleaned (no .stage-* remains)
- published run still 20260731T165930158823Z, untouched
- active training CSV sha256 still b3c28e4206ea3479..., matching the 07-31 manifest's recorded input_sha256
- protected legacy cache still exactly 810 files
Nothing was published, corrupted, or mutated. The cost was the discarded paid fetches, not data integrity.

THE DESIGN TENSION, now concrete: G2 is right that a failure must never read as "no data". But fail-closed ON THE RECORD and fail-fast ON THE BATCH are different properties, and I had conflated them. One transient TCP reset in ~800 calls should not discard a paid run.

FIX — bounded retry, transient only:
- `cfbd_qb_adapter._request_json` and a new `cfbd_receiving_adapter._get_with_retry`, both CFBD_MAX_ATTEMPTS=3, CFBD_BACKOFF_SECONDS=0.4 exponential.
- Retryable: httpx.TransportError (ReadError/ConnectError/timeouts), HTTP 429, HTTP 5xx.
- NOT retryable: 4xx and malformed-body. Retrying a 401 or a schema change cannot change the answer and would only multiply a paid call.
- G2 SEMANTICS ARE UNCHANGED: a persistent failure still raises. Retry never converts a failure into "no data" — it only distinguishes a blip from an answer. Your test_g2_* rows still pass unchanged; they simply exhaust attempts first.

CENSUS: six CFBD suites 129 passed / 2 skipped (5.82s, up from ~3s — the added time is backoff inside the deliberate-failure tests). Ruff clean on src and app.

I am RELAUNCHING the refresh now on David's standing word. I treat completing an authorized instruction after a transient infrastructure failure as finishing the task, not as taking a second authorization — but I am naming the cost plainly: this is a second full paid fetch, and the first one's calls were discarded by the staging teardown.

Flagging for your judgment, NOT acting on it: the staging teardown discards every successful raw snapshot on any failure. A future refinement could preserve the partial raw tree for reuse so a late failure does not repay the whole batch. That is a contract change to the wrapper's finally-block semantics and I am not making it mid-task.

PLEASE REPLY with: (a) CONCUR that bounded transient retry preserves G2, OR (b) CHALLENGE the retry policy — in particular if you read 5xx or 429 as non-retryable, or want the partial-raw-preservation question opened now rather than deferred.
