From Claude Code (source-pipeline implementation) — (a) ACCEPT both rows; classifier corrected, transitions tested, shared helper proven

Both findings accepted, neither challenged. I reproduced your classifier probe against my own code before accepting rather than taking it on your word — identical results: 408 False, 429 True, 500 True, 501 True, 502-504 True, 505 True; ReadError/ConnectError/UnsupportedProtocol/LocalProtocolError/RemoteProtocolError all True.

ROW 2 — the classifier was wrong at BOTH ends, which is worse than merely broad.
408 Request Timeout — the one status that IS explicitly a timeout — did NOT retry, because my rule was "4xx never retries". Meanwhile 501 Not Implemented, 505 HTTP Version Not Supported, UnsupportedProtocol (bad URL scheme) and LocalProtocolError (a malformed request WE built) all DID retry, each of them deterministic and each multiplying a paid call for nothing.
Your set adopted exactly: retry TimeoutException + NetworkError + RemoteProtocolError, statuses {408,429,500,502,503,504}. Verified after: 408 True, 501 False, 505 False, LocalProtocolError False, UnsupportedProtocol False, ReadError/ConnectTimeout/RemoteProtocolError True.
Retry-After honouring under a cap: agreed as preferable but secondary, and deliberately NOT built now.

SHARED HELPER, not two copies. New src/dynasty_genius/adapters/cfbd_http.py holds one classifier and one retry loop; both adapters use it, and the new suite exercises both. Two copies of a policy drift, and I had just created two.

A NEAR-MISS I AM DISCLOSING RATHER THAN QUIETLY FIXING. My first cut of the shared helper moved the `httpx.get` call itself into cfbd_http. That would have silently broken EVERY `patch("<adapter>.httpx.get")` target — including your G2 row in the green-review file — and the affected contract tests would have reached the REAL NETWORK instead of failing loudly. I caught it by reasoning about the patch targets before running the suite, not after.
Fix: cfbd_http shares the POLICY (`with_retry(operation)`), each adapter still issues its own httpx.get. Every existing patch target works untouched, and I did not have to edit your file. The module docstring records why, so nobody "simplifies" it back.

ROW 1 — transitions now tested. New tests/contract/test_cfbd_retry_policy.py, 29 rows, `time.sleep` patched so backoff costs no wall-clock:
- classification: 11 status rows + 8 transport rows, including the four cases your probe exposed
- ReadError -> success: exactly 2 calls, 1 sleep
- persistent ReadError: exactly 3 calls (== MAX_ATTEMPTS), 2 sleeps, typed raise
- retryable status (503) -> success: 2 calls
- definite status (401/404/501): exactly 1 call, 0 sleeps — "a definite answer must cost exactly one paid call"
- raw_sink untouched on exhaustion; populated exactly once on recovery
- both adapters exercised: QB transient-retry and QB definite-no-retry rows prove the shared policy applies on that side too

CENSUS: seven CFBD suites 158 passed / 2 skipped. Ruff clean on src, app and the new test file. Full-suite + tollgate come after the refresh as the final post-edit census.

REFRESH STATUS: still running, 848 raw files staged — well past the 245 where the first attempt died. Its in-process code is the pre-tightening policy, which as you noted classifies the actual ReadError as retryable either way, so the run remains valid.

PLEASE REPLY with: (a) RETRY POLICY CLEAR, OR (b) CHALLENGE naming the exact classification row or transition still unlocked.
