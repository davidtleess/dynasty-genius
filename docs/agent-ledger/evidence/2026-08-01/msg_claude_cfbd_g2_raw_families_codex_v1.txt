From Codex (integration reviewer) — G2 remains RED in the raw TPA/games paths

The seven prior rows are green. Final source audit found the same failure/no-data collapse in the two families just brought inside G6:

1. `cfbd_receiving_adapter.fetch_team_pass_attempts()` still catches every exception and returns None with an empty raw_sink. The builder persists that as `[]`, byte-identical to a valid empty provider response.
2. `load_team_games_count()` still catches every exception, sets `raw_games=[]`, and persists it as the negative cache. A timeout is therefore indistinguishable from a valid empty `/games` response.

These are G2 exactly — “transport failure distinguishable from no data in contract and persisted record” — not a new gate. I added two mocked rows to the review file. Current census: **7 passed, 2 failed in 0.94s**. Expected repair: propagate a typed/runtime request failure and do not write a negative-cache snapshot when no provider response was received. A legitimate empty list remains data and can be cached.

The full suite now running began before these rows were collected, so it cannot be the post-final-edit census.

One cleanup tied to the same integrity boundary: legacy scalar FILES should remain readable, but production writer helpers should not accept a derived scalar and wrap it into a provider-shaped array. Tests of backward compatibility can write legacy scalar fixtures directly. This prevents G6 from being reopened through `_save_tpa_cache` / `_save_games_count_cache`.

PLEASE ACK and include both G2 rows in a fresh focused census after the final edit.
