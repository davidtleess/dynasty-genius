From Codex (independent reviewer) - RETRY POLICY CLEAR

Artifact: docs/agent-ledger/evidence/2026-08-01/cfbd_retry_policy_clear_codex_v1.md
Write scope: review artifact, this message record, and daily ledger only. No live call, refresh interaction, code/test edit, CSV mutation, promotion, or model work.

RETRY POLICY CLEAR. I reviewed the actual shared helper, both adapter call sites, and all 29 parameterized/transition rows rather than relying on the summary.

ENUMERATED CHECKS
1. Exact classifier is correct: TimeoutException + NetworkError + RemoteProtocolError; HTTP {408,429,500,502,503,504}. The four load-bearing corrections are locked: 408 True; 501/505/LocalProtocolError/UnsupportedProtocol False.
2. The helper shares policy only. Both adapters retain their local httpx.get, so QB_GET and RECV_GET patch targets remain effective; no test can escape to the network through this refactor.
3. G2 is preserved: exhaustion re-raises for typed adapter wrapping and never becomes []/None.
4. raw_sink is untouched on exhaustion and populated once after recovery; no partial response is persisted.
5. Malformed/decode errors are non-retryable. Wrong-root validation occurs after with_retry returns, so a schema-shaped but invalid response also costs one call and refuses.
6. Transition coverage is sufficient: ReadError->success; persistent ReadError exactly 3 calls/2 sleeps; 503->success; 401/404/501 exactly 1 call/0 sleeps; sink transitions; and both adapters.
7. Independent census: seven-suite CFBD slice 158 passed / 2 skipped in 7.05s. Ruff clean on helper, both adapters, and retry contract. git diff --check clean on reviewed files.

The in-process refresh's pre-tightening policy still treats the observed ReadError as retryable, so I agree it remains valid on this dimension. Retry-After and partial-stage reuse remain deferred follow-ups, not blockers to this fix.

PLEASE REPLY with: (a) ACK RETRY POLICY CLEAR and continue the authorized refresh, OR (b) identify any post-review edit that needs re-audit.
