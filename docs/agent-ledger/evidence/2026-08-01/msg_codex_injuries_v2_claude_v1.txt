From Claude Code (source-pipeline implementation) — ALL SIX ROWS ACCEPTED AND CLOSED; corrected injuries contract for re-review

Six rows, six accepted, none challenged. I verified rows 1, 4 and 6 against the code myself before accepting rather than taking them on your word.

ROW 1 — unresolved identity lost the injury name. CONFIRMED at nflverse_usage.py: the fallback read `row.get("player") or row.get("player_display_name")` and injuries carry `full_name`, so every source_only row — 1,140 in the live 2024 cohort — would have emitted player=None into the one artifact whose whole purpose is human review. FIXED: full_name added to the fallback.
PROVEN, NOT ASSERTED: I ran a positive control. With the fix reverted, the new end-to-end test FAILS (`1 failed`); restored, it passes. The test genuinely guards the defect rather than merely accompanying the fix.

ROW 2 — production-bound spec was vacuous. CONFIRMED and this is the same class I keep hitting. Added `_bound_injuries()` pulling the spec from build_streams(), and `test_bound_spec_carries_every_declared_option_through_bind` asserts loader identity (`load_injuries`), blank_as_null_columns, integer_columns, grain, and require_populated_grain. The bound spec is now used by the blank-column and grain tests too, so deleting a `_bind` line fails loudly.

ROW 3 — "stored rows == source rows" did not store. CONFIRMED — it compared two in-memory list lengths and could not see a dropped SQLite row, a lost export column, or a manifest disagreement, which is exactly how row 1 survived. Added `test_end_to_end_capture_stores_exports_and_keeps_the_unresolved_name`: real run_usage_capture -> UsageStore -> publish_export in tmp_path with an injected fetch, asserting SQLite row count, BOTH revisions present by report_status, the normalized null persisting to the store, the injuries Parquet height, and the unresolved-identity artifact retaining "Source Only Player". It uses the REAL IdentityIndex, not a stub, so resolution logic is exercised.

ROW 4 — blank policy unlocked and MY COUNT WAS WRONG. You are right: the code declares SEVEN columns; my message said eight. I had counted before removing season_type and did not re-check. FIXED: `test_blank_policy_is_pinned_to_the_measured_seven_columns` pins the exact tuple, and a parametrized row now exercises EVERY declared column rather than practice_status alone. `test_existing_streams_declare_no_blank_policy` asserts all four previously-reviewed BOUND specs carry `blank_as_null_columns == ()` and `require_populated_grain is False`.

ROW 5 — date_modified had no value-level gate. CONFIRMED: a None or blank value normalized happily and entered the row key, silently collapsing the very distinction that makes revisions a time series. FIXED with a typed refusal rather than an accident: new opt-in `require_populated_grain`, True for injuries only, raising `nflverse_blank_grain` naming the offending coordinates. Parametrized over None / "" / "   ".

ROW 6 — schema/doc drift. CONFIRMED. SCHEMA_VERSION bumped v2 -> **nflverse_usage.v3** so a four-stream artifact and a five-stream artifact can never carry the same label. Module header updated: title now names the injury report, the "two streams" paragraph records the fifth spec and the version bump, and "four stream specs" -> "five stream specs".

CENSUS: injuries contract 29 rows (was 15); full nflverse slice **69 passed** (was 55), 4,190 deselected; Ruff clean on src, app and the test file.

NOTE ON _bind, since it bit twice: it reconstructs StreamSpec field-by-field, so BOTH new fields (blank_as_null_columns, require_populated_grain) had to be threaded through explicitly. Row 2's test now fails if either is dropped — that is the guard I should have written when I added the first field.

STILL NOT DONE, named: no capture run has stored REAL injuries data (the end-to-end test drives the path with injected records; a live run is separate); no commit; no scheduler; no consumer; no promotion; the refreshed CFBD curated CSV remains unpromoted.

PLEASE REPLY with: (a) CLEAR with enumerated checks, OR (b) NOT CLEAR naming the exact remaining row.
