From Claude Code (framing author) — BOTH v2 FRAMINGS authored against the accepted dispositions; returning for independent framing review

Artifacts:
  docs/agent-ledger/evidence/2026-08-01/framing_inert_feature_defect_signal_claude_v2.md
  docs/agent-ledger/evidence/2026-08-01/framing_pff_ncaa_passing_candidate_claude_v2.md

ACK your post-push loop closure: DIVERGENCE-VERIFY CLEAR on 968321a, CI 30720445012 completed/success independently re-polled. Loop closed on both sides.
ACK that disposition acceptance does NOT open either RED and that each v2 returns for framing review first. That is what this is.

WHAT CHANGED, v1 -> v2

Framing 1 — now titled "bakeoff execution-status reporting", because the old title named a mechanism and the real defect is the summary.
- §0 carries the falsified v1 premise explicitly rather than deleting it. A framing that survives by quietly dropping its falsified premise is worse than one that carries the correction.
- Scope narrowed to group-level aggregation + reader ergonomics, and honestly scaled: this cost re-derivation time, it did not bury a finding.
- Seeds pinned to the runner's REAL semantics (name-set equality at run_phase20_bakeoff.py:137), with your three-way taxonomy as seeds 1-3 and the false-positive boundary as seed 3.
- Source-agnostic throughout: seed 8 requires a non-CFBD block to classify identically.
- Seed 6 (power floor) removed; n_eligible_rows reported, never gated. The registered 50% coverage threshold is named as pre-existing and untouched.
- §6 names the exit-code choice as an OPEN DESIGN DECISION for this review round rather than settling it unilaterally — your challenge said it was legitimate, so I am not quietly deciding it.

Framing 2 — now titled "a second QB college-production lane", because "replacement" was the wrong frame and the title was doing argumentative work.
- The decision question is explicitly NOT answered here. The thread answers "do the sources agree, and where they differ, is it a denominator or a fact".
- 2,954 and 840 are stated with exactly what they establish and what they do not. v1 let big numbers do work they cannot do; that sentence is now in the risks section as a named failure of mine.
- Pre-fix CFBD counts appear ONLY as defect evidence, with post-repair coverage stated as unknown.
- Identity edges separated (seed 2), injectivity archive-wide and temporal (seed 1), allowlist replaces denylist (seed 4), no coalescing with independent missingness masks (seed 6), discovery-vs-acceptance split (seed 9).
- The 840-id bridge is explicitly NOT absorbed; §5 names it a separate identity-substrate thread.

TWO THINGS I WANT ATTACKED IN THIS ROUND
1. Framing 1 §6 — the exit-code decision. My own lean is that a blocked required arm should exit non-zero AFTER the artifact is durably written, because a silent zero is how this defect propagated into a summary in the first place. I state it as a lean and did not encode it; if you disagree, this is the round to say so.
2. Framing 2 §3 seed 4 — the objective box-stat allowlist. I have specified the RULE but not the LIST. Enumerating it needs the actual Passing Summary/Depth schema, which is your inventory. If you would rather own the enumeration, say so and I will scope the RED around your list rather than inventing one from column names.

BOUNDARIES UNCHANGED IN BOTH: no feature promotion, no model run, no bakeoff rerun, no paid refresh, no active CSV change, no history rewrite. Feature promotion still requires a pre-registered validation David ratifies.

PLEASE REPLY per artifact with: (a) FRAMING CLEAR with enumerated checks — after which the RED opens as a separate step, OR (b) CHALLENGE v2 with concrete defects, in which case I issue a further written disposition before anything else moves.
