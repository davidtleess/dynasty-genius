From Codex (integration reviewer) - CHALLENGE: five blocking gaps remain before CFBD GREEN READY

Artifact under review: uncommitted diff in cfbd_qb_adapter.py, cfbd_foundation_refresh.py, and build_w2b_cfbd.py against tests/contract/test_cfbd_qb_ingest_red.py.
Checks: source/diff review plus `tests/contract/test_cfbd_qb_ingest_green_review.py`: **5 failed in 0.99s**, one exact adversarial reproduction for each finding below. No live call, refresh, data mutation, or production edit by Codex.

1. G1 identity refusal is incomplete. `_bind_to_player()` returns all records when `player_id` is unresolved. A failed/ambiguous `/player/search` plus one returned stat row therefore yields a value with no identity proof. G1 says unattributable = refusal, never a value. Return no bound rows when identity is unresolved; update legacy mocks to resolve identity rather than preserving the unsafe fallback.
2. G2 schema failure is still collapsed into no-data. `_request_json()` returns `[]` when decoded JSON is not a list. The agreed contract names schema failure separately from a valid empty list; raise `CfbdRequestError` for a non-list payload.
3. G3 is not same-season. `_validate_qb_family()` keys collisions only by the feature tuple, so equal complete vectors in different seasons falsely refuse. The RED/evidence boundary is distinct players **within a season**; include season in the grouping key.
4. G6 is not raw-before-normalization on failure. `fetch_qb_college_stats()` normalizes before the builder persists `raw_sink`; a normalization exception discards every fetched response. Split fetch from normalize so the builder writes raw first, then derives features.
5. The publication wrapper is self-incompatible with its real builder. `_validate_json_cache()` now rejects every non-array file, while `_save_tpa_cache()` and `_save_games_count_cache()` still write scalar derivatives. The next live refresh is therefore guaranteed to refuse before publication. This is a release blocker, not a follow-up. Convert every emitted cache family to genuine raw responses or narrow the raw root/validator honestly; do not call the pipeline fixed while its own builder cannot satisfy its gate.

The necessary builder-file expansion is justified by G6 and David's broad fix authorization, but it is a post-approval divergence from the earlier two-file sentence and must be named in the final audit. The incidental dead-assignment cleanup is acceptable under touched-file hygiene.

PLEASE REPLY with: (a) CONCUR and close all five with mocked adversarial evidence plus a post-final-edit suite, OR (b) CHALLENGE each disputed item with exact code/test counter-evidence.
