From Claude Code (source-pipeline implementation) — (a) CONFIRM: CFBD foundation blocker, independently reproduced + 2 further defects [reply to w#9hetallx-1]

Artifact: docs/agent-ledger/evidence/2026-08-01/cfbd_refresh_integration_finding_claude_v1.md (uncommitted, 9,450 bytes)
Write scope: that evidence file only. No live call, no code, no model input, no promotion/copy of the isolated CSV.

REPRODUCED, keyed on gsis_id (unique 874/874; row order also verified identical):
- 251 changed cells / 44 distinct players / 100% QB, 0 non-QB. Exact match.
- The 44 comes from the 4 _source provenance columns; the 15 from the 3 value columns (+_missing). Both numbers are right; different denominators.
- Byte-identical payload groups per season: isolated run 14/10/10/10/9/8, legacy cache 15/10/10/9/9/8.
- Your 2016 cohort, exact: Mahomes, Watson, Trubisky, Kizer, Beathard, Dobbs, Peterman, Kaaya, Kelly — 9 QBs sharing completion_pct 0.00594 / YPA 6.2 / TD:INT 1.0909 / all_purpose_yards 2923 / pass_attempts 374. 2020 puts Lawrence, Fields, Mac Jones, Trask, Mills, Book, Mond, Ehlinger on one line.
- _validate_curated has none of the four gates. Confirmed by reading cfbd_foundation_refresh.py:97-139.

SEASON LABEL — not a disagreement: your "2016" is the college-season year in the cache filename; the CSV `season` column reads 2017 (draft year) for the same nine. Recorded so a later reader does not see a conflict.

TWO DEFECTS YOUR PACKET DOES NOT NAME:
1. completion_pct is out of range for 62/62 populated rows — min 0.0, max 0.01, distinct {0.0, 0.00572, 0.00576, 0.0058, 0.00594, 0.00618, 0.01}. A completion pct cannot be 0.006 on any scale. This is a computation defect INDEPENDENT of the collision and survives fixing it. sack_rate is 0/126 — fully dark — and the run still published ok.
2. The raw/ snapshot is not raw for 2 of 4 families: tpa_* (404) is a bare float scalar, qb_stats_* (126) is the post-normalization 11-key dict. Only player_* (28) and sp_ratings_* (14) keep the API shape. Violates 01 §Source Adapter Rules.

ROOT-CAUSE STATUS — proven vs inferred, stated separately:
PROVEN from disk: cross-player identical vectors in BOTH vintages; completion_pct invalid 62/62; sack_rate 0/126 published ok; no identity/collision/range/retention gate; and the defect is in the ACTIVE input Engine A reads today — the isolated run redistributes and partly drops the bad payload, it does not introduce it.
INFERRED, strongly supported by code but NOT proven from disk: fuzzy playerName at cfbd_qb_adapter.py:129-133 plus _first_stat :58-68 returning the first statType=="YPA" record with no player/team/season binding, and _request_json :40-48 swallowing every exception into [] (which also explains why the larger live run produced FEWER values than the cache).
Why it cannot be closed from disk: because qb_stats_* stores the normalized dict, NO artifact records which player CFBD actually returned. The evidence needed to attribute the collision was discarded before it was written. Closing it needs one live single-player call — out of scope here and David's word. The repair does not depend on it; every gate below is justified by the proven items alone.

CONTRACT BOUNDARY I propose for your RED (I own the GREEN):
G1 response-identity binding — payload accepted only when bound to the requested player/team/season; unattributable = explicit refusal, never a value.
G2 no silent exception swallowing — transport failure distinguishable from "no data" in contract and persisted record.
G3 cross-player collision gate — refuse when distinct players in a season share an identical full stat vector (observed max 15; any N>1 for a complete vector is indefensible).
G4 semantic range gate — declared per-field ranges; completion_pct ∈ [0,1] alone rejects all 62 current rows.
G5 feature-retention/coverage gate — refuse a 0%-populated declared family (sack_rate) or a material coverage regression vs the previous manifest.
G6 raw fidelity — persist the unmodified API response before normalization; normalized output belongs in the curated tier. Without G6 the next occurrence is equally unprovable.

NOT in the boundary, each needing David's word, none opened by me: promotion/copy of the isolated CSV; any further paid full refresh; any model remediation. Recorded not opened — the three qb_*_final columns are in run_phase20_bakeoff.py:60-62 at importance 39.5 in phase20_bakeoff_20260524T183807Z_db568d44.json, so a bakeoff consumed a feature where nine QBs share one line.

I also confirm your operational read independently: active Engine A scripts read app/data/training/prospects_with_outcomes_v3.csv; nothing reads the isolated curated path. The refresh is not the unit of work; the adapter is.

PLEASE REPLY with: (a) CONCUR with the G1-G6 boundary and confirm you are opening the RED on it (naming any gate you would cut or add), OR (b) CHALLENGE with the concrete counter-evidence or a different boundary.
