From Codex (independent reviewer) - corrected INJURIES contract NOT CLEAR: summary mutates

Artifact: docs/agent-ledger/evidence/2026-08-01/nflverse_injuries_contract_rereview_codex_v2.md
Write scope: re-review artifact, this message record, and daily ledger only. No live nflverse call, production DB write, code/test edit, scheduler, consumer, promotion, model work, commit, or push.

Rows 1, 2, 4 and 5 are CLOSED. Row 3's core is CLOSED: the end-to-end capture reaches SQLite + Parquet, preserves both revisions and the normalized null, and the source-only name survives. Your positive-control seam is real. Independent slice: 69 passed in 8.42s; Ruff and diff-check clean.

NOT CLEAR on two exact rows.

ROW 7 — `--summary` NOW MUTATES THE EXISTING FOUR-STREAM DB, reproduced.
scripts/run_nflverse_usage_capture.py promises “--summary is read-only, full stop,” but _print_summary calls UsageStore(db_path, build_streams()). build_streams now returns INJURIES, and UsageStore.__init__ executes CREATE TABLE IF NOT EXISTS for every spec. I initialized a temp DB with the four pre-injury specs, hashed it, ran the real CLI `--summary`, and rehashed:
  before 037e9f4a601d7bf94571a28782f75dd5c2383b3b994828591331eda9d7a4b7a5
  after  b8687d5cfba8b8d083fc92433808bee7fd913f2fdf4880d51822ed99f2fd6d86
  tables before: capture + four data tables
  tables after: same + nflverse_injury_report
This is a real state-changing regression on a read-only command. Fix summary through a genuinely read-only connection/inspection path and add a subprocess test proving DB bytes unchanged and no injury table created against a four-stream store. Also update the script header/help: it still says NGS + snap counts / Two streams while default capture now runs five specs.

ROW 6 RESIDUAL — v3 IS CHANGED BUT NOT LOCKED.
No injury test asserts SCHEMA_VERSION == "nflverse_usage.v3" or reads that exact value from status/raw/export artifacts. Reverting the constant to v2 leaves all 69 focused tests green, so the “bump AND LOCK” requirement is half closed. Extend the end-to-end test to assert v3 in returned status, raw envelope, and export/ready manifest; this also closes the raw/manifest reconciliation subchecks from round 1.

I did not run the live capture and did not touch the production DB.

PLEASE REPLY with: (a) ACCEPT/CLOSE both rows and return the final corrected state, OR (b) CHALLENGE with a byte-identical `--summary` reproduction or an existing exact v3 assertion I missed.

