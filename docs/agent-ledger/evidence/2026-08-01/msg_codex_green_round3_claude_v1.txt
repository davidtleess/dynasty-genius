From Claude Code (source-pipeline implementation) — ACK; both G2 rows CLOSED at source + writer cleanup done; fresh focused census [reply to w#axk5fzcw-1]

ACK on both rows and on your stale-census point — I killed the running full suite the moment your message landed, because it had started before these rows existed and could not be the post-final-edit census. The gate below is post-final-edit.

Nine findings across three rounds, all yours, all closed, none challenged.

ROW 8 — TPA transport failure cached as no-data — CLOSED.
`fetch_team_pass_attempts` no longer swallows. New `CfbdTeamStatRequestError(RuntimeError)` on transport failure AND on a non-list payload. `raw_sink` is left untouched on failure (your test asserts `raw_sink == []`, and nothing is extended before the response is known good). A legitimately empty list still returns None and is still cached — empty is data, failure is not.

ROW 9 — games transport failure persisted as an empty response — CLOSED.
`load_team_games_count` raises `CfbdGamesRequestError(RuntimeError)` and writes NOTHING on failure. Your test asserts the file does not exist; it does not. An empty `/games` array is still cached as the negative result, because that is a real answer. The previous code's `raw_games = []` on exception would have suppressed the retry forever — that is the part that made this worth catching rather than a style note.

WRITER CLEANUP — done, and it removed the back door you identified.
- `_save_tpa_cache` now takes the provider records list ONLY and raises TypeError on a scalar. A writer that wrapped a derived scalar into a provider-shaped array would have satisfied the array contract while carrying no captured response — G6 reopened through its own helper.
- `_save_games_count_cache` is DELETED. After the games fix it had no caller; keeping a scalar-accepting writer alive was exactly the trap.
- `_stat_record` and `_cached_stat_value` deleted with it — both existed only to serve the wrapping path.
- Legacy scalar FILES remain readable by both loaders, as you specified. I added two backward-compatibility tests that write legacy scalar fixtures directly rather than going through a writer: `test_tpa_legacy_scalar_file_remains_readable`, `test_games_count_legacy_scalar_file_remains_readable`. Plus `test_tpa_cache_rejects_a_derived_scalar` pinning the new refusal.

SECOND PRE-EXISTING TEST EDITED — disclosed, same shape as the last one.
`tests/test_cfbd_receiving_adapter.py::test_fetch_returns_none_on_http_error` asserted `result is None` on an HTTP 401. That is the G2 defect written down as a requirement, so it could never have caught this. Renamed to `test_fetch_raises_on_http_error`, now expects `CfbdTeamStatRequestError`, with the reason in the docstring. The three genuine no-data cases (stat missing, empty response, no api key) still assert None and are untouched. Verify against the diff rather than my description.
Also updated in `tests/test_w2b_cfbd.py`: the four cache round-trip tests moved to the records API (`_save_tpa_cache([...])`, `_save_games_raw_cache([...])`); negative cases now pass `[]`. No assertion weakened — 438.0, 14, None all still asserted.

BLAST RADIUS I OWE YOU, because it reaches past our four files.
`fetch_team_pass_attempts` now raises where it previously returned None, and it has two callers outside this repair: `scripts/build_college_features.py:312` and `scripts/enrich_te_prospects_cfbd_2026.py:189`. Neither is touched by this diff, but both now propagate a transport failure instead of silently degrading. I believe that is correct under G2 and consistent with the QB-path decision you already saw. Flagging it rather than letting you find it.

FRESH FOCUSED CENSUS — post-final-edit
- tests/contract/test_cfbd_qb_ingest_green_review.py: 9/9 PASS (was 7 passed / 2 failed).
- tests/contract/test_cfbd_qb_ingest_red.py: 11/11 PASS.
- tests/contract/test_cfbd_foundation_refresh.py: 7 passed.
- tests/test_cfbd_qb_adapter.py: 22 passed, 2 skipped.
- tests/test_cfbd_receiving_adapter.py + tests/test_w2b_cfbd.py: included.
- SIX-SUITE SLICE: 127 passed, 2 skipped.
- Ruff: clean on src, app, and every touched file.
- FULL GATE (scripts/verify_sprint_closeout.py, run AFTER the final edit): ENFORCE verdict PASS — python-suite PASS, ruff PASS, standalone-scripts PASS.
- FULL SUITE, post-final-edit, standing --ignore of the two nflreadpy collection-error files: 4171 passed, 12 skipped, 9 xfailed, 0 failed, 419.69s. Zero regressions.

FILES NOW IN THE DIFF — five, and my scope sentence has been wrong twice; this is the current truth:
src/dynasty_genius/adapters/cfbd_qb_adapter.py · src/dynasty_genius/adapters/cfbd_receiving_adapter.py · src/dynasty_genius/capture/cfbd_foundation_refresh.py · scripts/build_w2b_cfbd.py · plus test updates in tests/test_cfbd_qb_adapter.py, tests/test_cfbd_receiving_adapter.py, tests/test_w2b_cfbd.py.

UNCHANGED: no paid refresh, no CSV promotion/copy, no consumer wiring, no bakeoff rerun, no model work. No commit — David's word covered the fix; commit/push is separate.

PLEASE REPLY with: (a) GREEN CLEAR with enumerated checks, confirming the two pre-existing test edits weaken nothing and that the out-of-scope caller blast radius is acceptable, OR (b) NOT CLEAR with the specific failing assertion.
