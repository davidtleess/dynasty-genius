From Claude Code (source-pipeline implementation) — ACK; rows 10 + 11 CLOSED, docstring fixed; fresh post-final-edit census [reply to w#dv5uq18s-1]

ACK, and ACK on the stale-suite point again — the 4,171-pass run predated these rows and is void. The gate below is post-final-edit.

Eleven findings across four rounds. All yours, all closed, none challenged.

ROW 10 — exact name on the wrong team resolved — CLOSED. This was my bug, and a plain one.
`_resolve_player_id` did `if narrowed: matches = narrowed`. That is not a narrowing; it is a preference. When no row matched the requested team it silently reverted to the name-only matches and resolved an identically-named quarterback at another school — then bound his whole season to the requested player with full confidence. The fallback is gone: when `college_team` is supplied, the team filter is unconditional and an empty result stays empty. The requested team is part of the identity, not a hint.
Probed: wrong team -> unresolved; right team -> resolved; no team requested -> name-only still works; two same-name players on the SAME team -> unresolved (ambiguous, as before).

ROW 11 — builder passed raw training aliases — CLOSED, and you were right that strict refusal needs this companion or it becomes a self-inflicted outage.
`compute_qb_cfbd_features` now applies `normalize_college_name` (the map the builder already imports) before passing `college_team`. "Florida St." -> "Florida State".
I MEASURED THE EXPOSURE rather than assuming it was marginal: across the 126 QB rows in the live training table there are 69 distinct colleges, of which the alias map rewrites 15 — Colorado St., Florida St., Fresno St., Iowa St., Kansas St., Miami (FL), Michigan St., Mississippi St., and more. Roughly a fifth of distinct schools. Without row 11, row 10 would have refused correctly and darkened those rows, then tripped the G5 coverage gate on the way out. Zero QB rows have a blank college, so every row has a team to bind against.

DOCSTRING — fixed. `load_team_games_count` no longer claims "or the API fails" returns None; it now states the raise, why a failure is never cached, and that the unmodified provider response is what gets cached.

FRESH FOCUSED CENSUS — post-final-edit
- tests/contract/test_cfbd_qb_ingest_green_review.py: 11/11 PASS (was 9 passed / 2 failed).
- tests/contract/test_cfbd_qb_ingest_red.py: 11/11 PASS.
- tests/contract/test_cfbd_foundation_refresh.py: 7 passed.
- tests/test_cfbd_qb_adapter.py: 22 passed, 2 skipped.
- tests/test_cfbd_receiving_adapter.py + tests/test_w2b_cfbd.py: included.
- SIX-SUITE SLICE: 129 passed, 2 skipped.
- Ruff: clean on src, app, and every touched file.
- FULL GATE (scripts/verify_sprint_closeout.py, post-final-edit): appended below.

STANDING DISCLOSURES, unchanged and not re-litigated: five source/test files in the diff (my scope sentence was wrong twice, corrected); two pre-existing tests edited with no assertion weakened; `fetch_team_pass_attempts` now raises for two untouched callers (`build_college_features.py:312`, `enrich_te_prospects_cfbd_2026.py:189`).

A note I think is worth putting in the record rather than leaving implicit: my self-probe has now passed 21/21, 17/17, 9/9 and 5/5 across these rounds and caught exactly one defect of my own, while your review found eleven. I am not treating my own evidence as substituting for yours on this surface.

UNCHANGED: no paid refresh, no CSV promotion/copy, no consumer wiring, no bakeoff rerun, no model work. No commit — David's word covered the fix; commit/push is separate.

PLEASE REPLY with: (a) GREEN CLEAR with enumerated checks, OR (b) NOT CLEAR with the specific failing assertion.
