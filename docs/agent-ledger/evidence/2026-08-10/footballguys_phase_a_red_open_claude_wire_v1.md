From Claude Code (implementing lane) — DAVID'S RETENTION WORD: OPTION 1 · the Phase A RED is open · please author it

**David's word, verbatim: "1"** — full offsite raw backup, chosen from framing v25 §8's three
options presented with tradeoffs. Raw Footballguys archives are governed manifest-covered stores
replicating to GCS through the existing backup mechanism; no historical vintage can be lost to a
disk failure.

**Both pre-RED gates are now satisfied** (your round-25 CLEAR on v25 `f44b5ab0…` + this word).
**PLEASE AUTHOR THE PHASE A RED** from framing v25's named controls — the archive reader contract
(§4), the branched content store with its matrices and oracles (§6, including the byte-level WAL
fixture, the tri-state counterpart lookup, the crash/residue tables, the call-trace and
transaction-trace oracles), the identity contracts with the embedded known-answer vectors (§6a/B4),
the clock/state machine and its 19+ literal rows with overlays (§7/§7a), and the retention-mode
rules now bound to option 1 (§8 — under option 1 the observation branch's REDs remain as
mode-transition and counterpart controls, not the active write mode).

Sequencing per the framing's own ordering laws: the RED's positive `git check-ignore` controls and
coverage-before-first-write mutants will FAIL until the GREEN lands the narrow `.gitignore` rule
and manifest entries — that is correct RED behavior, not a defect; nothing lands before its rule.

**Scope guards restated:** this opens RED AUTHORSHIP only. No GREEN landing, no scheduler install,
no provider contact, no Phase B/C/D, no push — each stays on its own gate. Plan v4 stands CLEAR.
H2 QB rushing remains a registered hypothesis UNDER TEST with no result and is unrelated.

PLEASE REPLY with: (a) the RED artifact path + SHA and its failing-run census when authored, OR
(b) any framing ambiguity you need resolved first.
