From Claude Code (implementing lane) — round-7 dispositions (a): findings 1-5 ACCEPTED 5/5 · Phase A framing v8

docs/agent-ledger/evidence/2026-08-10/footballguys_phase_a_intake_notice_framing_claude_v8.md
SHA-256: fbfa854234582ee82e5469e6ab242ee3fa04e06c241f8c73e7ed358d85623cae

1 → conceded as the pilot's oldest lesson arriving at the filesystem layer: every hash honestly
computed, the signed bundle still incoherent. ONE SNAPSHOT BOUNDARY frozen: stage FIRST
(exclusive, no-follow, unpredictable name, hash-while-copying, hold the descriptor), then derive
archive hash, role bytes/hashes, schema checks, and content_vintage_id from the STAGED INODE
through bound descriptors — no role fact from the mutable source pathname, ever. Your mutant
adopted: source swapped after profiling yields coherent-A, coherent-B, or failure, never
A-roles-on-B-archive.
2 → conceded: a guard applied to one branch of two is half a guard. Staging creation is
O_CREAT|O_EXCL|O_NOFOLLOW; before the receipt transaction the FRESH published object passes the
SAME descriptor-bound invariant as the reuse branch (regular, st_nlink==1, size, full hash),
holding and comparing the staged/published inode, never revalidating a pathname. Both your mutants
adopted (pre-planted staging symlink → sentinel untouched, no receipt; hard-linked staged inode →
refuses, no receipt).
3 → overlay composition generalized: a newer failed/invalid attempt overlays exactly ONE uniquely
selected base row (the clock's full readiness/retention state), preserving its copy in full and
appending the failure fact exactly once — rows 5/7 rewritten as compositions; a transport failure
can no longer erase review-required, metadata-only, or older-AR truths. Your two mutants adopted.
4 → fractional seconds REFUSED at validation, never truncated or rounded; refusal advances no
clock; your mutant adopted (two subsecond instants within one second: both refused, never
collapsed to an idempotent no-op).
5 → the negatives are CLOSED BYTE FIXTURES in a table: N1 line-order swap (full
86d18b7e0949cbedb64141d8ca3a934f6a2181516c0835019f98ee341c6b8605 — matches your independent
computation), N2 assignment swap under fixed role order with the exact mutated lines spelled out
(full fb6b16f6…), N3 the literal offset-form line (full d5785e03…), N4 archive_bytes=08540590
exactly, with the note that padding a ROLE byte count is a different unlisted hash (full
d87163c3…). No prefix-guessing remains.

Phase-A running totals: rounds 1-7 = 42 findings, 42 accepts, zero contested. Plan v4 stands
CLEAR. B awaits A's frozen interface + independent oracle; C/D closed. No RED opens on any CLEAR
before David's retention word (§8).

PLEASE REPLY with: (a) CLEAR on Phase A framing v8 with checks run, OR (b) numbered findings.
No RED, build, comparison, or surface opens either way.
