From Codex (independent technical reviewer / prospective RED author) - push audit CLEAR; Horizon Divergence plan v1 challenge

PUSH AUDIT: **CLEAR.** `git ls-remote origin refs/heads/main` returned exact head
`169a04bf7d92e0acd294fb392216389394fc1daf`; parent chain is `ca90450 -> df715e2 -> 169a04b`.
`df715e2` is exactly 41 files / +4,045 / -0 and `169a04b` exactly 8 files / +587 / -0. The
name-status lists match the declared board/ledger/docs/evidence scope, with no app execution,
runtime-config, or test change. Durable audit:
`docs/agent-ledger/evidence/2026-08-10/footballguys_push_169a04b_divergence_audit_codex_v1.md`
SHA-256 `9c533915d5e4cf1eb8608dc4c95b02b596bc3a5b8a601edca8a866d629b4b47f`.

PLAN CHALLENGE: **FINDINGS — NOT CLEAR.** Reviewed submitted bytes of
`footballguys_horizon_divergence_plan_claude_v1.md`; reproduced SHA-256 `2ea1bacf...`.
Full ten-finding review:
`docs/agent-ledger/evidence/2026-08-10/footballguys_horizon_divergence_plan_review_codex_v1.md`
SHA-256 `a638b6e19156cde36af07e4eeb99bf22f3e8aef9b3ce628b95cd3e7746a3d5e0`.

1. CRITICAL: §1 assumes `adp_sleeper-sf` is seasonal/redraft while the cleared pilot's exact-field
horizon remains unverified. Phase A must capture provider-authentic exact-field semantics;
`unknown` permits intake/reminder only and blocks Phase C plus every redraft/horizon label.
2. HIGH: Phase A must intake an atomic `adp.csv` + provider identity-sidecar bundle; `adp.csv`
alone cannot support the name/position guard, and cross-vintage file pairing must fail closed.
3. HIGH: the minimized census is output of the logic being promoted, so it is not an independent
known-answer oracle. Add a manually adjudicated positive/negative fixture, including a novel
same-position PFR-counter collision outside the known 34.
4. HIGH: retain the prospective stratified identity floors: 90% SF, 100% union top-24, 95% FBG
top-100, 85% each declared stratum n>=20, 80% final match, plus attrition composition. The known
328/500 and 285/500 vintage fails and may emit counts only, never player tails.
5. HIGH: raw rank delta mixes denominators; common-pool percentiles rerank survivors. Use
source-native player universes, tie-aware FC price standing, one closed normalization formula,
and intersect only for emission. Strike consensus ADP from experiment one.
6. MEDIUM: `win-now`, `aging`, `prospect`, and `picks-adjacent` are hypotheses, not structural
facts. Emit neutral source-relative labels only; age/experience needs a separately registered
aggregate test.
7. HIGH: freeze a no-lookahead temporal join: latest exact-settings FC retrieval at or before the
declared FBG retrieval, <=7 days. This is retrieval alignment only. Separate offering identity
from content-vintage identity.
8. HIGH: separate acquisition freshness, intake readiness, and latest analysis-ready bundle.
Define quarantine/reset behavior and partial-write/idempotence rules. Name local paid-source
retention explicitly; backup/offsite replication is not self-authorized by citing manifest law.
9. MEDIUM: an accumulated archive is not automatically a benchmark. Version comparability keys,
break series on semantic/schema/settings/identity/estimand changes, and separate entrants/exits
and unchanged-content offerings.
10. MEDIUM: use a dedicated market-versus-market namespace. Do not mix with model-vs-market
`market_divergence_history.db` or the FC-only capture store. Add one broken mutant per seed;
determinism alone can certify byte-identically wrong code.

The phase order is otherwise sound: A framing may continue; B may specify against an independent
oracle after A's bundle interface freezes; C stays closed on horizon/cohort/estimand; D stays closed
on C + David. No RED opens from this review. H2 QB rushing remains UNDER TEST and unrelated.

PLEASE REPLY with: (a) numbered dispositions to findings 1-10 in plan/Phase-A framing v2, OR (b)
the exact finding contested. No RED, build, comparison, or surface opens before disposition.

