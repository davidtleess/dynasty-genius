From Codex (independent technical reviewer / prospective RED author) — plan v4 + Phase A framing v4 round-4 review

PLAN v4: **CLEAR.** PHASE A framing v4: **NOT CLEAR — four findings.**

Durable review:
`docs/agent-ledger/evidence/2026-08-10/footballguys_plan_v4_phase_a_framing_v4_review_codex_v1.md`

1. **CRITICAL — the archive guard rejects the real bundle.** V4 rejects path separators after
normalization, but the measured required members are
`DraftDominator.app/Contents/Resources/adp.csv` and
`DraftDominator.app/Contents/Resources/projections.csv`. Allow safe nested relative paths; freeze
exact role resolution; add the real path shape as positive control and same-basename ambiguity as a
mutant.

2. **HIGH — the state table is still not exhaustive.** It lacks option-3
`refresh_observation` current/due states, `due + review_required`, and invalid/future/malformed
latest attempts with and without a prior valid clock. Enumerate exact precedence/copy/pill/AR and
prove observations never become analysis-ready.

3. **HIGH — late semantic evidence has no honest immutable lifecycle.** Evidence refs and semantic
fields are signed into the offering receipt, so resolving today's `horizon=unknown` later either
conflicts or gets misrepresented as a new acquisition that can reset freshness. Add a separate
append-only semantic-evidence/assertion revision that never mutates receipt/acquisition identity or
clock; retention law applies; missing/unretained evidence cannot open Phase C.

4. **HIGH — canonical reuse verifies a pathname once, not an immutable object.** A matching
hard-link alias is regular/non-symlink and can be mutated after verification. Freeze descriptor-
based no-follow verification, multi-link policy, downstream rehash-on-use, true atomic no-replace,
and corruption quarantine. Mutate hard-link alias, path swap, post-receipt bytes, and
exists-then-overwrite.

Checks: both hashes/counts reproduced; v4 live pointers verified; real ZIP central directory listed
read-only with no extraction/execution; v2/v3 boundaries, state precedence, source registry,
daily-control, PFF store, manifest law, and existing status-drawer composition inspected.

No RED, build, intake, store, scheduler, provider contact, comparison, surface, commit, or push
opens. David's retention word remains a separate hard gate. Phase B waits; C/D remain closed. H2 QB
rushing remains UNDER TEST with no result and is unrelated.
