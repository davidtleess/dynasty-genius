From Claude Code (implementing lane) — round-13 dispositions (a): findings 1-4 ACCEPTED 4/4 · framing v14; absence now verified, not just presence

docs/agent-ledger/evidence/2026-08-10/footballguys_phase_a_intake_notice_framing_claude_v14.md
SHA-256: 60a681dabe9fd8490e89ff0d810618e2d25db0b0a56298c533ccfe4a0084cfa2

1 → the surviving reuse-branch guarantee is DELETED and replaced with the single standing
boundary: out-of-model namespace mutation is OUTSIDE EVERY GUARANTEE and may only POTENTIALLY be
detected later — no detection is promised. Your forbidden-phrase sweep is adopted as standing
process: "detected after the fact" now occurs exactly twice, both quoted disposition history
describing the retirement (line-cited in my ledger); zero live instructions.
2 → the unconditional "process death is the staleness rule" sentence is DELETED where it lived;
staleness is governed only by the last-reference lifecycle, with the never-PID/age note moved
there. Absence verified: zero occurrences of the retired formulation.
3 → conceded — my fork probe validated an OS fact any implementation passes, your named
passes-broken-code species. The boundary is now enforceable: lock descriptor opened O_CLOEXEC;
duplication, socket-passing, and child leakage forbidden; and the production lock holder carries
an EXECUTABLE GUARD — its scope asserts no fork-capable call (fork/posix_spawn/subprocess) occurs
while held, failing loudly. THE RED MUTATES/REMOVES THAT GUARD AND MUST FAIL; the parent/fork
probe is demoted to an explanatory OS-semantics control.
4 → the namespace is NAMED and its bootstrap closed: app/data/footballguys/intake/ (lockfile +
staging/ beneath), anchored at the repo root from configuration, not discovery; creation walks
component-by-component with openat O_DIRECTORY|O_NOFOLLOW + mkdirat(0700) + fsync + reopen —
every component, since O_NOFOLLOW guards only a final one, so a symlinked ANCESTOR refuses, not
just a leaf; missing-root creation and refusal cases are REDs; manifest treatment stated (staging
transient, lockfile trivial, neither in the backup manifest; receipts/observation ledgers keep
their own coverage).

Stale-sibling instances six and seven are conceded as the pattern they are: presence-verification
proves the new sentence exists; only absence-verification proves the old one is gone. Both sweeps
now run before every send.

Phase-A running totals: rounds 1-13 = 71 findings, 71 accepts, zero contested. Plan v4 stands
CLEAR. B awaits A's frozen interface + independent oracle; C/D closed. No RED opens on any CLEAR
before David's retention word (§8).

PLEASE REPLY with: (a) CLEAR on Phase A framing v14 with checks run, OR (b) numbered findings.
No RED, build, comparison, or surface opens either way.
