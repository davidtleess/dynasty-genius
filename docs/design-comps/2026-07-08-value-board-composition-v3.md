# Composition Package v3.14 — The Value Board (our value, with the Market Margin as its edge)

> **Status:** pre-code composition artifact (shape-before-code gate, DESIGN.md §Enforcement). No production code. Supersedes v2 (`2026-07-08-value-board-composition-v2.md`).
> **Date:** 2026-07-08 (v3) · 2026-07-11 (v3.3) · **Author:** Claude (impeccable `shape`) · **Register:** product
> **v3 reground (2026-07-08, historical):** the fresh 2026-07-08 divergence artifact (David-authorized one-time rebuild; `universe_market_divergence_latest.json`, captured 2026-07-08T19:43Z). v2's numbers came from a **stale June-23 snapshot** — Codex caught it. **Superseded by the v3.3 reground below — the live grounding is the 2026-07-11 artifact.**
> **v3.1 (cockpit re-review corrections):** Codex + Gemini independently converged on a real source-of-truth defect in v3 — it led with **DVS** as the focal "our value" while the margin is computed from **xVAR** percentiles (`universe_market_divergence.py:191–193`), so the headline rank and the margin direction could contradict each other. Fix: **pin the board to xVAR** (the same basis the margin uses) — which *also* resolves the §10 TE-wall (xVAR is the cross-position transform: FA-by-xVAR is a natural WR7/RB4/TE3/QB1 mix vs 13/15 TEs by DVS). Margin now renders in **percentile space** (native ranks are labels only, no misleading cross-denominator spot-delta), and ranks carry a **confidence/tier** frame (Gemini: pinpoint ranks overclaim precision). §§1,2,3,7,10 updated below.
> **v3.2 tier-calibration correction (David 2026-07-08):** fixed percentile tier labels are NOT an acceptable primary basis. "Elite" must reflect relative model value, production, age/longevity context, and historical field separation — not an arbitrary `top X%` cut. Until a tier-calibration artifact exists, the Value Board may show neutral rank spans / provisional value cohorts, but named labels (Generational / Elite / Cornerstone / Starter / Depth) are suppressed.
> **v3.3 reground (2026-07-11):** regrounded on the **2026-07-11 volatility-complete divergence artifact** (Phase-0b closed, PR #146; *provenance corrected in v3.4: the artifact came from the David-authorized manual runner run — the 09:40 LaunchAgent, installed the same day, first fires Jul 12*). Every figure re-verified via `docs/design-comps/comp-v33-extract.py` (single numeric source; bases in its docstring; rides this comp's PR). **The framing already converged, don't re-run it** (full record: `docs/agent-ledger/2026-07-11.md`, 16:45 ET entry). The locked laws from that convergence, applied throughout: **(1) volatility is receipt-only** — `market_volatility` is FantasyCalc's own source-computed metric with an undisclosed basis; under the disclosed-basis law it renders on no primary surface (§13); **(2) percentile space is the only arithmetic basis** — native ranks are labels, never subtracted; this reground also **corrects the v3.2 sketches**, whose signed readouts (`+35`) read as native-rank subtraction — every displayed signed magnitude is now the percentile delta (see the McCarthy/Gabriel proof pairs, §1); **(3) the 191 signals are descriptive disagreements only** — Gate-4 unvalidated, count-only copy; **(4) freshness anchors on the root vintage fields** — market vintage rendered distinct from build time, in manager prose. Board delta vs 07-08 **cleared by Codex against the PIT history** (07-09 held 334 comparable; 07-11 = 340 via fringe churn +12/−6 at the tail — source churn, not a math break). New honesty note: the top free agents all carry **negative xVAR** — receipts say "below replacement" plainly (§2).
> **v3.4 (2026-07-11, same-day: the Codex NOT-CLEAR round + two David rulings):** the v3.3 adversarial re-review returned 11 Codex findings (all conceded, all fixed here) plus Gemini product-edge items. **David rulings (2026-07-11): market-rank renormalization REJECTED** — native ranks stay labels, percentile space stays the only arithmetic — **and the day-2 overnight delta ALLOWED** — the 14-day depth gate binds multi-day *trend* renderings (sparklines/trajectories); a single named-window margin delta over the latest two same-player observation days is legal from day 2 (§4, §13). The Codex fixes: **DG ranks recomputed over the producer's model cohort** (468 valued players — the exact population the percentiles are computed over; a player's rank is now stable across every display filter); **displayed deltas read the artifact's stored `model_minus_market_delta`** (0 dp), never re-derived from rounded endpoints (Theo +33, Legette +28, Mendoza −20, … — *v3.5's NAMED rounding rule revises the two half-point rows: Henderson −11, Mendoza −21*); **provenance corrected** — FC's volatility capture began **Jul 10** (169 rows), OUR PIT record began **Jul 9**, and this grounding artifact came from the David-authorized manual runner run (the 09:40 LaunchAgent, installed the same day, first fires Jul 12); **"margin on 191" miscopy fixed** — every board row carries a margin; 191 is the disagreement count; **sketch bars now place both marks BY percentile on the shared 0–100 axis**; **board membership defined by signal status** (`gates_passed` / `inside_band`) and the producer's `volatile_market` gate reconciled with the receipt-only law (it is suppression-only — a blocked row renders a designed "signal withheld" state, joins no count; zero such rows today — *corrected in v3.5: that state is unreachable under the current runner, which fail-closes the whole refresh; it ships with the §12.9 quarantine ticket*); **team aliases normalized** (LVR→LV, SFO→SF — Fernando Mendoza is LV, not unknown); **xVAR correctly described** as the engine-aware unified transform (278 Engine-B + 62 Engine-A board rows); RED seeds hardened with the missing negative cases; extractor scope-honest and ruff-clean. Gemini items adopted, filtered through the end-user lens: the **FA replacement-line divider** (xVAR = 0) visible on the row surface, not only behind a receipt; **"aligned" → "within band"** (names the disclosed noise band instead of implying equivalence); an explicit **disagreement-size sort toggle** on Other Teams; a **QB scarcity model-character note** in QB receipts (§9); the **Garrett Wilson / Tank Dell identity gaps** promoted to a named pre-UI follow-up (§12). NOT adopted as proposed: Gemini's "league-derived replacement" copy — the replacement line is a model-defined per-position baseline (`pvo_assembler.py:471–490`), so the copy says "our model's replacement line," which is what it is. The framing already converged, don't re-run it.
> **v3.5 (2026-07-11, the end-user round — David's standing directive: "paying more attention to User Experience and User Interface — we've done research — you have tools — USE THEM"):** authored against the ratified research corpus, cited inline (`docs/strategies/2026-07-06-claude-fantasy-ui-research.md` = CR; `...-fantasy-app-data-display-research-codex.md` = XR; `2026-07-05-dynastynerds-visual-benchmark.md` = DN), Codex's v3.3 end-user NOT-CLEAR (8 findings) and v3.4 technical+end-user NOT-CLEAR. **Composition changes:** the **Daily Open now leads with the morning answer** — the named-window movement line first, changed rows second, the grouped roster as drill-down (the macro-answer-first law, PRODUCT.md p2; enabled honestly by David's day-2 overnight ruling) — with real desktop AND mobile sketches (the shape-before-code gate finally satisfied for this surface); **rows are simplified to a five-second scan** — focal DG rank + the percentile bar + ONE signed number (the display delta) + the familiar market label; the percentile endpoint numerals move to the receipt (CR §1: "one number owns the row… the cheapest, largest single upgrade"); **group headers carry the ratified value total + league rank + completeness** (`QUARTERBACKS (5) · −7 above repl · 10/12`; DN §1/§2.6) — real numbers today: QB −7 (10/12) · RB +33 (10/12) · WR −82 (**12/12 with Wilson+Dell UNVALUED — the rank is disclosed as incomplete; the §12.8 identity ticket is what makes this feature trustworthy**) · TE −12 (4/12); **the FA aggregate truth renders above the list** ("all 97 valued free agents sit below our model's replacement line"); **first-viewport plumbing consolidates to one quiet disclosure strip** (scaffolding-hide proportionality); **search + Showing-X-of-Y + adjacent-comparison + a user-initiated Trade Lab handoff are spec'd** (DN §1 table chrome; XR: Sleeper's Players tab leads with Search; KTC's value-adjacent players). **Technical corrections (Codex v3.4):** the "signal withheld" row state was UNREACHABLE — the scheduled runner's `_has_stale_market` aborts the whole refresh on any `gates_blocked` row, so v3.5 describes reality (whole-run fail-closed today; per-row quarantine = the already-named Phase-0b follow-up ticket) and the RED seeds no longer demand an impossible state; the **rounding rule is named** — display delta = stored delta × 100, **half-away-from-zero**, −0→0, rounded ONCE producer-side, frontend renders the integer verbatim (Henderson −11 and Mendoza −21 under the named rule); **tied values share a rank** — six WRs tie at the top raw value; sequential id-order ranks were false precision; ties render `T-WR1`-style with the tie disclosed (row ORDER may use id only as a stable sort key); **the alias contract covers the full observed set** (LVR·SFO·LA·NOR·KAN·NWE·TAM → canonical; unknown → designed fallback); movement + PIT eligibility get exact definitions (§13); sketches disclose they are ordinal approximations of the absolute axis the build must implement; the QB receipt note is date-bound descriptive, not causal. The framing already converged, don't re-run it.
> **v3.6 (2026-07-11, Codex dual-lane round 3 — 16 findings, all conceded except one routed to David):** **exact-decimal rounding** — the named rule now runs on the serialized decimal, never binary-float scaling (Bigsby −0.285 renders **−29**, not float's −28; board movement corrects to **204**); **competition ranking implemented** — ties share the lowest rank, and the honest labels land on the surface: Tucker Kraft is **T-TE1 ×11** (the §11 clamp tie, now visible), Jeanty T-RB18 ×2 / overall T-30 ×2, Kyle Williams T-WR104 ×2; **movement scope corrected** — 204 board-wide = 133 rostered + 71 free agents (v3.5's "league 202" was number- and scope-wrong); the macro line gains **materiality + coverage decomposition** ("14 of 23 moved — 5 by 2+ points · one newly comparable: Rasheen Ali · 3 forming"); **the moved number owns the WHAT-MOVED row** — the current gap demotes to a labeled secondary ("gap +28"), killing the two-competing-signed-numbers read; **sign-neutral tie order** for equal |move| (canonical id, never sign); **pairing law chosen** — the Daily Open uses the GLOBAL latest pair (one named window for the whole surface; per-player mixed windows rejected as illegible), and **basis stamping into history rows is a named producer prerequisite** for the movement feed (rows carry only `universe_pvo_batch.v1` today — the v3.5 basis-version guard was unenforceable as written); **the group-value layer is split honestly** — renderable today = count + valued x/y (+ missing names in the receipt); the value total + league rank are **withheld**: the raw-sum basis I asserted in v3.5 §12.10 collides with the EXISTING governed position-value definition (`team_value_matrix.py:153–220`, starter xVAR + positive decayed depth credit — raw sums let negative bench drag a group), comparator coverage is too thin to rank on (only 6/12 QB, 4/12 RB, 6/12 WR opponent groups fully valued; TE 12/12), and the header grammar is fixed for when it ships ("−7 vs replacement · 10th of 12 · 5/5 valued") — **the basis choice is the ONE item routed to David** (§12.10); **affordances separated** — row = player inspector, ⓘ = metric receipt, Trade Lab handoff = a neutral staging tray (auto Give/Get placement rejected in the Gemini screen); **mobile a11y contract** — both lanes labeled (DG and Mkt), every bar carries an aria-equivalent sentence, 44px sticky targets + safe-area insets + visible focus; **search scope made visible** (scope chip + zero state offering the global pool); alias map implemented in the extractor (LA→LAR · KAN→KC · NOR→NO · NWE→NE · TAM→TB + LVR/SFO); the §2 leftover present-tense suppressed-row sentence corrected. Gemini screen (per the David directive): retained its Wilson/Dell-untrustworthy-rank and empty-search/overflow items; rejected its "RB 10/12 is strong" gloss (verdict-adjacent), its AppShell repo-state assertion (out of lane, voided), and auto Give/Get. The framing already converged, don't re-run it.
> **v3.7 (2026-07-11, Codex round-4 dual NOT-CLEAR — 16 findings, all conceded; §12.10 still David-pending and unrelated):** **movement direction made honest (the round's core catch):** a bare "moved +4" is a GAP derivative that reads as player appreciation — live proof: Legette's market value FELL 555→450 while his row said "+4" (the gap widened toward us *because* the market dropped). WHAT-MOVED rows now carry direction-bearing gap prose — "gap widened 4 toward us · now +28" / "narrowed 4 · now within band" — never a bare signed move. **"Our model held" is now DERIVED, never asserted:** raw xVAR compared across the pair for every compared player (today: 0 changed — the claim HOLDS; 64 model percentiles shifted — *attribution corrected in v3.8: model-cohort composition 469→468, not "board makeup"*). **Mover tie order fixed to canonical DG ids** — history keys are sleeper ids (runner stores `sleeper_player_id`); mapped before ordering: Mac Jones → Legette → Bell. **Within-band marks sit at TRUE coordinates** — forcing overlap violated our own absolute-axis law (Kyle Williams, stored +7, band — *endpoints re-verified in v3.8: 48 vs 41*); band conveys via quiet styling + label, never moved marks. **FA scope corrected:** the model-valued FA pool is **223 (all below replacement)** — 97 with a market read (the board) + 126 valued/no-market behind "Show all valued"; calling 97 the whole valued pool was wrong with that toggle present. **Coverage decomposition made exact:** 23 = the compared intersection of the 27-man roster; "3 forming" split — *and re-split in v3.8 on data: 2 model reads forming (Braelon Mkt RB52, Wilson Mkt WR14 — both ENGINE_B/MODEL_UNCERTAIN/xVAR-null) + 1 pre-model (Dell, Mkt WR71); v3.7's "2 identity gaps" absorbed an unsupported label*. **Braelon removed from default-board sketches** (not model-valued → Show unranked, market-only state). Universe tie labels complete (×2/×11, T-116 hyphenated); pending group value/rank leaks scrubbed from every active contract (§4 headers = count + valued only); §12.7 pairing law corrected to the GLOBAL pair; extractor hardened (parse_float=Decimal end-to-end — a synthetic long-tail float misrounds without it; docstring rewritten); a11y sentence contract no longer arithmetic-couples rounded endpoints to the stored gap (*fixture re-based in v3.8 to Theo: 80−46=34≠+33 stored, after the pct() rule change shifted four endpoints*); sketches gain the ⓘ metric-receipt affordance distinct from the ▸ inspector; mobile sketches carry tie markers, BOTH lane labels, no false ✓ on incomplete groups, a scope chip, the DESIGN.md status pill, "moved" spelled out, and the summary in words. The framing already converged, don't re-run it.
> **v3.8 (2026-07-11, Codex round-5 dual NOT-CLEAR — 12 findings, all conceded):** **endpoint displays re-verified after my own pct() rule change** (Ayomanor mkt 39 · Legette model 57 · K.Williams mkt 41 · Mendoza model 59) and the fixtures that had been built on stale endpoints re-based — the rounded-separately fixture is now **Theo: endpoints 80/46 read "34" while the stored gap is +33**; **attribution corrected:** the 64 percentile shifts are **model-cohort composition** (469→468, +2 entrants/−3 exits — the producer's percentile population is all model-backed rows), never "board makeup"; **every macro clause is now derived** — "market moved" from market-value comparison (325 compared players changed; a `market_value` field-name bug in my first derivation compared None==None and printed 0 — caught against Codex's round-4 Legette 555→450 evidence), "model held" from raw-xVAR comparison on BOTH the full compared set and the surfaced roster scope (0 and 0), quiet-day copy derivation-gated; **coverage reclassified on data** — Braelon AND Wilson are model reads forming (ENGINE_B · MODEL_UNCERTAIN · xVAR null; market reads RB52/WR14 exist), Dell is pre-model (WR71) — v3.7's "2 identity gaps" had absorbed an unsupported label, exactly what the screening directive exists to catch; §12.8 rewritten accordingly; **lead-language movement prose with the unit named** — column header "PERCENTILE GAP (pp)", rows read "DG lead grew 4pp · now +28pp" (Codex's phrasing; "widened toward" was still ambiguous); mobile coverage includes Ali (23+1+2+1=27) and diagnostics shorthand (twd/ID/mvd) is banned; **the ⓘ metric-receipt control appears on every sketch row** and desktop search carries its scope chip; **the status pill speaks freshness** ("●mkt 11:07"), never developer health; **mobile mover rows are exactly two lines** with a "(12 more movers ▾)" route; **the FA hard state is composed** — a dedicated sketch with the 97→223 toggle, the aggregate pool line, and the replacement divider rendered above an all-below list (named rows extractor-verified: Allen 153/WR60/+49, Hunt 157/RB51/+46); **universe sample rows carry ⋯ separators** so non-adjacent ranks never read as a broken sort; pending-layer leaks scrubbed (§8 PositionGroup/MoveLine, §3 reference ranks withheld); extractor: market_value field fixed, roster-scope model-held, off-board classifier, cohort-composition print, `mine` shadowing bug fixed. The framing already converged, don't re-run it.
> **v3.9 (2026-07-11/12 boundary, Codex round-6 dual NOT-CLEAR — 12 findings, all conceded):** **derivations are now PER-SCOPE** — the Daily Open's market clause derives from David's compared set (**market prices moved on 22 of 23**; all 14 gap movers had market changes; the full-set 325 lives in the receipt), model-held uses separate booleans per scope (an off-roster change can never fail the roster clause), and the cohort-composition attribution is **verified before it renders** (raw xVAR across all 466 common model-backed rows: 0 changed) — the macro line no longer conflates "the market moved" with "the gap moved" (22 vs 14 are different facts and both render); **"market-led by construction" retired** — both lanes derive daily, no assumed off-season truths; **coverage labels tightened to strict evidence** — "DG read unavailable" (Braelon, Wilson) and "pre-model" (Dell); "forming" implied more knowledge than the data holds; the summary arithmetic no longer double-counts Ali (he is inside the 24; 24+3=27); **comparator completeness is opponent-scoped** (5/11 QB · 4/11 RB · 6/11 WR · 11/11 TE — David's own groups excluded); reference sums removed from the doc entirely (extractor-only means extractor-only); **sketch precision pass:** pp units on every gap (+33pp), "PERCENTILE GAP (pp)" headers, ⓘ on every row, Kyle Williams returned to a WR group (a WR had been sketched inside RUNNING BACKS), native Mkt ranks restored on mobile, "now −14pp" on mobile movers, the Daily Open scope chip + spelled-out pill ("● market 11:07"), FA sketch carries real NFL teams (Allen LAC · Hunt KC), no internal xVAR token on the divider label, and a composed Show-all-valued/no-market example row; the Legette name-collision spacing fixed. The framing already converged, don't re-run it.
> **v3.10 (2026-07-12, Codex round-7 dual NOT-CLEAR — 15 findings, all conceded — PLUS the §12.10 RULING):** **David ruled the group-value basis: OPTION A, the governed `team_value_matrix` starter+depth-credit definition, provisional ("for now")** — recorded in §12.10/§3/§8(b); rendering stays gated on the producer artifact + comparator coverage. Findings: **the strict coverage lexicon now propagates everywhere** — "forming/missing/awaiting" retired at every active site (extractor prints included) for "DG read unavailable" / "pre-model" / "no FantasyCalc read yet" (source-bounded — we only speak for our source, Codex r7 #15); manager-readable "DG leads / market leads / within band" replaces we-higher/mkt-higher compressions; **the percentile-population claim is corrected** — percentiles and position ranks share the PER-POSITION cohorts (QB 47 · RB 111 · WR 199 · TE 111); the 468-row union is the OVERALL rank's population only, a different denominator (`_model_cohorts` partitions by position); **the comparator seed matches the opponent-scoped truth** (5/11 · 4/11 · 6/11 · 11/11); **no §12.10 outcomes pre-pinned** — grammar examples are placeholder-shaped and the rounding seed is basis-agnostic; **extractor**: `payload_model_backed` now requires a truthy position (matching the producer), rank maps key by CANONICAL DG id (duplicate display names can no longer overwrite), the roster header prints "not compared," Braelon's line says "unavailable"; **the two disclosure routes are pinned as separate contracts** (Show all valued 128 / Show unranked 11,733); **mobile Daily restored to the full two-lane morning answer** (named window, 22/23 market fact, model-held, strict labels); **mobile movers are exactly two lines**; **mobile board rows put the focal DG rank on line 1** and the market's rank beside its own mark on line 2 (no cross-lane re-association); **the FA scope is TWO composed states** — default (97) and a visibly-selected Show-all-valued (223) whose no-read exemplar is real and extractor-verified (**Bam Knight, ARI RB, overall 146 · RB47, no FantasyCalc read**) — the v3.9 mixed state was impossible; pp headers on roster/FA + the declared mobile per-value-pp law; freshness is text-led ("market 11:07") with the dot as secondary, and the FA sketches carry freshness lines; the macro/quiet copy says "the gap shifted on 14 — 5 meaningfully (2+pp)" and "no percentile-gap movement," never "players moved." The framing already converged, don't re-run it.
> **v3.11 (2026-07-12, Codex round-8 dual NOT-CLEAR — 14 findings, all conceded; the Option-A ruling accepted and its downstream contract falsified):** **the coverage gate is rebuilt on the ruled basis's TRUE shape (the round's core catch):** the governed definition is CROSS-POSITION (FLEX/superflex starters + pooled bench depth), so same-position completeness cannot gate the rank layer — Codex's probe showed a WR's depth credit doubling when only an RB was nulled; the gate is now the **whole-skill-roster dependency closure** (extractor-derived: **1/11 opponents closed, David's own roster NOT closed**), same-position counts demoted to receipt facts, and the producer cycle inherits a named defect (`_xvar` null→0.0 must FAIL CLOSED, `team_value_matrix.py:18–20`); **all raw-sum computation removed** (extractor included — the ruled producer is the only legitimate value source) and every contract now requires ruling + §8(b) artifact + closure coverage, no two sufficing; **percentile-shift attribution is PER-POSITION** (percentiles live in position cohorts: TE 112→111 with 63 TE shifts + 1 RB; entrants RB×2, exits RB×2+TE×1; the 468 union demoted to context); **canonical IDs fail closed** (missing or duplicate `dg_player_id` aborts the extractor; no name fallback); **the strict lexicon fully lands** — surfaces say "not covered by DG" / "DG read unavailable" / "no FantasyCalc read" (no "yet"), route detail in receipts, `FormingReadCell` renamed `NoReadCell`; **freshness is date-qualified** everywhere ("market prices as of Sat Jul 11, 11:07 AM" — on Jul 12 before the 09:40 fire, a bare time reads as today); **mobile identity restored** (NFL team on every row — dropping it violated the canonical grammar); the FA control is one visible segment (state 2 keeps its column header) and the **mobile FA hard state is composed**; the five-second answer says "every player we currently value (468)"; "meaningfully" retired for "5 by 2+pp, 9 by 1pp"; WHAT MOVED header says "largest gap change first"; the mobile macro names the window (Jul 9→11) and says "DG values held"; width overruns trimmed. The framing already converged, don't re-run it.
> **v3.12 (2026-07-12, Codex round-9 dual NOT-CLEAR — 12 findings, all conceded):** **calendar truth** — 2026-07-11 was a SATURDAY; my v3.11 "date-qualified" freshness had qualified twelve sites with the wrong weekday (one line said "Sat Jul 11 … mkt Fri" about the same day); every stamp now reads Sat and the build time is date-qualified too ("updated Sat 12:43 PM"), with the weekday drawn from the calendar, never assumed; **closure no longer fails open on unknown position** — an unresolved rostered player MAY sit inside the Option-A dependency graph, so unknown position FAILS closure (the live artifact has one such rostered starter league-wide; IR/taxi active-set refinement is producer-owned); **the raw-sum block is actually deleted** (v3.11 had left a dead float accumulator still executing — 48 entries; gone); **mover ID mapping fails closed** (missing/duplicate canonical or sleeper ids abort — no empty-string or sleeper-id fallback ordering); **the union-only attribution prose is gone from §4/§13** (per-position: TE 112→111 drove 63 of 64 shifts; common-player position transitions are now GUARDED and derived — model-backed 0, all-rows 3 — never assumed); **the §8(b)/§12.10 handoffs pin the exact ruled closure output** + null/unknown fail-close (no generic "comparator coverage" that could regress to the rejected same-position test); the last "model reads forming" and the rejected "widened toward" prescription scrubbed; **mobile rows restored to full truth in two lines** — pp + "now" on every value, tie cardinalities (T-RB18 ×2, T-WR104 ×2), every market rank beside its OWN mark (RB3 rides the ○), and the compressed lead form is lead- and unit-bound ("DG lead +4pp"); **the mobile FA sketch is now the actual hard state** — "All 223" visibly selected, Bam Knight's no-FantasyCalc-read row present, the paired bar where both lanes exist, OVERALL ranks focal (#146/#153) per the unfiltered-scope law; the desktop selected-FA state gained its 146→153 ellipsis; **the width audit is now real** — a normalizer pass equalizes every │-line per block (0 unequal blocks; basis: character count, ambiguous-width glyphs as 1 — stated, not overclaimed); **materiality copy de-editorialized** — 1pp shifts are measured movement ("5 by 2+pp, 9 by 1pp; both measured"), and the quiet state names its ≥1pp threshold so sub-threshold drift is never denied. The framing already converged, don't re-run it.
> **v3.13 (2026-07-12, Codex round-10 dual NOT-CLEAR — 13 findings, all conceded — and REALITY MOVED):** **the first unattended 09:40 fire ran DEGRADED** (`market_source_prior_date`): a wake-coalesced ordering race — the runner finished 14:06:34Z, three seconds BEFORE the fresh Jul-12 FC capture landed (14:06:37Z) — so no new PIT pair exists and every "true overnight begins with the Jul 12 fire" claim was corrected to the degraded reality (today's real Daily Open is the degraded state; the healthy sketches are labeled as depicting Sat Jul 11; the ordering race is an operational follow-up, not a comp defect — the system fail-closed honestly). **The closure gate now mirrors the ruled dependency graph EXACTLY:** on_ir/on_taxi are excluded by the governed matrix itself (`team_value_matrix.py:39–49, 246–252`) — Wilson and Braelon (both IR) are OUTSIDE the graph, and **David's closure fails on exactly one active dependency: Tank Dell**; active unknown-position or null-xVAR rows fail closed; rosters derive from league context (extractor rebuilt; 1/11 opponents closed no longer by coincidence). **Movement identity gets a producer preflight prerequisite** (the runner silently skips missing sleeper ids and ON-CONFLICT-overwrites duplicates — `run_market_divergence_refresh.py:400–419`, probed; the extractor now also proves canonical-identity consistency across BOTH history days and rejects empty ids). **The attribution guard BRANCHES** — the composition-only cause claim renders only when both guards pass and is SUPPRESSED otherwise (an always-printing "VERIFIED" was print-only theater). Stale sweep completed (seed "model reads forming"; hardcoded "since Friday" → derived window; the extractor's pre-ruling comment block rewritten). **End-user:** ONE focal rank per scope (universe/FA = overall rank focal, position rank a named context chip — the double-primary column conflict resolved); mobile market marks carry their **Mkt** label; mobile movers say **"lead grew/shrank"** (levels vs change disambiguated); ONE pinned two-line grammar (identity+rank+controls / value-content) with the notes matching the drawings; **"within band" renders with NO magnitude** on the row (the "band +7pp" leak closed); mobile FA says "our model's replacement line" in full and every mobile header carries market vintage AND build time; the mobile macro leads with the gap answer (coverage demoted to a secondary line + receipt; "pre-model" no longer appears on any primary surface); the width normalizer now audits the WHOLE box grammar (separators included; 0 unequal). The framing already converged, don't re-run it.
> **v3.14 (2026-07-12, round-11 triaged under DAVID'S STRATEGIC RESET — this is the FINAL document pass; the medium changes to pixels):** contract findings fixed — **the dependency gate is now the EXACT governed graph** (skill positions only, K/DEF never block; numeric xVAR consumed regardless of engine route; null/unknown fail closed; empty-roster case named producer-owned; the Wilson sentence corrected — Dell is the active blocker); **two censuses, both extractor-emitted and labeled** (display vs dependency); **identity invariants scoped exactly** (renderable pairs: dg-id stable and non-empty, PROVEN; the 8 non-renderable dg-id churn rows DISCLOSED as a producer-preflight fact — name-equality proves nothing); **the Jul-12 degraded fire recorded in the backlog** (§12.3 mechanism-shipped vs unattended-reliability-open; §12.11 = the wake-ordering-guard ticket); the last lexicon residues fixed (seed + macro route detail receipt-only; "within band" always full; both denominators survive mobile compression; manager header labels — "DG OVRL"/"POS RANK", spec annotations never on the surface). **Composition findings (r11 #6–#10) land in the DISPOSABLE STATIC COMP** — the degraded-state composition, mobile action geometry (44px receipt targets, action track, zoom/wrap), dual freshness on every mobile header, and the final slot grammar are rendered there in real HTML, where they are testable facts instead of ASCII promises. The framing already converged, don't re-run it.
> **Uncommitted** pending David's word.

---

## 0. What changed from v2 — and why it matters

*(Historical record of the 2026-07-08 v2→v3 reground; its figures are as-of that date. The live grounding is §1.)*

v2 was structurally right (ranked Value Board hero, market margin as a column, paired positional rank, three tabs) but its **content was grounded on a stale June-23 artifact**, and that stale data told a false story. The fresh rebuild inverts three things:

1. **The board is 332 players, not 469.** Fresh population: **WR 138 · RB 89 · TE 61 · QB 44**. (183 gates-passed signals + 149 aligned = 332 with both lanes; 11,869 uncomparable; 12,201 total.)
2. **"Engine-A is the unlock" is dead.** v2 said David's marquee rookies (Jeanty, Henderson, Dart, Burden) were **blank** and Engine-A was the single unlock. **They are all valued now.** David is **24 of 27 on-board** (only Braelon Allen, Garrett Wilson, Tank Dell are off-board — availability/identity, not rookie-blindness). Engine-A is **demoted** from "the unlock" to a normal backlog item.
3. **The real divergence story is inverted — and it's the honest one.** The market ranks David's young studs **higher** than our model does (Jeanty market RB3 / our RB18; Dart market QB10 / our QB16; Henderson market RB15 / our RB27 — all **MODEL_LOW_MARKET_HIGH**). Meanwhile our model's biggest "we-see-more" calls are **proven veterans** — Jonnu Smith, Darren Waller, Keenan Allen (aging TEs/WRs, **MODEL_HIGH_MARKET_LOW**). This is a *humbling* pattern (is our model just an age-agnostic production scorer the market has already corrected for?) — exactly the counter-argument-first, `decision_supported=false` posture the constitution demands. **We show it honestly; we do not spin it as an edge.**

Also folded from cockpit re-review: header renamed **"DG Model Rank" / "vs Market Rank"** (kills the "our value is authoritative" overclaim); **FA sorts by our value** (David's ruling); Full Universe **defaults to the ranked board + a "Show uncomparable" toggle**; **all "target/trade" copy removed** (my own v2 leak Codex flagged); denominator basis disclosed in receipts, not as a surface footnote.

**A finding that surfaced and was then resolved in re-review (see §10):** sorting free agents by *raw DVS* surfaced a **wall of tight ends** (13 of the top 15). In v3 I flagged this for David's decision as the deferred PVO-normalization project. The cockpit re-review closed it: **xVAR is already the cross-position transform**, so pinning the board to xVAR (v3.1) yields a natural WR7/RB4/TE3/QB1 mix and needs no normalization project. Only a cosmetic market-comparable 0–2000 rescale remains deferred — no David decision required to ship an honest sort.

---

## 1. The real data this is grounded in (fresh, 2026-07-11 — the first volatility-complete day)

Verified against the live artifact via `comp-v33-extract.py`, not fixtures. Vintage honesty: **market vintage 11:07 AM ET** (`market_source_timestamp` 2026-07-11T15:07:55Z) · **built 12:43 PM ET** (`captured_at` 16:43:14Z) — two different facts, both rendered, never conflated (the D1 false-vintage defect stays dead in served data). Provenance: this artifact came from the **David-authorized manual runner run** on 07-11; the 09:40 LaunchAgent (installed the same day) first fires Jul 12.

- **Our value = xVAR** — the engine-aware unified cross-position transform: WR-equivalent points above a **model-defined positional replacement line**, with engine-specific replacement/λ maps (`pvo_assembler.py:471–490`) — the **same quantity the margin is computed from** (the divergence ranks players by within-position xVAR percentile, `universe_market_divergence.py:220`). **DG position ranks and model percentiles share the producer's PER-POSITION cohorts (WR 199 · TE 111 · RB 111 · QB 47 — `_model_cohorts` partitions by position); the OVERALL rank ranks the 468-row union, a different denominator than any percentile** (Codex r7 #2). Ranks never change when a display filter changes. **DVS 0–100** stays in the receipt.
- **The board = rows where a market comparison exists** (signal status `gates_passed` or `inside_band`): **340** of the 468 (**WR 138 · RB 89 · TE 68 · QB 45**; engines **278 Engine-B · 62 Engine-A**; 07-09 held 334, so the +6 is tail churn, Codex-cleared against the PIT history). Board membership is **status membership, not percentile presence** — a `gates_blocked` row (e.g. the producer's `volatile_market` gate, `universe_market_divergence.py:214–216`) carries percentiles but is NOT board content. **Reality check (v3.5, Codex):** under the current scheduled runner, a `gates_blocked` row never reaches a served artifact at all — `_has_stale_market` fail-closes the whole refresh — so no row-level "signal withheld" state is reachable today; per-row quarantine is the already-named Phase-0b follow-up ticket (§12.9), and the designed row state ships with it, not before. Zero such rows in any served artifact.
- **Market value = FantasyCalc** (Superflex 2QB / 12-team / PPR / dynasty), captured same-day, with a native position rank per player.
- **The margin is computed honestly in percentile space** (`universe_market_divergence.v1`): **191 gates-passed signals (145 model-rank-above-market · 46 market-rank-above-model), 149 within band; 11,861 of 12,201 rows are not board content** (128 valued-but-no-market-read + 11,733 unvalued). `decision_supported=false` throughout. Zero banned language; zero `decision_supported=true`. The 191 are **descriptive disagreements only** — nothing here is a validated edge (Gate-4 accrual-gated, ~Dec 2026).
- **David (Woodbury Riders, roster 1) — 24 of 27 on-board**, the most-covered roster in the league. Summary: **DG leads on 9 · market leads on 6 · within band on 9 · not compared 3** — 2 DG reads unavailable (Braelon Allen, Garrett Wilson; model-routed, xVAR null) + 1 pre-model (Tank Dell); named pre-UI follow-up, §12.8.

**The full paired-rank table — David's roster, fresh 2026-07-11 (sorted by the stored percentile delta — the only legal signed magnitude; Δ = the artifact's `model_minus_market_delta` × 100 under the NAMED rounding rule: half-away-from-zero to an integer computed in EXACT DECIMAL (never binary-float scaling), −0 → 0, rounded once producer-side; tie labels = competition ranking):**

| Player | Pos | Team | DG Rank (cohort) | vs Market Rank (FC native) | Model pct | Market pct | Δ (stored) | Read |
|---|---|---|---|---|---|---|---|---|
| Theo Johnson | TE | NYG | **TE23** | TE38 | 80 | 46 | +33 | we rank higher |
| Kaelon Black | RB | SF | **RB30** | RB63 | 73 | 42 | +31 | we rank higher |
| Elic Ayomanor | WR | TEN | **WR64** | WR94 | 68 | 39 | +30 | we rank higher |
| Xavier Legette | WR | CAR | **WR87** | WR109 | 57 | 29 | +28 | we rank higher |
| Chimere Dike | WR | TEN | **WR67** | WR89 | 67 | 42 | +25 | we rank higher |
| Rasheen Ali | RB | BAL | **RB88** | RB106 | 21 | 2 | +19 | we rank higher |
| Pat Bryant | WR | DEN | **WR84** | WR86 | 58 | 44 | +14 | we rank higher |
| AJ Barner | TE | SEA | **TE17** | TE20 | 85 | 72 | +13 | we rank higher |
| Parker Washington | WR | JAX | **WR28** | WR40 | 86 | 74 | +12 | we rank higher |
| Kyle Williams | WR | NE | T-WR104 ×2 | WR91 | 48 | 41 | +7 | within band |
| Chris Bell | WR | MIA | WR68 | WR57 | 66 | 63 | +3 | within band |
| Dillon Gabriel | QB | CLE | QB38 | QB57 | 20 | 18 | +2 | within band |
| Tucker Kraft | TE | GB | **T-TE1 ×11** | TE5 | 95 | 94 | +1 | within band |
| Tre Harris | WR | LAC | WR89 | WR67 | 56 | 56 | −1 | within band |
| Rome Odunze | WR | CHI | WR38 | WR26 | 81 | 83 | −2 | within band |
| Adonai Mitchell | WR | IND | WR98 | WR69 | 51 | 55 | −4 | within band |
| Omar Cooper Jr. | WR | NYJ | WR51 | WR33 | 75 | 79 | −4 | within band |
| Luther Burden | WR | CHI | WR47 | WR22 | 77 | 86 | −9 | within band |
| TreVeyon Henderson | RB | NE | RB27 | **RB15** | 76 | 87 | −11 | market higher |
| Ashton Jeanty | RB | LV | T-RB18 ×2 | **RB3** | 84 | 98 | −14 | market higher |
| Mac Jones | QB | SF | QB32 | **QB37** | 33 | 47 | −14 | market higher |
| Jaxson Dart | QB | NYG | QB16 | **QB10** | 67 | 86 | −19 | market higher |
| J.J. McCarthy | QB | MIN | QB30 | **QB30** | 37 | 57 | −20 | market higher |
| Fernando Mendoza | QB | LV | QB20 | **QB15** | 59 | 79 | −21 | market higher |

*(DG position ranks share the per-position cohorts with the percentiles (the overall # uses the 468-row union — a separate denominator) — so Rasheen Ali reads RB88, not the within-board RB83 v3.3 printed. Competition ranking makes ties honest: Kraft is T-TE1 ×11 — he shares the clamped top TE value with ten others (§11), and the label says so instead of manufacturing TE7. Rank labels never shift when a filter changes.)*

**Two proof pairs for why percentile space is the only arithmetic (the denominator law, live on David's own roster):**
- **J.J. McCarthy** — the native labels are *identical* (DG QB30 · Market QB30), yet the market ranks him **20 percentile points higher** (37 vs 57, stored Δ −20): the denominators differ (our 47-QB model cohort vs FantasyCalc's full QB universe). A native-rank subtraction would print "0 · agreement" — a false read.
- **Dillon Gabriel** — the native labels are *19 apart* (DG QB38 · Market QB57), yet the lanes are **within band** (20 vs 18, stored Δ +2). A native-rank subtraction would print a large fake disagreement.

Neither pair asks the manager to do coordinate math on the surface: the row's bar plots the two percentile marks on one axis, so McCarthy's wide gap and Gabriel's near-adjacent marks read at a glance; the native ranks ride beside it as the familiar labels they are. (Renormalizing market ranks into our cohort to force a shared denominator was proposed in re-review and **REJECTED by David, 2026-07-11** — native ranks stay labels; §12.)

Sign convention: **+ = our model ranks the player higher than the market; − = the market ranks him higher; within band = inside the disclosed noise band** (a statement about the two *valuations*, never about roster equivalence). Neutral. Never "buy/sell."

**The universe extremes (Full Universe content, fresh 2026-07-11; gates-passed only, by |percentile delta|):**
- **Biggest "we see more" (model rank above market):** Darren Waller (TE, +63), Zach Ertz (TE, +62), Jonnu Smith (TE, +59), Keenan Allen (WR, +49), Sam Roush (TE, +49). *Pattern unchanged: proven veterans / TEs.* Counter-argument surfaced in the receipt: our model reads production, not dynasty longevity — this is the known age blind spot, stated, not hidden.
- **Biggest "market pays more" (market rank above model):** Joe Burrow (QB, −40), Michael Penix Jr. (QB, −32), Carson Beck (QB, −32), Shedeur Sanders (QB, −30), Tank Bigsby (RB, −29). *Pattern unchanged: the Superflex youth/QB premium.*

---

## 2. The shared Value Row grammar (both surfaces compose from this)

Extends DESIGN.md's canonical AssetRow. **The focal value is OUR value/rank; the margin is a distinct column, not the hero.**

```
rank · pos-chip · IDENTITY (headshot + name + team-ring) · DG MODEL RANK (focal) · vs MARKET RANK col · trend(small) · receipt
```

- **Focal value = our value (xVAR), rendered as position rank** (Archivo, 2–3× weight, right-aligned tabular): "WR38" — the within-position xVAR percentile rank, the *same basis the margin uses*. DVS + the raw xVAR + the 0–100 index live in the receipt. **Scope decides the ONE focal number (r10 #6):** roster/position-group scopes → the position rank is focal; Full Universe/FA scopes → the OVERALL rank is focal and the position rank renders as a named context chip — never two primary rank columns in one scope. **Ranks are point estimates, not pinpoint truth** — the row treats a rank as the center of a value cohort, and the receipt discloses the confidence band. **Important v3.2 correction:** the named lexicon (Generational / Elite / Cornerstone / Starter / Depth) may not bind to fixed percentile cuts. It is gated on a `tier_calibration` artifact (§11) that derives cohort boundaries from unclamped model value plus historical outcome separation. Until that exists, render neutral math-first spans such as "Top cohort · WR1–7" or "Value band · RB18–31"; do not render "Elite" or "Cornerstone" in production.
- **Margin column ("vs Market")** — the honest comparable quantity is the **percentile gap**, not a raw spot-delta. A **slim two-mark bar** plots the two *percentiles* on a shared, absolute 0–100 axis — one **model-blue** mark, one **market-amber** mark, **each physically placed at its percentile position** and the gap length carrying magnitude (no intensity ramp, no hue-heat, no green/red). **The five-second-scan rule (v3.5, CR §1 law 1 + Codex end-user #2):** the row prints exactly ONE comparison number — the **display delta** — beside the bar, plus the familiar market label (`Mkt RB3`). The percentile endpoint numerals (84, 98) live in the **receipt**, not on the row; printing them on-row is what made v3.3/v3.4 rows a math puzzle. Within-band rows keep BOTH marks at their **true percentile coordinates** — forcing overlap falsified the axis (Kyle Williams is 48 vs 41 and still band; Codex round 4); the band conveys via quiet styling + label, never moved marks. Missing lane → the designed no-read state for that lane ("DG read unavailable" / "no FantasyCalc read"), never a fake 0 and never trajectory language.
- **The rounding contract (v3.5, named; v3.6 exact-decimal):** display delta = stored `model_minus_market_delta` × 100, rounded **half-away-from-zero** to an integer **in exact decimal arithmetic on the serialized value — binary-float scaling is itself a defect** (float renders Bigsby's −0.285 as −28; the exact value is −28.5 → **−29**). **Negative zero normalizes to 0**, computed **once, producer-side** (the production home is a producer-emitted integer field, §8) — the frontend renders the provided integer verbatim and never re-rounds. Under the named rule: Henderson −11, Mendoza −21, Bigsby −29.
- **Ties share a rank (v3.5 law; v3.6 IMPLEMENTED — competition ranking):** identical xVAR values share the tie group's lowest rank with the tie disclosed. Real labels today: **Tucker Kraft T-TE1 ×11** (the §11 clamp tie — ten TEs share his value; TE7 was manufactured precision), six WRs at T-WR1 ×6, Jeanty T-RB18 ×2 (overall T-30 ×2), Kyle Williams T-WR104 ×2; 66 cohort rows sit in position ties. Deterministic id order remains the stable **row-order** tiebreak only, never a rank-value tiebreak. The §11 unclamped-latent work is the real resolution; until then the tie marker is the honest rendering — and it puts the clamp story on the surface where the manager can see it.
- **Team alias contract (v3.5, full observed set):** source abbreviations normalize to the frontend's canonical team-color map — today's observed alias set is `LVR→LV · SFO→SF · LA (Rams) · NOR→NO · KAN→KC · NWE→NE · TAM→TB` (19 board rows beyond the original two; Codex). An abbreviation outside the map renders the designed fallback (abbreviation as-is, neutral mark) — never a raw token error, never a missing color treated as "no team."
- **Cohort rail background (David line-graph correction):** the paired-rank rail should also show where the player sits inside the position's calibrated model-value field. The background may be divided into neutral shaded cohort spans, but those spans come from the tier-calibration artifact, not arbitrary percentiles. Example shape: if the calibrated RB top cohort is RB1–RB6 this run, the rightmost rail segment spans exactly RB1–RB6; if the next run's historical/current distribution supports RB1–RB11, the segment changes. The rail still carries no verdict hue; model/market marks remain blue/amber.
- **Denominator honesty (Codex #3 + Gemini risk 2; cohort-corrected v3.4):** our position rank is within the per-position cohort (the percentile population; the overall rank uses the 468 union — its own denominator); the market rank is FantasyCalc's native rank over its full universe — the two denominators differ, so a raw spot-delta between them is *distorted*. Live proof on David's own roster (2026-07-11): J.J. McCarthy's native labels are identical — DG QB30-of-47-cohort-QBs · Market QB30-of-FC's-full-QB-universe — so a naive subtraction prints "0 · agreement," while the honest percentile read is **"Model 37th pct · Market 57th pct"** (stored Δ −20): the market ranks him 20 points higher. The receipt shows that derivation explicitly, so the sign is never misread. The stored percentile delta is the single source of magnitude + sort.
- **Direction stated once, not thrice** (v1 bug): the bar + signed number carry it; no redundant "Model higher" chip.
- **Receipts** (focusable, beside the row): raw DVS, 0–100 index, raw market value + rank, source + as-of date, the denominator basis, and — in **manager prose, never schema nouns** — "a descriptive disagreement, not a recommendation."
- **Below-replacement honesty (v3.3; row-visible v3.4; scope-exact v3.7):** raw xVAR renders in the receipt **even when negative, stated plainly** — the model-valued FA pool is **223 players and ALL 223 sit below the replacement line** (97 with a market read on the board + 126 valued/no-market behind "Show all valued"; Keenan Allen −2.0 leads the board slice). The receipt says so in manager prose — *"below our model's replacement line for the position"* (the line is a model-defined per-position baseline, `pvo_assembler.py:471–490`; it is **not** derived from this league's roster settings, so the copy never claims it is). And per the v3.4 end-user correction (Gemini): this fact is **visible on the row surface, not only behind a click** — FA views render a **replacement-line `ValueBandDivider` at xVAR = 0** (disclosed basis, neutral hue), so a below-replacement pool can never scan as a shelf of viable adds. The FA sort stays honest (highest xVAR first) without implying the top of a below-replacement pool is a find.
- **Volatility is receipt-only (v3.3, the disclosed-basis law; provenance corrected v3.4):** `market_volatility` is **FantasyCalc's own source-computed metric** (their moving-deviation field) with an **undisclosed basis/window**. A number whose basis we cannot disclose may not carry primary-surface weight — it renders **only in the receipt**, with the corrected copy: *"FantasyCalc-source metric; basis undisclosed by source; our capture of it began Jul 10."* No urgency copy, no motion, no sort attaches to it. **Reconciliation with the producer (v3.4, Codex #1):** the producer already uses this metric as a **suppression-only gate** (`volatile_market` → `SUPPRESSED_VOLATILE_MARKET`, `universe_market_divergence.py:214–216`) — it can only *withhold* a disagreement signal, never create or amplify one, which is fail-conservative and compatible with this law. No suppressed row can reach a served artifact under the current runner (it fail-closes the whole refresh, §13); the designed "signal withheld" row state ships with the §12.9 per-row-quarantine ticket. Our **own** PIT-derived margin trend is the disclosed-basis alternative — depth-gated for trends, overnight-legal from day 2 (§13).

**Row coverage states** (all first-class, all designed):
1. **Both lanes** → value rank + margin bar.
2. **Within band** → marks at their true coordinates (often near, never forced together); quiet "within band" label + subdued styling, no magnitude drama.
3. **Our value, no FantasyCalc read** → value rank + "no FantasyCalc read" (source-bounded, no future implied — other markets may price him; we only speak for our source).
4. **Market only, DG read unavailable** → identity + market rank + draft capital + age + "DG read unavailable" (strict evidence — no trajectory implied). A *fantasy* state, not a blank.

---

## 3. SURFACE 1 — The Value Board (the hero)

### 5-second answer
> "Here's where **we** rank every player we currently value (468; coverage stated on the strip) — and, next to it, where the market ranks him and by how much we disagree."

### Focal hierarchy
1. **Player identity** (headshot + name + team ring) — recognition is the strongest pull; it leads.
2. **Our value (xVAR)** — the focal rank, scope-decided (§2): position rank in roster/group tabs, **overall rank in Full Universe/FA**; the other renders as named context. Headers use MANAGER labels ("DG OVERALL", "POS RANK") — spec annotations like "# FOCAL"/"(ctx)" never reach the surface; typography carries focality.
3. **The margin column** ("vs Market Rank": percentile-gap bar with native ranks as labels) — the edge, second read.
4. Trend (small), receipts — quiet / drawer.
5. Coverage/health — a quiet persistent scope label, never the headline.

### Lane-order statement
Our model (blue) is the product's voice and owns the **value** (the focal). Market (amber) appears **only** in the margin column, as an overlay — equal visual weight *within that column*, never blended into the value read. Position/identity hues are orthogonal, desaturated. No verdict hue anywhere.

### Layout & scope
One board engine, three durable **tabs** (top-left):
- **My Roster** — your team, **position-grouped** (Sleeper-style): `QUARTERBACKS (n) · RUNNING BACKS (n) · …`, each group ranked by our value; band dividers carry the group's count.
- **Other Teams** — browse each league-mate's roster (team picker), same position-grouped grammar + margin column, plus an explicit **sort toggle: position groups · disagreement size** (|stored Δ|, a declared transparent sort — v3.4, Gemini). Descriptive lens on where our model and the market disagree about the players *another manager* holds. (No "trade target" language — the disagreement is the content; the manager decides.)
- **Full Universe** — **overall rank by our value (xVAR)** across the **468-player model cohort** (1..468; rank labels stable under every filter) with **position filters** (All · QB · RB · WR · TE) and a **Free Agents / Rostered** filter. Defaults to the **340 rows with a market read**; a **"Show all valued"** toggle reveals the 128 valued-but-no-market-read rows (126 of them free agents; the designed "our value, no market" state), and **"Show unranked"** reaches the remaining 11,733 (never a rainbow of empty cells). Free Agents sorts by **our value (xVAR)**, highest first. Real today: the model-valued FA pool is **223 players — 97 on the board (market read) + 126 valued/no-market via "Show all valued"**; the board slice's xVAR sort yields a natural WR7/RB4/TE3/QB1 top-15 mix (Keenan Allen, Kareem Hunt, Marvin Mims on top), not the TE-wall a raw-DVS sort produced. **All 223 sit below our model's replacement line — the aggregate line names the pool scope, the divider marks the line, receipts say it plainly (§2).** *(See §10 — xVAR is the honest cross-position basis; the 0–2000 rescale is a separate deferred producer project.)*
- **One quiet disclosure strip (v3.5 — scaffolding-hide proportionality; Codex end-user #5):** honesty + coverage consolidate into a single line — *"Descriptive, not validated · 468 ranked · 340 with a market read · 191 disagreements ▸"* — whose ▸ opens the provenance drawer (full honesty sentence, coverage detail, basis notes). The freshness whisper sits at the viewport's bottom edge. v3.3/v3.4 stacked four plumbing rows above the first player; the first viewport is a fantasy surface, not a status board.
- **Search is standard equipment (v3.5 — DN ships search + "Showing 300 of 336"; Sleeper's Players tab leads with Search, XR):** a persistent, keyboard-focusable player search (name match, jumps to the row in the current scope) plus a **"Showing N of 468"** count beside the filters. A 468-player surface without a quick jump is a scroll punishment, not a terminal.
- **Position-group headers — the honest layer now, the ratified triple when it's earned (v3.6, Codex #6/#7/#8/#11):** what renders TODAY is `count · valued x/y` — the DISPLAY census (full roster), which the extractor now emits separately from the DEPENDENCY census (the governed active graph; r11 #2 — two censuses, both labeled, one numeric source): **QB (5) · 5/5 valued — RB (5) · 4/5 (Braelon: DG read unavailable) — WR (14) · 12/14 (Wilson: DG read unavailable · Dell: pre-model) — TE (3) · 3/3**. The DN-ratified `value + league rank` layer (DESIGN.md signature) is **withheld pending two gates**: (i) **the value basis — RULED by David (2026-07-12, "for now"): OPTION A, the governed `team_value_matrix.py:153–220` starter+depth-credit definition**, delivered via the §8(b) producer artifact (its own cockpit cycle; nothing renders until it ships) — v3.5's raw-sum assertion had collided with this existing governed definition; (ii) **coverage gates the rank on the RULED basis's EXACT dependency graph (Codex r8 #1, r10 #2)** — the governed definition is CROSS-POSITION (FLEX/SUPER_FLEX starters, `team_value_matrix.py:52–77`; pooled bench depth, `:97–105`) and its input set is the **ACTIVE roster: on_ir/on_taxi are EXCLUDED by the matrix itself** (`:39–49, 246–252`). The gate = the EXACT governed graph: active **SKILL_POSITIONS** rows only (K/DEF/FB are never dependencies) with **numeric xVAR consumed regardless of engine route/status** (the matrix does not filter on route — a numeric non-MODEL_BACKED row is a valid input, not a blocker); blockers = active skill rows with null/non-numeric xVAR, or active unknown-position rows (they MAY be skill). Rosters iterate the league snapshot's roster list in the producer (the extractor's player-row derivation cannot see an empty roster — limitation named). Today: **1/11 opponents closed; David fails on exactly ONE active blocker — Tank Dell** (Wilson and Braelon are on IR, outside the graph). Same-position counts remain receipt facts, never the gate. When it ships, the grammar is **"<value> vs replacement · <n>th of 12 · x/y valued"**, and the render requires the ruling AND the §8(b) producer artifact AND closure coverage — no two of the three suffice. Raw sums are no longer computed anywhere (extractor included): the ruled producer is the only legitimate value source. The §12.8 coverage ticket still matters for ROW coverage (Wilson's row reads "not covered by DG"), but under the ruled graph it is NOT what blocks the group value — Wilson is IR, outside Option-A arithmetic; **Dell is the active blocker**.
- **The FA aggregate truth renders above the list (v3.5 — Codex end-user #4):** the Free Agents scope opens with one line — *"All 223 model-valued free agents sit below our model's replacement line (97 of them carry a market read)."* — before any row, with the xVAR = 0 `ValueBandDivider` marking the line in the list itself (§2). The pool-wide conclusion must never live only inside per-row receipts while a ranked list implies opportunity.

### Desktop sketch — universe scope, overall rank (~1440px; real 2026-07-11 data; v3.5 simplified rows)
```
┌────────────────────────────────────────────────────────────────────────────────────────────┐
│ THE VALUE BOARD   [ My Roster · Other Teams · Full Universe ] ⌕ Search [All] ⚙             │
│ Descriptive, not validated · 468 ranked · 340 with market read · 191 disagr. ▸             │
│ [FA · Rostered]  pos: All ▾                                Showing 340 of 468              │
├─────────┬────────────────────────┬───────────┬───────────────────────────┬─────────────────┤
│ DG OVRL │ PLAYER                 │ POS RANK  │ PERCENTILE GAP (pp)      │                  │
├─────────┼────────────────────────┼───────────┼───────────────────────────┼─────────────────┤
│ T-30 ×2 │ ◍ Ashton Jeanty LV·RB  │ T-RB18 ×2 │    ●───○  −14pp · Mkt RB3 │ ▸ⓘ              │
│    ⋯    │                        │           │                           │                 │
│ 97      │ ◍ Rome Odunze  CHI·WR  │  WR38     │        ●○  within band · Mkt WR26│ ▸ⓘ       │
│    ⋯    │                        │           │                           │                 │
│ T-116×11│ ◍ Tucker Kraft GB·TE   │ T-TE1 ×11 │         ○● within band · Mkt TE5 │ ▸ⓘ       │
│    ⋯    │                        │           │                           │                 │
│ 184     │ ◍ Theo Johnson NYG·TE  │  TE23     │ ○──────● +33pp · Mkt TE38 │ ▸ⓘ              │
│    ⋯    │                        │           │                           │                 │
│ 227     │ ◍ X. Legette   CAR·WR  │  WR87     │ ○──────● +28pp · Mkt WR109│ ▸ⓘ              │
├─────────┴────────────────────────┴───────────┴───────────────────────────┴─────────────────┤
│ ● our pct (blue) · ○ market pct (amber) · marks ALWAYS at true coordinates —               │
│ band rows sit close because they ARE close, never forced together · row → the              │
│ player inspector (▸) · ⓘ on the number → the metric receipt · display delta =              │
│ the one signed number · ("Show unranked" reaches non-modeled players, e.g.                 │
│ Braelon Allen — DG read unavailable · Mkt RB52)                                            │
│ market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM                            │
└────────────────────────────────────────────────────────────────────────────────────────────┘
```
*(Row = focal DG rank + the bar + ONE signed number + the familiar market label. Endpoint percentiles live in the receipt. The ASCII marks are ordinal approximations — the build contract (§7 seed) requires absolute 0–100 positioning with gap length proportional to the delta. The rightmost ▸ opens the PLAYER INSPECTOR; the ⓘ beside the number opens the METRIC RECEIPT — two controls, never one; trend slots stay dark until the PIT depth gate opens, §13.)*

### Desktop sketch — roster scope, position-grouped (real 2026-07-11 data; v3.5 group headers + simplified rows)
```
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│ MY ROSTER — Woodbury Riders  [ grouped ]  ⌕ Search [in My Roster]                        │
│ PLAYER                          DG MODEL   PERCENTILE GAP (pp)                           │
│ DG leads 9 · market leads 6 · band 9 · 3 not covered by DG ▸                             │
│ QUARTERBACKS (5) · 5/5 valued ▸                                                          │
│  ◍ Jaxson Dart      NYG  QB16    ●───○   −19pp · Mkt QB10    ▸ⓘ                          │
│  ◍ Fernando Mendoza LV   QB20    ●───○   −21pp · Mkt QB15    ▸ⓘ                          │
│  ◍ J.J. McCarthy    MIN  QB30    ●───○   −20pp · Mkt QB30    ▸ⓘ                          │
│  ◍ Mac Jones        SF   QB32    ●──○    −14pp · Mkt QB37    ▸ⓘ                          │
│  ◍ Dillon Gabriel   CLE  QB38    ○●       within band · Mkt QB57    ▸ⓘ                   │
│ RUNNING BACKS (5) · 4/5 valued · 1 DG read unavailable ▸                                 │
│  ◍ Ashton Jeanty    LV  T-RB18×2 ●──○    −14pp · Mkt RB3     ▸ⓘ                          │
│  ◍ TreVeyon Henderson NE RB27    ●──○    −11pp · Mkt RB15    ▸ⓘ                          │
│  ◍ Kaelon Black     SF   RB30    ○───●   +31pp · Mkt RB63    ▸ⓘ                          │
│  ◍ Rasheen Ali      BAL  RB88    ○──●    +19pp · Mkt RB106   ▸ⓘ                          │
│  ◍ Braelon Allen    NYJ  DG read unavailable · Mkt RB52 ▸ⓘ                               │
│ WIDE RECEIVERS (14) · 12/14 valued · 2 not covered by DG ▸                               │
│ TIGHT ENDS (3) · 3/3 valued ▸                                                            │
│ market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM                          │
└──────────────────────────────────────────────────────────────────────────────────────────┘
```
*(The QB group is the denominator law rendered without homework: McCarthy's native labels are identical — QB30 · Mkt QB30 — yet his marks sit 20 points apart; Gabriel's labels are 19 apart yet his marks sit nearly together (18/20 — true coordinates, not forced). Group headers render the honest layer — count + valued x/y, missing names in the receipt; the value + league-rank triple requires the §12.10 ruling (satisfied) AND the §8(b) ruled-basis producer artifact AND dependency-closure coverage (1/11 opponents today) — the ruling alone un-gates nothing. Kraft renders T-TE1 ×11: the clamp tie, visible.)*

### Mobile sketch (~390px, TRUE two-line rows + thumb bar)
```
┌────────────────────────────────────┐
│ VALUE BOARD · mkt Sat11:07         │
│ RUNNING BACKS (5) · 4/5            │
│ valued · 1 not covered             │
│ ◍ Jeanty LV · T-RB18 ×2  ▸ⓘ        │
│   now −14pp · DG●──Mkt○ RB3        │
│ ◍ Black SF · RB30        ▸ⓘ        │
│   now +31pp · RB63 Mkt○──●DG       │
│ WIDE RECEIVERS (14) · 12/14        │
│ ◍ K.Williams NE T-WR104×2 ▸ⓘ       │
│   within within band · Mkt○WR91 ●DG│
│ ⌕ Search [Roster]  ⧉ Scope         │
└────────────────────────────────────┘
 THE two-line grammar (one law, both surfaces): line 1 =
 identity + rank/tie + controls (▸ⓘ, right edge, always);
 line 2 = the value content (now-gap in pp, the labeled
 true-coordinate bar with each rank beside its OWN mark —
 or the lead-verb move on Daily). Within band shows NO
 magnitude on the row (receipt only).
 bottom: thumb-reachable persistent search + scope switch
 (XR: the category's most-used experiences are mobile-native;
 v3.4's "two-line" row was actually three lines — Codex)
```

### Desktop sketches — Free Agents scope, BOTH states composed (real 2026-07-11 data)
```
STATE 1 — default (market-read slice)
┌────────────────────────────────────────────────────────────────────────────────────┐
│ FULL UNIVERSE · Free Agents   ⌕ Search [Free Agents]                               │
│ All 223 model-valued free agents sit below our model's replacement line.           │
│ [ ●Market read (97) · ○Show all valued (223) ]                                     │
├─────────┬────────────────────────┬───────────┬───────────────────────────┬─────────┤
│ DG OVRL │ PLAYER                 │ POS RANK  │ PERCENTILE GAP (pp)      │          │
│ ═══════════ MODEL REPLACEMENT LINE — every player below it ═══════════             │
│ 153     │ ◍ Keenan Allen LAC·WR  │  WR60     │ ○──────● +49pp · Mkt WR121│ ▸ⓘ      │
│    ⋯    │                        │           │                           │         │
│ 157     │ ◍ Kareem Hunt   KC·RB  │  RB51     │ ○─────● +46pp · Mkt RB99  │ ▸ⓘ      │
│ market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM                    │
└────────────────────────────────────────────────────────────────────────────────────┘

STATE 2 — "Show all valued" SELECTED (223; the no-FantasyCalc-read rows join)
┌────────────────────────────────────────────────────────────────────────────────────┐
│ FULL UNIVERSE · Free Agents   ⌕ Search [Free Agents]                               │
│ All 223 model-valued free agents sit below our model's replacement line.           │
│ [ ○Market read (97) · ●Show all valued (223) ]                                     │
├─────────┬────────────────────────┬───────────┬───────────────────────────┬─────────┤
│ DG OVRL │ PLAYER                 │ POS RANK  │ PERCENTILE GAP (pp)      │          │
│ ═══════════ MODEL REPLACEMENT LINE — every player below it ═══════════             │
│ 146     │ ◍ Bam Knight   ARI·RB  │  RB47     │ no FantasyCalc read       │ ▸ⓘ      │
│    ⋯    │                        │           │                           │         │
│ 153     │ ◍ Keenan Allen LAC·WR  │  WR60     │ ○──────● +49pp · Mkt WR121│ ▸ⓘ      │
│    ⋯    │                        │           │                           │         │
│ market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM                    │
└────────────────────────────────────────────────────────────────────────────────────┘
```
*(Every named row is extractor-verified: Allen LAC, overall 153 · WR60 · Mkt WR121 · +49pp; Hunt KC, overall 157 · RB51 · Mkt RB99 · +46pp; Bam Knight ARI, overall 146 · RB47, the highest-xVAR valued/no-FantasyCalc-read FA. NFL teams render — an unrostered-in-this-league player still has a real team. The two states are separate compositions: the no-read row exists ONLY in state 2 (a mixed default was an impossible state, Codex r7 #11); toggle selection is visible (●/○); both states carry the pp header and the freshness line. ⋯ rows mark elided ranks only. The replacement divider renders from artifact values (§2); below-replacement is stated in receipts player-by-player.)*

### Mobile sketch — Free Agents hard state (~390px)
```
┌─────────────────────────────┐
│ FREE AGENTS                 │
│ mkt Sat 11:07 · upd 12:43   │
│ All 223 valued FAs sit      │
│ below our model's           │
│ replacement line. ⓘ         │
│ [○Mkt read 97 · ●All 223]   │
│ ══ MODEL REPLACEMENT ══     │
│ ◍ B.Knight ARI · #146  ▸ⓘ   │
│   RB47 · no FantasyCalc read│
│ ◍ K.Allen LAC · #153   ▸ⓘ   │
│   +49pp · Mkt○WR121 ───●DG  │
│ ⌕ Search [Free Agents]      │
└─────────────────────────────┘
 THE hard state: "All 223" visibly SELECTED; the no-read
 row (Bam Knight) present with its designed state (three
 lines by necessity — the no-read prose is the content);
 focal = OVERALL rank (#146/#153 — the unfiltered-scope
 law); the paired bar renders where both lanes exist; the
 aggregate line + divider survive compression; mobile
 lead-language compresses to "DG lead +4pp" — lead-bound
 and unit-bound, never a bare signed move
```

---

## 4. SURFACE 2 — The Daily Open (the morning answer first; the roster as drill-down)

**v3.5 inversion (Codex end-user #1 + PRODUCT.md p2 macro-answer-first + David's day-2 overnight ruling):** v3.3/v3.4 opened with the static roster and bottom-carded the only daily element. That is backwards for a daily-login product — the manager's 7am question is *"did anything change for me overnight?"*, not *"what were my ranks again?"* The Daily Open now leads with the change.

### 5-second answer
> "Since Thursday the market moved prices on 22 of my 23 compared players; the gap shifted on 14 — 5 by 2+pp, 9 by 1pp — here are the biggest. My roster: DG leads 9 / market leads 6 / within band 9."

### Focal hierarchy
1. **The macro movement line (the morning answer, manager prose, named window, materiality- and coverage-honest — Codex #16, r4 #6/#11):** *"Since Thu Jul 9 — market prices moved on 22 of your 23 compared players; the percentile gap shifted 1pp+ on 14 (5 by 2+pp). Newly compared: Rasheen Ali. Not compared: 3 not covered by DG ⓘ. Our model's values held."* (route detail — IR-unavailable vs active-unavailable vs pre-model — is receipt-only) **"Compared" is exact:** 23 = the comparable intersection of the 27-man roster (23 + 1 newly compared + 3 not covered by DG = 27; route detail in receipts) — never "tracked," and the not-compared states never collapse. **Every clause is DERIVED IN ITS OWN SCOPE (Codex r6 #1/#2):** the roster market clause from David's compared set (22 of 23 changed; all 14 gap movers changed) — the full-set figure (325) lives in the receipt, never as the surfaced claim; "our model's values held" from separate booleans per scope (full set 0 → holds; roster 0 → holds; an off-roster change can never fail the roster clause); percentile-shift attribution is **verified before it renders** — raw xVAR compared across ALL 466 common model-backed rows (0 changed today), THEN attributed **per position**: TE cohort 112→111 drove 63 of the 64 shifts (+1 RB); entrants RB×2, exits RB×2 + TE×1; common-player position transitions are guarded (model-backed: 0 today), and the union (469→468) is context only. The coverage classification is strict evidence: "DG read unavailable" (model-routed, xVAR null — Braelon, Wilson; market reads RB52/WR14 exist) and "pre-model" (Dell, WR71); anything finer than "unavailable" awaits the producer classifier (§12.8). Real numbers, today, from the PIT history; the receipt carries the wider scope honestly labeled — **204 board-wide = 133 rostered + 71 free agents** (v3.5's "league 202" was number- and scope-wrong). The window is always named — never "overnight" across a gap (Jul 10 had no scheduled run). **LIVE STATE UPDATE (Jul 12):** the first unattended 09:40 fire ran **DEGRADED** (`market_source_prior_date` — a wake-coalesced ordering race: the runner finished 14:06:34Z, three seconds BEFORE the fresh FC capture landed at 14:06:37Z); no new PIT pair exists, so **today's real Daily Open is the degraded state** — banner up, market lane dashed, last-successful labeled "Sat Jul 11" — and true overnight arrives with the next SUCCESSFUL fire. The healthy sketches depict the Sat Jul 11 state and say so. Movement = |display-delta change| ≥ 1 between the GLOBAL latest capture pair (§13 pairing law); the decomposition states magnitude plainly — 5 by 2+pp, 9 by 1pp; both are measured movement, neither is editorialized; entrants/exits are a coverage note, never counted as movement.
2. **WHAT MOVED — the changed rows** under a declared sort: largest |move| first, **equal |move| ordered by CANONICAL DG id** (sign-neutral; and the id space matters — history keys are sleeper ids and must map first; Codex r4 #1). Real order today: **Mac Jones, Legette, Bell** (the |4| trio), Gabriel, Bryant. **The move renders as lead-language GAP prose with the unit named, never a bare signed number** (Codex r4 #10, r5 #5) — a bare "+4" reads as player appreciation (Legette's market value FELL 555→450 while the gap moved +4 toward us), and "widened toward" was still ambiguous. Final form — the column header names **PERCENTILE GAP (pp)** and rows read: `◍ Legette WR87 · DG lead grew 4pp · now +28pp ▸ⓘ` / `◍ Bell WR68 · DG lead shrank 4pp · now within band ▸ⓘ` / `◍ Mac Jones QB32 · market lead grew 4pp · now −14pp ▸ⓘ`. The receipt decomposes which lane moved. Not a system-nominated hero — a declared-sort list of the manager's own players.
3. **YOUR ROSTER — position groups as drill-down**, with the v3.6 group headers (count + valued x/y; the value/league-rank layer stays withheld per §12.10); groups expand to full rows. The static summary line — "DG leads 9 · market leads 6 · within band 9 (Ali, newly compared, is among these 24) · 3 not covered by DG" (route detail — unavailable vs pre-model — lives in the receipt) — 24 + 3 = 27; Ali is never double-counted outside the 24 (Codex r6 #6). Rides with this block, not above the movement answer.
4. **Freshness whisper** (ambient) — date-qualified whenever the vintage is not today (Codex r8 #7): "market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM" (or "yesterday, 11:07 AM"; weekday from the calendar, never assumed). On Jul 12 before the 09:40 fire, a bare "11:07" would read as today — a false-freshness read. Vintage and build time both render; zero-attention when healthy.
5. **Degraded banner** (exception only) — loud, dashes the affected lane, only on real failure.

Multi-day trend renderings (sparklines/trajectories) stay behind the 14-day depth gate — a 2-point series is a delta, not a trend. FantasyCalc's volatility metric never substitutes for either (undisclosed basis; receipt-only, §13).

### Lane-order statement
Identical to the board: our value leads (blue focal), market appears only in the margin column (amber overlay), equal weight within that column, orthogonal identity hues. The movement line names both lanes' DERIVED behavior each day ("market prices moved on 22… our model's values held") — never an assumed off-season truth; the day the model publishes new values, the same derivation says so. Symmetry is preserved by deriving both lanes, not by asserting either.

### Desktop sketch — Daily Open (~1440px; real 2026-07-11 data)
```
┌───────────────────────────────────────────────────────────────────────────────────────────┐
│ DAILY OPEN · Sat Jul 11  Woodbury Riders ⌕ Search [My Roster] mkt Sat 11:07 ●             │
│                                                                                           │
│  SINCE THU JUL 9 — market prices moved on 22 of your 23 compared players;                 │
│  the gap shifted 1pp+ on 14 (5 by 2+pp). Newly compared: Rasheen Ali.                     │
│  Not compared: 3 not covered by DG ⓘ. DG values held (derived).                           │
│                                                                                           │
│  WHAT MOVED — largest gap change first        PERCENTILE GAP (pp)                         │
│   ◍ Mac Jones      SF·QB    QB32    market lead grew 4pp · now −14pp      ▸ⓘ              │
│   ◍ Xavier Legette CAR·WR   WR87    DG lead grew 4pp · now +28pp          ▸ⓘ              │
│   ◍ Chris Bell     MIA·WR   WR68    DG lead shrank 4pp · now within band         ▸ⓘ       │
│   (11 more movers ▾)                                                                      │
│                                                                                           │
│  YOUR ROSTER — DG leads 9 · market leads 6 · band 9 · 3 not covered by DG ▸               │
│   QUARTERBACKS (5) · 5/5 valued ▸                                                         │
│   RUNNING BACKS (5) · 4/5 valued · 1 DG read unavailable ▸                                │
│   WIDE RECEIVERS (14) · 12/14 valued · 2 not covered by DG ▸                              │
│   TIGHT ENDS (3) · 3/3 valued ▸                                                           │
│                                                                                           │
│  market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM · about these ▸          │
└───────────────────────────────────────────────────────────────────────────────────────────┘
```

### Mobile sketch — Daily Open (~390px; the one-handed morning check)
```
┌──────────────────────────────┐
│ DAILY OPEN                   │
│ mkt Sat 11:07 · upd 12:43    │
│ Jul 9→11: your gaps moved    │
│ on 14 of 23 — 5 by 2+pp.     │
│ Market moved · DG held ⓘ     │
│ WHAT MOVED (pp)              │
│ ◍ Mac Jones SF QB32       ▸ⓘ │
│   mkt lead grew 4pp·now −14pp│
│ ◍ Legette CAR WR87        ▸ⓘ │
│   DG lead grew 4pp·now +28pp │
│ (12 more movers ▾)           │
│ new: Ali · 3 not covered ⓘ   │
│ ROSTER ▾ DG leads 9 ·        │
│ market leads 6 · band 9      │
│ ⌕ Search [Roster] ⧉ Board    │
└──────────────────────────────┘
 first screen = macro line + changed rows + collapsed
 groups (DESIGN.md mobile law); "mkt Sat 11:07" = the required status pill speaking
 FRESHNESS in manager terms (never developer health);
 [Roster] = the visible search-scope chip; every mover
 row = exactly two lines (identity+now-gap / move prose);
 "(12 more movers ▾)" is the route to the full list;
 inspector = bottom sheet; BOTH lanes labeled (DG/Mkt) and
 every bar carries an aria sentence stating the read + the
 STORED gap, with endpoints as separate rounded facts —
 never endpoint arithmetic — endpoints 80/46 and the stored +33 are
 separate facts, none derived from another; summary in words,
 never opaque digit strings; MOBILE pp LAW: per-value pp
 units on every gap (no table headers on phone — declared,
 not both-claimed); sticky controls: 44px targets,
 safe-area inset, visible focus, bottom padding; no
 horizontal overflow
```

### Key states
- **Default:** macro movement line → WHAT MOVED (declared sort) → grouped roster with summary → freshness whisper.
- **Quiet day:** the macro line IS the content — *"No percentile-gap movement of 1pp or more on your players since <the prior day of the GLOBAL pair>."* (window DERIVED, never hardcoded weekday copy; threshold named so sub-threshold drift is never denied; gap language, never "players moved") — then the grouped roster; the model-held clause renders ONLY when its raw-xVAR derivation passes that day (never unconditional copy). Designed, not empty.
- **Day-1 / no eligible pair:** the macro line renders the forming state — *"our daily record began Jul 9 — N days of tape so far"* — and the roster leads until a same-player pair exists.
- **Stale/failed market capture:** loud banner; market column dashes (`—`); never last-known-as-fresh.
- **DG read unavailable:** first-class market-context row (never a text bin, never a trajectory claim).

---

## 5. Interaction & motion
- **Three distinct affordances, never conflated (v3.6, Codex #13):** (1) **row activation** (tap/click on identity or row body) opens the **player inspector** (desktop drawer / mobile bottom-sheet) — the person: identity, percentile endpoints ("Model 84th pct · Market 98th pct of position cohort"), raw DVS, 0–100 index, raw xVAR (negative stated plainly), raw market value + native rank, adjacent comparison, navigation; (2) an **ⓘ receipt control** on the metric itself opens the **metric receipt** — the number's provenance: source + as-of, denominator basis, rounding rule, the "descriptive disagreement, not a recommendation" line; (3) the **Trade Lab handoff** is a distinct action inside the inspector that places the player in a **neutral staging tray** — the manager assigns Give/Get in Trade Lab; the tool never pre-assigns a side (auto-placement rejected in the Gemini screen as a nudge). All three keyboard-first, focusable, Esc closes.
- **Search (v3.5; scope made visible v3.6, Codex #15):** persistent, keyboard-focusable (`/` shortcut on desktop; thumb-bar on mobile); the field carries a **visible scope chip** ("in My Roster"), and the **zero state offers the wider pool**: "No matches in My Roster — search all players?" (never a silent empty list). Name match jumps to the row and opens its group. Mobile sticky controls: **44px minimum targets, safe-area insets, visible focus ring, bottom padding** so the last row is never trapped under the bar. Discovery on a 468-player surface must not be scroll-only (DN/Sleeper table chrome, CR §1/XR).
- **Adjacent comparison (v3.5, KTC "value-adjacent players" pattern, CR §3):** the inspector shows the player's nearest neighbors by our value (±3 in the position cohort) with their margins — a declared-basis list, not a suggestion.
- **Decision-surface handoff (v3.5, user-initiated only):** the inspector offers navigation — "Open in Trade Lab (prefills this player)" · "See roster context." Navigation is not advice: David initiates, the tool carries context. No tool-initiated prompt ever suggests the trip.
- **Scope/filter/sort:** instant, FLIP row-reorder for object constancy; no count-up on re-sort. Rank labels never change under filters (cohort-stable ranks, §1–2).
- **Motion:** productive only (150–240ms); the paired-rank marks settle on load (reduced-motion-safe); no urgency, no magnitude "pulse," nothing drawn past the Hard Right Edge on any spark. Motion never implies confidence or action.

## 6. Copy — manager prose, translation table (no schema nouns on the surface)
| Never show | Show instead |
|---|---|
| `decision_supported=false` | "A descriptive disagreement, not a recommendation." |
| "failed-gate reason" / UNAVAILABLE | "DG read unavailable" / "Why no DG read" (strict evidence; never "forming") |
| "active model" / "model_status" | "NFL-usage model" |
| "MODEL HIGHER THAN MARKET (145)" + "we rate above the crowd" | "Model rank above market (145)" — count only, no gloss |
| "market pays a premium we don't" | "Market rank above model (46)" |
| `universe_market_divergence` / "producer join" / "Phase 17.4" | (never surfaced — internal only) |
| `market_volatility` / "maybeMovingStandardDeviation" | receipt only: "FantasyCalc-source metric; basis undisclosed by source; our capture of it began Jul 10" |
| "aligned" (implies an equivalence judgment) — and bare "band"/"now band" compressions | "within band", always the full phrase |
| "Market moved · DG held" (denominator dropped) | both denominators survive compression: "mkt moved 22/23 · gap 14/23 · DG held" |
| `K` / `KTC` as the market mark label | "Mkt" — the source is FantasyCalc; "K" invites a KTC misread (Codex end-user #8) |
| a group total silently missing players | "· 2 not covered by DG" on the header; the receipt names them (Wilson, Dell) with route detail (unavailable vs pre-model) |
| a run-on group header | "<value> vs replacement · <n>th of 12 · x/y valued" — only after the ruled producer artifact + closure coverage |
| a bare bar with no text alternative | every bar carries an aria sentence: "We rank him higher; stored gap +33 percentile points. Our model: 80th percentile; market: 46th (rounded separately)." |
| "14 of your players moved" (undecomposed) | "the percentile gap moved on 14 of 23 compared — 5 by 2+pp, 9 by 1pp · newly compared: Ali · 3 not covered by DG" |
| a bare signed move ("moved +4") — and the retired "widened toward" form | lead-language with units: "DG lead grew 4pp · now +28pp" (a bare +4 reads as appreciation — Legette's market price FELL while the gap moved 4pp toward us) |
| "mvd" / "twd" / "ID" / "9·6·9·3" (diagnostics shorthand) | plain words only: "moved", "toward", the summary spelled out ("DG leads 9 · market leads 6 · within band 9 · 3 not covered by DG") |
| "our model held" (asserted) | derived per scope (full set AND roster, separate booleans); receipt attributes percentile shifts to model-cohort composition, verified across all common model-backed rows first |
| "overnight" across a capture gap | the actual window, named: "since Thu Jul 9" |
| `SUPPRESSED_VOLATILE_MARKET` / "gates_blocked" | (unreachable in served data today — the runner fail-closes the whole refresh; the designed "signal withheld" row state ships with the per-row quarantine ticket, §12.9) |
| `source_timestamp_is_fetch_time_not_publish_time` (row caveat token) | (never surfaced — freshness renders from the root vintage fields, DATE-QUALIFIED when the vintage is not today: "market prices as of Sat Jul 11, 11:07 AM · updated Sat 12:43 PM") |
| any "target / trade / add / drop" verb (incl. my own v2 leak) | state the disagreement; the manager acts |

Banned on the surface everywhere: buy/sell/hold/start/sit, under/over-priced, edge/target/value-window/arbitrage, recommended/priority/act, "crowd," tool-nominated "player of the day."

## 7. States & edge-case matrix (RED seeds)
our-value-only (no FantasyCalc read) · market-only (DG read unavailable) · both · within-band/±0 (render "within band," never visible −0.0) · unresolved identity · stale divergence artifact · stale market cache · stale PVO · empty roster · roster with zero DG-covered players · malformed/out-of-range rank or percentile · signal/percentile mismatch · duplicate identity or rank · position with <N ranked (thin position) · market rank present but no position (fallback) · league-scope player on multiple rosters · deterministic tie ordering (canonical id).

**Read-path seeds Codex added in v3.1 re-review (these are the ones that keep the surface honest):**
- **Rank-basis consistency** — the API must pin ONE model basis: the focal "our value" rank and the margin percentile are both **xVAR**; DVS is never a sort/rank basis (else contradictory signs — a QB reads market-higher by percentile while DVS text implies model-higher).
- **Raw spot-delta forbidden** unless a shared-population rank exists — the bar renders **percentile positions**, and any displayed spot-count is a label, never subtracted across mismatched denominators.
- **Phase-0 marker missing/stale degrades the READ path** — when the daily-recompute status marker is absent or stale, the board shows a degraded/freshness state, never last-known-as-fresh.
- **Two disclosure routes, both pinned (Codex r7 #7)** — "Show all valued" reveals the 128 valued/no-market rows (126 FAs) as designed no-FantasyCalc-read rows; "Show unranked" reaches the 11,733 non-modeled rows; virtualization/pagination on both (never render all at once); a single collapsed "uncomparable" population is a failing test.
- **Missing `league_context.rostered`/`roster_id`/`owner`** — scope tabs degrade gracefully.
- **No visible schema nouns / action words in receipts**; keyboard/focus receipt tests (the inspector is keyboard-first).

**v3.3–v3.5 reground RED seeds (framing convergence + both Codex re-reviews + the end-user pass — the framing already converged, don't re-run it):**
- **Volatility never on a primary surface** — a test fails if `market_volatility` (or any rendering derived from its VALUE) appears in a board row, Daily Open row, summary line, tape, or sort. Receipt-only, exact copy ("FantasyCalc-source metric; basis undisclosed by source; our capture of it began Jul 10"), no urgency copy or motion.
- **Gates-blocked rows never join the board — and the UI must not spec unreachable states** — board membership = signal status ∈ {gates_passed, inside_band}; a synthetic `gates_blocked` fixture must not shift the 340/191 counts. Under the CURRENT runner (`_has_stale_market` fail-closes the whole refresh on any such row) no served artifact can contain one; the designed "signal withheld" row state is spec'd WITH the per-row quarantine ticket (§12.9), and a test asserting it against today's producer is itself a defect (Codex v3.4).
- **The 14-day depth gate binds multi-day TREND renderings, with eligibility DEFINED and the basis guard honest (v3.6, Codex #4)** — an eligible observation for a player = a history row on a capture day whose `signal_status` is renderable (gates_passed/inside_band). **The basis-version guard is a producer PREREQUISITE, not a present-tense filter:** history rows today carry only the row schema (`universe_pvo_batch.v1`) and never retain the root divergence basis — stamping it into every history row is a named prerequisite of the movement feed (§8); until stamped, comparisons are documented as single-era (v1 since inception) and a basis change without stamping blocks the feed rather than silently comparing across bases. Sparklines/trajectories require ≥14 eligible observations (counted, not calendar span; gaps allowed). Live shape: record began 2026-07-09; after the renderable filter, 328 players hold 2 eligible observations and 18 hold 1 (the raw table stores all 12,201 rows both days — eligibility is the filter, and the RED pins that distinction).
- **Movement is defined, window-named, intersection-only, GLOBAL-paired, and scope-honest (David's day-2 ruling; v3.6 Codex #4/#9/#10/#16)** — the **pairing law is the GLOBAL latest capture pair** (one named window for the whole surface; per-player latest-two was rejected — mixed windows in one list are illegible); movement = |display-delta change| ≥ 1 on that pair's same-player renderable intersection; scope is always labeled — **204 board-wide = 133 rostered + 71 free agents** (a test fails on "league" copy for a board-wide count); the macro line decomposes materiality (5 of David's 14 moved 2+; nine were 1-point display shifts) and coverage (newly compared: Rasheen Ali; 3 not covered by DG — surface state; route detail is receipt-only and data-backed, never a hand-set label); equal |move| orders **sign-neutrally by CANONICAL DG id, mapped from the history's sleeper-id keys before ordering** (a test fails if sign breaks ties OR if sleeper-id order substitutes for canonical order — the |4| trio fixture: Mac Jones, Legette, Bell); the rendered move is **lead-language gap prose with the pp unit named** ("DG lead grew 4pp" / "market lead grew 4pp"; column header names PERCENTILE GAP (pp); Legette fixture: market fell 555→450 while the gap moved 4pp toward us — a bare "moved +4", a unit-less delta, or ambiguous "widened toward" copy is a failing test); **"DG values held"/"market moved" render only when DERIVED** (raw xVAR equal across the pair on BOTH the full compared set and the surfaced scope; market-value comparison for the market clause); **percentile-shift attribution is PER-POSITION and GUARD-GATED** — the composition-only cause renders ONLY when both guards pass (zero model-backed raw-xVAR changes AND zero model-backed position transitions; otherwise SUPPRESSED — an always-printing attribution is itself a failing test); today both pass (TE 112→111 drove 63 shifts + 1 RB; entrants RB×2, exits RB×2 + TE×1; all-row transitions disclosed separately: 3); the union is context, never the attribution; coverage decomposes exactly (compared / newly compared / NOT COVERED BY DG are the surface states; route detail — unavailable vs pre-model — is receipt-only; collapsing or hand-labeling is a failing test); entrants/exits never count as movement.
- **The rounding contract, exact-decimal** — display delta = stored `model_minus_market_delta` × 100, **half-away-from-zero to an integer computed in exact decimal arithmetic (binary-float scaling is a failing test: float renders −0.285 as −28), −0 normalized to 0, rounded once producer-side** (production home = a producer-emitted integer field); the frontend renders the provided integer verbatim. Fixtures: McCarthy native QB30·QB30 renders −20 (never 0); Theo endpoint math says +34, the stored delta says +33; Mendoza −20.5 → **−21**; **Tank Bigsby −0.285 → −29** and Darnell Washington +0.145 → +15 (the exact-decimal pair); a frontend that re-rounds is a failing test.
- **Ties share a rank (competition ranking)** — rows with equal xVAR share the tie group's lowest rank, disclosed (`T-TE1 ×11` fixture: Kraft's clamp tie; `T-WR104 ×2`, `T-RB18 ×2`, overall `T-30 ×2`/`T-116 ×11`); a test fails if canonical-id order manufactures distinct rank VALUES (id order is legal only as row-order tiebreak).
- **Bar marks encode the ABSOLUTE axis — band included** — each mark's rendered position derives from its percentile on the 0–100 axis and the gap length is proportional to the delta; a test fails if lane identity determines order, if positions are merely ordinal, **or if a within-band row's marks are moved/merged from their true coordinates** (Kyle Williams fixture: 48 vs 41, band, marks visibly apart; band = quiet styling + label only). The ASCII sketches are declared approximations; the build is not.
- **Root-vintage freshness, fail-closed** — freshness renders only from root `market_snapshot_date` / `market_source_timestamp` / `captured_at`; missing/malformed root vintage renders the designed degraded state — the producer's build-clock fallback (`universe_market_divergence.py:155–158`) must never masquerade as market vintage; the row caveat token `source_timestamp_is_fetch_time_not_publish_time` never surfaces.
- **Negative xVAR renders plainly + the replacement line is visible + the FA aggregate leads** — values never clamped/hidden; the xVAR = 0 `ValueBandDivider` renders from artifact values; the FA scope opens with the aggregate line ("All 223 model-valued free agents sit below our model's replacement line") — scope = the FULL valued pool, not only the board slice (a 97-only claim is a failing test while "Show all valued" exposes 126 more); a test fails if a fully-below-replacement pool renders without it.
- **Team aliases normalize over the OBSERVED set** — the fixture enumerates today's live aliases with their canonical targets (LVR→LV · SFO→SF · LA→LAR · KAN→KC · NOR→NO · NWE→NE · TAM→TB — implemented in the extractor); an unmapped abbreviation renders the designed fallback, never a raw token and never a dropped team mark.
- **Group headers: honest layer now, gated triple later** — count + valued x/y render today (WR fixture: 12/14, receipt names Wilson/Dell with route detail); the value + league-rank layer renders ONLY with ALL THREE: the §12.10 ruling (satisfied) + the §8(b) ruled-basis producer artifact + **dependency-closure coverage** (the ruled basis is cross-position, so the gate = whole-skill-roster closure per roster — 1/11 opponents today, David not closed; same-position counts are receipt facts, and gating on them is a failing test; the producer must also FAIL CLOSED on null xVAR — `_xvar`'s current null→0.0 at `team_value_matrix.py:18–20` is a named defect for that cycle); single-step integer rounding on the ruled basis; grammar "<value> vs replacement · <n>th of 12 · x/y valued"; no numeric outcomes pre-pinned.
- **Search, affordances + accessibility** — search keyboard-reachable in every scope with a visible scope chip and a zero state offering the wider pool; the three affordances stay distinct (row → player inspector; ⓘ → metric receipt; handoff → neutral staging tray, never auto Give/Get); every paired bar carries an aria-equivalent sentence stating the read + the STORED display gap, with endpoints as separate "rounded separately" facts — never phrased as endpoint arithmetic (Theo fixture: endpoints 80/46 read "34" while the stored gap is +33; Codex r4 #15/r5 #1); BOTH lane labels present (DG and Mkt); mobile sticky controls at 44px+ with safe-area insets, visible focus, and bottom padding; the thumb bar present on both board and Daily Open.
- **Daily Open order is the macro answer first** — a test fails if the static roster block renders above the movement line when an eligible movement pair exists (and the day-1 forming state governs when none does).
- **Comparable-set churn is quiet** — day-over-day board growth/shrink at the tail renders without "system got smarter" copy (the Phase-0b §3.1 ban) and without movement drama.

**Tier-calibration RED seeds added after David's v3.2 correction:**
- **No fixed-percentile primary rule** — a test fails if "Elite", "Generational", "Starter", "Depth", or any shaded cohort span is assigned from a static percentile ladder (`>=99`, `95-99`, etc.) without the tier-calibration artifact.
- **No market-as-input** — market rank/value may appear on the margin lane, but never influences DG cohort boundaries.
- **No clamped top-end basis** — a test fails if named labels or top-cohort breaks are derived from public clamped DVS/xVAR when an unclamped latent value exists; current data shows RB/WR/TE top-end plateaus under the public fields.
- **Historical support required for named labels** — named labels render only when the artifact carries sufficient historical outcome support for that cohort; otherwise the UI renders neutral rank/value spans.
- **Boundary honesty** — players near a cohort break render a boundary/uncertainty marker instead of snapping confidently into a named tier.
- **Stale/low-coverage degradation** — stale calibration, low historical sample, or missing latent-value support suppresses named labels before it suppresses numeric rank/value.

## 8. New / extended primitive work
- **PairedRankBar** — NEW: two marks (blue model, amber market) on one **shared 0–100 percentile axis**, each mark positioned by its percentile (lower left, whichever lane — v3.4 axis correction), gap = magnitude, no intensity fill; within-band = true coordinates + quiet label/styling (never forced overlap — v3.7). (SpreadBar today renders a *single* dot on one lane — this is an extension or a new primitive.)
- **ValueRow** — extend the local Daily-What-Changed AssetRow into a shared primitive with a focal value-rank + margin column + the four coverage states.
- **ScopeSwitcher** (My Roster / Other Teams / Full Universe), **PositionGroup** header rendering count + valued x/y today (the value/league-rank triple joins only after the ruled §8(b) producer artifact + closure coverage), **NoReadCell** (the designed per-lane no-read states — renamed from FormingReadCell; "forming" implied trajectory), **SearchJump** (persistent player search, keyboard + mobile thumb bar, visible scope chip), **MoveLine** (the Daily Open macro movement line + per-row lead-language gap prose — never a bare ±N chip). DailyTape stays capture/provenance only.
- **Producer follow-ups the v3.6 contract implies (each its own cockpit cycle):** (a) a **display-delta integer field** on the divergence payload (the single exact-decimal rounding site); (b) a **group-value artifact** — basis RULED (§12.10, David 2026-07-12): the governed `team_value_matrix.py:153–220` starter+depth-credit definition; emitting the EXACT ruled dependency-closure result per roster (whole-skill-roster closure incl. unknown-position fail-close and the producer-owned IR/taxi active-set rule) plus per-group completeness lists — never a generic same-position "comparator coverage" flag; the producer FAILS CLOSED on null/unknown xVAR (`team_value_matrix.py:18–20` named defect); production reads a producer output, never client math; (c) **per-row quarantine** in the scheduled runner (the Phase-0b residual, §12.9) — prerequisite for any row-level "signal withheld" state; (d) the **movement feed** — prerequisites: **root basis-version stamping into every history row** (rows carry only `universe_pvo_batch.v1` today) + the global-pair law + the exact-decimal display integers + **producer identity preflight** (the runner today silently skips missing sleeper ids and ON-CONFLICT-overwrites duplicates, `run_market_divergence_refresh.py:400–419`; the feed must fail closed on missing/duplicate history identity) + **scoped canonical invariants** (r11 #3): for every RENDERABLE pair, `dg_player_id` must be non-empty and stable across both days (live: 8 non-renderable rows changed dg ids Jul 9→11 — disclosed as producer-preflight fact, never asserted stable; name-equality proves nothing).

## 9. Model character — the honest finding (replaces v2's "Engine-A unlock")
The fresh data retires v2's premise. The interesting, honest pattern is now the **shape of the disagreement**:
- Our model's biggest "we-see-more" calls cluster on **proven veterans and TEs** (Jonnu Smith, Waller, Keenan Allen). The market's biggest premiums cluster on **young QBs/RBs** (Burrow, rookie QBs, rookie RBs) — the Superflex youth premium.
- **Counter-argument (mandatory, per the constitution):** this could mean our model is a **production scorer that under-weights dynasty longevity/age** — i.e., the market may already be *correct* to pay the youth premium. We surface this as a descriptive disagreement with the age/experience context in the receipt; we do **not** claim the gap is an edge. Gate-4 validation (deferred, ~Dec 2026 accrual) is the only thing that could earn the word "edge."
- Engine-A (rookie capital+age prior) is **demoted** to a normal backlog item — no longer "the unlock," since the rookies are already valued. If it lands, its values ship with their own experimental grade and the same `decision_supported=false`.
- **QB model-character receipt note (v3.4 Gemini; v3.5 date-bound per Codex):** QB-row receipts carry the observed pattern as a dated fact, not a causal law — *"as of Jul 11, the four largest market-over-model gaps on this board are all QBs"* — regenerated with each artifact, dropped when the data stops supporting it. Descriptive context riding where the number lives — a receipt line, never a header alarm (layered-caveats law), never advice, and never a standing claim about why.

## 10. Cross-position value basis — resolved by xVAR (was: "the TE-wall decision")
**The finding (v3):** sorting the Full Universe / Free Agents by **raw DVS** surfaces a wall of tight ends — 13 of the top 15 free agents by DVS are TEs (Colby Parkinson 81.6, Cade Otton 81.6 … vs the top FA WR at 58.6). DVS is a within-position production composite; comparing it across positions is not meaningful.

**The resolution (v3.1, Codex #6, verified):** we do **not** need to defer or build the 0–2000 rescale to fix this — **xVAR already is the cross-position transform** (`pvo_assembler.py:471`), and it is the basis the margin already uses. Sorting free agents by xVAR yields a natural **WR7 / RB4 / TE3 / QB1** mix (Keenan Allen, Kareem Hunt, Marvin Mims on top) instead of 13/15 TEs. So pinning the board's "our value" to xVAR (v3.1) resolves the rank-basis contradiction **and** the TE-wall in one move — no David decision required to ship an honest overall sort.

**What remains genuinely deferred (unchanged from David's ruling):** the **market-comparable 0–2000 rescale** — i.e. expressing our value on a scale directly comparable to FantasyCalc's magnitude. Per Codex, that should be a **producer field with explicit provenance, not a UI-side normalization**. It is a nicety for magnitude-comparison, not a blocker: the percentile-space margin (§2) is already the correct apples-to-apples comparison. Still David-gated, still low priority.

*(Net: the one "open decision" from v3 is closed by xVAR. Nothing here blocks the Phase-0 build or the static comp.)*

---

## 11. Tier calibration contract (v3.2)

David's correction changes the tier contract materially: **"Elite" is not a percentile bucket.** It is a named rendering of a calibrated value cohort only after the model proves that cohort is meaningfully different from the field.

**Producer artifact required before named labels ship:** `tier_calibration_latest.json` (name illustrative; exact path is a future RED/GREEN decision), built from the valuation layer, not the frontend. The artifact owns cohort boundaries, label eligibility, receipts, and stale/degraded states. UI code reads it; UI code does not invent tiers.

**Calibration basis:**
- Primary input = an **unclamped latent DG value** by position. The current public DVS is explicitly clamped to 0–100 (`pvo_assembler.py:389–407`), and current public xVAR is derived from that clamped DVS (`pvo_assembler.py:471–487`), so current RB/WR/TE top-end values flatten exactly where tier precision matters. The calibration producer may expose a raw/unclamped xVAR-like field or a calibrated latent value, but it must name its formula and provenance.
- Current proof point (2026-07-08 artifact; re-verified unchanged on 2026-07-11): public xVAR ties Puka Nacua, Jaxon Smith-Njigba, Ja'Marr Chase, Amon-Ra St. Brown, Rashee Rice, and George Pickens at the top WR value (39.4) because their public DVS is 100, but their unclamped projection-derived raw value separates materially. TE is more extreme: Trey McBride and ten other TEs share public xVAR 2.85 despite projection-derived raw value separating McBride from the field. A tier calibrator that cannot see through that ceiling cannot accurately define "Elite."
- Context inputs = production projection, age / fitted aging-curve state, role/sample reliability, engine path/version, model grade, and completeness flags. These are model/value-context inputs, not UI heuristics.
- Explicitly excluded = FantasyCalc/KTC/market values, ADP, expert consensus, or any market-derived field. Market remains overlay-only.

**Break-finding method:**
- Derive candidate cohorts within position from the current model-valued field using a robust one-dimensional segmentation method such as optimal natural breaks / dynamic-programming changepoints / model-selected mixtures. The chosen method must penalize tiny unstable cohorts, bottom-tail noise, and day-over-day boundary churn.
- Do not predeclare cohort counts. Some positions may have a small top shelf; others may not have a true top break in a given run.
- Persist rank spans, value ranges, cohort size, uncertainty, method version, and data-health status.

**Historical validation:**
- Walk the same rule through historical Engine B feature seasons and realized outcomes (`engine_b_features_v2.csv` outcome `avg_ppg_t1_t2`) with no lookahead. A named label earns display only if its cohort shows material and reasonably stable separation from adjacent cohorts on future production/value outcomes, with a sample-size floor.
- If historical support is weak or incomplete, render the cohort neutrally: "Top cohort · WR1–7" / "Value band · RB18–31", not "Elite" or "Cornerstone."
- Cornerstone additionally requires an empirically derived age/longevity rule from fitted aging curves or a David-ratified taste rule disclosed as such.

**Display contract:**
- The row/rail can show neutral shaded cohort spans immediately once the artifact exists; named labels are an additional gated layer.
- Every cohort display includes a nearby receipt basis: population, position, value basis, calibration version, captured date, historical-support state, and `decision_supported=false`.
- Cohort shading is neutral and low-saturation. It must not use verdict hues, intensity ramps, or action copy.

**Current-state implication:** v3.1's static comp tier chips are directional placeholders only. Production implementation must suppress named labels until the tier-calibration artifact is specified, RED-tested, and built.

## 12. Open items (defaulted where possible)
1. **RESOLVED (David 2026-07-08):** three tabs — My Roster · Other Teams · Full Universe (Free Agents / Rostered + position filters). Data-supported: `league_context.rostered` + `roster_id`/`owner`.
2. **Denominator basis** — our ranks (per-position cohorts for position ranks/percentiles; the 468 union for the overall rank only, v3.10) vs market's full-FC native rank; the signed display delta is the apples-to-apples magnitude. *Default: paired native ranks as readout, stored delta for sort/magnitude, basis in receipt.* Confirmed acceptable in cockpit.
3. **Fresh-data recompute** — MECHANISM SHIPPED (Phase-0b, PR #146; the 09:40 LaunchAgent installed); **unattended reliability OPEN**: the first unattended fire (Jul 12) ran DEGRADED (`market_source_prior_date` — wake-coalesced ordering race; the runner read the FC store 3s before the fresh capture landed; honest fail-close, no PIT pair). See item 11. This comp's grounding artifact came from the David-authorized manual runner run on 07-11.
4. **Cross-position basis** — RESOLVED by xVAR (§10, v3.1); only the market-comparable 0–2000 rescale remains deferred (producer-side field with provenance, low priority — the percentile margin is already the apples-to-apples comparison).
5. **Tier-calibration producer** — REQUIRED before named tier labels ship (§11). Neutral cohort spans are acceptable only from the artifact; static percentiles are not.
6. **Market-rank renormalization — REJECTED (David, 2026-07-11).** Re-ranking FantasyCalc values inside our cohort to force a shared denominator was proposed in re-review and rejected: native ranks stay familiar labels; the stored percentile delta stays the only arithmetic. (The underlying UX concern is answered by the percentile-axis bar, not by inventing a new derived rank.)
7. **Day-2 overnight delta — ALLOWED (David, 2026-07-11).** The 14-day depth gate binds multi-day trend renderings only; a named-window margin delta over the GLOBAL latest capture pair (same-player renderable intersection) is legal from day 2 (§4, §13, RED seeds; pairing law chosen v3.6).
8. **NAMED PRE-UI FOLLOW-UP — roster model-coverage gaps (classification corrected v3.8, data-backed).** All three off-board players HAVE market reads; what's missing is OUR side: **Braelon Allen and Garrett Wilson are DG reads unavailable** (ENGINE_B, MODEL_UNCERTAIN, xVAR null; market RB52/WR14) and **Tank Dell is pre-model** (PRE_MODEL route, market WR71) — the earlier "identity gaps" label was unsupported for Wilson and is retired; a finer producer-side classifier (why is each xVAR null?) is part of this ticket. A roster audit with the manager's WR1 unvalued is not credible, and the WR group total computed without Wilson/Dell makes the cost concrete. Resolve before the Value Board UI build ships. David sequences the ticket.
9. **NAMED FOLLOW-UP — per-row quarantine in the scheduled runner** (the Phase-0b residual, elevated by Codex's v3.4 finding): `_has_stale_market` currently fail-closes the entire refresh on any `gates_blocked` row, so no row-level "signal withheld" state is reachable. The quarantine change (its own cockpit cycle — it touches the compounding history contract) is the prerequisite for that designed state; until it lands, the UI simply never sees such a row.
10. **Group-value basis — RULED (David, 2026-07-12, verbatim "yes for now you can use option A the same governed matrix"): OPTION A — the governed `team_value_matrix.py:153–220` starter+depth-credit definition, provisional ("for now").** One definition of position value exists in this product; the §8(b) producer artifact delivers it (its own cockpit cycle: spec → RED → GREEN → dual-CLEAR → David authorizes). Until that artifact ships, group headers render count + valued x/y only — the ruling selects the basis; it does not un-gate the rendering, and the DEPENDENCY-CLOSURE gate (§7 seed; whole skill roster, unknown-position fail-closed) still applies. A future change away from A is a David re-ruling, not a drift.
11. **NAMED FOLLOW-UP — wake-ordering guard for the daily jobs (from the Jul-12 degraded fire).** The 09:00 FC capture and the 09:40 runner have only schedule spacing between them, which collapses when launchd wake-coalesces after sleep; the runner must wait/retry for a same-day capture (or gain an explicit dependency). Its own cockpit cycle; David sequences. A manual rerun on a fresh capture day succeeds and is David-gated.

---

## 13. Market volatility vs our PIT trend — the two-metric separation (v3.3; provenance + depth semantics corrected v3.4)

The 2026-07-11 framing convergence separated two metrics that a naive build would have blended into one "volatility" display. **The framing already converged, don't re-run it** (record: `docs/agent-ledger/2026-07-11.md`, 16:45 ET).

**Metric 1 — FantasyCalc's `market_volatility` (theirs, undisclosed basis).** A source-computed moving-deviation field FantasyCalc ships with its values (Codex identified it as their `maybeMovingStandardDeviation`). We do not know its window, formula, or population. Under the disclosed-basis law (a rank or number with no disclosed basis is a defect — PRODUCT.md principle 2), it may not carry primary-surface weight:
- **Receipt-only**, with the exact copy: *"FantasyCalc-source metric; basis undisclosed by source; our capture of it began Jul 10"* (first captured rows 2026-07-10, 169 of them — v3.3's "Jul 11" was wrong; Codex #5).
- Never a sort key, never a row glyph, never urgency copy or motion.
- **The producer's one sanctioned use (v3.4 Codex #1; reality corrected v3.5):** a suppression-only gate — a value above threshold marks the row `SUPPRESSED_VOLATILE_MARKET` (`gates_blocked`). It can only withhold a signal, never create or amplify one. **But no such row can reach a served artifact today:** the scheduled runner's `_has_stale_market` treats any `gates_blocked` row as a whole-run stale condition and fail-closes the entire refresh (Codex probe, v3.4). The designed row-level "signal withheld" state therefore ships WITH the per-row quarantine follow-up (§12.9), not before; v3.4's present-tense spec of that state was itself a defect.
- Board coverage today: 159 of 340 rows `captured` · 181 `source_omitted` — a coverage receipt fact, never a row-level drama.

**Metric 2 — OUR PIT-derived margin record (ours, fully disclosed; trends depth-gated, overnight legal from day 2).** The honest volatility story is the one we can define: day-over-day movement of the percentile margin in `market_divergence_history` (**began 2026-07-09**; the daily 09:40 owner appends going forward — v3.3's "began Jul 11" was wrong; Codex #5).
- **Eligibility, defined (v3.5; basis guard made honest v3.6):** an eligible observation = a history row on a capture day with a **renderable status** (gates_passed / inside_band). The basis-version guard is a **producer prerequisite** — history rows carry only `universe_pvo_batch.v1` today and never retain the root divergence basis, so root-basis stamping (§8) must land before the movement feed ships; until then, comparability is documented as single-era (v1 since inception), and any basis change without stamping BLOCKS the feed rather than comparing silently. The raw table stores all 12,201 players on both days; eligibility is the filter — after it, 328 players hold 2 eligible observations and 18 hold 1 (Jul 9 · Jul 11, nonconsecutive; no scheduled owner existed on Jul 10).
- **Multi-day trend renderings** (sparklines, trajectories) are **depth-gated at 14 eligible observations** per player. Below the gate, trend slots render the designed forming state — *"our daily record began Jul 9 — N days of tape so far"* — honest compounding-product copy: the tape visibly grows.
- **The overnight delta (David ruling, 2026-07-11): legal from day 2, and it powers the Daily Open's macro answer (§4).** **Pairing law (v3.6): the GLOBAL latest capture pair** — one named window for the whole surface ("since Thu Jul 9" today; the Jul 12 fire ran DEGRADED — ordering race, §4 — so true overnight arrives with the next successful fire); per-player latest-two pairing was rejected (mixed windows in one list are illegible). Movement = |display-delta change| ≥ 1 on that pair's same-player renderable intersection, exact-decimal integers; entrants/exits as a quiet coverage note. Live today: **14 of David's 23 eligible players moved (5 by 2+ points; nine were 1-point display shifts — the macro line says so); one newly comparable (Rasheen Ali); 204 board-wide = 133 rostered + 71 free agents**; top movers (sign-neutral CANONICAL-id order — history keys are sleeper ids, mapped first) Mac Jones −4 · Legette +4 · Bell −4 · Gabriel +3 · Bryant −2; "DG values held" DERIVED on both scopes (0 and 0); "market moved" DERIVED (325 full-set; 22/23 roster); the 64 model-percentile shifts attribute PER POSITION — TE 112→111 (63 TE shifts + 1 RB; entrants RB×2, exits RB×2 + TE×1; position transitions guarded, model-backed 0 today) — never the union, never "board makeup", never model movement. A 2-point series is a delta the manager can use this morning; it is not, and is never drawn as, a trend.
- So in the v3.6 static comp: the Daily Open leads with the named-window movement answer; **no sparkline or trajectory renders anywhere**; the Hard Right Edge discipline applies from day one.

**Why the separation matters:** blending them would let an undisclosed third-party number wear our blue lane's credibility, and would let a 2-day sample masquerade as a trend — both verdict-by-the-back-door failures. Each metric keeps its own provenance, its own gate, and its own (receipt vs forming-state vs named-window-delta) rendering.

---
*Composition v3.14 — the FINAL document pass under David's strategic reset. The doc is now the CONTRACT SOURCE: its laws, seeds, and derivations feed the Value Board build REDs. Composition-fidelity review moves to the disposable static comp (real HTML, real data, healthy + degraded states) → unanchored fresh-agent audits + Codex dual-lane + Gemini's end-user protocol → David's directional preview. §12.10 RULED (Option A, provisional); §12.11 = the wake-ordering guard. The framing already converged, don't re-run it. No production code until cleared and David authorizes.*