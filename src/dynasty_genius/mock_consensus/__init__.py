"""Subsystem 1 — NFL mock-draft consensus aggregation (overlay-only, S1-owned).

Pure, model-blind consensus aggregation. No Engine A/B / scoring /
``backtest_mock_draft`` imports (one-directional S4->math edge only; spec v4 U2);
``decision_supported=False``; overlay-only, not wired to any David-facing surface.
"""
