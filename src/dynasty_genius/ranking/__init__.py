"""DG-178 — David's dynasty ranking composition: one contract, one assembler.

Every forecast lane (veteran, rookie) hands the assembler the same object — an UNCONDITIONAL
expected value above the position's replacement, per season horizon, on one clock, in one
unit, with the event it was integrated over written down. The assembler only weights and
sums. It never multiplies one producer's probability by another producer's conditional mean;
that is each adapter's job, done inside one declared event.
"""
